"""
Εστία (Estia) — CONDIAN HOTELS · Κεντρική πλατφόρμα προσωπικού (v12.25)
Ασφάλεια (v12.25): SECRET_KEY enforced σε production, session cookies (HttpOnly/SameSite/Secure),
rate-limit στο login, bootstrap admin & default κωδικοί ομάδας μέσω env (όχι σταθεροί στον κώδικα).
Backend: Flask + PostgreSQL + SMTP + AI Assistant

Modules:
  - Water Log (νερά χρήσης) — multi-hotel / multi-δίκτυο (v12.3)
  - Pool Log (πισίνες) — multi-hotel / multi-pool
  - SpithaAI (chat bubble) — provider-agnostic (Anthropic/OpenAI)
  - Records feed (v12.4) — ενιαία λίστα υποβολών πισινών & νερών
  - v12.5 — Πίνακας Πισινών: κεντρικό πάνελ (σημερινές μετρήσεις + charts + φίλτρα)·
            Records: εξαγωγή PDF & XLSX (έτοιμα προς εκτύπωση)
  - v12.6 — Shell header: ζωντανό ρολόι + καιρός/location, κουμπιά Guest/Staff/Admin (placeholder)·
            Footer (Εστία · All Rights Reserved 2026 · version/build · CONDIAN Hotels)
  - v12.7 — Βοηθός μετονομάστηκε «SpithaAI»· Διαχείριση Ξενοδοχείων & Πισινών (/dashboard/hotels) —
            μεταφέρθηκε από τον Πίνακα Πισινών στο μενού Διαχείρισης
  - v12.8 — Header: 4ο κουμπί Operations · ειδοποιήσεις στο καμπανάκι · dropdown προφίλ
            (προφίλ/μηνύματα/γλώσσα/ρυθμίσεις/λογαριασμός/θέμα/έξοδος) · Day/Dark/System (cookie) · γλώσσα el/en/uk
  - v12.9 — Login: φουτουριστικό AI background (neural-net canvas)· Μηνύματα μεταξύ χρηστών (Message)·
            σελίδες Ρυθμίσεις & Λογαριασμός μου· badge αδιάβαστων μηνυμάτων στο header
  - v12.10 — Pre-login redesign (login + εγγραφή): κοινό static/auth.css + auth.js· μεγαλύτερο logo με glow·
             tagline «we reinvent the way we work» (Space Grotesk)· language selector (globe popover)·
             footer (© 2026 CONDIAN Hotels + facebook/linkedin)· εγγραφή με Google/Apple (UI placeholders)
  - v12.11 — Εμφάνιση: ξεχωριστές ρυθμίσεις login/register (login_logo, μέγεθος, γραμματοσειρά, tagline)
             + social links (facebook/linkedin)· αφαιρέθηκε ο κύκλος πίσω από το logo
  - v12.12 — Dark mode ΠΑΝΤΟΥ μέσω after_request (auto-inject theme snippet σε κάθε σελίδα,
             εκτός login/register)· Χρήστες: πλήρες edit (username+γλώσσα) + reveal νέου κωδικού
  - v12.13 — Χρήστες: χρωματικά badges για όλους τους ρόλους (masteradmin/admin/manager/staff/viewer)·
             η διαχείριση χρήστη σε pop-up modal (αντί για ανάπτυξη)· delete μέσα στο modal
  - v12.14 — Module Βλαβοληψία Φάση 1 (faults.py): δέντρο κατηγοριών (240), 9 καταστάσεις/state machine,
             αυτόματη/χειροκίνητη ανάθεση, σχόλια 2 καναλιών, audit log, SLA seed, μαζική «Ολοκλήρωση»
  - v12.15 — Βλαβοληψία Φάση 2: admin ρυθμίσεις (ειδικότητες/SLA/tags/χάρτης editable)· δέντρο κατηγοριών·
             SLA badges· export xlsx/csv/pdf· ειδικότητες ανά χρήστη· overview KPI σε νέο Fault
  - v12.16 — Βλαβοληψία Φάση 2β: φωτό/συνημμένα (cover + γκαλερί/upload)· email ειδοποίηση νέας βλάβης
  - v12.17 — Μενού: «Πίνακας Βλαβών» στην Παρακολούθηση· «Αναφορά βλάβης»→«Δήλωση βλάβης» στη Συντήρηση
"""

from flask import Flask, render_template, request, redirect, url_for, session, jsonify, Response
from flask_sqlalchemy import SQLAlchemy
from werkzeug.security import generate_password_hash, check_password_hash
from sqlalchemy import text
from datetime import datetime, date, timedelta
import os, smtplib, threading, json, time, secrets, urllib.request, urllib.error, urllib.parse
# v12.91 — Ολόκληρη η πλατφόρμα σε ώρα Ελλάδος (Europe/Athens)
os.environ.setdefault('TZ', 'Europe/Athens')
try:
    time.tzset()
except Exception:
    pass
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText

app = Flask(__name__)

# Railway/Heroku δίνουν 'postgres://' — η SQLAlchemy 2.x θέλει 'postgresql://'
_db_url = os.environ.get('DATABASE_URL', 'sqlite:///water.db')
if _db_url.startswith('postgres://'):
    _db_url = _db_url.replace('postgres://', 'postgresql://', 1)
_IS_PRODUCTION = not _db_url.startswith('sqlite')

# ── v12.25 Ασφάλεια: SECRET_KEY ──
# Σε production (Postgres) ΑΠΑΙΤΕΙΤΑΙ SECRET_KEY από env — αλλιώς η εφαρμογή δεν ξεκινά.
# Το dev fallback επιτρέπεται ΜΟΝΟ τοπικά (SQLite).
_secret = os.environ.get('SECRET_KEY')
if not _secret:
    if _IS_PRODUCTION:
        raise RuntimeError('SECRET_KEY δεν εχει οριστει. Ορισε SECRET_KEY στις μεταβλητες περιβαλλοντος (Railway) πριν το deploy.')
    _secret = 'estia-dev-secret-change-me'   # μονο για τοπικο dev (SQLite)
app.secret_key = _secret

app.config['SQLALCHEMY_DATABASE_URI'] = _db_url
app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False
app.config['SQLALCHEMY_ENGINE_OPTIONS'] = {'pool_pre_ping': True}
app.config['MAX_CONTENT_LENGTH'] = 2 * 1024 * 1024   # 2MB (avatar upload)

# ── v12.25 Ασφάλεια: session cookies ──
app.config['SESSION_COOKIE_HTTPONLY']    = True             # οχι προσβαση απο JavaScript (XSS)
app.config['SESSION_COOKIE_SAMESITE']    = 'Lax'            # μετριαζει CSRF
app.config['SESSION_COOKIE_SECURE']      = _IS_PRODUCTION   # cookies μονο μεσω HTTPS σε production
app.config['PERMANENT_SESSION_LIFETIME'] = timedelta(days=7)

# ── v12.25 Ασφαλεια: απλο rate-limit στο login (in-memory ανα worker) ──
_LOGIN_MAX_FAILS = 5     # μετα απο τοσες αποτυχιες...
_LOGIN_WINDOW    = 300   # ...μεσα σε τοσα δευτερολεπτα → προσωρινο κλειδωμα
_login_fails     = {}    # key(IP) -> [timestamps αποτυχιων]

def _login_key():
    xff = request.headers.get('X-Forwarded-For', '')
    return xff.split(',')[0].strip() if xff else (request.remote_addr or 'unknown')

def _login_blocked(key):
    now = time.time()
    fails = [t for t in _login_fails.get(key, []) if now - t < _LOGIN_WINDOW]
    _login_fails[key] = fails
    return len(fails) >= _LOGIN_MAX_FAILS

def _login_record_fail(key):
    _login_fails.setdefault(key, []).append(time.time())

def _login_reset(key):
    _login_fails.pop(key, None)

SMTP_SERVER    = 'condian.gr'
SMTP_PORT      = 465
# ΣΗΜ.: όρισε EMAIL_FROM/GRAPH_SENDER σε πραγματικό mailbox @condianhotels.gr μέσω env στο Railway
EMAIL_FROM     = os.environ.get('EMAIL_FROM', 'report@condian.gr')
EMAIL_PASSWORD = os.environ.get('EMAIL_PASSWORD', '')
EMAIL_TO_LIST  = ['dimitris@condianhotels.gr', 'm.xypakis@condianhotels.gr', 'g.giakoumakis@condianhotels.gr']
APP_URL        = os.environ.get('APP_URL', 'https://estia.condianhotels.gr')   # βάση για absolute links

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
REG_CODE = os.environ.get('REG_CODE', 'condian2026')          # κωδικός εγγραφής προσωπικού
REGISTRATION_OPEN = False   # v12.89 — κρυφή εγγραφή μέχρι να πάμε live (κρύβει link + κλειδώνει /register)
ENABLE_SCHEDULER = os.environ.get('ENABLE_SCHEDULER', 'true').lower() in ('1','true','yes','on')
REMINDER_HOUR = int(os.environ.get('REMINDER_HOUR', '18'))    # ώρα υπενθύμισης (Europe/Athens)

# Microsoft Graph (Office 365) — ενεργό αν οριστεί GRAPH_CLIENT_ID
GRAPH_TENANT_ID     = os.environ.get('GRAPH_TENANT_ID', '')
GRAPH_CLIENT_ID     = os.environ.get('GRAPH_CLIENT_ID', '')
GRAPH_CLIENT_SECRET = os.environ.get('GRAPH_CLIENT_SECRET', '')
GRAPH_SENDER        = os.environ.get('GRAPH_SENDER', EMAIL_FROM)

# ── AI Assistant config (provider-agnostic) ──
AI_PROVIDER       = os.environ.get('AI_PROVIDER', 'auto')   # auto | anthropic | openai
ANTHROPIC_API_KEY = os.environ.get('ANTHROPIC_API_KEY', '')
OPENAI_API_KEY    = os.environ.get('OPENAI_API_KEY', '')
AI_MODEL          = os.environ.get('AI_MODEL', '')          # override; αλλιώς default ανά πάροχο

POOL_ASSISTANT_PROMPT = """Είσαι το «SpithaAI», ο ψηφιακός βοηθός της πλατφόρμας Εστία (CONDIAN HOTELS).
Βοηθάς το προσωπικό συντήρησης και τους υπεύθυνους βάρδιας στην ασφαλή, καθαρή και
νόμιμη καθημερινή λειτουργία των πισινών και των δικτύων νερού/ΖΝΧ (νερά χρήσης, legionella).
# ΑΡΜΟΔΙΟΤΗΤΕΣ
- Χημεία νερού: ερμηνεία μετρήσεων (pH, ελεύθερο/ολικό χλώριο, αλκαλικότητα,
  κυανουρικό οξύ, σκληρότητα ασβεστίου, θερμοκρασία) και προτάσεις διόρθωσης.
- Δοσολογία χημικών: υπολογισμός ποσότητας με βάση τον όγκο κάθε πισίνας και την
  απόκλιση από τις επιθυμητές τιμές.
- Πρόγραμμα καθαρισμού: skimming, βούρτσισμα, σκούπισμα πυθμένα, καθαρισμός
  καλαθιών skimmer/προφίλτρου, backwash φίλτρων.
- Έλεγχοι εξοπλισμού: αντλίες, κυκλοφορία, στάθμη νερού, πίεση φίλτρων, διαρροές.
- Ασφάλεια & συμμόρφωση: διαύγεια νερού, σήμανση, βάθη, ναυαγοσώστης, τήρηση
  αρχείου μετρήσεων.
- Ημερολόγιο: καταγραφή μετρήσεων και ενεργειών ανά βάρδια και ανά πισίνα.
# ΛΕΙΤΟΥΡΓΙΑ
1. Ρώτα πρώτα ποια πισίνα αφορά και ζήτα τις τρέχουσες μετρήσεις αν δεν τις έχεις.
2. Δίνε σαφή, πρακτικά βήματα — «τι να κάνω τώρα», όχι θεωρία.
3. Στη δοσολογία δείξε όγκο, απόκλιση και τελική ποσότητα προϊόντος. Πάντα
   επιβεβαίωνε τον όγκο της πισίνας.
4. Σήμανε ως ΕΠΕΙΓΟΝ ό,τι είναι εκτός ορίων (χλώριο, θολό νερό, βλάβη αντλίας).
5. Αν μια τιμή ήταν προβληματική νωρίτερα, υπενθύμισε επανέλεγχο.
# ΟΡΙΑ ΑΣΦΑΛΕΙΑΣ
- Δεν αντικαθιστάς πιστοποιημένο τεχνικό ή τη νομοθεσία· τα όρια τιμών διαφέρουν
  ανά περιοχή — σύστησε επαλήθευση με τους τοπικούς υγειονομικούς κανονισμούς.
- Σε σοβαρά περιστατικά (υπερδοσολογία, ηλεκτρικό πρόβλημα, ατύχημα) δώσε άμεση
  οδηγία ασφαλείας, σύστησε κλείσιμο πισίνας και ειδοποίηση υπευθύνου.
- ΠΟΤΕ μην προτείνεις ανάμειξη χλωρίου με οξέα ή χημικών μεταξύ τους.
# ΥΦΟΣ
- Σύντομος, πρακτικός, απλή γλώσσα για προσωπικό βάρδιας.
- Checklists για ελέγχους και προγράμματα.
- Στο τέλος κάθε αναφοράς, πρότεινε τι να καταγραφεί στο log."""

db = SQLAlchemy(app)

# ──────────────────────────────────────────────────────────────────────────
#  POOL LIMITS  (min, max) — None means no limit on that side.
# ──────────────────────────────────────────────────────────────────────────
POOL_LIMITS = {
    'free_chlorine':     (0.4, 1.5),
    'combined_chlorine': (None, 0.5),
    'ph':                (7.2, 7.8),
    'temp':              (None, 32.0),
    'turbidity':         (None, 1.0),
    'cyanuric_acid':     (None, 75.0),
    'total_alkalinity':  (80.0, 120.0),
    'orp':               (650.0, None),
}

PARAM_LABELS = {
    'free_chlorine': 'Ελεύθερο χλώριο', 'combined_chlorine': 'Συνδεδεμένο χλώριο',
    'ph': 'pH', 'temp': 'Θερμοκρασία', 'turbidity': 'Θολότητα',
    'cyanuric_acid': 'Κυανουρικό οξύ', 'total_alkalinity': 'Ολική αλκαλικότητα', 'orp': 'ORP',
}

# Κανόνας ενεργειών όταν τιμή εκτός ορίων (low = κάτω από min, high = πάνω από max)
ACTION_RULES = {
    'free_chlorine':     {'low': 'Χαμηλό χλώριο — κάνε χλωρίωση και επανέλεγξε σε 30΄.',
                          'high': 'Υψηλό χλώριο — σταμάτα τη δοσομέτρηση/άσε να πέσει· απόφυγε χρήση μέχρι <1.5 mg/L.'},
    'combined_chlorine': {'high': 'Υψηλό δεσμευμένο χλώριο — υπερχλωρίωση (shock) + αερισμός· έλεγξε ανανέωση νερού.'},
    'ph':                {'low': 'Χαμηλό pH — πρόσθεσε pH plus (ανθρακική σόδα).',
                          'high': 'Υψηλό pH — πρόσθεσε pH minus (οξύ), σταδιακά.'},
    'temp':              {'high': 'Υψηλή θερμοκρασία — έλεγξε/μείωσε θέρμανση· παρακολούθησε χλώριο.'},
    'turbidity':         {'high': 'Θολό νερό — backwash φίλτρου, έλεγξε διήθηση/κυκλοφορία, εξέτασε κροκίδωση.'},
    'cyanuric_acid':     {'high': 'Υψηλό κυανουρικό οξύ — μερική ανανέωση νερού (αραίωση)· μείωσε σταθεροποιητή.'},
    'total_alkalinity':  {'low': 'Χαμηλή αλκαλικότητα — πρόσθεσε alkalinity up (ανθρακική σόδα).',
                          'high': 'Υψηλή αλκαλικότητα — πρόσθεσε οξύ σταδιακά.'},
    'orp':               {'low': 'Χαμηλό ORP — ανέβασε ελεύθερο χλώριο και ρύθμισε pH στο 7.2–7.6.'},
}
SAFETY_NOTE = 'Ποτέ μην αναμειγνύεις χημικά μεταξύ τους (ειδικά χλώριο με οξύ). Πρόσθεσε ένα-ένα, με την κυκλοφορία ανοιχτή.'

AREA_LABELS = {'pump': 'Αντλία', 'filter': 'Φίλτρο', 'chemicals': 'Χημικά / Δοσομέτρηση',
               'water': 'Νερό / Στάθμη', 'electrical': 'Ηλεκτρικό', 'app': 'Εφαρμογή', 'other': 'Άλλο'}

def _urgent(param, val):
    return ((param == 'free_chlorine' and val < 0.2) or
            (param == 'combined_chlorine' and val > 1.0) or
            (param == 'turbidity' and val > 2.0))

def compute_pool_actions(rec):
    out = []
    for key, rule in ACTION_RULES.items():
        val = getattr(rec, key, None)
        if val is None:
            continue
        mn, mx = POOL_LIMITS.get(key, (None, None))
        action = None
        if mn is not None and val < mn:
            action = rule.get('low')
        elif mx is not None and val > mx:
            action = rule.get('high')
        if action:
            out.append({'label': PARAM_LABELS.get(key, key), 'action': action, 'urgent': _urgent(key, val)})
    return out


# ── ΖΝΧ / Δίκτυο νερού: κανόνες προτεινόμενων ενεργειών (legionella) ─────────
def _clo2_act(name):
    return (f'ClO2 {name} <1 ppm: αύξησε τη δοσομέτρηση ClO2· έλεγξε δοσομετρική αντλία/απόθεμα.',
            f'ClO2 {name} >2 ppm: μείωσε τη δοσομέτρηση ClO2.')

# key WaterRecord -> (min, max, ενέργεια_low, ενέργεια_high). Όρια ίδια με το report νερού.
WATER_ACTION_RULES = {
    'temp_dhw_out':     (60.0, None, 'Κολεκτέρ ΖΝΧ <60°C: ανέβασε τη θερμοκρασία αποθήκευσης ≥60°C (κίνδυνος legionella)· έλεγξε λέβητα/εναλλάκτη/θερμοστάτη.', None),
    'temp_dhw_return':  (50.0, None, 'Επιστροφή ανακυκλοφορίας <50°C: ανεπαρκής ανακυκλοφορία· έλεγξε αντλία & βάνες ανακυκλοφορίας· εξέτασε θερμική απολύμανση/flushing.', None),
    'temp_kitchen_hot': (50.0, None, 'Ζεστό Κουζίνας <50°C: flushing του σημείου· έλεγξε ανακυκλοφορία/μόνωση γραμμής.', None),
    'temp_remote_hot':  (50.0, None, 'Ζεστό Απομακρυσμένου <50°C: flushing· έλεγξε ανακυκλοφορία (κρίσιμο τελευταίο σημείο δικτύου).', None),
    'temp_tank':        (None, 20.0, None, 'Δεξαμενή (κρύο) >20°C: εξέτασε ψύξη/μόνωση/ανανέωση νερού· κίνδυνος ανάπτυξης μικροβίων.'),
    'clo2_dhw_out':     (1.0, 2.0, *_clo2_act('Αναχώρηση ΖΝΧ')),
    'clo2_dhw_return':  (1.0, 2.0, *_clo2_act('Επιστροφή ΖΝΧ')),
    'clo2_tank':        (1.0, 2.0, *_clo2_act('Δεξαμενή')),
    'clo2_kitchen':     (1.0, 2.0, *_clo2_act('Κουζίνα')),
    'clo2_remote':      (1.0, 2.0, *_clo2_act('Απομακρυσμένο')),
    'clo2_ro':          (1.0, 2.0, *_clo2_act('Αντ. Όσμωση')),
}
WATER_ACTION_LABELS = {
    'temp_dhw_out': 'Κολεκτέρ ΖΝΧ (Αναχ.)', 'temp_dhw_return': 'Κολεκτέρ Ανακυκλ. (Επιστρ.)',
    'temp_kitchen_hot': 'Κουζίνα Ζεστό', 'temp_remote_hot': 'Απομακρυσμένο Ζεστό', 'temp_tank': 'Δεξαμενή',
    'clo2_dhw_out': 'ClO2 Αναχώρηση ΖΝΧ', 'clo2_dhw_return': 'ClO2 Επιστροφή ΖΝΧ',
    'clo2_tank': 'ClO2 Δεξαμενή', 'clo2_kitchen': 'ClO2 Κουζίνα', 'clo2_remote': 'ClO2 Απομακρυσμένο', 'clo2_ro': 'ClO2 Αντ. Όσμωση',
}

def _water_urgent(key, val):
    if key == 'temp_dhw_out' and val < 50: return True
    if key in ('temp_dhw_return', 'temp_kitchen_hot', 'temp_remote_hot') and val < 45: return True
    if key in ('clo2_dhw_out', 'clo2_dhw_return') and val < 0.3: return True
    return False

def compute_water_actions(rec):
    """Προτεινόμενες ενέργειες ΖΝΧ/δικτύου νερού όταν μέτρηση εκτός ορίων. Δεν αγγίζει πισίνες."""
    out = []
    for key, (mn, mx, low, high) in WATER_ACTION_RULES.items():
        val = getattr(rec, key, None)
        if val is None:
            continue
        action = None
        if mn is not None and val < mn:
            action = low
        elif mx is not None and val > mx:
            action = high
        if action:
            out.append({'label': WATER_ACTION_LABELS.get(key, key), 'action': action, 'urgent': _water_urgent(key, val)})
    return out


def notify(user_id, text, link=None):
    if not user_id:
        return
    try:
        db.session.add(Notification(user_id=user_id, text=(text or '')[:300], link=link))
        db.session.commit()
    except Exception:
        db.session.rollback()

def notify_admins(text, link=None):
    for u in User.query.filter(User.is_active == True, User.role.in_(['admin', 'masteradmin'])).all():
        notify(u.id, text, link)

def send_fault_email(fr):
    if not EMAIL_PASSWORD and not GRAPH_CLIENT_ID:
        return False
    label = AREA_LABELS.get(fr.area, fr.area)
    who = fr.user.full_name if fr.user else '-'
    where = (fr.pool.hotel.name + ' / ' + fr.pool.name) if (fr.pool and fr.pool.hotel) else (fr.pool.name if fr.pool else '-')
    html = ('<div style="font-family:Arial,sans-serif;max-width:600px;">'
            '<div style="background:#b91c1c;color:white;padding:16px;border-radius:8px 8px 0 0;"><h2 style="margin:0;">Αναφορα βλαβης</h2></div>'
            '<div style="background:#f9f9f9;padding:16px;border:1px solid #eee;">'
            + '<p><b>Τομεας:</b> ' + label + '</p><p><b>Σημειο:</b> ' + where + '</p><p><b>Απο:</b> ' + who + '</p>'
            + '<p><b>Περιγραφη:</b><br>' + (fr.message or '') + '</p></div></div>')
    return send_email('Αναφορα βλαβης - ' + label, html, EMAIL_TO_LIST)


# ── ROLES & PERMISSIONS ──
ROLE_RANK = {'viewer': 0, 'accountant': 1, 'staff': 1, 'manager': 2, 'admin': 3, 'masteradmin': 4}
ROLE_LABELS = {'masteradmin': 'Masteradmin / Διοίκηση', 'admin': 'Admin', 'manager': 'Manager (επόπτης)',
               'accountant': 'Λογιστήριο', 'staff': 'Staff (καταγραφή)', 'viewer': 'Viewer (προβολή)'}

def role_rank(role):
    return ROLE_RANK.get(role, 0)

def current_user():
    uid = session.get('user_id')
    return User.query.get(uid) if uid else None

@app.context_processor
def inject_nav():
    uid = session.get('user_id')
    unread = Notification.query.filter_by(user_id=uid, is_read=False).count() if uid else 0
    msgs   = Message.query.filter_by(recipient_id=uid, is_read=False).count() if uid else 0
    return {'nav_unread': unread, 'msg_unread': msgs}

@app.context_processor
def inject_theme():
    return {'theme': get_theme()}

@app.template_filter('gr')
def _gr_time(dt, fmt='%d/%m %H:%M'):
    """v12.226 — οι ώρες αποθηκεύονται UTC (server). Εμφάνιση σε ώρα Ελλάδος (Europe/Athens, με θερινή ώρα).
    Μετατροπή στο σημείο εμφάνισης ώστε να είναι σωστή ανεξάρτητα από το TZ του server."""
    if not dt:
        return ''
    try:
        from datetime import timezone as _tz
        from zoneinfo import ZoneInfo
        d = dt if dt.tzinfo else dt.replace(tzinfo=_tz.utc)
        return d.astimezone(ZoneInfo('Europe/Athens')).strftime(fmt)
    except Exception:
        try:
            return dt.strftime(fmt)
        except Exception:
            return str(dt)

# έκδοση/build για το footer του shell
APP_VERSION = '12.286'
APP_BUILD   = '567'

# ── v12.36 — Ιστορικό εκδόσεων («Τι νέο»). Newest first. ──────────────────────
CHANGELOG = [
    {'v': '12.286', 'b': '567', 'date': '25/06/2026', 'time': '08:50', 'title': 'Καταχώρηση/Σήμερα: μόνο σημεία με ανατεθειμένες μετρήσεις',
     'items': ['Στον συντηρητή («Σήμερα»/«Καταχώρηση») εμφανίζονται πλέον ΜΟΝΟ τα σημεία που έχουν τουλάχιστον μία μέτρηση πάνω τους. Σημείο χωρίς καμία μέτρηση δεν εμφανίζεται.',
               'Κάθε σημείο δείχνει μόνο τις μετρήσεις που του έχουν ανατεθεί.']},
    {'v': '12.285', 'b': '566', 'date': '25/06/2026', 'time': '08:20', 'title': 'Δομή: codes ξενοδοχείων, μόνο ενεργά, full-width οθόνη',
     'items': ['Τα chips ξενοδοχείων στη «Δομή» δείχνουν πλέον κωδικούς (αντί ολόκληρο όνομα) και ΜΟΝΟ ενεργά ξενοδοχεία (έφυγαν τα ανενεργά/Mini Market).',
               'Η οθόνη Ρυθμίσεων έγινε πιο πλατιά (full-width) και πιο ευανάγνωστη (μεγαλύτερα περιθώρια/τίτλοι).']},
    {'v': '12.284', 'b': '565', 'date': '25/06/2026', 'time': '07:40', 'title': 'Δομή: σύνδεση κόμβου με ξενοδοχεία (ποια το έχουν)',
     'items': ['Στη «Δομή», κάθε κόμβος (δίκτυο/υποδίκτυο) δείχνει τα ξενοδοχεία στα οποία είναι αντιστοιχισμένος, με chips. Κλικ σε ένα ξενοδοχείο = σύνδεση/αποσύνδεση.',
               'Έτσι ορίζεις, πάνω στην κοινή δομή, ποια δίκτυα έχει το κάθε ξενοδοχείο. Νέο πεδίο στον υπάρχοντα πίνακα — χωρίς νέο πίνακα. Κενό = ισχύει για όλα (συμβατότητα).']},
    {'v': '12.283', 'b': '564', 'date': '25/06/2026', 'time': '07:00', 'title': 'Μετρήσεις · Ρυθμίσεις: αναδιοργάνωση ροής (Δομή→Σημεία→Μετρήσεις→Περίοδοι)',
     'items': ['Σειρά καρτελών στη λογική στησίματος: Δομή → Σημεία → Μετρήσεις → Περίοδοι. Στη «Δομή» φαίνονται τα σημεία κάτω από κάθε κόμβο.',
               'Στα «Σημεία»: ανάθεση σε κόμβο της δομής + πεδίο «Χώρος» (με προτάσεις). Στις «Μετρήσεις»: η ανάθεση μετρήσεων στα σημεία (σύρσιμο). Αποσύρθηκε το παλιό «Αυτο-δημιουργία ΖΝΧ». Τα νέα σημεία είναι γενικού τύπου.']},
    {'v': '12.282', 'b': '563', 'date': '25/06/2026', 'time': '06:30', 'title': 'Καταχώρηση: κουμπί «Σήμερα» + σειρά tabs ρυθμίσεων',
     'items': ['Στη φόρμα καταχώρησης το κουμπί «Άλλο σημείο» έγινε «Σήμερα» — γυρνά στην αρχική οθόνη «Σήμερα».',
               'Οι καρτέλες στις «Μετρήσεις · Ρυθμίσεις» μπήκαν στη λογική σειρά στησίματος: Σημεία → Δομή → Μετρήσεις → Περίοδοι.']},
    {'v': '12.281', 'b': '562', 'date': '25/06/2026', 'time': '06:00', 'title': 'Καταχώρηση: όψη «Ανά χώρο» δειγματοληψίας',
     'items': ['Στην «Καταχώρηση μετρήσεων» προστέθηκε εναλλαγή όψης: «Ανά δίκτυο» (δέντρο) και «Ανά χώρο» (φυσικό σημείο δειγματοληψίας — η «θέση» του σημείου). Ο συντηρητής μπορεί να βρει γρήγορα όλα τα σημεία ενός χώρου (π.χ. Μηχανοστάσιο/Κουζίνα).',
               'Ο «χώρος» = το πεδίο θέσης κάθε σημείου (ορίζεται στις Ρυθμίσεις → Σημεία). Όσα δεν έχουν χώρο μπαίνουν σε «Χωρίς χώρο».']},
    {'v': '12.280', 'b': '561', 'date': '25/06/2026', 'time': '05:30', 'title': 'Καταχώρηση μετρήσεων: ώρα δίπλα στην ημερομηνία',
     'items': ['Στη φόρμα καταχώρησης μπήκε πεδίο «Ώρα» δίπλα στην «Ημερομηνία» (προεπιλογή τρέχουσα ώρα). Η μέτρηση αποθηκεύεται με τον ακριβή χρόνο (ημ/νία + ώρα) — χρήσιμο και για πέρασμα από χαρτί.']},
    {'v': '12.279', 'b': '560', 'date': '25/06/2026', 'time': '05:00', 'title': 'Καταχώρηση μετρήσεων: επιλογή ημερομηνίας',
     'items': ['Στην «Καταχώρηση μετρήσεων» προστέθηκε πεδίο «Ημερομηνία» (προεπιλογή σήμερα) — μπορείς να καταχωρείς μετρήσεις για συγκεκριμένη/προηγούμενη ημέρα. Δεν επιτρέπεται μελλοντική ημερομηνία.']},
    {'v': '12.278', 'b': '559', 'date': '25/06/2026', 'time': '04:40', 'title': 'Δομή: δημιουργία σημείου μέτρησης σε οποιονδήποτε κόμβο',
     'items': ['Στην «Δομή» νέα καρτέλα «Νέο σημείο μέτρησης (σε κόμβο)»: φτιάχνεις σημείο κάτω από οποιαδήποτε ομάδα/υποομάδα (γενικός τύπος), για όποιο ξενοδοχείο. Μετά του προσθέτεις μετρήσεις με σύρσιμο από την καρτέλα «Σημεία».',
               'Αντικαθιστά την ανάγκη για το παλιό καρφωτό «Αυτο-δημιουργία ΖΝΧ» — πλέον φτιάχνεις σημεία ελεύθερα όπου θες στο δέντρο.']},
    {'v': '12.277', 'b': '558', 'date': '25/06/2026', 'time': '04:10', 'title': 'Μετρήσεις · Ρυθμίσεις: drag&drop στη Δομή + καθάρισμα',
     'items': ['Η «Δομή» γίνεται με σύρσιμο (drag & drop) όπως το Οργανόγραμμα: σύρε μια ομάδα/υποομάδα πάνω σε άλλη για να γίνει υποομάδα της, ή στη μπάρα «κορυφαίο επίπεδο».',
               'Η καρτέλα «Βιβλιοθήκη» μετονομάστηκε σε «Μετρήσεις». Αφαιρέθηκε η περιττή γραμμή-υπότιτλος και η καρτέλα «Μεταφορά» (δεν χρειάζεται πλέον).']},
    {'v': '12.276', 'b': '557', 'date': '25/06/2026', 'time': '03:30', 'title': 'Καθάρισμα: απόσυρση παλιάς coarse ομαδοποίησης μετρήσεων',
     'items': ['Αφαιρέθηκε ο παλιός κώδικας ομαδοποίησης (Πισίνες/Νερά Χρήσης/Λοιπά βάσει τύπου). Πλέον «Σήμερα»/«Καταγραφή» ομαδοποιούν ΜΟΝΟ ανά δέντρο δικτύων.',
               'Όσα σημεία δεν έχουν ακόμη κόμβο εμφανίζονται σε ένα ουδέτερο κουτί «Χωρίς δίκτυο — προς αντιστοίχιση» (κανένα δεν χάνεται). Καμία αλλαγή σε δεδομένα/μετρήσεις/οδηγίες.']},
    {'v': '12.275', 'b': '556', 'date': '25/06/2026', 'time': '03:10', 'title': 'Δομή Δικτύων Φ-Δ-2: «Σήμερα»/«Καταγραφή» ομαδοποιούν ανά δέντρο',
     'items': ['Η «Σήμερα» και η «Καταγραφή μετρήσεων» δείχνουν πλέον τα σημεία οργανωμένα ανά δέντρο δικτύων (Δίκτυο Νερού → ΖΝΧ/Επεξεργασία → σημεία) αντί για τα παλιά πλατιά κουτιά. Ίδια σημεία/μετρήσεις/οδηγίες — αλλάζει μόνο η ομαδοποίηση.',
               'Όποιο σημείο δεν έχει ακόμη κόμβο πέφτει στο παλιό κουτί (Πισίνες/Νερά Χρήσης) — καμία απώλεια. Νέα υπο-δίκτυα (π.χ. Άρδευση/Spa) εμφανίζονται αυτόματα όταν αναθέσεις σημεία.']},
    {'v': '12.274', 'b': '555', 'date': '25/06/2026', 'time': '02:40', 'title': 'Δομή Δικτύων Φ-Δ: Συμμόρφωση ανά δίκτυο στα Στατιστικά',
     'items': ['Στα «Μετρήσεις · Στατιστικά» νέα κάρτα «Συμμόρφωση ανά δίκτυο»: % συμμόρφωσης ανά ομάδα/υποομάδα του δέντρου (Δίκτυο Νερού, ΖΝΧ, Όσμωση, Πισίνες…), με roll-up (κάθε κόμβος αθροίζει και τους απογόνους του).',
               'Καθαρά ανάγνωση — καμία αλλαγή σε καταχωρήσεις, μετρήσεις ή στις φόρμες. Εμφανίζεται μόνο όταν υπάρχουν σημεία αντιστοιχισμένα σε κόμβους.']},
    {'v': '12.273', 'b': '554', 'date': '25/06/2026', 'time': '02:10', 'title': 'Δομή Δικτύων Φ-Γ: αυτόματη αντιστοίχιση σημείων στο δέντρο',
     'items': ['Στην καρτέλα «Δομή» κουμπί «Αυτόματη αντιστοίχιση»: τοποθετεί κάθε υπάρχον σημείο κάτω από τον σωστό κόμβο βάσει τύπου (Πισίνες· ΖΝΧ → Ζεστό νερό· Όσμωση → Επεξεργασία).',
               'Τρέχει με το χέρι (κουμπί), idempotent, μόνο σε σημεία χωρίς κόμβο — δεν θίγει χειροκίνητες αναθέσεις ούτε καμία καταγραφή/μέτρηση.']},
    {'v': '12.272', 'b': '553', 'date': '25/06/2026', 'time': '01:40', 'title': 'Δομή Δικτύων Φ-Β: οθόνη «Δομή» (δέντρο ομάδων/υποομάδων)',
     'items': ['Στο «Μετρήσεις · Ρυθμίσεις» νέα καρτέλα «Δομή»: δεντρική διαχείριση δικτύων — ομάδες/υποομάδες (Δίκτυο Νερού → ΖΝΧ/Όσμωση/…), με μετονομασία, αλλαγή γονέα, σειρά, ενεργοποίηση/απενεργοποίηση και προσθήκη νέων.',
               'Ανάθεση κάθε σημείου σε κόμβο του δέντρου. Όσα σημεία μένουν χωρίς κόμβο δουλεύουν ακριβώς όπως σήμερα. Διαγραφή κόμβου μόνο αν είναι άδειος (καμία απώλεια). Καμία αλλαγή σε καταχωρήσεις/μετρήσεις/οδηγίες.']},
    {'v': '12.271', 'b': '552', 'date': '25/06/2026', 'time': '01:00', 'title': 'Δομή Δικτύων Φ-Α: υποδομή ιεραρχίας (αόρατη αλλαγή)',
     'items': ['Βάση για τη νέα «Δομή Δικτύων» (Δίκτυο Νερού → υπο-δίκτυα → σημεία): νέος πίνακας κόμβων-δέντρου + σύνδεση σημείου με κόμβο. Σπάρθηκε ο κατάλογος-αναφορά (Δίκτυο Νερού, Αερόλυμα/υψηλού κινδύνου, Πισίνες, Άρδευση, Λύματα).',
               'ΚΑΜΙΑ ορατή αλλαγή — όσο ένα σημείο δεν έχει κόμβο, δουλεύει ακριβώς όπως πριν. Καμία αλλαγή σε καταχωρήσεις, μετρήσεις ή οδηγίες. Το UI της δομής έρχεται σε επόμενη φάση.']},
    {'v': '12.270', 'b': '551', 'date': '25/06/2026', 'time': '00:30', 'title': 'Μετρήσεις Φ3: Βιβλιοθήκη Μετρήσεων (μία κοινή) με πλήρη επεξεργασία',
     'items': ['Το tab «Παράμετροι» στο «Μετρήσεις · Ρυθμίσεις» έγινε «Βιβλιοθήκη»: μία κοινή λίστα όλων των μετρήσεων, ομαδοποιημένη ανά κατηγορία (Πισίνα/Νερό/…).',
               'Πλήρης επεξεργασία κάθε μέτρησης επιτόπου: όνομα, μονάδα, κάτω/άνω όριο, ενέργεια αν χαμηλό/υψηλό, είδος εισόδου (αριθμός/ναι-όχι/κείμενο), κατηγορία. Προσθήκη νέας μέτρησης & ενεργοποίηση/απενεργοποίηση.',
               'Μια αλλαγή σε μέτρηση ισχύει παντού όπου χρησιμοποιείται. Οι «Τύποι παρακολούθησης» παραμένουν για διαχείριση. Καμία αλλαγή σε καταχωρήσεις/δεδομένα.']},
    {'v': '12.269', 'b': '550', 'date': '24/06/2026', 'time': '21:30', 'title': 'Μετρήσεις Φ2: βιβλιοθήκη + drag & drop μετρήσεων ανά σημείο (auto-save)',
     'items': ['Στο «Μετρήσεις · Ρυθμίσεις → Σημεία» μπήκε «Βιβλιοθήκη μετρήσεων» (όλες οι μετρήσεις ως chips). Σύρε μια μέτρηση πάνω σε ένα σημείο για να την προσθέσεις· × για αφαίρεση· σύρσιμο για σειρά.',
               'Αποθηκεύεται αυτόματα ανά σημείο (χωρίς κουμπί). Όσο ένα σημείο δεν έχει δικές του, δείχνει/χρησιμοποιεί τις μετρήσεις του τύπου του (συμβατότητα).']},
    {'v': '12.268', 'b': '549', 'date': '24/06/2026', 'time': '21:00', 'title': 'Μετρήσεις Φ1: υποδομή για μετρήσεις ανά σημείο (αόρατη αλλαγή)',
     'items': ['Βάση για το νέο μοντέλο «βιβλιοθήκη μετρήσεων + drag&drop ανά σημείο»: νέος πίνακας ανάθεσης μετρήσεων ανά σημείο. Καμία ορατή αλλαγή ακόμη — όσο ένα σημείο δεν έχει δική του ανάθεση, παίρνει κανονικά τις μετρήσεις του τύπου του (πλήρης συμβατότητα). Το UI έρχεται σε επόμενη φάση.']},
    {'v': '12.267', 'b': '548', 'date': '24/06/2026', 'time': '20:40', 'title': 'Αναίρεση συγκεντρωτικής καταχώρησης (επιστροφή στη ροή ανά σημείο)',
     'items': ['Αφαιρέθηκε η «Συγκεντρωτική καταχώρηση» της v12.266 (routes, κουμπί στη «Σήμερα», στοιχείο μενού) — θα το ξανασχεδιάσουμε. Η καταχώρηση παραμένει ανά σημείο όπως πριν.']},
    {'v': '12.266', 'b': '547', 'date': '24/06/2026', 'time': '20:25', 'title': 'Μετρήσεις: Συγκεντρωτική καταχώρηση ανά ξενοδοχείο (όλα τα σημεία με τη μία)',
     'items': ['Νέα οθόνη «Συγκεντρωτική καταχώρηση»: όλα τα σημεία ενός ξενοδοχείου (Πισίνες + Νερά Χρήσης + λοιπά) σε μία σελίδα, ομαδοποιημένα· διαλέγεις βάρδια μία φορά και πατάς «Καταχώρηση όλων». Όσα σημεία μένουν κενά αγνοούνται.',
               'Πρόσβαση από τη «Σήμερα» (κουμπί δίπλα στο ξενοδοχείο) — ιδανικό για όταν ο συντηρητής είναι στο μηχανοστάσιο και μετράει τα πάντα μαζί.']},
    {'v': '12.265', 'b': '546', 'date': '24/06/2026', 'time': '20:05', 'title': 'Καταχώρηση μέτρησης: κατευθείαν στη φόρμα όταν έρθεις από συγκεκριμένο σημείο',
     'items': ['Όταν πατάς μέτρηση από τη «Σήμερα», η φόρμα ανοίγει απευθείας στο σωστό σημείο — ο επιλογέας ξενοδοχείου/σημείων κρύβεται (φαίνεται μόνο αν μπεις χωρίς σημείο).',
               'Στον τίτλο της φόρμας μπήκε το ξενοδοχείο + σύνδεσμος «Άλλο σημείο» αν χρειαστεί να αλλάξεις.']},
    {'v': '12.264', 'b': '545', 'date': '24/06/2026', 'time': '19:50', 'title': 'Σήμερα: πολλές μετρήσεις/μέρα ανά σημείο + τίτλοι ενοτήτων από το όνομα template',
     'items': ['Κάθε σημείο έχει πλέον κουμπί «+ Νέα μέτρηση» (πέρα από τις βάρδιες) ώστε ο συντηρητής να καταχωρεί όσες φορές χρειάζεται μέσα στη μέρα· εμφανίζεται και μετρητής «N σήμερα».',
               'Οι τίτλοι των ενοτήτων (π.χ. «Πισίνες», «Νερά Χρήσης») προέρχονται πλέον από το όνομα του αντίστοιχου template — μετονομάζεις από «Μετρήσεις · Ρυθμίσεις → Παράμετροι».']},
    {'v': '12.263', 'b': '544', 'date': '24/06/2026', 'time': '19:35', 'title': 'Σήμερα (Μετρήσεις): καθαρή ροή συντηρητή — οργανωμένα σημεία, χωρίς κουμπιά',
     'items': ['Αφαιρέθηκαν τα κουμπιά «Νέα καταχώρηση» & «Δήλωση βλάβης» από τη «Σήμερα» — η ροή είναι: ανοίγεις, βλέπεις τα σημεία του ξενοδοχείου σου και πατάς τη βάρδια (Πρωί/Απόγευμα) για να καταχωρήσεις.',
               'Τα σημεία εμφανίζονται πλέον οργανωμένα σε ενότητες: Πισίνες · Νερά Χρήσης (όλα τα ΖΝΧ μαζί) · Τομείς/λοιπά, ανά ξενοδοχείο.']},
    {'v': '12.262', 'b': '543', 'date': '24/06/2026', 'time': '19:20', 'title': 'Log Μετρήσεων: polish + πλήρες πλάτος',
     'items': ['Ανανεωμένη εμφάνιση: επικεφαλίδα σελίδας, καθαρή μπάρα (tabs + export + Νέα καταγραφή), πίνακας με sticky κεφαλίδα, hover, στρογγυλεμένα badges & εικονίδια ενεργειών. Η σελίδα είναι πλέον πλήρους πλάτους.']},
    {'v': '12.261', 'b': '542', 'date': '24/06/2026', 'time': '19:05', 'title': 'Διορθώσεις: /records crash (κλειδί «values») + ώρα Διαγνωστικών σε ώρα Αθήνας',
     'items': ['Fix: η λίστα «Καταγραφές - Μετρήσεις» έσκαγε γιατί το κλειδί δεδομένων «values» συγκρουόταν με τη μέθοδο dict.values στη Jinja — μετονομάστηκε. Οι τιμές εμφανίζονται κανονικά.',
               'Fix: στα «Διαγνωστικά», η στήλη «Ώρα» του log σφαλμάτων δείχνει πλέον ώρα Αθήνας (φίλτρο |gr) αντί UTC.']},
    {'v': '12.260', 'b': '541', 'date': '24/06/2026', 'time': '20:45', 'title': 'Καταγραφές - Μετρήσεις: εμφάνιση των τιμών κάθε καταχώρησης',
     'items': ['Στη λίστα «Καταγραφές - Μετρήσεις» (/records) προστέθηκε στήλη «Μετρήσεις» που δείχνει τις πραγματικές τιμές κάθε καταχώρησης (π.χ. Ελ. χλώριο 0.8 mg/L · pH 7.2 · Θερμοκρασία 26°C) — για μηχανή (Reading) και legacy πισίνες/νερά.']},
    {'v': '12.259', 'b': '540', 'date': '24/06/2026', 'time': '20:15', 'title': 'Βλάβες · Ρυθμίσεις: κύριες κατηγορίες σε οριζόντιες μπάντες',
     'items': ['Στο board κατηγοριών, κάθε κύριος κλάδος είναι πλέον οριζόντια μπάντα πλήρους πλάτους και οι υποκατηγορίες παρατάσσονται πλάι-πλάι μέσα του (αντί για στενές κατακόρυφες στήλες). Το drag & drop παραμένει ίδιο.']},
    {'v': '12.258', 'b': '539', 'date': '24/06/2026', 'time': '20:00', 'title': 'Βλάβες · Ρυθμίσεις: γενικό polish (τυπογραφία, κάρτες, badges, hover)',
     'items': ['Ανανεωμένη εμφάνιση: επικεφαλίδα σελίδας, κάρτες με σκιές & χρωματικό accent, badges πλήθους ανά στήλη/κάρτα, hover/focus εφέ, καθαρά κενά states.',
               'Τα μικρά πάνελ (Ειδικότητες / SLA / Tags) μπήκαν σε grid πλάι-πλάι. Όλη η λειτουργικότητα drag & drop παραμένει ίδια.']},
    {'v': '12.257', 'b': '538', 'date': '24/06/2026', 'time': '19:30', 'title': 'Βλάβες · Ρυθμίσεις: καθαρή δομή τύπου «Διαμόρφωση μενού» (στήλες → κάρτες → chips)',
     'items': ['Ξαναστήθηκε το board κατηγοριών με το ίδιο μοτίβο που ξέρεις: στήλη = κύριος κλάδος, κάρτα = υποκατηγορία, chip = επιμέρους. Σύρε chips ανάμεσα σε κάρτες, κάρτες ανάμεσα σε στήλες, στήλες για σειρά — όλα με λαβές. «Αποθήκευση δομής».',
               'Πιο ξεκάθαρα drop targets από την προηγούμενη έκδοση (τέλος τα φωλιασμένα zones). Ο χάρτης Κατηγορία→Ειδικότητα παραμένει drag & drop. Επαληθεύτηκε το template (jinja parse).']},
    {'v': '12.256', 'b': '537', 'date': '24/06/2026', 'time': '19:00', 'title': 'Βλάβες · Ρυθμίσεις: πλήρες πλάτος + drag & drop για κατηγορίες & χάρτη ειδικοτήτων',
     'items': ['Η σελίδα έγινε πλήρους πλάτους. Νέο board «Κατηγορίες βλαβών»: κύριοι κλάδοι = στήλες, υποκατηγορίες = κάρτες· σύρε για αναδιάταξη ή μετακίνηση σε άλλον κλάδο (αλλάζει αυτόματα parent/level). «Αποθήκευση δομής».',
               'Ο «Χάρτης Κατηγορία → Ειδικότητα» έγινε drag & drop: σύρε κατηγορίες από τα «Διαθέσιμα» στις στήλες ειδικοτήτων, × για αφαίρεση, «Αποθήκευση χάρτη».',
               'Διατηρήθηκαν ανέπαφες οι ενότητες Ειδικότητες/SLA/Tags και η υπάρχουσα ομαδοποίηση. Προσθήκη/μετονομασία/ενεργό-ανενεργό κατηγοριών inline.']},
    {'v': '12.255', 'b': '536', 'date': '24/06/2026', 'time': '18:20', 'title': 'Κατηγορίες βλαβών: το περιεχόμενο ενσωματώθηκε στις «Βλάβες · Ρυθμίσεις»',
     'items': ['Στις «Βλάβες · Ρυθμίσεις» εμφανίζεται πλέον ΟΛΟ το δέντρο κατηγοριών + φόρμα «Νέα κατηγορία» (προσθήκη/μετονομασία/ενεργοποίηση) — όχι σύνδεσμος.',
               'Η παλιά σελίδα /dashboard/faults/categories ανακατευθύνει εκεί· οι ενέργειες επιστρέφουν στην ίδια οθόνη.']},
    {'v': '12.254', 'b': '535', 'date': '24/06/2026', 'time': '18:00', 'title': 'Templates: το περιεχόμενο ενσωματώθηκε στο «Μετρήσεις · Ρυθμίσεις → Παράμετροι»',
     'items': ['Στο tab «Παράμετροι» εμφανίζεται πλέον ΟΛΟΣ ο πίνακας τύπων παρακολούθησης (με Επεξεργασία ανά τύπο) + φόρμα «Νέος τύπος» — όχι απλός σύνδεσμος.',
               'Η παλιά σελίδα /dashboard/templates ανακατευθύνει εκεί. Η επεξεργασία παραμέτρων ενός τύπου γίνεται όπως πριν (ίδια οθόνη).']},
    {'v': '12.253', 'b': '534', 'date': '24/06/2026', 'time': '17:40', 'title': 'Μενού: κατάργηση ξεχωριστού «Κατηγορίες βλαβών» — μέσα από Βλάβες · Ρυθμίσεις',
     'items': ['Αφαιρέθηκε το ξεχωριστό στοιχείο «Κατηγορίες βλαβών». Ανοίγει πλέον μέσα από «Βλάβες · Ρυθμίσεις» (νέο κουμπί «Άνοιγμα Κατηγορίες βλαβών» — ορατό και σε embed, σε αντίθεση με τον παλιό σύνδεσμο που κρυβόταν).',
               'Για να φύγει από αποθηκευμένο μενού: «Διαμόρφωση μενού → Επαναφορά προεπιλογής» ή σύρε το στα Κρυμμένα + Αποθήκευση.']},
    {'v': '12.252', 'b': '533', 'date': '24/06/2026', 'time': '17:20', 'title': 'Μενού: κατάργηση ξεχωριστού «Templates» — μέσα από Μετρήσεις · Ρυθμίσεις → Παράμετροι',
     'items': ['Αφαιρέθηκε το ξεχωριστό στοιχείο «Templates» από το μενού. Η ίδια κονσόλα ανοίγει πλέον από «Μετρήσεις · Ρυθμίσεις → Παράμετροι → Άνοιγμα Templates» (υπήρχε ήδη ο εσωτερικός σύνδεσμος).',
               'Για να φύγει εντελώς από το αποθηκευμένο μενού, κάνε «Διαμόρφωση μενού → Επαναφορά προεπιλογής» (ή απλώς σύρε το στα Κρυμμένα και Αποθήκευση).']},
    {'v': '12.251', 'b': '532', 'date': '24/06/2026', 'time': '17:00', 'title': 'Μενού admin: κάθε στοιχείο μόνο στο δικό του workspace (όχι διπλά στο Admin)',
     'items': ['Διόρθωση: στο μενού του admin τα στοιχεία του Operations (π.χ. Υποδοχή → Ερωτηματολόγια) δεν εμφανίζονται πλέον και μέσα στο workspace Admin. Ο admin βλέπει τα πάντα, αλλά το καθένα στο workspace όπου το έβαλες.']},
    {'v': '12.250', 'b': '531', 'date': '24/06/2026', 'time': '16:45', 'title': 'Διαμόρφωση μενού: μετονομασία κονσολών (κλικ στο όνομα)',
     'items': ['Κάθε στοιχείο έχει πλέον λαβή ⠿ για σύρσιμο και το όνομά του γίνεται επεξεργάσιμο με κλικ — όπως ακριβώς οι τίτλοι των ομάδων. Το όνομα που βάζεις εμφανίζεται στο μενού όλων.']},
    {'v': '12.249', 'b': '530', 'date': '24/06/2026', 'time': '16:20', 'title': 'Μενού: το custom μενού (Διαμόρφωση) ισχύει ΠΑΝΤΑ για όλους — τέλος ο διακόπτης',
     'items': ['Αφαιρέθηκε ο διακόπτης «Εφαρμογή σε όλους τους ρόλους». Πλέον το μενού που φτιάχνεις στη «Διαμόρφωση μενού» ΕΙΝΑΙ το μενού όλων: ο admin βλέπει τα πάντα, οι υπόλοιποι βλέπουν μόνο όσα στοιχεία έχουν τον ρόλο τους στο ⚙.',
               'Προσοχή: ρόλος χωρίς κανένα στοιχείο → κενό μενού. Φρόντισε κάθε ρόλος να έχει τα στοιχεία που του αναλογούν. Πάτα Αποθήκευση για να ενεργοποιηθεί.']},
    {'v': '12.248', 'b': '529', 'date': '24/06/2026', 'time': '16:00', 'title': 'Διαμόρφωση μενού: κουμπί ✕ σε κάθε στοιχείο → στα Κρυμμένα',
     'items': ['Κάθε στοιχείο μέσα σε ομάδα έχει πλέον ✕ — με ένα κλικ κατεβαίνει στα «Κρυμμένα / Διαθέσιμα» και δεν εμφανίζεται πουθενά (μέχρι να το ξανασύρεις σε ομάδα).']},
    {'v': '12.247', 'b': '528', 'date': '24/06/2026', 'time': '15:40', 'title': 'Διαμόρφωση μενού: σύρε τα workspaces για να τα βάλεις σε όποια σειρά θες',
     'items': ['Κάθε στήλη workspace έχει λαβή (⠿) στην κεφαλίδα της — σύρε για να αναδιατάξεις τις στήλες δίπλα-δίπλα και να κάνεις τις συγκρίσεις που θες. Η σειρά θυμάται.']},
    {'v': '12.246', 'b': '527', 'date': '24/06/2026', 'time': '15:20', 'title': 'Διαμόρφωση μενού: τα workspaces δίπλα-δίπλα σε στήλες, στοιχεία κάθετα',
     'items': ['Τα workspaces (Operations / Staff HUB / Guest App / Admin) εμφανίζονται πλέον δίπλα-δίπλα ως στήλες — βλέπεις με μια ματιά ποιος ρόλος βλέπει τι.',
               'Τα στοιχεία μέσα σε κάθε ομάδα είναι σταθερά κάθετα (το ένα κάτω από το άλλο). Αφαιρέθηκε ο διακόπτης διάταξης.']},
    {'v': '12.245', 'b': '526', 'date': '24/06/2026', 'time': '15:00', 'title': 'Διαμόρφωση μενού: διακόπτης «Κάθετα / Πλάι-πλάι» ανά workspace',
     'items': ['Κάθε workspace έχει πλέον στην κεφαλίδα του κουμπί «↕ Κάθετα / ↔ Πλάι-πλάι» που βάζει τα στοιχεία σε μία στήλη (το ένα κάτω από το άλλο) ή τα αφήνει να ρέουν πλάι-πλάι. Η επιλογή θυμάται ανά workspace.']},
    {'v': '12.244', 'b': '525', 'date': '24/06/2026', 'time': '14:30', 'title': 'Διαμόρφωση μενού: τα στοιχεία ρέουν αυτόματα & σύρε τα να τα διατάξεις',
     'items': ['Αφαιρέθηκε το κουμπί διάταξης/πλάτους από τη μπάρα. Τα στοιχεία γεμίζουν αυτόματα το πλάτος της κάθε ομάδας (auto-fit) και αναδιπλώνονται σε νέα σειρά όταν χρειάζεται.',
               'Σύρε τα στοιχεία για να τα διατάξεις όπως θες μέσα στην ομάδα ή ανάμεσα σε ομάδες/workspaces.']},
    {'v': '12.243', 'b': '524', 'date': '24/06/2026', 'time': '14:15', 'title': 'Ρόλοι: «Masteradmin / Διοίκηση» στο ⚙ μενού + πλήρης λίστα ρόλων στους Χρήστες',
     'items': ['Στη «Διαμόρφωση μενού» (⚙ ορατότητας) προστέθηκε ο ρόλος «Masteradmin / Διοίκηση».',
               'Στους Χρήστες, το πεδίο ρόλου περιλαμβάνει πλέον όλους τους ρόλους: Masteradmin/Διοίκηση, Admin, Manager, Λογιστήριο, Staff, Viewer (με αντίστοιχα badges).']},
    {'v': '12.242', 'b': '523', 'date': '24/06/2026', 'time': '14:00', 'title': 'Διαμόρφωση μενού: εναλλαγή κάθετα/πλάι-πλάι + ρυθμιζόμενο πλάτος',
     'items': ['Κουμπί «↔ Πλάι-πλάι / ↕ Κάθετα» αλλάζει τη διάταξη των στοιχείων. Στην πλάι-πλάι, slider «Πλάτος» ρυθμίζει πόσο φαρδιά είναι τα chip (πόσα χωράνε ανά σειρά).',
               'Η επιλογή σου (διάταξη + πλάτος) θυμάται για την επόμενη φορά.']},
    {'v': '12.241', 'b': '522', 'date': '24/06/2026', 'time': '13:45', 'title': 'Διαμόρφωση μενού: τα στοιχεία (chips) σε κάθετη διάταξη',
     'items': ['Στη «Διαμόρφωση μενού» τα στοιχεία εμφανίζονται πλέον το ένα κάτω από το άλλο (κάθετα) μέσα σε κάθε ομάδα — πιο ευανάγνωστα.']},
    {'v': '12.240', 'b': '521', 'date': '24/06/2026', 'time': '13:30', 'title': 'Διαμόρφωση μενού: drag & drop στα Workspaces (+ υπο-ομάδες) · ρόλοι στο ⚙',
     'items': ['Η «Διαμόρφωση μενού» δείχνει πλέον τα 4 Workspaces (Operations / Staff HUB / Guest App / Admin) ως ενότητες. Σύρε κάθε στοιχείο στο workspace που ανήκει — κάθε στοιχείο σε ένα σημείο.',
               'Μέσα σε κάθε workspace φτιάχνεις ομάδες (υπο-κατηγορίες) και σύρεις στοιχεία ανάμεσά τους. Οι ομάδες μετακινούνται και ανάμεσα σε workspaces.',
               'Το ⚙ κάθε στοιχείου ορίζει πλέον ΜΟΝΟ τους ρόλους που το βλέπουν (manager/staff/λογιστήριο/viewer — πολλαπλοί). Κενό = μόνο admin. Το workspace προκύπτει από την ενότητα όπου το έβαλες.',
               'Μετά την εφαρμογή κάνε «Επαναφορά προεπιλογής» για καθαρή αφετηρία.']},
    {'v': '12.239', 'b': '520', 'date': '24/06/2026', 'time': '12:00', 'title': 'Μενού: εφαρμογή πλάνου — αυτόματη ανάθεση workspace & ρόλων σε όλα τα στοιχεία',
     'items': ['Όλα τα στοιχεία πήραν προεπιλεγμένο workspace + ρόλους κατά το πλάνο (Operations=εργαλεία δουλειάς· Staff HUB=σημείο εργαζομένου· Admin=διοίκηση/πλατφόρμα).',
               'Η «Διαμόρφωση μενού → Επαναφορά προεπιλογής» τα εφαρμόζει όλα μαζί (καθαρό layout + workspace/ρόλοι). Ο διακόπτης «master για όλα» μένει κλειστός μέχρι να τον ανοίξεις.']},
    {'v': '12.238', 'b': '519', 'date': '24/06/2026', 'time': '10:30', 'title': 'Μενού: αναδιοργάνωση + ορατότητα ανά workspace & ρόλο (master = admin)',
     'items': ['Καθαρότερη προεπιλεγμένη δομή μενού (μικρές εστιασμένες ομάδες· Μετρήσεις όλες μαζί). Υιοθέτησέ τη με «Διαμόρφωση μενού → Επαναφορά προεπιλογής».',
               'Στη «Διαμόρφωση μενού», κάθε στοιχείο έχει ⚙ για ορισμό σε ποια workspaces & ρόλους εμφανίζεται. Φτιάχνεις το master (admin) και διαλέγεις τι βγαίνει αλλού.',
               'Διακόπτης «Χρήση αυτού του μενού για ΟΛΑ τα workspaces/ρόλους» — κλειστός by default (καμία αλλαγή για τους υπόλοιπους μέχρι να τον ανοίξεις).']},
    {'v': '12.237', 'b': '518', 'date': '24/06/2026', 'time': '09:00', 'title': 'Συντήρηση — καθάρισμα: απόσυρση παλιάς «Σήμερα» & «Εβδομαδιαίας κάλυψης»· tile «Μετρήσεις σήμερα» από τη μηχανή',
     'items': ['Η παλιά «Σήμερα» (/katagrafes) και η «Εβδομαδιαία κάλυψη» αποσύρθηκαν — οδηγούν πλέον στη νέα «Σήμερα» και στα Στατιστικά (ενότητα Κάλυψη).',
               'Το tile «Μετρήσεις πισινών/νερών σήμερα» στο Dashboard μετρά πλέον από τη νέα μηχανή (Reading) — όχι από τις παγωμένες παλιές δομές.']},
    {'v': '12.236', 'b': '517', 'date': '23/06/2026', 'time': '23:40', 'title': 'Πρόγραμμα: Ctrl+κλικ μπλοκ κελιών (εβδομαδιαίο & μηνιαίο)',
     'items': ['Ctrl+κλικ σε πολλά κελιά χτίζει μπλοκ επιλογής (μη συνεχόμενα) χωρίς να ανοίγει αμέσως ο editor. Εμφανίζεται κουμπί «Επεξεργασία N κελιών» (ή Enter) για μαζική επεξεργασία.',
               'Το σύρσιμο (drag) για ορθογώνιο μπλοκ συνεχίζει να δουλεύει όπως πριν.']},
    {'v': '12.235', 'b': '516', 'date': '23/06/2026', 'time': '23:15', 'title': 'Πρόγραμμα · Μηνιαίο: φίλτρο τμημάτων πολλαπλής επιλογής (παράλληλη προβολή)',
     'items': ['Στο μηνιαίο πλάνο, το φίλτρο τμημάτων έγινε λίστα με checkboxes: μπορείς να επιλέξεις πολλά τμήματα ταυτόχρονα και να τα βλέπεις παράλληλα (αντί για ένα-ένα).']},
    {'v': '12.234', 'b': '515', 'date': '23/06/2026', 'time': '22:45', 'title': 'Στατιστικά μετρήσεων: Excel + φίλτρο ξενοδοχείου + περίοδοι (μέρα/εβδ/μήνα/έτος) + κάλυψη· απόσυρση Πινάκων Πισινών/Νερών',
     'items': ['Export σε Excel (εκτυπώσιμο) — π.χ. φιλτράρεις «SRG» και βγάζεις έτοιμη αναφορά «πώς πάνε οι μετρήσεις».',
               'Φίλτρο ξενοδοχείου + γρήγορη επιλογή περιόδου: Ημέρα / Εβδομάδα / Μήνας / Έτος.',
               'Νέα ενότητα «Κάλυψη»: ανά περίοδο δείχνει τι μετρήθηκε και τι λείπει (αντικαθιστά την «Εβδομαδιαία κάλυψη»).',
               'Αποσύρθηκαν τελείως οι Πίνακες Πισινών & Νερών (οδηγούν πλέον στα ενιαία Στατιστικά).']},
    {'v': '12.233', 'b': '514', 'date': '23/06/2026', 'time': '22:00', 'title': 'Μετρήσεις — πλουσιότερα στατιστικά (KPIs + γραφήματα) + αφαίρεση παλιών φορμών από μενού',
     'items': ['Στατιστικά: προστέθηκαν δείκτες (KPIs) — σύνολο μετρήσεων, % συμμόρφωσης, εκτός ορίων, σημεία — και γραφήματα (συμμόρφωση ανά σημείο, εκτός ορίων ανά παράμετρο), μαζί με τους πίνακες & export CSV.',
               'Καθάρισμα: οι παλιές φόρμες «Πισίνες» & «Νερά Χρήσης» αφαιρέθηκαν εντελώς από τα μενού (παραμένει η ενιαία «Καταγραφή μετρήσεων»).']},
    {'v': '12.232', 'b': '513', 'date': '23/06/2026', 'time': '21:30', 'title': 'Μετρήσεις Φ4 — Στατιστικά ανά σημείο & παράμετρο (+ export CSV)',
     'items': ['Νέα οθόνη «Μετρήσεις · Στατιστικά»: ανά σημείο/παράμετρο για επιλεγμένη περίοδο — πλήθος, μέσος, ελάχ/μέγ, εκτός ορίων, % συμμόρφωσης.',
               'Export σε CSV. Διαβάζει από την ενιαία μηχανή (όλες οι μετρήσεις, ιστορικές + νέες).']},
    {'v': '12.231', 'b': '512', 'date': '23/06/2026', 'time': '21:00', 'title': 'Μετρήσεις Φ3c-3 — πάγωμα παλιών φορμών· πλήρης μετάβαση στη νέα ροή',
     'items': ['Οι παλιές φόρμες Πισίνες/Νερά Χρήσης αποσύρθηκαν: ανοίγοντάς τες σε πάει στη νέα «Καταγραφή μετρήσεων» (καμία νέα εγγραφή στις παλιές δομές).',
               'Αφαιρέθηκαν από το μενού του προσωπικού (μένει η ενιαία «Καταγραφή μετρήσεων» + «Σήμερα»). Τα ιστορικά δεδομένα παραμένουν διαθέσιμα.',
               'Η μετάβαση ολοκληρώθηκε: υποβολή, «Σήμερα», κονσόλα και ενέργειες όλα στη νέα μηχανή.']},
    {'v': '12.230', 'b': '511', 'date': '23/06/2026', 'time': '20:30', 'title': 'Πρόγραμμα: managers σε κλειδωμένες εβδομάδες (ρύθμιση) + μετρήσεις (ώρα/καθάρισμα φόρμας)',
     'items': ['Πρόγραμμα · Ρυθμίσεις: νέα επιλογή «Managers επεξεργάζονται κλειδωμένες/ληγμένες εβδομάδες» (Όχι/Ναι). Με «Ναι», οι managers μπορούν να αλλάζουν και κλειδωμένες εβδομάδες (η αλλαγή δημιουργεί νέα έκδοση, όπως πάντα).',
               'Μετρήσεις: η ώρα στις «Πρόσφατες» της φόρμας διορθώθηκε (ώρα Ελλάδος). Αφαιρέθηκε το πάνελ «ενέργειες τελευταίας καταγραφής» από τη φόρμα — μένουν οι ζωντανές ενέργειες καθώς πληκτρολογείς.']},
    {'v': '12.229', 'b': '510', 'date': '23/06/2026', 'time': '20:00', 'title': 'Μετρήσεις — ζωντανές προτεινόμενες ενέργειες στη φόρμα (καθώς πληκτρολογείς)',
     'items': ['Στη φόρμα καταχώρησης, οι προτεινόμενες διορθωτικές ενέργειες εμφανίζονται πλέον ΖΩΝΤΑΝΑ μόλις βάλεις τιμή εκτός ορίου — χωρίς να χρειάζεται να πατήσεις «Καταχώρηση».']},
    {'v': '12.228', 'b': '509', 'date': '23/06/2026', 'time': '19:30', 'title': 'Μετρήσεις Φ3c-2b — νέα «Σήμερα» (engine): σημεία ανά περιοχή + status ημέρας',
     'items': ['Η «Σήμερα» δείχνει πλέον τα σημεία ανά περιοχή με κατάσταση ημέρας (πράσινο=έγινε / διακεκομμένο=εκκρεμεί) ανά περίοδο· tap → φόρμα καταχώρησης.',
               'Πάνελ «Εκτός ορίων σήμερα» με προτεινόμενες ενέργειες + γρήγορα κουμπιά. Scope στα ξενοδοχεία του χρήστη. (Η παλιά «Σήμερα» μένει ως εφεδρεία.)']},
    {'v': '12.227', 'b': '508', 'date': '23/06/2026', 'time': '19:00', 'title': 'Μετρήσεις Φ3c-2 — το προσωπικό αποκτά τη νέα φόρμα «Καταγραφή μετρήσεων»',
     'items': ['Νέο στοιχείο μενού «Καταγραφή μετρήσεων» για προσωπικό & managers (η νέα φόρμα ανά περιοχή).',
               'Πρόσβαση με δικαίωμα καταγραφής (can_log), με αυτόματο περιορισμό στα ξενοδοχεία του χρήστη. Οι παλιές φόρμες μένουν προς το παρόν ως εφεδρεία (πάγωμα στη Φ3c-3).']},
    {'v': '12.226', 'b': '507', 'date': '23/06/2026', 'time': '18:30', 'title': 'Διόρθωση ώρας — όλες οι ώρες σε ώρα Ελλάδος (UTC→Αθήνα στην εμφάνιση)',
     'items': ['Οι ώρες αποθηκεύονται UTC από τον server· τώρα μετατρέπονται σε ώρα Ελλάδος (Europe/Athens, με θερινή ώρα) τη στιγμή της εμφάνισης — διορθώνει το −3 ωρών σε ΟΛΕΣ τις οθόνες/εξαγωγές (Καταγραφές, πίνακες, βλάβες κ.λπ.).']},
    {'v': '12.225', 'b': '506', 'date': '23/06/2026', 'time': '18:00', 'title': 'Μετρήσεις Φ3c-1 — η κονσόλα «Καταγραφές» δείχνει & τις νέες καταχωρήσεις της μηχανής',
     'items': ['Η κονσόλα «Καταγραφές - Μετρήσεις» εμφανίζει πλέον ΚΑΙ τις καταχωρήσεις από τη νέα φόρμα (ανά περιοχή), μαζί με τις παλιές — χωρίς διπλά.',
               'Καθαρά reads — οι υποβολές του προσωπικού δεν αλλάζουν ακόμη (Φ3c-2). Η διαγραφή δρομολογείται σωστά ανά πηγή (legacy ή μηχανή).']},
    {'v': '12.224', 'b': '505', 'date': '23/06/2026', 'time': '17:20', 'title': 'Hotfix: φόρμα/κονσόλα μετρήσεων (σφάλμα ομαδοποίησης ανά ξενοδοχείο)',
     'items': ['Διορθώθηκε σφάλμα («not iterable») στη φόρμα καταχώρησης & στην κονσόλα σημείων που εμφανιζόταν κατά την ομαδοποίηση ανά ξενοδοχείο.']},
    {'v': '12.223', 'b': '504', 'date': '23/06/2026', 'time': '17:00', 'title': 'Μετρήσεις Φ3b-2 — σημεία ανά περιοχή + ΕΝΙΑΙΑ κονσόλα ρυθμίσεων + φόρμα με ενέργειες',
     'items': ['Όλες οι ρυθμίσεις μετρήσεων σε ΜΙΑ κονσόλα (Admin → «Μετρήσεις · Ρυθμίσεις») με καρτέλες: Σημεία · Περίοδοι · Παράμετροι · Μεταφορά.',
               'Σημεία ανά περιοχή (Option B): κουμπί «Αυτο-δημιουργία» σπάει το ΖΝΧ σε Δεξαμενή/Κουζίνα/Απομακρυσμένο/Αναχ.-Επιστρ./Αντ.Όσμωση + σημείο ανά πισίνα. Rename/ενεργοποίηση/νέο σημείο.',
               'Φόρμα καταχώρησης: επιλογέας ξενοδοχείου + σημεία ανά περιοχή· μετά την υποβολή εμφανίζονται οι προτεινόμενες διορθωτικές ενέργειες (όρια→ενέργειες).',
               'Καθαρά προσθετικό — οι φόρμες του προσωπικού (Πισίνες/Νερά) παραμένουν ως έχουν μέχρι τη Φ3c.']},
    {'v': '12.222', 'b': '503', 'date': '23/06/2026', 'time': '16:00', 'title': 'Μετρήσεις Φ3b — δοκιμαστική ενιαία φόρμα καταχώρησης (admin)',
     'items': ['Νέα οθόνη (Admin → «Μετρήσεις · Καταχώρηση (δοκιμή)»): επιλέγεις σημείο (πισίνα/δίκτυο), περίοδο και τιμές — η νέα generic φόρμα οδηγείται από τις παραμέτρους & περιόδους που όρισες.',
               'Γράφει στη νέα ενιαία δομή (Reading) και δείχνει τις πρόσφατες καταγραφές του σημείου. ΔΟΚΙΜΑΣΤΙΚΟ/admin — δεν αλλάζει τις φόρμες του προσωπικού (το switch γίνεται στη Φ3c).']},
    {'v': '12.221', 'b': '502', 'date': '23/06/2026', 'time': '15:30', 'title': 'Μετρήσεις Φ3a — οθόνη «Περίοδοι/Βάρδιες» ανά τύπο (admin)',
     'items': ['Νέα οθόνη (Admin → «Μετρήσεις · Περίοδοι»): ορίζεις εσύ τις περιόδους μέτρησης ανά τύπο (Πισίνα/ΖΝΧ/…) — Πρωί/Απόγευμα/Βράδυ/ό,τι θες, με ώρα και σειρά.',
               'Προσθήκη/επεξεργασία/διαγραφή περιόδου. Η διαγραφή δεν θίγει παλιές καταγραφές. Καθαρά προσθετικό — δεν αλλάζει τις τωρινές φόρμες/κονσόλα (έρχεται στη Φ3b).']},
    {'v': '12.220', 'b': '501', 'date': '23/06/2026', 'time': '15:00', 'title': 'Μενού: «Μετρήσεις · Μεταφορά (Φ2)» και στο προσαρμοσμένο μενού',
     'items': ['Η οθόνη μεταφοράς μπήκε στον κατάλογο του προσαρμοσμένου μενού (Διαμόρφωση μενού) — εμφανίζεται αυτόματα στην ομάδα «🆕 Νέα» για όσους χρησιμοποιούν custom μενού (πριν φαινόταν μόνο στο προεπιλεγμένο μενού).']},
    {'v': '12.219', 'b': '500', 'date': '23/06/2026', 'time': '14:30', 'title': 'Hotfix: εκκίνηση εφαρμογής (seed μετρήσεων σε app context)',
     'items': ['Διορθώθηκε σφάλμα εκκίνησης της 12.218: το seed της μηχανής μετρήσεων έτρεχε εκτός application context και μπλόκαρε το boot. Τυλίχτηκε σε app context (όπως τα υπόλοιπα modules).']},
    {'v': '12.218', 'b': '499', 'date': '23/06/2026', 'time': '14:00', 'title': 'Συντήρηση/Μετρήσεις — Φ2: μεταφορά παλιών καταγραφών στη νέα δομή (admin)',
     'items': ['Νέα οθόνη (Διαχείριση → Συντήρηση → «Μετρήσεις · Μεταφορά (Φ2)»): δημιουργεί σημεία μέτρησης από τις πισίνες/δίκτυα και ΑΝΤΙΓΡΑΦΕΙ τις παλιές καταγραφές στην ενιαία μηχανή.',
               'Ασφαλές: αντιγραφή (τα παλιά μένουν ως έχουν), idempotent (ξανατρέχει χωρίς διπλά), με μετρητές ελέγχου. Πάρε backup πριν.',
               'Τα νέα σημεία/αντίγραφα είναι κρυφά από τις υπάρχουσες οθόνες μέχρι την ενοποίηση UI (Φ3) — καμία ορατή αλλαγή στη ροή σου.']},
    {'v': '12.217', 'b': '498', 'date': '23/06/2026', 'time': '13:00', 'title': 'Συντήρηση/Μετρήσεις — Φ1 ενοποίησης (παρασκήνιο, καμία ορατή αλλαγή)',
     'items': ['Θεμελίωση ενιαίας μηχανής μετρήσεων: προστέθηκαν τα templates «Πισίνα» & «ΖΝΧ/Δίκτυο νερού» με τις παραμέτρους/όρια/ενέργειες που χρησιμοποιεί ήδη η εφαρμογή.',
               'Νέο: παραμετροποιήσιμες περίοδοι/βάρδιες ανά template (αρχικά Πρωί/Απόγευμα). Θα ρυθμίζονται από οθόνη διαχείρισης σε επόμενη φάση.',
               'Καθαρά προσθετικό — οι υπάρχουσες φόρμες/κονσόλα/πίνακες δουλεύουν ακριβώς όπως πριν. Καμία μετακίνηση δεδομένων.']},
    {'v': '12.216', 'b': '497', 'date': '23/06/2026', 'time': '12:00', 'title': 'Καταγραφές - Μετρήσεις: ένα κουμπί «Νέα καταγραφή» + όλες οι καταγραφές',
     'items': ['Το «Νέα καταγραφή» έγινε ΕΝΑ κουμπί που ανοίγει επιλογέα (Πισίνες · Νερά Χρήσης · Τομείς · ό,τι προστεθεί) και σε πάει στη φόρμα υποβολής.',
               'Η κονσόλα δείχνει πλέον ΟΛΕΣ τις καταγραφές που κάνει το προσωπικό (όχι δείγμα)· για παλαιότερες/συγκεκριμένη περίοδο, χρησιμοποίησε τον επιλογέα περιόδου.']},
    {'v': '12.215', 'b': '496', 'date': '23/06/2026', 'time': '11:30', 'title': 'Συντήρηση: «Καταγραφές - Μετρήσεις» — μία κονσόλα για όλες τις καταγραφές',
     'items': ['Το «Records» μετονομάστηκε σε «Καταγραφές - Μετρήσεις» και έγινε κεντρική κονσόλα: από εδώ διαχειρίζεσαι όλες τις καταγραφές μαζί.',
               'Νέο tab «Τομείς» δίπλα σε Πισίνες & Νερά Χρήσης (Όλα / Πισίνες / Νερά / Τομείς) — προβολή, διόρθωση όπου υπάρχει, και διαγραφή (admin).',
               'Κουμπιά «Νέα καταγραφή» στην κορυφή (Πισίνες · Νερά Χρήσης · Τομείς) ώστε καταχώρηση και διαχείριση να γίνονται από ένα μέρος.',
               'Ό,τι νέος τύπος καταγραφής έρθει στο μέλλον θα συγκεντρώνεται κι αυτός εδώ.']},
    {'v': '12.214', 'b': '495', 'date': '23/06/2026', 'time': '10:30', 'title': 'Μηνιαίο: «Συμπαγές» κρύβει ώρες (μόνο κωδικός) + διόρθωση παγωμένης κεφαλίδας',
     'items': ['Το κουμπί «Συμπαγή» δείχνει πλέον μόνο τον κωδικό βάρδιας (κρύβονται οι ώρες) για πιο πυκνή εικόνα.',
               'Διορθώθηκε η παγωμένη κεφαλίδα ημερών: το μηνιαίο grid κάνει εσωτερικό scroll (max-height) ώστε οι ημερομηνίες και η στήλη ονόματος να μένουν σταθερές καθώς σκρολάρεις.']},
    {'v': '12.213', 'b': '494', 'date': '23/06/2026', 'time': '10:00', 'title': 'Μηνιαίο πλάνο: υποβολή μήνα, export Excel, σύνολα/μέρα, sticky, σήμερα, πυκνότητα, φίλτρο dropdown',
     'items': ['«Υποβολή μήνα»: υποβάλλει με ένα κουμπί όλες τις εβδομάδες του μήνα (που έχουν δεδομένα και περνούν τους κανόνες). «Excel»: εξαγωγή του μηνιαίου πλάνου (εργαζόμενοι × μέρες) σε αρχείο.',
               'Footer «Δουλεύουν / μέρα» (κάλυψη ανά ημέρα + ρεπό), παγωμένη κεφαλίδα ημερών στο κάθετο scroll, σήμανση σημερινής μέρας, κουμπί «Συμπαγή» για πυκνότερη προβολή.',
               'Το φίλτρο τμημάτων μπήκε ως dropdown μέσα στο toolbar του μηνιαίου (μαζί με ξενοδοχείο/μήνα) αντί για chips από πάνω.']},
    {'v': '12.212', 'b': '493', 'date': '22/06/2026', 'time': '20:00', 'title': 'Μηνιαίο πλάνο: φίλτρο τμημάτων',
     'items': ['Το φίλτρο τμημάτων (chips «Όλα» + τμήματα) εμφανίζεται πλέον και στο μηνιαίο πλάνο και φιλτράρει την ενιαία λίστα προσωπικού ανά τμήμα — όχι μόνο στην εβδομαδιαία.']},
    {'v': '12.211', 'b': '492', 'date': '22/06/2026', 'time': '19:30', 'title': 'Πρόγραμμα: σπαστές βάρδιες σε 2 γραμμές',
     'items': ['Στα τσιπ του προγράμματος, οι σπαστές βάρδιες (πολλά διαστήματα ωρών) εμφανίζονται πλέον σε ξεχωριστές γραμμές (π.χ. «07:00 - 11:15 &» / «19:00 - 23:15») αντί για μία μεγάλη γραμμή — πιο ευανάγνωστο σε εβδομαδιαία και μηνιαία.']},
    {'v': '12.210', 'b': '491', 'date': '22/06/2026', 'time': '19:00', 'title': 'Πρόγραμμα: επικόλληση ΜΠΛΟΚ πολλών κελιών από Excel',
     'items': ['Διάλεξε ένα κελί-αφετηρία και κάνε Ctrl+V: ολόκληρο το μπλοκ που αντέγραψες από το Excel πέφτει στα κελιά (γραμμές→εργαζόμενοι, στήλες→μέρες), όπως ακριβώς στο Excel.',
               'Μία τιμή εξακολουθεί να γεμίζει όλα τα επιλεγμένα κελιά. Κλειδωμένες εβδομάδες προσπερνώνται. Κάθε κελί γίνεται parse ξεχωριστά (ΕΡΓ/ΕΩ/ΑΝ).']},
    {'v': '12.209', 'b': '490', 'date': '22/06/2026', 'time': '18:30', 'title': 'Πρόγραμμα: επικόλληση ΑΠΟ Excel + αντιγραφή που μένει ενεργή',
     'items': ['Ctrl+V επικολλά πλέον και κείμενο από Excel πάνω στα κελιά: αναγνωρίζει αυτόματα κωδικό/ώρες (π.χ. «ΕΡΓ 07:00-15:30», «ΑΝ», 12ωρο→ΕΩ)· μία τιμή γεμίζει όλα τα επιλεγμένα, πολλές κατά σειρά· τυχόν στήλη ονόματος αγνοείται.',
               'Η αντιγραφή (Ctrl+C) μένει ενεργή για ΠΟΛΛΑΠΛΑ paste μέχρι να αντιγράψεις κάτι άλλο (δεν χάνεται στο reload). Η εσωτερική αντιγραφή βάρδιας κρατά πλήρη στοιχεία· από Excel γίνεται parse.']},
    {'v': '12.208', 'b': '489', 'date': '22/06/2026', 'time': '17:45', 'title': 'Πρόγραμμα: αντιγραφή κελιών για Excel (Ctrl+C)',
     'items': ['Το Ctrl+C στο πρόγραμμα αντιγράφει πλέον τα επιλεγμένα κελιά και στο πραγματικό clipboard, σε μορφή πίνακα (όνομα + κωδικός/ώρες) — μπορείς να κάνεις επικόλληση κατευθείαν στο Excel, σε στήλες/γραμμές.',
               'Λειτουργεί και στην εβδομαδιαία και στη μηνιαία όψη· η εσωτερική αντιγραφή/επικόλληση βάρδιας σε άλλα κελιά παραμένει ως έχει.']},
    {'v': '12.207', 'b': '488', 'date': '22/06/2026', 'time': '17:15', 'title': 'Ρυθμίσεις: παραμετροποιήσιμος πίνακας κωδικών (κατηγορία + διάλειμμα)',
     'items': ['Στο Πρόγραμμα · Ρυθμίσεις, ο πίνακας «Κωδικοί βαρδιών» έχει πλέον ανά κωδικό: Κατηγορία (Εργάσιμη/Έξτρα/Ρεπό/Άδεια) και αν αφαιρείται διάλειμμα 30΄ (≥6ω).',
               'Η μηχανή ωρών (Ώρες/Έξτρα/Εργάσιμες/Ρεπό σε πρόγραμμα, μηνιαίο και μισθοδοσία) διαβάζει αυτές τις ρυθμίσεις — δεν είναι πια καρφωμένο στον κώδικα. Νέοι κωδικοί ρυθμίζονται χωρίς αλλαγή κώδικα.']},
    {'v': '12.206', 'b': '487', 'date': '22/06/2026', 'time': '16:30', 'title': 'Ώρες/Έξτρα/Εργάσιμες: σωστή λογική ανά κωδικό (ΕΡΓ/ΕΩ/ΑΝ)',
     'items': ['Ξεκαθαρίστηκε η λογική: ΕΡΓ = 8 εργάσιμες ώρες (8,5 − 30΄ διάλειμμα) + μετράει εργάσιμη μέρα. ΕΩ = όλες οι ώρες του (−30΄ αν ≥6ω) μετράνε ΣΤΙΣ ΕΞΤΡΑ, ΔΕΝ μετράει εργάσιμη. ΑΝ = ρεπό.',
               'Στήλες: «Ώρες» = μόνο ΕΡΓ· «Έξτρα» = ώρες ΕΩ· «Εργάσιμες» = πλήθος ΕΡΓ (πριν μετρούσε λάθος και τα ΕΩ). Ίδιο και στη μισθοδοσία — διορθώθηκε και το διπλομέτρημα ημερομισθίου σε σπαστές μέρες.']},
    {'v': '12.205', 'b': '486', 'date': '22/06/2026', 'time': '15:45', 'title': 'Διόρθωση έξτρα ωρών: υπολογισμός ανά μέρα (όχι ανά βάρδια)',
     'items': ['Οι έξτρα ώρες (παρουσία πάνω από 8,5/μέρα) υπολογίζονταν λάθος όταν μια μέρα είχε πολλές βάρδιες (π.χ. ΕΡΓ 8,5 + ΕΩ 3,5): κάθε βάρδια χωριστά έβγαζε 0, άρα σύνολο 0.',
               'Τώρα αθροίζεται η συνολική παρουσία της ΜΕΡΑΣ και μετά αφαιρείται το 8,5ωρο — σωστές έξτρα ώρες. Διορθώθηκε παντού: εβδομαδιαίο, μηνιαίο και υπολογισμός μισθοδοσίας (συνεπές).']},
    {'v': '12.204', 'b': '485', 'date': '22/06/2026', 'time': '15:15', 'title': 'Μηνιαίο πλάνο: ξεχωριστή καθαρή όψη, όλο το προσωπικό μαζί, ώρες ορατές',
     'items': ['Το «Μηνιαίο πλάνο» ανοίγει πλέον ως καθαρή όψη (κρύβεται το εβδομαδιαίο toolbar) με ΟΛΟ το προσωπικό σε μία ενιαία λίστα — όχι χωρισμένο ανά τμήμα.',
               'Τα κελιά δείχνουν ξανά τις ώρες της βάρδιας (ώρα + σύνολο). Προστέθηκε στήλη «Ώρες» δίπλα στις «Έξτρα», ώστε να φαίνονται καθαρά οι μηνιαίες ώρες και οι έξτρα ανά εργαζόμενο.']},
    {'v': '12.203', 'b': '484', 'date': '22/06/2026', 'time': '14:30', 'title': 'Πρόγραμμα: παγωμένη στήλη ονόματος + auto-scroll στο drag-select',
     'items': ['Διορθώθηκε η στήλη «Εργαζόμενος»: μένει σταθερή/αδιαφανής στο οριζόντιο scroll (πριν τα κελιά «περνούσαν από πάνω» — z-index + σκιά διαχωρισμού).',
               'Όταν σέρνεις πολλαπλή επιλογή και φτάνεις στη δεξιά/αριστερή άκρη, ο πίνακας κάνει αυτόματο scroll και η επιλογή επεκτείνεται μαζί — ιδανικό για το μηνιαίο grid.']},
    {'v': '12.202', 'b': '483', 'date': '22/06/2026', 'time': '14:00', 'title': 'Πρόγραμμα: πλήρες πλάτος οθόνης',
     'items': ['Το πλαίσιο του Προγράμματος Εργασίας πιάνει πλέον όλο το πλάτος της οθόνης (πριν είχε όριο 1240px και κενά αριστερά/δεξιά) — περισσότερος χώρος ειδικά για το μηνιαίο grid. Αλλαγή μόνο στο Πρόγραμμα, οι άλλες σελίδες μένουν ως έχουν.']},
    {'v': '12.201', 'b': '482', 'date': '22/06/2026', 'time': '13:30', 'title': 'Πρόγραμμα: μηνιαίο πλάνο (toggle Εβδομάδα/Μήνας στο ίδιο board)',
     'items': ['Νέο κουμπί «Μήνας» στο Πρόγραμμα: εμφανίζει όλον τον μήνα σε ΕΝΑ grid (μία γραμμή/εργαζόμενο, όλες οι μέρες ως στήλες), με τα ΙΔΙΑ κελιά και τον ίδιο editor (κλικ/σύρσιμο, πολλαπλές βάρδιες, pop-up επιστροφής).',
               'Ίδια δεδομένα με την εβδομαδιαία → αμφίδρομο αυτόματα. Σύνολα μηνιαία. Κλειδωμένες εβδομάδες δεν επεξεργάζονται. Διαθέσιμο και σε managers. Η υποβολή προς το παρόν μένει στην εβδομαδιαία όψη.']},
    {'v': '12.200', 'b': '481', 'date': '22/06/2026', 'time': '12:15', 'title': 'Πρόγραμμα: κανόνας υποβολής — όλες οι μέρες με κωδικό (διόρθωση)',
     'items': ['Ο κανόνας «πλήρης εβδομάδα» (μπλοκ υποβολής σε κενές μέρες) μετράει πλέον ΔΙΑΚΡΙΤΕΣ ημέρες — πριν μετρούσε βάρδιες, οπότε με πολλές βάρδιες/μέρα ξέφευγαν κενά.',
               'Το μήνυμα λέει τώρα ΠΟΙΕΣ μέρες λείπουν ανά εργαζόμενο (π.χ. «Σάββατο, Κυριακή»). Εφαρμόζεται σε όσους έχουν ≥1 καταχώρηση τη βδομάδα· όσοι απουσιάζουν εντελώς δεν μπλοκάρουν.']},
    {'v': '12.199', 'b': '480', 'date': '22/06/2026', 'time': '11:30', 'title': 'Πρόγραμμα: pop-up «νέα περίοδος ή συνέχεια;» στην επιστροφή',
     'items': ['Όταν ο manager βάζει βάρδια σε εργαζόμενο που επιστρέφει μετά από κενό ή σε άλλο ξενοδοχείο, εμφανίζεται pop-up με context (τελευταία βάρδια, πού ήταν, αιτία) και ρωτά: νέα περίοδος ή συνέχεια;',
               '«Συνέχεια» ενώνει το κενό (δεν σπάει περίοδο)· «νέα περίοδος» κρατά το strict σπάσιμο. Οι δηλώσεις αποθηκεύονται (SchedulePeriodMark) και τις διαβάζουν οι «Περίοδοι χρήσης» στην καρτέλα. Εμφανίζεται μόνο σε μεμονωμένη καταχώρηση, όχι σε μαζικές.']},
    {'v': '12.198', 'b': '479', 'date': '22/06/2026', 'time': '10:30', 'title': 'Καρτέλα: Περίοδοι χρήσης στο Πρόγραμμα (ανά ξενοδοχείο)',
     'items': ['Το μπλοκ «Δραστηριότητα στο Πρόγραμμα» έγινε «Περίοδοι χρήσης»: δείχνει κάθε ξεχωριστό πέρασμα του εργαζομένου στο Πρόγραμμα (Από–Έως · Μέρες), σπασμένο ανά ξενοδοχείο.',
               'Strict ανίχνευση: κάθε κενή μέρα ή αλλαγή ξενοδοχείου ξεκινά νέα περίοδο — ώστε να φαίνεται καθαρά πότε κάποιος έμεινε εκτός και ξαναμπήκε. Auto από τις βάρδιες (μαζί ρεπό/άδεια), read-only.']},
    {'v': '12.197', 'b': '478', 'date': '22/06/2026', 'time': '09:30', 'title': 'Καρτέλα: Έναρξη/Τελευταία εμφάνιση στο Πρόγραμμα (από βάρδιες)',
     'items': ['Νέο μπλοκ «Δραστηριότητα στο Πρόγραμμα» στην καρτέλα εργαζομένου: πρώτη και τελευταία ημερομηνία που εμφανίζεται στο Πρόγραμμα Εργασίας — πότε ξεκίνησε και πότε σταμάτησε να τον χρησιμοποιεί ο manager.',
               'Αυτόματα από ΟΛΕΣ τις βάρδιες (μαζί ρεπό/άδεια), read-only. Διαφορετικά από την «Πρόσληψη/Αποχώρηση» του Λογιστηρίου (Epsilon), που μένουν ως έχουν.']},
    {'v': '12.196', 'b': '477', 'date': '21/06/2026', 'time': '21:40', 'title': 'Καρτέλα: «Αναθέσεις (Management)» καθαρά ως παλιό αρχείο',
     'items': ['Η παλιά ανάθεση Management δεν εμφανίζεται πια ως «τρέχουσα»/πράσινη — γράφει «παλιό αρχείο», ώστε να μη συγχέεται με το τρέχον τμήμα/θέση που ορίζει το Οργανόγραμμα.']},
    {'v': '12.195', 'b': '476', 'date': '21/06/2026', 'time': '21:10', 'title': 'Καρτέλα: ανάθεση τμήμα/θέση/ξενοδοχείο μέσα στην Εστία + ιστορικό θέσης',
     'items': ['Νέος editor «Ανάθεση (Εστία)» στην καρτέλα: dropdowns ξενοδοχείο/τμήμα/θέση που γράφουν στο προφίλ (οργανόγραμμα) με ιστορικό.', 'Η αλλαγή θέσης καταγράφει πλέον ProfileEvent (helper set_user_position), όπως το τμήμα.']},
    {'v': '12.194', 'b': '475', 'date': '21/06/2026', 'time': '20:30', 'title': 'Μητρώο: τμήμα/θέση από το Οργανόγραμμα',
     'items': ['Η λίστα και η καρτέλα μητρώου δείχνουν πλέον τμήμα/θέση από το ΟΡΓΑΝΟΓΡΑΜΜΑ (προφίλ), κενό όπου δεν έχει οριστεί ακόμη.', 'Το «Αναθέσεις (Management)» έγινε παλιό αρχείο/ιστορικό.']},
    {'v': '12.193', 'b': '474', 'date': '21/06/2026', 'time': '19:50', 'title': 'Φίλτρο οργανογράμματος: λείπει τμήμα Ή θέση Ή ξενοδοχείο',
     'items': ['Το φίλτρο πιάνει πλέον όποιον του λείπει οποιοδήποτε από τα τρία (τμήμα, θέση, ξενοδοχείο) και δείχνει με ⚠ τι λείπει σε κάθε κάρτα.']},
    {'v': '12.192', 'b': '473', 'date': '21/06/2026', 'time': '19:30', 'title': 'Οργανόγραμμα: φίλτρο «χωρίς τμήμα & θέση»',
     'items': ['Κουμπί στο οργανόγραμμα που μαζεύει (σε όλα τα ξενοδοχεία) όσους δεν έχουν ούτε τμήμα ούτε θέση εργασίας — με «+ θέση» για άμεση ανάθεση.']},
    {'v': '12.191', 'b': '472', 'date': '21/06/2026', 'time': '18:55', 'title': 'FIX: άλλαγη θέσης εργασίας — δεν εμφανίζονταν επιλογές σε άλλα ξενοδοχεία',
     'items': ['Ο επιλογέας θέσης έδειχνε μόνο θέσεις της ομάδας του τμήματος· αν δεν υπήρχαν συνδεδεμένες, η λίστα έβγαινε κενή (π.χ. CNT). Τώρα δείχνει ΠΑΝΤΑ όλες τις θέσεις: «Σχετικές με το τμήμα» πρώτα, «Άλλες θέσεις» από κάτω.']},
    {'v': '12.190', 'b': '471', 'date': '21/06/2026', 'time': '18:20', 'title': 'Κλασικό οργανόγραμμα: τυπικό top-down + readable View',
     'items': ['Επιστροφή σε τυπικό κάθετο (top-down) οργανόγραμμα με τα άτομα πάντα ορατά.', 'Το View ανοίγει σε ΚΑΝΟΝΙΚΟ μέγεθος με scroll (διαβάζεται)· zoom −/100%/+ και «Προσαρμογή πλάτους» προαιρετικά (όχι αυτόματη σμίκρυνση).']},
    {'v': '12.189', 'b': '470', 'date': '21/06/2026', 'time': '17:45', 'title': 'Κλασικό οργανόγραμμα: οριζόντια διάταξη (αριστερά→δεξιά)',
     'items': ['Το δέντρο απλώνεται οριζόντια ώστε να πιάνει το πλάτος της οθόνης και να είναι πιο ευανάγνωστο (αντί κάθετο/μακρύ).', 'Το View (fit-to-window) προσαρμόζει ανάλογα.']},
    {'v': '12.188', 'b': '469', 'date': '21/06/2026', 'time': '17:10', 'title': 'Κλασικό οργανόγραμμα: κουμπί View (fit-to-window)',
     'items': ['Το «Άνοιγμα σε νέα καρτέλα» έγινε «View»: ανοίγει πλήρη σελίδα που αυτο-προσαρμόζει (scale) ΟΛΟ το οργανόγραμμα ώστε να χωρά στο παράθυρο, με zoom +/− και Προσαρμογή.']},
    {'v': '12.187', 'b': '468', 'date': '21/06/2026', 'time': '16:35', 'title': 'Κλασικό οργανόγραμμα: άνοιγμα σε νέα καρτέλα',
     'items': ['Κουμπί «Άνοιγμα σε νέα καρτέλα» δίπλα στην Εκτύπωση — δείχνει την όψη εκτός εφαρμογής, σε πλήρη σελίδα browser.']},
    {'v': '12.186', 'b': '467', 'date': '21/06/2026', 'time': '16:05', 'title': 'Κλασικό οργανόγραμμα: γραφιστικό polish',
     'items': ['Νέες κάρτες με χρωματιστή μπάρα, avatars (φωτο προφίλ + αρχικά fallback), κάρτα-υπευθύνου με ρόλο, badges πλήθους και καθαρότερες γραμμές.', 'Άτομα ομαδοποιημένα ανά θέση με avatar· managers με ★.']},
    {'v': '12.185', 'b': '466', 'date': '21/06/2026', 'time': '15:20', 'title': 'Οργανόγραμμα: κλασική (δεντρική) όψη',
     'items': ['Νέα read-only όψη /dashboard/org/chart: κλασικό οργανόγραμμα Ξενοδοχείο → Ομάδες → Υποομάδες → Τμήματα → Θέσεις → Άτομα, με υπευθύνους και εκτύπωση/PDF.', 'Κουμπί «Κλασική όψη» στη διαχείριση οργανογράμματος, ανά ξενοδοχείο.']},
    {'v': '12.184', 'b': '465', 'date': '21/06/2026', 'time': '14:10', 'title': 'Οργανόγραμμα: αναδιάταξη ομάδων με σύρσιμο (drag & drop)',
     'items': ['Στις Ρυθμίσεις σέρνεις μια ομάδα από τη λαβή μέσα σε άλλη (γίνεται υποομάδα) ή στο πάνω επίπεδο (γίνεται κορυφή).', 'Δεν επιτρέπεται να μπει ομάδα μέσα στον εαυτό της ή σε δική της υποομάδα.']},
    {'v': '12.183', 'b': '464', 'date': '21/06/2026', 'time': '13:25', 'title': 'Διόρθωση: ίδιο όνομα υποομάδας σε διαφορετικούς κλάδους',
     'items': ['Μπορείς πλέον να φτιάξεις π.χ. «Kitchen» κάτω από διαφορετικές ομάδες — δεν μπλοκάρει πια το καθολικό μοναδικό όνομα. Απαγορεύεται μόνο διπλό όνομα στο ΙΔΙΟ επίπεδο (ίδιος γονέας).']},
    {'v': '12.182', 'b': '463', 'date': '21/06/2026', 'time': '13:05', 'title': 'Οργανόγραμμα: υπεύθυνος ομάδας / υποομάδας',
     'items': ['Σε κάθε ομάδα και υποομάδα (στις Ρυθμίσεις) ορίζεις υπεύθυνο — όπως ορίζεις υπεύθυνο τμήματος.', 'Ο υπεύθυνος της ομάδας εμφανίζεται και στην κεφαλίδα της ομάδας μέσα στο οργανόγραμμα.']},
    {'v': '12.182', 'b': '463', 'date': '21/06/2026', 'time': '13:05', 'title': 'Οργανόγραμμα: υπεύθυνος ομάδας / υποομάδας',
     'items': ['Σε κάθε ομάδα και υποομάδα (στις Ρυθμίσεις) ορίζεις υπεύθυνο — όπως ορίζεις υπεύθυνο τμήματος.', 'Ο υπεύθυνος της ομάδας εμφανίζεται και στην κεφαλίδα της ομάδας μέσα στο οργανόγραμμα.']},
    {'v': '12.181', 'b': '462', 'date': '21/06/2026', 'time': '12:40', 'title': 'Οργανόγραμμα: υποομάδες (δέντρο) + οθόνη Ρυθμίσεων',
     'items': ['Οι ομάδες αποκτούν υποομάδες (απεριόριστο βάθος): π.χ. F&B → Κουζίνες / Εστιατόρια·Μπαρ, Room Division → Front Office / Housekeeping / Maintenance.', 'Νέο κουμπί «Ρυθμίσεις» μέσα στο οργανόγραμμα: ξεχωριστή οθόνη όπου φτιάχνεις/τακτοποιείς το δέντρο (ομάδες/υποομάδες) και τις θέσεις εργασίας — με ξεχωριστή λίστα «θέσεις χωρίς ομάδα».', 'Η κύρια οθόνη έμεινε καθαρή για drag&drop· οι κεφαλίδες ομάδων δείχνουν πλέον τη διαδρομή (π.χ. F&B › Κουζίνες).']},
    {'v': '12.180', 'b': '461', 'date': '21/06/2026', 'time': '11:55', 'title': 'Οργανόγραμμα: σύνδεση θέσεων με ομάδες (φιλτράρισμα)',
     'items': ['Κάθε θέση εργασίας μπορεί να συνδεθεί με μια ομάδα (π.χ. Chef→Κουζίνες, Σερβιτόρος→F&B) μέσα από το παράθυρο επεξεργασίας της θέσης.', 'Όταν αναθέτεις θέση σε εργαζόμενο, το παράθυρο δείχνει ΜΟΝΟ τις σχετικές θέσεις (της ομάδας του τμήματός του) — λιγότερο μπέρδεμα.', 'Η μπάρα «Θέσεις εργασίας» πλέον επεξεργάζεται κάθε θέση σε κανονικό παράθυρο (όνομα/χρώμα/ομάδα/διαγραφή).']},
    {'v': '12.179', 'b': '460', 'date': '21/06/2026', 'time': '11:35', 'title': 'Οργανόγραμμα: ανάθεση θέσης ανά εργαζόμενο',
     'items': ['Κλικ στην ετικέτα θέσης μιας κάρτας (ή στο «+ θέση») → διαλέγεις τη θέση του εργαζομένου από τον κατάλογο. Το οργανόγραμμα κρατά πλέον τη θέση κάθε ατόμου.', 'Αν δεν έχεις ορίσει θέση χειροκίνητα, εμφανίζεται αυτόματα η θέση από τα δεδομένα του προσωπικού (Management).']},
    {'v': '12.178', 'b': '459', 'date': '21/06/2026', 'time': '11:10', 'title': 'Οργανόγραμμα: θέσεις εργασίας (από το προσωπικό)',
     'items': ['Νέα μπάρα «Θέσεις εργασίας» με κουμπί «Δημιουργία από προσωπικό»: φτιάχνει αυτόματα τον κατάλογο θέσεων (Chef/Μάγειρας/Σερβιτόρος…) από τις θέσεις που ήδη έχει το προσωπικό. Μετά τις επεξεργάζεσαι/σβήνεις (adjustable).', 'Η θέση του καθενός εμφανίζεται πλέον ως ετικέτα πάνω στην κάρτα του στο οργανόγραμμα.', 'Επόμενο: χειροκίνητη ανάθεση θέσης ανά άτομο + σύνδεση θέσεων με ομάδες (το τυπικό οργανόγραμμα).']},
    {'v': '12.178', 'b': '459', 'date': '21/06/2026', 'time': '11:10', 'title': 'Οργανόγραμμα: θέσεις εργασίας (από το προσωπικό)',
     'items': ['Νέα μπάρα «Θέσεις εργασίας» με κουμπί «Δημιουργία από προσωπικό»: φτιάχνει αυτόματα τον κατάλογο θέσεων (Chef/Μάγειρας/Σερβιτόρος…) από τις θέσεις που ήδη έχει το προσωπικό. Μετά τις επεξεργάζεσαι/σβήνεις (adjustable).', 'Η θέση του καθενός εμφανίζεται πλέον ως ετικέτα πάνω στην κάρτα του στο οργανόγραμμα.', 'Επόμενο: χειροκίνητη ανάθεση θέσης ανά άτομο + σύνδεση θέσεων με ομάδες (το τυπικό οργανόγραμμα).']},
    {'v': '12.177', 'b': '458', 'date': '21/06/2026', 'time': '10:35', 'title': 'Οργανόγραμμα: ορατή μπάρα «Ομάδες» (διαχείριση master groups)',
     'items': ['Νέα μπάρα «Ομάδες (master groups)» στην κορυφή — βλέπεις όλες τις ομάδες ως chips, φτιάχνεις «+ Νέα ομάδα» και τις επεξεργάζεσαι/σβήνεις (μολύβι). Πριν φαινόντουσαν μόνο όταν είχαν τμήμα μέσα τους.', 'Το τμήμα μπαίνει σε ομάδα από το ✎ του τμήματος (πεδίο «Ομάδα»), όπως πριν.']},
    {'v': '12.176', 'b': '457', 'date': '21/06/2026', 'time': '10:15', 'title': 'Οργανόγραμμα: ομάδες τμημάτων (master groups)',
     'items': ['Νέο επίπεδο «ομάδα» πάνω από τα τμήματα (Κουζίνες, F&B, Όροφοι…). Από το παράθυρο επεξεργασίας ενός τμήματος (πεδίο «Ομάδα») το βάζεις σε μια ομάδα· τα τμήματα εμφανίζονται ομαδοποιημένα στο οργανόγραμμα.', 'Όλα adjustable: φτιάχνεις/μετονομάζεις/σβήνεις ομάδες (μολύβι στην κεφαλίδα της ομάδας).', 'Επόμενο: θέσεις εργασίας (job positions) από τα δεδομένα του προσωπικού.']},
    {'v': '12.175', 'b': '456', 'date': '21/06/2026', 'time': '09:30', 'title': 'Οργανόγραμμα: οι 2 βοηθοί (Διαθέσιμο/Χωρίς τμήμα) πάνω-πάνω',
     'items': ['Το «Διαθέσιμο προσωπικό» και το «Χωρίς τμήμα» δεν είναι τμήματα — είναι δύο βοηθητικές οθόνες για να χτίζεις το οργανόγραμμα. Μετακινήθηκαν στην ΚΟΡΥΦΗ της σελίδας (πάνω από τα τμήματα), σε δική τους ζώνη με search & scroll, ώστε να σέρνεις εύκολα από εκεί προς τα τμήματα.', 'Στο πλέγμα από κάτω μένουν πλέον μόνο τα τμήματα.']},
    {'v': '12.174', 'b': '455', 'date': '21/06/2026', 'time': '09:00', 'title': 'Οργανόγραμμα: νέα διάταξη — όλα σε ένα πλέγμα',
     'items': ['Έφυγε η αριστερή κολόνα. Τα τμήματα, το «Διαθέσιμο προσωπικό» και το «Χωρίς τμήμα» είναι τώρα όλα στο ίδιο πλέγμα, με την ίδια όψη — βλέπεις εύκολα και το ανατεθειμένο και το διαθέσιμο προσωπικό.', 'Το «Διαθέσιμο προσωπικό» έχει το search μέσα του και δικό του scroll· σέρνεις άνετα από και προς παντού (το «Διαθέσιμο» μπαίνει πρώτο, μετά το «Χωρίς τμήμα»).']},
    {'v': '12.173', 'b': '454', 'date': '21/06/2026', 'time': '08:15', 'title': 'Οργανόγραμμα/Πρόγραμμα: επιλεγμένα τμήματα = μοναδικό φίλτρο (+ fix drag)',
     'items': ['Τα τμήματα παραμένουν κοινά, αλλά πλέον εμφανίζονται ΜΟΝΟ όσα έχεις επιλέξει στο «Τμήματα αυτού του ξενοδοχείου» — και στο οργανόγραμμα και στο Πρόγραμμα Εργασίας. Τα μη-επιλεγμένα δεν δείχνουν μπλοκ.', 'Όποιος είναι σε μη-επιλεγμένο τμήμα εμφανίζεται προσωρινά στο «Χωρίς τμήμα» μέχρι να επιλέξεις ξανά το τμήμα του.', 'Διόρθωση: τα άτομα στο «Χωρίς τμήμα» ξανασέρνονται κανονικά (η αναδιάταξη στηλών τα είχε μπλοκάρει).']},
    {'v': '12.172', 'b': '453', 'date': '21/06/2026', 'time': '07:30', 'title': 'Οργανόγραμμα: διαχείριση τμημάτων (επεξεργασία/διαγραφή + αναδιάταξη)',
     'items': ['Επεξεργασία τμήματος: πάτα το μολύβι στην κεφαλίδα μιας στήλης → άλλαξε όνομα/χρώμα (ισχύει σε όλα τα ξενοδοχεία — κοινός κατάλογος).', 'Διαγραφή τμήματος από το ίδιο παράθυρο: όσοι ανήκουν πάνε αυτόματα στο «Χωρίς τμήμα» και το τμήμα αφαιρείται από τα ξενοδοχεία· το ιστορικό (παλιά προγράμματα) διατηρείται.', 'Αναδιάταξη: πιάσε το εικονίδιο σύρσιμο στην κεφαλίδα και μετακίνησε τη στήλη για να αλλάξεις σειρά — αποθηκεύεται αυτόματα.']},
    {'v': '12.171', 'b': '452', 'date': '21/06/2026', 'time': '06:45', 'title': 'Οργανόγραμμα: λωρίδα «Διεύθυνση / Προϊστάμενοι»',
     'items': ['Νέα λωρίδα στην κορυφή του οργανογράμματος για τη Διεύθυνση/προϊσταμένους — ώστε να φαίνεται όλη η ιεραρχία, όχι μόνο το προσωπικό στα τμήματα.', 'Μαρκάρεις όποιο τμήμα θες ως «Διεύθυνση» με το κουμπί 👑 στην κεφαλίδα της στήλης και ανεβαίνει πάνω-πάνω σε ξεχωριστή λωρίδα· με το βελάκι ⬇ το επαναφέρεις σε κανονικό τμήμα.', 'Εσύ ορίζεις ποιοι μπαίνουν: τους σέρνεις μέσα με drag&drop όπως σε κάθε τμήμα.']},
    {'v': '12.170', 'b': '451', 'date': '21/06/2026', 'time': '06:00', 'title': 'Οργανόγραμμα νικά: το Epsilon γίνεται «πρόταση» + πίνακας διαφωνιών',
     'items': ['Το import (Epsilon) σταματά να αλλάζει το ξενοδοχείο όποιου υπάρχει ήδη — βάζει ξενοδοχείο μόνο σε νέους/κενούς. Αν διαφωνεί, καταγράφει την πρότασή του αντί να την επιβάλλει (τα ΑΦΜ/ΑΜΚΑ/επίσημα συμπληρώνονται κανονικά).', 'Νέα καρτέλα «⚠ Διαφωνίες» στη Διαχείριση Προσωπικού: όπου το Epsilon λέει άλλο ξενοδοχείο, με κουμπιά «Κράτα οργανόγραμμα» / «Δέξου Epsilon».', 'Ειδοποίηση στην κορυφή (admin) όσο υπάρχουν ασυμφώνητες διαφωνίες, με σύνδεσμο στη λίστα.', 'Ροή νέου εργαζομένου = συνδυασμός: χειροκίνητα (υπάρχει στο «Πρόγραμμα·Προσωπικό») για άμεση πρόσβαση + περιοδικό Epsilon που ταιριάζει & συμπληρώνει τα επίσημα.']},
    {'v': '12.169', 'b': '450', 'date': '21/06/2026', 'time': '05:00', 'title': 'Οργανόγραμμα: ένα σημείο για τις αλλαγές ανάθεσης (+ ιστορικό)',
     'items': ['Όλες οι αλλαγές ξενοδοχείου/τμήματος ενός εργαζομένου περνάνε πλέον από ΕΝΑ σημείο, ώστε να γίνονται με τον ίδιο τρόπο παντού και να καταγράφεται πάντα ποιος/πότε άλλαξε τι (ιστορικό προφίλ).', 'Διακριτική διόρθωση: η παλιά οθόνη επεξεργασίας του Προγράμματος έγραφε τα ίδια πεδία χωρίς ιστορικό — τώρα περνάει κι αυτή από το ίδιο σημείο.', 'Καμία ορατή αλλαγή — εσωτερική τάξη/θεμέλιο. Επόμενο (αφού αποφασιστεί πώς μπαίνει νέος εργαζόμενος): πίνακας διαφωνιών Οργανόγραμμα↔Epsilon.']},
    {'v': '12.168', 'b': '449', 'date': '21/06/2026', 'time': '04:15', 'title': 'Οργανόγραμμα: φωτογραφία προφίλ + ο υπεύθυνος φαίνεται στο Πρόγραμμα',
     'items': ['Στις κάρτες του Οργανογράμματος εμφανίζεται η φωτογραφία προφίλ του καθενός· αν δεν έχει φωτό, πέφτει στα αρχικά του ονόματος (π.χ. «ΓΠ»).', 'Ο υπεύθυνος που ορίζεις σε ένα τμήμα εμφανίζεται πλέον και μέσα στο Πρόγραμμα Εργασίας, στην κεφαλίδα του τμήματος, ως «υπεύθυνος προγράμματος» (προς το παρόν ενημερωτικά).', 'Σημείωση: η ιδιότητα του υπευθύνου είναι ακόμη ετικέτα — ΔΕΝ δίνει αυτόματα login ή δικαιώματα. Στο roadmap: μελέτη ώστε ο υπεύθυνος να μπορεί να υποβάλλει το πρόγραμμα του τμήματός του.']},
    {'v': '12.167', 'b': '448', 'date': '21/06/2026', 'time': '03:30', 'title': 'Οργανόγραμμα: υπεύθυνος ανά τμήμα + νέα γραφιστική κονσόλας',
     'items': ['Όρισε υπεύθυνο (supervisor) σε κάθε τμήμα ανά ξενοδοχείο μέσα από το Οργανόγραμμα (κουμπί «Υπεύθυνος» στην κορυφή κάθε στήλης). Αποθηκεύεται στο HotelDepartment — ο υπεύθυνος μπορεί να διαφέρει ανά ξενοδοχείο.', 'Νέα, πιο καθαρή κονσόλα: χρωματιστή μπάρα τμήματος, avatar με αρχικά, σήμανση ρόλου (manager/admin), στοιχεία μητρώου (κωδικός/ΑΦΜ) read-only στις κάρτες.', 'Το Πρόγραμμα Εργασίας δείχνει πλέον τα τμήματα που ορίζει το Οργανόγραμμα — ακόμη κι αν είναι άδεια (κενά τμήματα εμφανίζονται έτοιμα να γεμίσουν).', 'Αρχιτεκτονική: Μητρώο/Προφίλ = η βάση· το Οργανόγραμμα διαχειρίζεται μόνο τμήμα/ξενοδοχείο/υπεύθυνο και γράφει πίσω στο ίδιο προφίλ· όλα τα Modules διαβάζουν από εκεί.']},
    {'v': '12.166', 'b': '447', 'date': '21/06/2026', 'time': '02:00', 'title': 'Οργανόγραμμα: διαχείριση τμημάτων ανά ξενοδοχείο + fix «Χωρίς τμήμα»',
     'items': ['Στο Οργανόγραμμα μπορείς να δημιουργήσεις τμήμα (όνομα/χρώμα) και να ορίσεις ποια τμήματα έχει κάθε ξενοδοχείο (πάτα τα chips). Κοινός κατάλογος + per-hotel (δεν πειράζει πρόγραμμα/μισθοδοσία/αξιολόγηση).',
               'Όποιος έχει ξενοδοχείο αλλά όχι/άγνωστο τμήμα → εμφανίζεται στο «Χωρίς τμήμα» (δεν χάνεται κανείς).',
               'Νέος πίνακας HotelDepartment (single source «τμήματα ανά ξενοδοχείο»). Supervisors = επόμενος γύρος.']},
    {'v': '12.165', 'b': '446', 'date': '21/06/2026', 'time': '01:30', 'title': 'Διακυβέρνηση: επεκτείνεται σε ΟΛΑ τα modules (owner-screens, διπλή επιβεβαίωση)',
     'items': ['Οι αρχές «μία πηγή αλήθειας» & impact-first ισχύουν για ΟΛΗ την πλατφόρμα (Βλάβες/Πελάτες/κ.λπ.). Κάθε οντότητα = ένα owner-screen για edit· αλλού read ή βοηθός με ΙΔΙΟ endpoint. Δομικές αλλαγές = ρητή εντολή + διπλή επιβεβαίωση. Πλήρες contract: 03_REFERENCE/ΑΡΧΕΣ_ΑΡΧΙΤΕΚΤΟΝΙΚΗΣ.md.']},
    {'v': '12.164', 'b': '445', 'date': '21/06/2026', 'time': '01:00', 'title': 'Roadmap: Διασφαλίσεις & Διακυβέρνηση (single source of truth, impact-first)',
     'items': ['Προστέθηκε ενότητα roadmap «Διασφαλίσεις & Διακυβέρνηση»: impact-note πριν από δομικές αλλαγές, ΜΙΑ πηγή αλήθειας (People=User, link με user_id, καμία παράλληλη βάση), τεστ backup-restore, πειθαρχία migrations, indexes/απόδοση, CSRF, αυτόματα smoke-tests.']},
    {'v': '12.163', 'b': '444', 'date': '21/06/2026', 'time': '00:35', 'title': 'Fix: το Οργανόγραμμα στο μενού + auto-εμφάνιση νέων στοιχείων μενού',
     'items': ['Διορθώθηκε γιατί δεν φαινόταν το «Οργανόγραμμα»: ο custom μενού-κατάλογος (Διαμόρφωση μενού) δεν το είχε. Προστέθηκε.',
               'Auto-sync μενού: όποιο νέο στοιχείο προστίθεται στην εφαρμογή εμφανίζεται πλέον ΜΟΝΟ του σε ομάδα «🆕 Νέα» (αν έχεις προσαρμοσμένο μενού) — δεν χρειάζεται χειροκίνητο σύρσιμο.']},
    {'v': '12.162', 'b': '443', 'date': '21/06/2026', 'time': '00:10', 'title': 'Οργανόγραμμα HR (v1) — drag&drop ανάθεση τμήμα+ξενοδοχείο',
     'items': ['Νέα κονσόλα «Οργανόγραμμα» (HR): tabs ανά ξενοδοχείο, στήλες τμημάτων + «Διαθέσιμο προσωπικό». Σύρε άτομο σε τμήμα → ανατίθεται ΤΑΥΤΟΧΡΟΝΑ τμήμα + ξενοδοχείο. Σύρε στη λίστα = βγαίνει από το ξενοδοχείο. Κάθε αλλαγή καταγράφεται στο ιστορικό (ProfileEvent).',
               'Επόμενα: ορισμός supervisors ανά τμήμα + διπλό ιστορικό (έτη/περίοδοι).']},
    {'v': '12.161', 'b': '442', 'date': '20/06/2026', 'time': '23:50', 'title': 'Roadmap: + Οργανόγραμμα (κονσόλα στο HR)',
     'items': ['Προστέθηκε στο roadmap: Οργανόγραμμα ως κονσόλα/λειτουργία στο HR (ιεραρχία ξεν.→τμήμα→προϊστάμενος→εργαζόμενοι, drag&drop αναθέσεις, ορισμός supervisors).']},
    {'v': '12.160', 'b': '441', 'date': '20/06/2026', 'time': '23:30', 'title': 'Αξιολόγηση — κατάργηση snapshot/τρέχον + Roadmap auto-ενημέρωση ανά έκδοση',
     'items': ['Αφαιρέθηκε το πεδίο «Snapshot/Τρέχον» στα στατιστικά — πάντα τα στοιχεία της στιγμής της αξιολόγησης. (Για εργαζόμενο σε >1 τμήμα → διπλή αξιολόγηση, στο roadmap.)',
               'Το Roadmap (/dashboard/roadmap) δείχνει πλέον αυτόματα ενότητα «Πρόσφατα υλοποιήθηκαν» από τις εκδόσεις — ενημερώνεται μόνο του σε κάθε νέα version.']},
    {'v': '12.159', 'b': '440', 'date': '20/06/2026', 'time': '23:05', 'title': 'Αξιολόγηση — admin διαγράφει και εγκεκριμένες',
     'items': ['Admin/masteradmin μπορεί πλέον να διαγράψει οποιαδήποτε αξιολόγηση, ακόμη και εγκεκριμένη. (Ο αξιολογητής μόνο το δικό του πρόχειρο.)']},
    {'v': '12.158', 'b': '439', 'date': '20/06/2026', 'time': '22:50', 'title': 'Αξιολόγηση — λίστα: μόνο hotel code + πίνακας μέσα στην κάρτα',
     'items': ['Στήλη Ξενοδοχείο: μόνο ο κωδικός ξενοδοχείου (το πλήρες όνομα σε tooltip).',
               'Ο πίνακας μπήκε σε οριζόντιο scroll μέσα στην κάρτα — τα κουμπιά ενεργειών δεν βγαίνουν πια εκτός.']},
    {'v': '12.157', 'b': '438', 'date': '20/06/2026', 'time': '22:30', 'title': 'Αξιολόγηση — email με PDF συνημμένο, νέο PDF/Excel, tags & στήλη Status, στατιστικά managers',
     'items': ['Email: στέλνει το PDF ως ΣΥΝΗΜΜΕΝΟ (όνομα = ο κωδικός εγγράφου)· παραλήπτης αντλείται από το προφίλ του εργαζομένου (αλλιώς ρωτά).',
               'PDF export ξαναχτίστηκε (header+logo, πίνακας με ομάδες/zebra, βαθμοί, σύνολο). Excel export βελτιωμένο.',
               'Λίστα: Τμήμα/Ερωτηματολόγιο/Ξενοδοχείο(+κωδικός)/Περίοδος ως tags· νέα ξεχωριστή στήλη «Κατάσταση» (status) δίπλα στη «Βαθμίδα».',
               'Στατιστικά τμήματος/ξενοδοχείου διαθέσιμα και στους managers (scoped στα ανατεθειμένα ξενοδοχεία).']},
    {'v': '12.156', 'b': '437', 'date': '20/06/2026', 'time': '21:40', 'title': 'Αξιολόγηση — προβολή με logo + κουμπιά Εκτύπωση/PDF/Excel/Email',
     'items': ['Στο pop-up προβολής: κουμπιά πάνω — Εκτύπωση, PDF, Excel, Email (αποστολή του εντύπου σε email παραλήπτη μέσω της υπάρχουσας υποδομής).',
               'Εμφανίζεται το logo της εταιρείας στο έντυπο (από Εμφάνιση → logo, αλλιώς το προεπιλεγμένο).']},
    {'v': '12.155', 'b': '436', 'date': '20/06/2026', 'time': '21:10', 'title': 'Αξιολόγηση — κωδ. εργαζομένου, popup προβολή, διαγραφή submitted, διατήρηση σελίδας σε refresh',
     'items': ['Κωδικός: προστέθηκε ο κωδικός εργαζομένου στο τέλος (π.χ. 2026-AST-30062026-E0040) για μοναδικό tracking.',
               'Το κουμπί προβολής ανοίγει pop-up με την ΕΚΤΥΠΩΣΙΜΗ μορφή της αξιολόγησης (χωρίς αλλαγή σελίδας) + κουμπί εκτύπωσης.',
               'Admin/masteradmin μπορεί να διαγράψει και submitted (όχι εγκεκριμένες). Επαναφέρθηκε κουμπί διαγραφής (admin, πριν την έγκριση).',
               'Σε refresh η εφαρμογή σε κρατά στη σελίδα που είσαι (δεν επιστρέφει στην αρχική).',
               'Polish λίστας αξιολογήσεων (header/spacing/ενέργειες).']},
    {'v': '12.154', 'b': '435', 'date': '20/06/2026', 'time': '20:35', 'title': 'Αξιολόγηση — αλλαγή φύλλου κριτηρίων και στην επεξεργασία (πριν την έγκριση)',
     'items': ['Στην επεξεργασία πρόχειρης/υποβλημένης αξιολόγησης μπορείς να αλλάξεις το φύλλο κριτηρίων — έτσι διορθώνονται και όσες είχαν μπει με λάθος πρότυπο. (Μετά την έγκριση κλειδώνει.)']},
    {'v': '12.153', 'b': '434', 'date': '20/06/2026', 'time': '20:15', 'title': 'Αξιολόγηση — fix επιλεγμένου προτύπου στο save + βελτιώσεις λίστας',
     'items': ['ΔΙΟΡΘΩΣΗ: η προσωρινή αποθήκευση/υποβολή κρατά πλέον το ΕΠΙΛΕΓΜΕΝΟ φύλλο κριτηρίων (πριν έπεφτε στο Γενικό — ο επιλογέας δεν υποβαλλόταν).',
               'Λίστα: στήλη «Ερωτηματολόγιο» δείχνει και το πεδίο/τμήμα του προτύπου.',
               'Στήλη «Ημ/νία» δείχνει την ημερομηνία υποβολής/καταχώρησης (όχι την καταληκτική).',
               'Περίοδος: αφαιρέθηκε το διπλό «2026».',
               'Κωδικός: αφαιρέθηκε ο αύξων αριθμός — μορφή ΕΤΟΣ-ΞΕΝΟΔΟΧΕΙΟ-ΠΡΟΘΕΣΜΙΑ (π.χ. 2026-SRG-30062026).']},
    {'v': '12.152', 'b': '433', 'date': '20/06/2026', 'time': '19:45', 'title': 'Αξιολόγηση — φόρμα δεν χάνει επιλογές, ξενοδοχείο στο info, κλείδωμα μετά την έγκριση',
     'items': ['Αλλαγή φύλλου κριτηρίων ΔΕΝ μηδενίζει πια εργαζόμενο/περίοδο/ξενοδοχείο — διατηρούνται.',
               'Αφαιρέθηκε η επιλογή «Όλα τα δικά μου» από το φίλτρο ξενοδοχείου· επιλογή εργαζομένου δείχνει και το Ξενοδοχείο δίπλα.',
               'Επεξεργασία/διαγραφή επιτρέπονται ΜΟΝΟ πριν την έγκριση· μετά την έγκριση κλειδώνει (ο admin κάνει «Επιστροφή» αν χρειαστεί). Αφαιρέθηκε το κουμπί διαγραφής από τη λίστα.']},
    {'v': '12.151', 'b': '432', 'date': '20/06/2026', 'time': '19:10', 'title': 'Αξιολόγηση — στήλη ερωτηματολογίου + διαγραφή/edit πρόχειρου από τον αξιολογητή',
     'items': ['Στη λίστα αξιολογήσεων φαίνεται πλέον ποιο ερωτηματολόγιο (πρότυπο) χρησιμοποιήθηκε.',
               'Το κουμπί «Προσωρινή αποθήκευση» (πρόχειρο) είναι σαφές· το status (Πρόχειρο/Προς έγκριση/Εγκρίθηκε) φαίνεται στη γραμμή.',
               'Πριν την υποβολή, ο αξιολογητής μπορεί να ΔΙΟΡΘΩΣΕΙ ή να ΔΙΑΓΡΑΨΕΙ το δικό του πρόχειρο (όχι μόνο ο admin).']},
    {'v': '12.150', 'b': '431', 'date': '20/06/2026', 'time': '18:50', 'title': 'Αξιολόγηση — τέλος αυτόματης δημιουργίας προτύπων',
     'items': ['Η εφαρμογή ΔΕΝ δημιουργεί πλέον αυτόματα πρότυπα F&B σε κάθε εκκίνηση. Τα reference μπήκαν μία φορά· από εδώ και πέρα τα πρότυπα τα φτιάχνεις/επεξεργάζεσαι εσύ. (Το βασικό CONDIAN seed μένει μόνο για εντελώς άδεια βάση.)']},
    {'v': '12.149', 'b': '430', 'date': '20/06/2026', 'time': '18:30', 'title': 'Αξιολόγηση — «σφραγίδα» προτύπων (το auto-seed σταματά οριστικά)',
     'items': ['Ο μηχανισμός που φύτευε/καθάριζε τα πρότυπα F&B τρέχει πλέον ΜΙΑ τελευταία φορά και μετά σφραγίζεται (Setting eval_dept_templates_done) — δεν ξαναγγίζει ποτέ τα πρότυπα σε επόμενα deploys. Τα πρότυπα είναι πλέον 100% στον έλεγχό σου.']},
    {'v': '12.148', 'b': '429', 'date': '20/06/2026', 'time': '18:10', 'title': 'Πρότυπα Αξιολόγησης — εξαγωγή σε έντυπο PDF & Excel (παρουσίασης)',
     'items': ['Στα Πρότυπα, κουμπιά «Έντυπο PDF» & «Έντυπο Excel» ανά πρότυπο: κενή φόρμα αξιολόγησης με header CONDIAN, πεδία στοιχείων, πίνακα κριτηρίων ανά ομάδα με συντελεστές % και κενές στήλες Βαθμός/Σχόλιο, σύνολο βαρών.']},
    {'v': '12.147', 'b': '428', 'date': '20/06/2026', 'time': '17:35', 'title': 'Αξιολόγηση — οριστική εκκαθάριση διπλών προτύπων (bulletproof)',
     'items': ['Το auto-dedupe των προτύπων F&B τώρα συγκρίνει ΚΑΝΟΝΙΚΟΠΟΙΗΜΕΝΟ όνομα (αγνοεί διαφορές παύλας —/- και κενών) → πιάνει και τα διπλά που ξέφευγαν. Σβήνει μόνο τα αχρησιμοποίητα, κρατά ένα, αφήνει άθικτα τα δικά σου.']},
    {'v': '12.146', 'b': '427', 'date': '20/06/2026', 'time': '17:10', 'title': 'Αξιολόγηση — νέα μορφή κωδικού + «Εκκρεμείς» χωρίς ειδικότητα',
     'items': ['Κωδικός νέων αξιολογήσεων: ΕΤΟΣ-ΞΕΝΟΔΟΧΕΙΟ-ΠΡΟΘΕΣΜΙΑ-ID (π.χ. 2026-SRG-30062026-0001).',
               'Στη σελίδα «Εκκρεμείς» αφαιρέθηκε η στήλη Ειδικότητα.']},
    {'v': '12.145', 'b': '426', 'date': '20/06/2026', 'time': '16:45', 'title': 'Auto-refresh σε νέα έκδοση (deploy)',
     'items': ['Η εφαρμογή ελέγχει κάθε λεπτό αν ανέβηκε νέα έκδοση (endpoint /version).',
               'Όταν αλλάξει η έκδοση: αν ΔΕΝ συμπληρώνεις φόρμα, ανανεώνεται αυτόματα το περιεχόμενο (κρατά τη θέση σου, φέρνει φρέσκα δεδομένα)· αν συμπληρώνεις (αξιολόγηση/πρόγραμμα), εμφανίζεται διακριτικό banner «Ανανέωση» για να μη χαθούν δεδομένα.']},
    {'v': '12.144', 'b': '425', 'date': '20/06/2026', 'time': '16:20', 'title': 'Αξιολόγηση — διόρθωση διπλών προτύπων (race) + αυτόματος καθαρισμός',
     'items': ['Διορθώθηκε η αιτία διπλών προτύπων F&B: ο seeder έτρεχε σε 2 workers ταυτόχρονα.',
               'Τώρα είναι self-healing: στο boot καθαρίζει τα αχρησιμοποίητα διπλά (κρατά το παλαιότερο) και δημιουργεί μόνο αν λείπουν. Τα προσαρμοσμένα/χρησιμοποιημένα πρότυπα μένουν άθικτα.']},
    {'v': '12.143', 'b': '424', 'date': '20/06/2026', 'time': '16:00', 'title': 'Αξιολόγηση — στοιχεία εργαζομένου στη φόρμα: Τμήμα + Κωδικός + ΑΦΜ',
     'items': ['Όταν ο αξιολογητής επιλέγει εργαζόμενο, η γραμμή πληροφοριών δείχνει Τμήμα, Κωδικό (#) και ΑΦΜ — όχι πια την ειδικότητα.']},
    {'v': '12.142', 'b': '423', 'date': '20/06/2026', 'time': '15:30', 'title': 'Αξιολόγηση — εξαγωγή PDF, φίλτρο ξενοδοχείου, πρακτική συμπλήρωση',
     'items': ['Εξαγωγή αξιολόγησης σε PDF (κουμπί στην προβολή + εικονίδιο στη λίστα), δίπλα στο Excel.',
               'Στη φόρμα: ο αξιολογητής βλέπει φίλτρο με τα ανατεθειμένα του ξενοδοχεία → επιλέγει ξενοδοχείο και βλέπει μόνο το προσωπικό του.',
               'Πρακτικότερη συμπλήρωση: γρήγορα κουμπιά βαθμού 1–10 (tap) ανά κριτήριο + «✕» καθαρισμός, μαζί με το πεδίο αριθμού.']},
    {'v': '12.141', 'b': '422', 'date': '20/06/2026', 'time': '14:40', 'title': 'Αξιολόγηση — διαγραφή προτύπου, ανάθεση σε ξενοδοχείο, polish λίστας',
     'items': ['Οριστική διαγραφή προτύπου (μπλοκάρεται αν χρησιμοποιείται σε αξιολογήσεις — τότε κάνε το «ανενεργό»).',
               'Ανάθεση προτύπου σε ξενοδοχείο: κάθε ξενοδοχείο μπορεί να έχει τα δικά του πρότυπα (νέα στήλη eval_template.hotel_id). Στη φόρμα νέας αξιολόγησης ο επιλογέας προτύπου φιλτράρεται με βάση το ξενοδοχείο του εργαζομένου (+ κοινά πρότυπα «Όλα»).',
               'Στήλη «Ξενοδοχείο» στα Πρότυπα + dropdown ανάθεσης στην επεξεργασία/κλωνοποίηση.',
               'Polish κεντρικής σελίδας αξιολογήσεων (hover/zebra, focus φίλτρων, μπάρα ενεργειών, κενή κατάσταση).']},
    {'v': '12.140', 'b': '421', 'date': '20/06/2026', 'time': '14:00', 'title': 'Αξιολόγηση — κουμπί «Νέα αξιολόγηση» + πρότυπα F&B Κουζίνα/Σέρβις',
     'items': ['Εμφανές πράσινο κουμπί «Νέα αξιολόγηση» στη λίστα αξιολογήσεων (πέρα από το πάνω κουμπί).',
               'Δύο νέα πρότυπα-variants από τα reference 2026: «F&B — Κουζίνα» (25 κριτήρια) & «F&B — Σέρβις» (30 κριτήρια), σταθμίσεις που αθροίζουν 100%. Επιλέγονται από τον επιλογέα προτύπου στη φόρμα.']},
    {'v': '12.139', 'b': '420', 'date': '20/06/2026', 'time': '13:10', 'title': 'Πρόγραμμα — ενιαία φόρμα βάρδιας για ένα ΚΑΙ για πολλά κελιά',
     'items': ['Η φόρμα καταχώρησης είναι πλέον ΙΔΙΑ είτε επιλέξεις ένα κελί είτε πολλά: η πλήρης φόρμα (πολλαπλές βάρδιες, +Προσθήκη, ξενοδοχείο εργασίας, σπαστό).',
               'Στην αποθήκευση γράφει ΟΛΕΣ τις βάρδιες σε όλα τα επιλεγμένα κελιά μαζί (μέσω paste_cells, σέβεται κλειδώματα).',
               'Έτσι η αντιγραφή/επικόλληση (Ctrl+C/Ctrl+V) και η μαζική καταχώρηση δουλεύουν και με πολλαπλές βάρδιες/μέρα. Board + Μηνιαία.']},
    {'v': '12.138', 'b': '419', 'date': '20/06/2026', 'time': '12:30', 'title': 'Πρόγραμμα — Μηνιαία με τσιπ, αντιγραφή/επικόλληση κελιών, fix φίλτρου τμημάτων',
     'items': ['Μηνιαία συγκεντρωτική (Αναλυτικό): τα κελιά έγιναν χρωματιστά τσιπ ανά κωδικό (όπως το board) + όνομα με #Κωδικό/ΑΦΜ από κάτω· ώρες/ξενοδοχείο σε tooltip.',
               'Αντιγραφή/Επικόλληση: επίλεξε κελί, Ctrl+C, μετά επίλεξε άλλα κελιά και Ctrl+V — αντιγράφει ΟΛΕΣ τις βάρδιες της μέρας (board + Μηνιαία· δουλεύει και με ελληνικό πληκτρολόγιο).',
               'Διόρθωση: το φίλτρο ανά τμήμα στο board (κλικ σε τμήμα) δεν δούλευε λόγω σύγκρουσης ονόματος — διορθώθηκε.',
               'Επιπλέον γυάλισμα φίλτρων (focus ring/hover) σε board και Μηνιαία.']},
    {'v': '12.137', 'b': '418', 'date': '20/06/2026', 'time': '11:40', 'title': 'Πρόγραμμα — Ctrl+κλικ και στη Μηνιαία + γυάλισμα σελίδας/φίλτρων',
     'items': ['Μηνιαία (Αναλυτικό): Ctrl/Cmd+κλικ για πολλαπλή επιλογή κελιών (όπως στο board).',
               'Γυάλισμα φίλτρων: η μπάρα έγινε καθαρή κάρτα με ομάδες & ετικέτες (Ξενοδοχείο / Εβδομάδα / Πλήθος εβδ. / Μετάβαση σε μήνα).',
               'Γυάλισμα σελίδας: τμήματα σε καθαρή μπάρα, hover σε γραμμές/κελιά, καλύτερα headers τμημάτων, υπόμνημα σε κάρτα.']},
    {'v': '12.136', 'b': '417', 'date': '20/06/2026', 'time': '11:00', 'title': 'Πρόγραμμα — Ctrl+κλικ πολλαπλή επιλογή κελιών στο board',
     'items': ['Στο board: με πατημένο Ctrl (ή Cmd σε Mac) κάνε κλικ σε πολλά κουτιά για να τα επιλέξεις/αποεπιλέξεις — ημέρες ή άτομα, ακόμη και από διαφορετικά τμήματα.',
               'Λειτουργεί μαζί με το υπάρχον σύρσιμο (rectangle) για εύρος. Μετά διάλεξε κωδικό/ώρες μία φορά και εφαρμόζεται σε όλα τα επιλεγμένα.',
               'Προστέθηκε διακριτική οδηγία πάνω από το πρόγραμμα.']},
    {'v': '12.135', 'b': '416', 'date': '20/06/2026', 'time': '10:30', 'title': 'Πρόγραμμα — γυάλισμα τσιπ/μάσκας + draggable παράθυρο + χρώματα κωδικών',
     'items': ['Τσιπ βάρδιας σε 3 κεντραρισμένες γραμμές: «ΕΡΓ 07:00 - 15:30» / «8.0h» / σύντομος κωδικός ξενοδοχείου (π.χ. CNT).',
               'Κελί ονόματος: tag «Οριστικοποιημένο/Management» κάτω από το όνομα, και από κάτω ο Κωδικός (#) + ΑΦΜ (όπως στη λίστα προφίλ).',
               'Παράθυρο καταχώρησης βάρδιας: ξεκόλλησε από το κάτω μέρος (σωστή θέση + εσωτερικό scroll) και ΣΕΡΝΕΤΑΙ με το ποντίκι από τον τίτλο.',
               'Dropdown λεπτών: μόνο 00 / 15 / 30 / 45.',
               'Χρώματα κωδικών βαρδιών παραμετροποιήσιμα από Ρυθμίσεις προγράμματος (color picker ανά κωδικό).',
               'Γυάλισμα μάσκας πολλαπλών βαρδιών/μέρα (κάρτες με χρώμα κωδικού). Ίδιες βελτιώσεις και στη Μηνιαία.']},
    {'v': '12.134', 'b': '414', 'date': '19/06/2026', 'time': '12:15', 'title': 'Πρόγραμμα: διάρκεια στο τσιπ + «σπαστό» + fix διπλό ξενοδοχείο',
     'items': ['Η διάρκεια κάθε βάρδιας (π.χ. 8h) εμφανίζεται πλέον ΜΕΣΑ στο τσιπ.',
               'Το κουμπί «+ τμήμα» μετονομάστηκε σε «+ σπαστό» (σπαστή/διακεκομμένη βάρδια — άλλο διάστημα ωρών στην ίδια βάρδια), ώστε να μην μπερδεύεται με το «τμήμα = department».',
               'Διόρθωση: στο dropdown «Ξενοδοχείο εργασίας» εμφανιζόταν δύο φορές η έδρα (η προεπιλογή ● + η λίστα)· τώρα μία.']},
    {'v': '12.133', 'b': '413', 'date': '19/06/2026', 'time': '11:45', 'title': 'Πρόγραμμα: γυάλισμα εμφάνισης (τσιπ βάρδιας + παράθυρο)',
     'items': ['Τα τσιπ βάρδιας έγιναν καθαρότερα: κωδικός έντονος + ώρες, και το ξενοδοχείο ως ΣΥΝΤΟΜΟΣ κωδικός (π.χ. CNT αντί «Central Hersonissos Hotel») σε μικρό badge — δεν χαλάει πια τη στοίχιση. Πλήρες όνομα στο tooltip.',
               'Το παράθυρο δήλωσης τακτοποιήθηκε: φαρδύτερο, καλύτερες αποστάσεις στις κάρτες βαρδιών, «+ Προσθήκη βάρδιας» σε όλο το πλάτος.']},
    {'v': '12.132', 'b': '412', 'date': '19/06/2026', 'time': '11:10', 'title': 'Πρόγραμμα: χρωματιστά τσιπ ανά βάρδια + προθάλαμος πολλαπλών/μέρα',
     'items': ['Στο πρόγραμμα, όταν μια μέρα έχει βάρδιες, η καθεμία εμφανίζεται ως χρωματιστό τσιπ με κωδικό + ώρες + ξενοδοχείο (⇄ αν είναι άλλο). Πιο ευδιάκριτο σε μέρες με 2 βάρδιες.',
               'Ο προθάλαμος ταυτοποίησης δέχεται πλέον πολλές βάρδιες/μέρα ανά όνομα· η σύνδεση με master μεταφέρει ΟΛΕΣ τις βάρδιες της μέρας (idempotent — χωρίς διπλά σε re-import).']},
    {'v': '12.131', 'b': '411', 'date': '19/06/2026', 'time': '10:30', 'title': 'Πολλές βάρδιες/μέρα (ΕΡΓ έδρα + ΕΩ άλλο ξενοδοχείο) + ιστορικό σε περιόδους',
     'items': ['Μια μέρα μπορεί πλέον να έχει ΠΟΛΛΕΣ βάρδιες: π.χ. ΕΡΓ 8ωρο στην έδρα + ΕΩ 1 ώρα το βράδυ σε άλλο ξενοδοχείο. Στο παράθυρο δήλωσης (πρόγραμμα + μηνιαία) προσθέτεις πολλές εγγραφές, καθεμία με δικό της κωδικό + ξενοδοχείο + ώρες.',
               'Τα σύνολα μετράνε σωστά: η μέρα = 1 εργάσιμη, οι ώρες αθροίζονται, και κάθε βάρδια πηγαίνει στο ξενοδοχείο της (μεροκάματα ανά ξενοδοχείο).',
               'Ιστορικό απασχόλησης ανά ΠΕΡΙΟΔΟ: για κάθε έτος/ξενοδοχείο φαίνεται το διάστημα (από–έως) + μέρες/ώρες — π.χ. AST 15/3–30/6, SRG 1/7–31/10.',
               'Τεχνικά: αφαιρέθηκε το «μία βάρδια/μέρα» (DB constraint, αναστρέψιμο)· νέο endpoint αποθήκευσης μέρας· πλήρης επαλήθευση με tests.']},
    {'v': '12.130', 'b': '410', 'date': '19/06/2026', 'time': '08:25', 'title': 'Πρόγραμμα: ορθογώνιο drag-select (γραμμές × στήλες)',
     'items': ['Στο πρόγραμμα, το drag-select πιάνει πλέον ΟΡΘΟΓΩΝΙΟ: κρατάς και τραβάς διαγώνια → επιλέγεις πολλούς εργαζόμενους × πολλές μέρες μαζί και δηλώνεις βάρδια σε όλους ταυτόχρονα.',
               'Το endpoint cells_bulk δέχεται πλέον λίστα κελιών (πολλοί εργαζόμενοι), με σωστή ενημέρωση WeekPlan ανά εργαζόμενο/εβδομάδα.']},
    {'v': '12.129', 'b': '409', 'date': '19/06/2026', 'time': '07:55', 'title': 'Κονσόλα = κέντρο ελέγχου: η καρτέλα γυρνά πίσω στη Διαχείριση Προσωπικού',
     'items': ['Στην πλήρη καρτέλα εργαζομένου μπήκε σύνδεσμος «← Διαχείριση Προσωπικού» (η κονσόλα γίνεται το μοναδικό σημείο ελέγχου· το Μητρώο μένει ως grid/βάση).',
               'Στο πάνελ της κονσόλας, ο σύνδεσμος «Συγχώνευση διπλών» δείχνει πλέον στο εσωτερικό tab «Διπλά» (όχι έξω). Η «Πλήρης καρτέλα» ανοίγει με επιστροφή στην κονσόλα.']},
    {'v': '12.128', 'b': '408', 'date': '19/06/2026', 'time': '07:20', 'title': 'Πρόγραμμα: στήλες Έξτρα/Ρεπό/Εργάσιμες + ιστορικό στην καρτέλα + work-hotel όνομα έδρας',
     'items': ['Στο board αφαιρέθηκε η στήλη «Ώρες» (είναι κανονικές εργάσιμες)· μένουν Έξτρα Ώρες / Ρεπό / Εργάσιμες.',
               'Στο παράθυρο δήλωσης βάρδιας, η προεπιλογή «Ξενοδοχείο εργασίας» δείχνει πλέον το όνομα του ανατεθειμένου ξενοδοχείου (● ... ανατεθειμένο), όχι γενικό «έδρα».',
               'Το «Ιστορικό απασχόλησης» (έτος × ξενοδοχείο) μπήκε και μέσα στην πλήρη καρτέλα εργαζομένου.']},
    {'v': '12.127', 'b': '407', 'date': '19/06/2026', 'time': '06:40', 'title': 'Ιστορικό απασχόλησης (έτος×ξενοδοχείο) + work-hotel στη βάρδια + rename',
     'items': ['Στην καρτέλα προφίλ (κονσόλα) μπήκε «Ιστορικό απασχόλησης»: πίνακας έτος × ξενοδοχείο με μέρες/ώρες/έξτρα — υπολογισμένος αυτόματα από τις βάρδιες (παρελθοντικά έτη + κατανομή ανά ξενοδοχείο).',
               'Στο παράθυρο δήλωσης βάρδιας προστέθηκε «Ξενοδοχείο εργασίας» (προεπιλογή: η έδρα). Αν κάποιος κάνει έξτρα σε άλλο ξενοδοχείο, το επιλέγεις και τροφοδοτεί τη γραμμή του δεύτερου ξενοδοχείου στο ιστορικό.',
               'Μετονομασία ετικέτας προέλευσης «Λογιστήριο» → «Οριστικοποιημένο» (παντού στα badges).']},
    {'v': '12.126', 'b': '406', 'date': '19/06/2026', 'time': '05:55', 'title': 'Κονσόλα Phase 2: Ασύνδετα & Διπλά μέσα + καλύτερος εντοπισμός διπλών',
     'items': ['Τα «Ασύνδετα» (ταυτοποίηση) και «Διπλά» (συγχώνευση) λειτουργούν πλέον ΜΕΣΑ στην κονσόλα — σύνδεση σε master, αγνόηση, ταυτοποίηση κλειδωμένων, επιβεβαίωση ακριβών, συγχώνευση/«δεν είναι διπλό» — χωρίς να φεύγεις από τη σελίδα.',
               'Ο εντοπισμός διπλών ελέγχει τώρα ΚΑΙ επίθετο ΚΑΙ όνομα (κοινά tokens, ανεξαρτήτως σειράς) — πιάνει και ανάποδη γραφή (π.χ. «Mihaela Varariu» = «VARARIU MIHAELA ANDREEA»).']},
    {'v': '12.125', 'b': '405', 'date': '19/06/2026', 'time': '05:20', 'title': 'Νέα κονσόλα «Διαχείριση προσωπικού» (1ο βήμα ενοποίησης)',
     'items': ['Μία οθόνη για ΟΛΑ τα προφίλ: αναζήτηση (όνομα/ΑΦΜ/κωδικός/username) + φίλτρα (Όλοι / 🔒 Λογιστήριο / Management / Λογαριασμοί) + δεξί πάνελ λεπτομερειών (κλικ → καρτέλα δίπλα).',
               'Ενέργειες πάνω στη γραμμή: πλήρης καρτέλα, ενεργοποίηση/κλείσιμο login, οριστική διαγραφή, συγχώνευση. Τα 🔒 Λογιστηρίου προστατεύονται.',
               'Τα «Ασύνδετα» και «Διπλά» συνδέονται προς τις υπάρχουσες ροές (θα ενσωματωθούν πλήρως στο επόμενο βήμα). Οι παλιές οθόνες παραμένουν μέχρι να επιβεβαιωθεί ότι όλα καλύπτονται.']},
    {'v': '12.124', 'b': '404', 'date': '19/06/2026', 'time': '04:45', 'title': 'Καθολική Αναζήτηση «τα πάντα» + οριστική διαγραφή χρήστη',
     'items': ['Η Αναζήτηση (/dashboard/search) ψάχνει πλέον ΟΛΑ τα προφίλ — login users + εργαζόμενους + χωρίς PII — με ετικέτα προέλευσης (🔒 Λογιστήριο / Management / λογαριασμός / ανενεργός) + Κωδικό/ΑΦΜ/ξενοδοχείο, καθώς και ασύνδετα (ταυτοποίηση) και ΟΛΕΣ τις λειτουργίες/σελίδες. Κάθε αποτέλεσμα οδηγεί κατευθείαν εκεί που ενεργείς.',
               'Στους Χρήστες: η «Διαγραφή» έγινε ΟΡΙΣΤΙΚΗ (πλήρης, με προστασία κλειδωμένων Λογιστηρίου). Για προσωρινό «εκτός» υπάρχει η «Απενεργοποίηση». Λύνει το «έσβησα αλλά μένει στη βάση».']},
    {'v': '12.123', 'b': '403', 'date': '19/06/2026', 'time': '04:10', 'title': 'Χρήστες: καθαρό header + νέος χρήστης εμφανίζεται/κάνει login',
     'items': ['Αφαιρέθηκε το διπλό εσωτερικό header μέσα στη σελίδα Χρηστών (το παρέχει ήδη το πάνω μενού).',
               'Ο νέος χρήστης που δημιουργείς από admin είναι πλέον εγκεκριμένος + ενεργός για login και εμφανίζεται αμέσως στη λίστα. Αν το username υπάρχει ή λείπει κωδικός, βγαίνει σαφές μήνυμα (πριν αποτύγχανε σιωπηλά).']},
    {'v': '12.122', 'b': '402', 'date': '19/06/2026', 'time': '03:40', 'title': 'Διόρθωση: παράθυρο βάρδιας δεν άνοιγε σε επόμενες εβδομάδες (scroll)',
     'items': ['BUG: σε προβολή πολλών εβδομάδων, μόνο η πρώτη άνοιγε το παράθυρο δήλωσης· οι επόμενες όχι, γιατί το popup (position:fixed) τοποθετούνταν εκτός οθόνης όταν είχες κατέβει στη σελίδα. Τώρα τοποθετείται σωστά στο σημείο του κελιού.']},
    {'v': '12.121', 'b': '401', 'date': '19/06/2026', 'time': '03:15', 'title': 'Διόρθωση: ο admin επεξεργάζεται πάντα + κωδικοί ανά ρόλο',
     'items': ['ΔΙΟΡΘΩΣΗ: η δήλωση σε επόμενη εβδομάδα μπλοκαριζόταν μετά την προθεσμία (Πέμπτη). Τώρα ο admin επεξεργάζεται ΠΑΝΤΑ κάθε εβδομάδα· οι managers παραμένουν στην προθεσμία.',
               'Επιτρεπτοί κωδικοί καταχώρησης ΑΝΑ ΡΟΛΟ: ξεχωριστές λίστες για Admin και για Managers (Πρόγραμμα·Ρυθμίσεις).']},
    {'v': '12.120', 'b': '400', 'date': '19/06/2026', 'time': '02:50', 'title': 'Κωδικοί: ΕΡΓ μόνο 8,5 + νέος ΕΩ (Έξτρα Ώρες)',
     'items': ['Ο κωδικός ΕΡΓ καταχωρείται ΜΟΝΟ για 8 ώρες + 30΄ διάλειμμα (8,5 παρουσία). Για λιγότερες/περισσότερες ώρες το σύστημα ζητάει τον νέο κωδικό ΕΩ.',
               'Νέος κωδικός ΕΩ (Έξτρα Ώρες): μετράει ως εργάσιμη, με τις δικές του ώρες (πάνω ή κάτω από το ΕΡΓ). Υπολογίζεται κανονικά σε ώρες/έξτρα.',
               'Η εισαγωγή/επικόλληση κωδικοποιεί αυτόματα: ώρες = 8,5 → ΕΡΓ, αλλιώς → ΕΩ.']},
    {'v': '12.119', 'b': '399', 'date': '19/06/2026', 'time': '02:20', 'title': 'Καταχώρηση βάρδιας: επιτρεπτοί κωδικοί, 24ωρο, πολλαπλές βάρδιες, στοίχιση',
     'items': ['Πρόγραμμα·Ρυθμίσεις: επιλέγεις ποιοι κωδικοί εμφανίζονται στο παράθυρο δήλωσης (πρόγραμμα + μηνιαία).',
               'Το ρολόι ώρας έγινε 24ωρο (επιλογή ώρας/λεπτών, χωρίς ΠΜ/ΜΜ).',
               'Πολλαπλές βάρδιες: το παράθυρο δείχνει τις ήδη καταχωρημένες ώρες και με «+ Επιπλέον βάρδια» προσθέτεις κι άλλη (σπαστή).',
               'Όταν κάποιος έχει >1 βάρδιες εμφανίζονται στοιχισμένες σε γραμμές (07:00 - 15:30 / 15:30 - 17:00).']},
    {'v': '12.118', 'b': '398', 'date': '19/06/2026', 'time': '01:45', 'title': 'Πρόγραμμα: ώρες χωρίς διάλειμμα + στήλη Έξτρα + drag-select',
     'items': ['Οι ΩΡΕΣ πλέον δεν μετράνε το 30λεπτο διάλειμμα: παρουσία 8,5 → 8 ώρες. Διάλειμμα αφαιρείται μόνο σε βάρδιες ≥6 ωρών (το 4ωρο μένει 4)· σε σπαστή βάρδια αφαιρείται επιπλέον 30πτο.',
               'Νέα στήλη «Έξτρα» στα εβδομαδιαία σύνολα (ώρες πάνω από 8,5 παρουσίας). Στο τέλος εβδομάδας: Ώρες, Έξτρα, Ρεπό, Εργάσιμες.',
               'Στο πρόγραμμα μπήκε το drag-select: κλικ-και-τράβηγμα πάνω στη γραμμή εργαζομένου → δήλωση βάρδιας σε όλο το εύρος ημερών μαζί.',
               'Οι ώρες εμφανίζονται ως «07:00 - 15:30». (Ισχύει και στη Μηνιαία συγκεντρωτική — ίδιος υπολογισμός.)']},
    {'v': '12.117', 'b': '397', 'date': '19/06/2026', 'time': '01:10', 'title': 'Μηνιαία: tags ID/ΑΦΜ + σύρσιμο πίνακα + drag-select εύρους',
     'items': ['Στα ονόματα μπήκαν badge προέλευσης + Κωδικός (ID) + ΑΦΜ, όπως αλλού.',
               'Οι πίνακες σύρονται με το ποντίκι (drag-to-scroll, χεράκι) — δεν χρειάζεται η κάτω μπάρα· η σελίδα πιάνει όλο το πλάτος της οθόνης.',
               'Αναλυτικό: κλικ-και-τράβηγμα πάνω στη γραμμή εργαζομένου επιλέγει εύρος ημερών και με το άφημα ανοίγει το παράθυρο δήλωσης — εφαρμόζεται σε όλες τις επιλεγμένες μέρες μαζί (νέο bulk endpoint). Πλήρως αμφίδρομο με το πρόγραμμα εργασίας.']},
    {'v': '12.116', 'b': '396', 'date': '19/06/2026', 'time': '00:30', 'title': 'Εποπτεία προγράμματος: τι κάνουν οι managers (μηνιαία)',
     'items': ['Νέα οθόνη (HR, admin): «Εποπτεία προγράμματος» — μηνιαία εικόνα του τι κάνουν οι managers στο πρόγραμμα εργασίας.',
               'Όψη «Ανά ξενοδοχείο»: πίνακας τμήμα×εβδομάδα με κατάσταση (Πρόχειρο/Υποβλήθηκε/Κλειδώθηκε) + ποιος το έκανε & πότε + γραμμή υποβολών ξενοδοχείου (version) + ποσοστό κάλυψης (ελλείψεις).',
               'Όψη «Ανά manager»: τι έκανε ο καθένας (εβδ. προγράμματα, υποβολές, ξενοδοχεία, τελευταία δραστηριότητα) + λίστα υποβολών με read-only προβολή περιεχομένου.']},
    {'v': '12.115', 'b': '395', 'date': '19/06/2026', 'time': '23:55', 'title': 'Μηνιαία συγκεντρωτική κατάσταση προγράμματος (για μεροκάματα)',
     'items': ['Νέα οθόνη (HR): «Μηνιαία συγκεντρωτική» ανά ξενοδοχείο/μήνα με σύνολα Ώρες/Έξτρα ώρες/Κυριακές/Εργάσιμες/Ρεπό ανά εργαζόμενο + υποσύνολα ξενοδοχείου + γενικό σύνολο. Για χρήση από Λογιστήριο + Management.',
               'Δύο όψεις: «Σύνοψη» (μία γραμμή/άτομο) και «Αναλυτικό» (πλήρες μηνιαίο grid 1–31 — κλικ σε κελί αλλάζει βάρδια, αυτόματη αποθήκευση, κλειδωμένες εβδομάδες προστατεύονται).',
               'Φίλτρα: μήνας/έτος/ξενοδοχείο/τμήμα. Εξαγωγή σε Excel και PDF. Πρόσβαση: admin + Λογιστήριο (accountant read-only).']},
    {'v': '12.114', 'b': '394', 'date': '19/06/2026', 'time': '23:30', 'title': 'Εκκαθάριση εισηγμένων: fix purge + «Καθαρισμός προθαλάμου» + ενιαία οθόνη',
     'items': ['ΔΙΟΡΘΩΣΗ: το «Καθαρισμός εισαγμένου προσωπικού» έσκαγε (foreign key) γιατί καθάριζε μόνο 3 πίνακες πριν τη διαγραφή. Τώρα χρησιμοποιεί πλήρη cascade (_hard_delete_user) ανά άτομο — ένα σφάλμα δεν μπλοκάρει τους υπόλοιπους, τα κλειδωμένα προστατεύονται.',
               'ΝΕΟ «Καθαρισμός προθαλάμου» στην Ταυτοποίηση προσωπικού: σβήνει ΟΛΕΣ τις βάρδιες σε αναμονή με 1 κλικ (ιδανικό για reset πριν σωστό re-import, π.χ. όταν τα ξενοδοχεία ήταν λάθος στην πηγή).',
               'ΝΕΑ οθόνη «Εισηγμένα προφίλ» (HR): κάθε keyless προφίλ με πρόταση επίσημου master (κωδικός+ΑΦΜ) + κουμπιά «Συγχώνευση με Master» (βάρδιες→επίσημο, μόνιμη σύνδεση) ή «Διαγραφή». Όλα σε ένα μέρος.']},
    {'v': '12.113', 'b': '393', 'date': '19/06/2026', 'time': '05:35', 'title': 'Πίνακας Νερών: πρόσβαση με το προφίλ (όχι μόνο admin) + scope ξενοδοχείων',
     'items': ['Όταν έδινες τον «Πίνακα Νερών» σε manager, το στοιχείο εμφανιζόταν αλλά η σελίδα ζητούσε login (ήταν κλειδωμένη σε admin). Τώρα η πρόσβαση ακολουθεί το δικαίωμα του μενού (water_dash) — όπως ο «Πίνακας Πισινών».',
               'Για μη-admin, ο Πίνακας Νερών δείχνει ΜΟΝΟ τα ανατεθειμένα ξενοδοχεία (scope προφίλ) και κρύβει τη διαχείριση δικτύων (προσθήκη/επεξεργασία/διαγραφή = admin-only).']},
    {'v': '12.112', 'b': '392', 'date': '19/06/2026', 'time': '05:10', 'title': 'Μενού ανά ρόλο: κοινό μητρώο (auto-sync) + drag & drop ανά ρόλο',
     'items': ['ΑΡΧΙΤΕΚΤΟΝΙΚΗ: τα ρυθμιζόμενα στοιχεία μενού ορίζονται πλέον ΜΙΑ φορά (MENU_REG στο extras.py). Από εκεί τραβάνε ΚΑΙ το πλαϊνό μενού (η ομάδα «Συντήρηση» γίνεται render με loop) ΚΑΙ ο επεξεργαστής. Νέο στοιχείο «Συντήρησης» = εμφανίζεται αυτόματα παντού.',
               'ΔΙΟΡΘΩΣΗ: ο «Πίνακας Νερών» ήταν κλειδωμένος σε admin (δεν φαινόταν στη λίστα ανά ρόλο). Τώρα είναι κανονικό ρυθμιζόμενο στοιχείο — μπορείς να τον δώσεις σε Manager/Staff. Όλα τα στοιχεία της «Συντήρησης» περνούν πλέον σωστά από έλεγχο ορατότητας (πριν κάποια εμφανίζονταν ανεξάρτητα από το tick).',
               'Νέος επεξεργαστής «Μενού ανά ρόλο»: στήλες Admin (κλειδωμένο=όλα) / Manager / Staff με drag & drop (ή κουμπιά M/S). Διατηρήθηκε το section για τα πάνω κουμπιά (workspaces).']},
    {'v': '12.111', 'b': '391', 'date': '19/06/2026', 'time': '04:30', 'title': 'Μόνιμο στοιχείο μενού «Ταυτοποίηση προσωπικού» (με μετρητή)',
     'items': ['Η οθόνη ταυτοποίησης μπήκε στο μενού (HR — Ανθρώπινο Δυναμικό) με ζωντανό πορτοκαλί μετρητή εκκρεμοτήτων — πριν φαινόταν μόνο σαν banner στην Εισαγωγή. Διαθέσιμη και στον προσαρμογέα μενού.']},
    {'v': '12.110', 'b': '390', 'date': '19/06/2026', 'time': '04:10', 'title': 'Μια ενιαια σελιδα συγχωνευσης διπλων (οχι δυο)',
     'items': ['Ενοποιηση (αιτημα Giannis «δεν χρειαζονται 2 φορμες που κανουν το ιδιο»): η συγχωνευση διπλων γινεται πλεον ΜΟΝΟ απο τη σελιδα «Πιθανες διπλοεγγραφες» (/dashboard/payroll/duplicates) — πληρες merge (βαρδιες + μισθοδοσια + αναθεσεις, αρχειοθετηση του αλλου). Παντα manual: εσυ διαλεγεις ποιον κρατας + επιβεβαιωνεις.',
               'Το «Προγραμμα·Προσωπικο» δεν εχει πια δικη του φορμα συγχωνευσης — δειχνει ποσα πιθανα διπλα υπαρχουν και κουμπι που οδηγει στην ενιαια σελιδα.',
               'Η ανιχνευση εγινε ΠΛΗΡΗΣ: πιανει ιδιο ΑΦΜ, ιδιο επωνυμο ΚΑΙ παρομοιο ονομα σε ΟΛΟΥΣ τους ενεργους (ακομα και schedule-imported χωρις μητρωο μισθοδοσιας).']},
    {'v': '12.109', 'b': '389', 'date': '19/06/2026', 'time': '03:40', 'title': 'Ταυτοποιηση: μαζικες κινησεις (ταυτοτητα κλειδωμενων + επιβεβαιωση ακριβων)',
     'items': ['Για να μη χρειαστουν εκατονταδες κλικ στην 1η εισαγωγη: κουμπι «Ταυτοποιηση κλειδωμενων» (φτιαχνει NameLink καθε locked master -> στο ιδιο του το ονομα· ταυτοτητα, ΟΧΙ merge· skip ασαφη) + κουμπι «Επιβεβαιωση ακριβων ταιριασματων» (συνδεει ολα τα ονοματα του προθαλαμου με ΑΚΡΙΒΕΣ 100% match· τα αμφιβολα μενουν για ελεγχο).',
               'Καμια αυτοματη ενεργεια χωρις το κλικ σου — εσυ πατας το κουμπι.']},
    {'v': '12.108', 'b': '388', 'date': '19/06/2026', 'time': '03:10', 'title': 'Ταυτοποιηση ΠΡΙΝ την εισοδο: το import δεν φτιαχνει/δεν ενωνει ποτε μονο του',
     'items': ['ΑΠΟΦΑΣΗ ΑΡΧΙΤΕΚΤΟΝΙΚΗΣ: η εισαγωγη προγραμματος ΔΕΝ δημιουργει πια προφιλ και ΔΕΝ κανει κανενα merge μονη της. Ολα τα ονοματα χωρις επιβεβαιωμενη συνδεση με κλειδωμενο master (αριθμος E0xxx + ΑΦΜ) πανε σε ΠΡΟΘΑΛΑΜΟ (PendingShift) και ΔΕΝ εμφανιζονται πουθενα στην πλατφορμα μεχρι να τα ταυτοποιησεις εσυ.',
               'Νεα οθονη «Ταυτοποιηση προσωπικου» (/dashboard/schedule/identify): για καθε ονομα σε αναμονη δειχνει προτεινομενα master (ταξινομημενα κατα ομοιοτητα) με τον αριθμο + ΑΦΜ τους. Εσυ κανεις το οριστικο «Συνδεσε» -> φτιαχνεται μονιμη συνδεση (NameLink) και οι βαρδιες περνουν στο master. Καμια αυτοματη ενεργεια ποτε.',
               'Χειροκινητη αναζητηση master (ονομα/ΑΦΜ/κωδικος) + κουμπι «Αγνοησε/σκουπιδι» για λαθος γραμμες. Μολις γινει η συνδεση μια φορα, οι επομενες εισαγωγες του ιδιου ονοματος πανε αυτοματα στο σωστο master (κανονας που οριζεις εσυ).',
               'Το ιδιο ισχυει και στην επικολληση (paste): ασυνδετα ονοματα -> «προς ταυτοποιηση», δεν εισαγονται. Αναιρει το αυτοματο fuzzy κουμπωμα του v12.107 (εκανε merge χωρις εγκριση).',
               'ΕΚΚΡΕΜΕΙ: ενοποιηση των δυο σελιδων συγχωνευσης σε μια (Προγραμμα·Προσωπικο + Μητρωο/Διπλοεγγραφες).']},
    {'v': '12.107', 'b': '387', 'date': '19/06/2026', 'time': '02:20', 'title': 'Εισαγωγη προγραμματος: εξυπνο ταιριασμα ονοματος (αναθεωρηθηκε στο v12.108)',
     'items': ['ΡΙΖΙΚΗ ΛΥΣΗ: η εισαγωγη προγραμματος ταιριαζε μονο με ΑΚΡΙΒΕΣ ονομα -> για καθε παραλλαγη (π.χ. ΝΙΚΟΣ vs ΝΙΚΟΛΑΟΣ) εφτιαχνε νεο διπλο προφιλ Management. Τωρα οι βαρδιες κουμπωνουν απευθειας στο επισημο προφιλ, με προτεραιοτητα στο master Λογιστηριου.',
               'Εξυπνο ταιριασμα 3 επιπεδων: (1) ακριβες ονομα, (2) ιδιες λεξεις σε αλλη σειρα (Επωνυμο/Ονομα αναποδα), (3) ασφαλες fuzzy (ιδιο επωνυμο + σχεδον ιδιο μικρο, π.χ. ΝΙΚΟΣ-ΝΙΚΟΛΑΟΣ, NATALIA-NATALIIA) - το fuzzy κουμπωνει ΜΟΝΟ σε κλειδωμενο master, ποτε δεν ενωνει δυο υπαρχοντα προφιλ μονο του.',
               'Σε ταιριασμα με υπαρχον προφιλ μεταφερονται ΜΟΝΟ οι βαρδιες - θεση/τμημα/ξενοδοχειο του master μενουν ως εχουν. Νεα στοιχεια γραφονται μονο οταν δημιουργειται νεο προφιλ.',
               'Το ιδιο εξυπνο ταιριασμα ισχυει και στην επικολληση προγραμματος (paste).']},
    {'v': '12.106', 'b': '386', 'date': '19/06/2026', 'time': '01:40', 'title': 'Συγχώνευση από Μητρώο μεταφέρει ΚΑΙ βάρδιες (consistency)',
     'items': ['Το merge στο Μητρώο/Διπλοεγγραφές μετέφερε μόνο στοιχεία μισθοδοσίας — ΟΧΙ βάρδιες (footgun). Τώρα μεταφέρει και τις βάρδιες, όπως και το merge στο Πρόγραμμα·Προσωπικό. Όποιο κι αν χρησιμοποιήσεις, οι βάρδιες πάνε στο προφίλ που κρατάς.']},
    {'v': '12.105', 'b': '385', 'date': '19/06/2026', 'time': '01:10', 'title': 'Πρόγραμμα Εργασίας: σωστό badge προέλευσης (αντί για μπερδεμένο λουκέτο)',
     'items': ['Στο board, το λουκετάκι «χωρίς login» αντικαταστάθηκε με το σωστό badge 🔒 Λογιστήριο / Management — ώστε να ξεχωρίζεις ποιο είναι το επίσημο προφίλ (πριν μπέρδευε: όλα τα imported έδειχναν λουκέτο).']},
    {'v': '12.104', 'b': '384', 'date': '19/06/2026', 'time': '00:45', 'title': 'Οριστική διαγραφή προφίλ (Καρτέλα + μαζικά), προστασία Λογιστηρίου',
     'items': ['Νέο κουμπί «Οριστική διαγραφή προφίλ» στην Καρτέλα + μαζική «Διαγραφή επιλεγμένων» στη Μαζική επεξεργασία (checkboxes). Καθαρίζει βάρδιες/προφίλ/PII/αναθέσεις/αξιολογήσεις και σβήνει τον χρήστη.',
               '🔒 ΠΡΟΣΤΑΣΙΑ: τα κλειδωμένα προφίλ Λογιστηρίου (Epsilon) ΔΕΝ διαγράφονται ποτέ. Για διπλά → προτιμήστε συγχώνευση (κρατά βάρδιες).']},
    {'v': '12.103', 'b': '383', 'date': '19/06/2026', 'time': '00:15', 'title': 'Συγχώνευση διπλών: μόνο βάρδιες + καθαρό UI + default το Λογιστήριο',
     'items': ['Στη συγχώνευση μεταφέρονται ΜΟΝΟ οι βάρδιες· θέση/τμήμα/ξενοδοχείο/προφίλ του προφίλ που κρατάς ΜΕΝΟΥΝ ως έχουν (τα ήδη assigned).',
               'Πιο καθαρό UI: η επιλεγμένη γραμμή (●) φωτίζεται πράσινη με σήμανση «ΚΡΑΤΑΣ», οι άλλοι «δίνει βάρδιες». Προεπιλογή keep = το 🔒 Λογιστήριο (κλειδωμένο master).']},
    {'v': '12.102', 'b': '382', 'date': '18/06/2026', 'time': '23:55', 'title': 'Badge προέλευσης προφίλ (Λογιστήριο vs Management) παντού',
     'items': ['Δίπλα σε κάθε εργαζόμενο, σε όλους τους πίνακες/καρτέλες/λειτουργίες (Μητρώο, Μαζική επεξεργασία, Διπλοεγγραφές, Έλεγχος, Πρόγραμμα·Προσωπικό, Αξιολόγηση), badge: 🔒 «Λογιστήριο» (κλειδωμένο master/Epsilon) ή «Management» (μη κλειδωμένο/εισαγωγή). Εξαιρούνται οι εκτυπώσεις/exports.']},
    {'v': '12.101', 'b': '381', 'date': '18/06/2026', 'time': '23:40', 'title': 'Πρόγραμμα: μαζική επικόλληση από Excel (copy-paste)',
     'items': ['Νέα σελίδα «Επικόλληση Προγράμματος»: επικολλάς απευθείας από Excel (όνομα + Δ–Κ ανά γραμμή), ταίριασμα ονομάτων με το Μητρώο, αναγνώριση κωδικών/ωρών (ίδιος parser με το import), preview πριν την αποθήκευση. Σύνδεσμος από Πρόγραμμα & Εισαγωγή.']},
    {'v': '12.100', 'b': '380', 'date': '18/06/2026', 'time': '23:00', 'title': 'Αξιολόγηση: HR-ορισμένες περίοδοι (dropdown)',
     'items': ['Το HR ορίζει τις περιόδους αξιολόγησης (π.χ. «1η αξιολόγηση» 15/05/2026) από «Περίοδοι». Στη φόρμα η περίοδος γίνεται dropdown με συγκεκριμένες επιλογές — ορίζει αυτόματα ονομασία, ημερομηνία & έτος. Fallback σε ελεύθερο κείμενο αν δεν έχουν οριστεί περίοδοι.']},
    {'v': '12.99', 'b': '379', 'date': '18/06/2026', 'time': '22:30', 'title': 'Αξιολόγηση: ρυθμιζόμενη στο «Μενού ανά ρόλο» + polish φόρμας',
     'items': ['Η «Αξιολόγηση προσωπικού» μπήκε ως γραμμή στο «Μενού ανά ρόλο» (ρυθμίζεις αν τη βλέπει ο Manager). Staff δεν τη βλέπει ποτέ (route manager+).',
               'Polish στη μάσκα αξιολόγησης: καθαρές κάρτες, ομάδες κριτηρίων, μεγάλα πεδία βαθμού με χρωματικό feedback (πράσινο/πορτοκαλί/κόκκινο), sticky μπάρα με progress + βαθμός + βαθμίδα.']},
    {'v': '12.98', 'b': '378', 'date': '18/06/2026', 'time': '22:00', 'title': 'Αξιολόγηση — πρόσβαση Managers (scoped ανά ξενοδοχείο)',
     'items': ['Οι managers αποκτούν το μενού «Αξιολόγηση προσωπικού»: δημιουργούν & υποβάλλουν αξιολογήσεις ΜΟΝΟ για το προσωπικό των ανατεθειμένων ξενοδοχείων τους. Δεν εγκρίνουν, δεν διαγράφουν, δεν βλέπουν πρότυπα/στατιστικά/άλλα ξενοδοχεία.',
               'Η τελική έγκριση/επιστροφή μένει στο HR/admin. Αν ένας manager δεν έχει ανάθεση ξενοδοχείων, βλέπει όλους (όπως σήμερα) — όρισε αναθέσεις από Διαχείριση → Χρήστες.']},
    {'v': '12.97', 'b': '377', 'date': '18/06/2026', 'time': '21:30', 'title': 'Αξιολόγηση Φ3 (απλό) — workflow έγκρισης + στόχοι + εκκρεμείς',
     'items': ['Στάδια: Πρόχειρο → Προς έγκριση → Εγκρίθηκε (+ Επιστροφή). Στη λίστα KPI «Προς έγκριση» & status chips. Στατιστικά μετρούν τις εγκεκριμένες.',
               'Στόχοι/ενέργειες βελτίωσης ανά αξιολόγηση (με παρακολούθηση — toggle ολοκλήρωσης στην προβολή). Λίστα «Εκκρεμείς αξιολογήσεις» (εργαζόμενοι χωρίς εγκεκριμένη για το έτος). Μένει HR-only — managers/αυτο-αξιολόγηση σταδιακά αργότερα.']},
    {'v': '12.96', 'b': '376', 'date': '18/06/2026', 'time': '21:00', 'title': 'Αξιολόγηση Φ2 ολοκληρωμένη — Group roll-up matrix + export',
     'items': ['Σελίδα «Group Evaluation»: πίνακας κριτήρια × υπάλληλοι ανά τμήμα/περίοδο (όπως το reference Group Evaluation Form), με μέσο όρο τμήματος ανά κριτήριο, σύνολο % ανά υπάλληλο, snapshot/τρέχον, Excel export.',
               'Η λειτουργία Αξιολόγησης Προσωπικού είναι πλήρης (Φ1+Φ2): φόρμα, πρότυπα/variants, records/versioning, στατιστικά, καρτέλα εργαζομένου, group matrix, exports.']},
    {'v': '12.95', 'b': '375', 'date': '18/06/2026', 'time': '20:30', 'title': 'Αξιολόγηση Φ2 — Διαχείριση & variants προτύπων',
     'items': ['Σελίδα «Πρότυπα Αξιολόγησης»: επεξεργασία κριτηρίων & βαρών (ζωντανό σύνολο=1.0), προσθήκη/διαγραφή κριτηρίων (όσα δεν χρησιμοποιούνται), δημιουργία variant ανά τμήμα (clone), ενεργό/ανενεργό.',
               'Στη φόρμα νέας αξιολόγησης διαλέγεις ποιο πρότυπο/φύλλο θα χρησιμοποιήσεις.']},
    {'v': '12.94', 'b': '374', 'date': '18/06/2026', 'time': '20:00', 'title': 'Αξιολόγηση Φ2 — Στατιστικά (τμήμα/property/περίοδος, trends, κατατάξεις)',
     'items': ['Σελίδα «Αξιολόγηση · Στατιστικά»: KPIs, roll-up ανά τμήμα & ανά ξενοδοχείο, top performers & «χρειάζονται προσοχή» (<60%), φίλτρα έτος/περίοδος/ξενοδοχείο.',
               'Διακόπτης ομαδοποίησης Snapshot (όπως τότε) vs Τρέχον (Μητρώο). Καρτέλα εργαζομένου: διαχρονική εξέλιξη + μέσος όρος ανά κριτήριο (σημεία βελτίωσης). Στη φόρμα φαίνεται καθαρά «Αξιολογείς για: τμήμα/ξενοδοχείο».']},
    {'v': '12.93', 'b': '373', 'date': '18/06/2026', 'time': '19:00', 'title': 'HR — Module Αξιολόγησης Προσωπικού (Φ1)',
     'items': ['Νέο module evaluations.py: φόρμα αξιολόγησης με το πρότυπο CONDIAN (25 σταθμισμένα κριτήρια, κλίμακα 1-10), ζωντανό σταθμισμένο % + βαθμίδα (Εξαιρετική/Καλή/Χρειάζεται προσοχή).',
               'Κάθε αξιολόγηση = ξεχωριστό record με μοναδικό κωδικό EVAL-έτος-νο, version/ιστορικό, σύγκριση με προηγούμενη. Λίστα με φίλτρα, προβολή, επεξεργασία, διαγραφή, export Excel. Μόνο HR/admin.',
               'Μενού HR → «Αξιολόγηση προσωπικού». Στοιχεία υπαλλήλου/τμήματος/property από το Μητρώο.']},
    {'v': '12.92', 'b': '372', 'date': '18/06/2026', 'time': '18:00', 'title': 'Dashboard Συντήρησης («Σήμερα») + κρυφά tabs όταν έρχεσαι από slot',
     'items': ['Το «Σήμερα» έγινε dashboard Συντήρησης: εκκρεμότητες καταγραφής + ειδοποιήσεις εκτός ορίου (σημερινές) + ανοιχτές βλάβες + γρήγορες ενέργειες (Δήλωση βλάβης/Καταγραφή τομέων). Πάντα ορατό στο μενού Συντήρηση για όσους κάνουν καταγραφή.',
               'Όταν ανοίγεις φόρμα από slot, τα Πρωί/Απόγευμα κρύβονται (έχει ήδη επιλεγεί η ώρα).']},
    {'v': '12.91', 'b': '371', 'date': '18/06/2026', 'time': '17:30', 'title': 'ΟΛΗ η πλατφόρμα σε ώρα Ελλάδος (Europe/Athens)',
     'items': ['Όλη η διεργασία τρέχει σε TZ=Europe/Athens και ΑΠΟΘΗΚΕΥΕΙ τις ώρες τοπικά (όχι UTC) — υποβολές, ημερομηνία καταγραφής, βλάβες, πρόγραμμα, μισθοδοσία, logs, ειδοποιήσεις, backups, σχόλια: όλα σε ώρα Ελλάδος.',
               'Το date.today()/τώρα δείχνει σωστή ημέρα Ελλάδος (διορθώνει και υποβολές αργά το βράδυ). Σχεδιαστές υπενθυμίσεων/backup ήδη σε Αθήνα.']},
    {'v': '12.90', 'b': '370', 'date': '18/06/2026', 'time': '17:00', 'title': 'Ώρα Ελλάδος στις υποβολές (όχι UTC)',
     'items': ['Οι ώρες των υποβολών (Records, Πίνακας Πισινών, Πίνακας Νερών, εξαγωγές) δείχνουν πλέον ώρα Ελλάδος (Europe/Athens) αντί για UTC — π.χ. 09:54 αντί 06:54. Η αποθήκευση παραμένει UTC, μετατροπή στην εμφάνιση (filter |gr).']},
    {'v': '12.89', 'b': '369', 'date': '18/06/2026', 'time': '16:30', 'title': 'Κρυφή εγγραφή μέχρι το go-live',
     'items': ['Ο σύνδεσμος «Εγγραφή νέου χρήστη» κρύφτηκε από το login και το /register κλειδώθηκε (redirect στο login) — διακόπτης REGISTRATION_OPEN=False. Όταν πάμε live γίνεται True.']},
    {'v': '12.88', 'b': '368', 'date': '18/06/2026', 'time': '16:00', 'title': 'Redesign φορμών μετρήσεων + καθαρό header + mobile + υποχρεωτικά πεδία',
     'items': ['Νέο, καθαρό UI στις φόρμες Πισινών & Νερών (mobile-first): tabs Πρωί/Απόγευμα, μεγάλα πεδία, sticky κουμπί αποθήκευσης με μετρητή «X από N».',
               'Όλα τα πεδία μέτρησης ΥΠΟΧΡΕΩΤΙΚΑ (εκτός Σημειώσεων) — το κουμπί ενεργοποιείται μόνο όταν συμπληρωθούν όλα.',
               'Εκτός ορίου: κόκκινο περίγραμμα + μικρή ένδειξη στο πεδίο, και ΜΙΑ καθαρή κάρτα «Προτεινόμενες ενέργειες» (όχι σκόρπια μηνύματα).',
               'Header: αφαιρέθηκαν logo/«CONDIAN HOTELS», ο εναλλάκτης ξενοδοχείου, και το όνομα χρήστη από τις φόρμες. Στο κινητό κρύβεται το footer & διορθώθηκε το μενού προφίλ.']},
    {'v': '12.87', 'b': '367', 'date': '18/06/2026', 'time': '15:00', 'title': 'Records: προβολή + επεξεργασία + διαγραφή καταγραφών',
     'items': ['Στο «Records» κάθε καταγραφή (πισίνας/νερού) έχει πλέον κουμπί επεξεργασίας ΚΑΙ διαγραφής (με επιβεβαίωση). Η διαγραφή είναι μόνο για admin/masteradmin.',
               'Νέα routes: /pools/record/<id>/delete & /water/record/<id>/delete (POST, admin-only).']},
    {'v': '12.86', 'b': '366', 'date': '18/06/2026', 'time': '14:30', 'title': 'Ενημέρωση = toggle ανά ρόλο + αφαίρεση φίλτρου περιόδου από την μπάρα',
     'items': ['Η ομάδα μενού «Ενημέρωση» (Αναζήτηση/Roadmap/FAQ/feedback) γίνεται κρυφή/ορατή ανά ρόλο από «Μενού ανά ρόλο» (νέα γραμμή «Ενημέρωση»). Οι admin τη βλέπουν πάντα.',
               'Αφαιρέθηκε το global φίλτρο περιόδου («Όλα» με ημερομηνία) από την πάνω μπάρα — ήταν κατάλοιπο. Το φιλτράρισμα παραμένει διαθέσιμο server-side (default: όλα)· αν χρειαστεί φίλτρο σε συγκεκριμένο πίνακα, μπαίνει τοπικά εκεί.']},
    {'v': '12.85', 'b': '365', 'date': '18/06/2026', 'time': '14:00', 'title': 'Πάνω κουμπιά (Workspaces) ανά ρόλο + ρύθμιση από UI',
     'items': ['Τα κουμπιά Operations / Staff HUB / Admin / Guest App ελέγχονται ανά ρόλο: Staff -> Operations + Staff HUB· Manager -> + Guest App· Admin/Master -> και τα 4.',
               'Ρυθμίζεται & φαίνεται με μια ματιά στο: Διαχείριση -> Χρήστες & Πρόσβαση -> «Μενού ανά ρόλο» (νέος πίνακας «Πάνω κουμπιά ανά ρόλο»), αποθήκευση στη βάση χωρίς redeploy.',
               'Το Admin workspace μένει πάντα μόνο για admin/masteradmin.']},
    {'v': '12.84', 'b': '364', 'date': '18/06/2026', 'time': '13:00', 'title': 'Ενοποίηση μενού: ένας πυλώνας «Συντήρηση»',
     'items': ['Οι ομάδες «Καταγραφές», «Παρακολούθηση» και «Συντήρηση» ενώθηκαν σε έναν πυλώνα «Συντήρηση»: Σήμερα · Πισίνες · Νερά · Καταγραφή τομέων · Δήλωση βλάβης, και (για admin) Πίνακας Βλαβών/Πισινών/Νερών/τομέων · Records · Εβδομαδιαία κάλυψη.',
               'Πιο καθαρή οργάνωση ευθυγραμμισμένη με το business domain: οι καταγραφές πισίνας/νερού ανήκουν εννοιολογικά στη Συντήρηση/Βλαβοληψία.',
               'Καμία αλλαγή λειτουργίας/δεδομένων — μόνο ομαδοποίηση μενού (ρυθμίζεται και από Διαμόρφωση μενού).']},
    {'v': '12.83', 'b': '363', 'date': '18/06/2026', 'time': '12:00', 'title': 'Νέα οθόνη «Σήμερα» καταγραφών + redesign φορμών Πισινών/Νερών',
     'items': ['Νέα οθόνη «Σήμερα» (Καταγραφές): κάρτες ανά πισίνα & δίκτυο νερού με 2 slots (πρωί 08:00 / απόγευμα 17:00), πράσινο = έγινε. Ένα tap ανοίγει τη φόρμα έτοιμη (λιγότερα κλικ).',
               'Οι φόρμες ανοίγουν προ-επιλεγμένες (πισίνα/δίκτυο + ώρα) και προ-συμπληρώνονται αν υπάρχει ήδη καταγραφή (επεξεργάσιμες).',
               'Ζωντανό πάνελ «Επόμενα βήματα»: καθώς γράφεις τις μετρήσεις, προτείνει ενέργειες για εύρυθμη λειτουργία (όρια -> ενέργειες). Έτοιμο για ανάλυση SpithaAI όταν ενεργοποιηθεί το AI.',
               'Ο συντηρητής (staff) βλέπει στο μενού ΜΟΝΟ την καταγραφή· τα στατιστικά/πίνακες μένουν σε admin/masteradmin.']},
    {'v': '12.82', 'b': '362', 'date': '17/06/2026', 'time': '22:30', 'title': 'Πλήρες lossless round-trip των 2 μητρώων (ανεξάρτητα από Epsilon)',
     'items': ['Το export βγάζει ΟΛΑ τα πεδία: ταυτότητα (ΑΦΜ/ΑΜΚΑ/ΙΚΑ/IBAN/ημ.πρόσληψης-αποχώρησης/κέντρο κόστους/ειδικότητα) + όλα τα οικονομικά ανά μήνα (μικτά/ΕΦΚΑ/ΦΜΥ/καθαρά/κόστος εργοδότη).',
               'Η «Φόρτωση Μητρώου» διαβάζει πλέον και το φύλλο «Μισθοδοσία ανά μήνα» -> γεμίζει τα ποσά (δεν εξαρτάται από τα 11 Epsilon). Το export Management βγαίνει σε φύλλο «Μητρώο Management» (επαναφορτώσιμο).',
               'Καθαρισμός βάσης πλέον καθαρίζει και MgmtAssignment/ιστορικότητα/flags. Round-trip: wipe -> φόρτωση 2 αρχείων -> πλήρης βάση.']},
    {'v': '12.81', 'b': '361', 'date': '17/06/2026', 'time': '21:30', 'title': 'Ανθεκτική φόρτωση modules (no total crash)',
     'items': ['Τα plug-in modules φορτώνονται πλέον ανθεκτικά: αν ένα λείπει/σπάσει, καταγράφεται προειδοποίηση και η εφαρμογή συνεχίζει — δεν πέφτει ΟΛΗ (όπως έγινε με το ξεχασμένο menu.py).']},
    {'v': '12.80', 'b': '360', 'date': '17/06/2026', 'time': '20:00', 'title': 'Export μητρώων από τη βάση + Διαμόρφωση μενού (self-service)',
     'items': ['Εξαγωγή σε Excel ΑΠΟ ΤΗ ΖΩΝΤΑΝΗ ΒΑΣΗ για κάθε μητρώο (Λογιστηρίου & Management) — τα 2 master γίνονται πάντα καθρέφτης της βάσης.',
               'Νέα «Διαμόρφωση μενού» (admin, drag & drop): ομάδες/σειρά/μετονομασία/απόκρυψη του αριστερού μενού, αποθήκευση στη βάση. Ενεργό μόνο όταν αποθηκευτεί (αλλιώς προεπιλεγμένο)· ισχύει για admin.']},
    {'v': '12.79', 'b': '359', 'date': '17/06/2026', 'time': '19:00', 'title': 'Μαζική επεξεργασία προσωπικού (grid) + διαγραφή ανάθεσης',
     'items': ['Νέα «Μαζική επεξεργασία προσωπικού» (HR): συγκεντρωτικός πίνακας όλου του προσωπικού με inline edit (μονάδα/τμήμα/θέση/συμφωνία/τηλ/email/κέντρο κόστους/κατάσταση) και αποθήκευση όλων μαζί — γρήγορες διορθώσεις live πάνω στη βάση.',
               'Κουμπί «Διαγραφή ανάθεσης» στην καρτέλα εργαζομένου (ολοκλήρωση live editing αναθέσεων).']},
    {'v': '12.78', 'b': '358', 'date': '17/06/2026', 'time': '18:30', 'title': 'Κέντρο Εισαγωγής: συμμάζεμα κάρτας Μισθοδοσίας (3 βήματα με σειρά)',
     'items': ['Η κάρτα «Μισθοδοσία/Προσωπικό» έγινε 3 καθαρά βήματα με σειρά: ① Φόρτωση Μητρώου (ταυτότητες) → ② Εισαγωγή Epsilon (ποσά) → ③ Εισαγωγή Management (αναθέσεις/θέσεις/συμφωνίες).',
               'Αφαιρέθηκε το απαρχαιωμένο «Συγχρονισμός Master (Excel→Βάση)» από την κάρτα (route παραμένει).']},
    {'v': '12.77', 'b': '357', 'date': '17/06/2026', 'time': '18:00', 'title': 'Κεντρικές πιθανές διπλοεγγραφές + «Δεν είναι διπλό» + δέντρο μηνών',
     'items': ['Νέα σελίδα «Πιθανές διπλοεγγραφές» (HR): όλα τα ζεύγη με ίδιο επώνυμο/ΑΦΜ, με λόγο + στοιχεία/ιστορικό και των δύο + κουμπιά Ένωσε(Α/Β) ή «Δεν είναι διπλό».',
               '«Δεν είναι διπλό»: σταματά να εμφανίζεται το ζεύγος (π.χ. αδέρφια με ίδιο επώνυμο). Διαθέσιμο και στην καρτέλα.',
               'Οικονομικά καρτέλας: κάθε έτος ανοίγει σε ΜΗΝΕΣ (δέντρο) με τα καθαρά ανά μήνα.']},
    {'v': '12.76', 'b': '356', 'date': '17/06/2026', 'time': '17:00', 'title': 'Διαχωρισμός Ειδικότητας (Λογιστήριο) από Θέση (Management)',
     'items': ['Στο μητρώο η στήλη «Τμήμα» δείχνει πλέον ΜΟΝΟ Management (καμία ανάμειξη με την ειδικότητα Λογιστηρίου).',
               'Νέο flag «Λείπει θέση (Management)» ξεχωριστό από «Λείπει ειδικότητα (Λογιστήριο)» — δύο διαφορετικά πεδία, ποτέ συγχωνευμένα.']},
    {'v': '12.75', 'b': '355', 'date': '17/06/2026', 'time': '16:00', 'title': 'Κουμπί «Αντιγραφή» σε κάθε πίνακα (→ Excel)',
     'items': ['Κάθε πίνακας στις σελίδες αποκτά κουμπί «⧉ Αντιγραφή» που τον αντιγράφει σε μορφή TSV — επικολλάται κατευθείαν σε στήλες στο Excel/Sheets.']},
    {'v': '12.74', 'b': '354', 'date': '17/06/2026', 'time': '15:30', 'title': 'Διαγνωστικά & Logs + SQL console + Έξυπνο Search',
     'items': ['Νέα «Διαγνωστικά» (masteradmin): καταγραφή σφαλμάτων (log), εντολές (HEALTH/COUNTS/SCHEMA/EMP/FIND/FLAGS/ERRORS/TABLE) και read-only SQL console (μόνο SELECT) — για στοχευμένη υποστήριξη.',
               'Νέα «Αναζήτηση» σε όλη την πλατφόρμα: εργαζόμενοι/ΑΦΜ, βλάβες, ερωτηματολόγια, ξενοδοχεία, σελίδες — με συνδέσμους.']},
    {'v': '12.73', 'b': '353', 'date': '17/06/2026', 'time': '14:00', 'title': 'Μενού: ενοποίηση σε «HR — Ανθρώπινο Δυναμικό»',
     'items': ['Η Μισθοδοσία και το «Πρόγραμμα & Προσωπικό» ενοποιήθηκαν σε μία ενότητα «HR — Ανθρώπινο Δυναμικό» (Μητρώο, Πρόγραμμα Εργασίας, Υποβολές, Εκτελέσεις, Έλεγχος, Χρειάζονται προσοχή, Εταιρείες, Συντελεστές, ρυθμίσεις προγράμματος).',
               'Οι εισαγωγές παραμένουν στο Κέντρο Εισαγωγής (με σύνδεσμο από το HR).']},
    {'v': '12.72', 'b': '352', 'date': '17/06/2026', 'time': '13:00', 'title': 'Σάρωμα πληρότητας προφίλ — όλα τα κενά στο «Χρειάζονται προσοχή»',
     'items': ['Νέο «Σάρωση τώρα»: ελέγχει ΟΛΑ τα προφίλ (Λογιστηρίου + Management) και σηκώνει flag για κάθε κενό πεδίο (ΑΦΜ/ΑΜΚΑ/ΙΚΑ/πατρώνυμο/IBAN/τράπεζα/ημ.πρόσληψης/ειδικότητα/τηλέφωνο/email/κέντρο κόστους/ανάθεση/συμφωνία).',
               'Το «Χρειάζονται προσοχή» έγινε δυναμικό: chips ανά τύπο με πλήθος + φίλτρο, για μαζική τακτοποίηση. Τρέχει αυτόματα και μετά το import Management.']},
    {'v': '12.71', 'b': '351', 'date': '17/06/2026', 'time': '12:00', 'title': 'Management layer: αναθέσεις με ιστορικό, ιστορικότητα προφίλ, «Χρειάζονται προσοχή», merge',
     'items': ['Αναθέσεις Management ανά εργαζόμενο με ιστορικό (τμήμα/θέση/συμφωνία + από/έως). «Αλλαγή τμήματος/θέσης» κλείνει αυτόματα την προηγούμενη.',
               'Καρτέλα εργαζομένου: ενότητα Αναθέσεων + «Ιστορικότητα προφίλ» (audit: δημιουργία/import/merge/αλλαγές) + banner εκκρεμοτήτων.',
               'Νέο κέντρο «Χρειάζονται προσοχή»: ορφανά, χωρίς κλειδί/ΑΦΜ, αναθέσεις χωρίς ημερομηνία, πιθανά διπλά — κοινό για όλα τα modules.',
               'Εργαλείο συγχώνευσης (merge) διπλών προφίλ με καταγραφή στην ιστορικότητα.',
               'Εισαγωγή «Management» στο Κέντρο Εισαγωγής (αναθέσεις/κλειδιά/ορφανά/flags, idempotent).']},
    {'v': '12.70', 'b': '350', 'date': '17/06/2026', 'time': '10:30', 'title': 'Μενού Μισθοδοσίας: συγκέντρωση + εισαγωγές στο Κέντρο Εισαγωγής',
     'items': ['Το μενού «Διαχείριση → Μισθοδοσία» απέκτησε στην κορυφή «Μητρώο εργαζομένων» (+ Εκτελέσεις, Έλεγχος & Έγκριση) ώστε να βρίσκεται εύκολα.',
               'Οι εισαγωγές (Epsilon, Master, Φόρτωση Μητρώου) αφαιρέθηκαν από το μενού Μισθοδοσίας — βρίσκονται πλέον ΜΟΝΟ στο «Κέντρο Εισαγωγής» (μία πηγή για όλες τις εισαγωγές), με σύνδεσμο-συντόμευση από τη Μισθοδοσία.']},
    {'v': '12.69', 'b': '349', 'date': '17/06/2026', 'time': '09:00', 'title': 'Μητρώο: κουμπιά κατάστασης + έτος + ποσά· καρτέλα οικονομικών· ένωση εισαγωγών',
     'items': ['Μητρώο: κουμπιά «Ενεργοί / Ανενεργοί / Όλοι» στην κορυφή (αντί κρυμμένων ανενεργών) + επιλογέας «Έτος εικόνας» (κάθε χρονιά ή «Όλα»).',
               'Νέες στήλες ποσών στο μητρώο: «Καθαρά τακτικά» του έτους + «Σύνολο (+δώρα/άδεια)» + πλήθος μηνών.',
               'Καρτέλα εργαζομένου: πίνακας οικονομικών (Λογιστήριο/Epsilon) ανά έτος — καθαρά τακτικά, δώρα/άδεια, σύνολο, Μ.Ο./μήνα — δίπλα στη Συμφωνία (Management).',
               'Κέντρο Εισαγωγής: όλες οι εισαγωγές μισθοδοσίας (Epsilon, Φόρτωση Μητρώου, Συγχρονισμός Master) συγκεντρώθηκαν σε μία κάρτα.']},
    {'v': '12.68', 'b': '348', 'date': '17/06/2026', 'time': '06:30', 'title': 'Φόρτωση Μητρώου από κεντρικό Excel (νέα πηγή αλήθειας)',
     'items': ['Νέα σελίδα «Φόρτωση Μητρώου»: διαβάζει το φύλλο «ΛΟΓΙΣΤΗΡΙΟ (σύνοψη)» του κεντρικού Excel και χτίζει το μητρώο εργαζομένων (298 κλειδωμένες ταυτότητες, πηγή=Λογιστήριο).',
               'Καθαρισμός & φόρτωση από την αρχή (masteradmin), με επιβεβαίωση ΦΟΡΤΩΣΗ.',
               'Σημείωση δεδομένων: τα αρχεία 2023–2025 δεν έχουν Κωδ.ΥΠΟΚ — άρα το νομικό ξενοδοχείο προκύπτει μόνο για τους ενεργούς (2026). Το κέντρο κόστους θα έρθει από το Management.']},
    {'v': '12.67', 'b': '347', 'date': '15/06/2026', 'time': '18:30', 'title': 'Διόρθωση εισαγωγής ιστορικών (μεγάλες ταμπέλες περιόδου)',
     'items': ['Διορθώθηκε σφάλμα στην εισαγωγή Epsilon ιστορικών ετών: ταμπέλες όπως «Επίδομα αδείας προηγούμενου έτους» ξεπερνούσαν το όριο και έσκαγαν. Τώρα συντομεύονται σωστά.']},
    {'v': '12.66', 'b': '346', 'date': '15/06/2026', 'time': '02:00', 'title': 'Μητρώο: ιστορικό + Ενεργός/Ανενεργός',
     'items': ['Το master υποστηρίζει ΟΛΟΥΣ τους εργαζόμενους όλων των ετών (2023–2026) με στήλη «Κατάσταση» (Ενεργός/Ανενεργός).',
               'Η εισαγωγή Master διαβάζει την Κατάσταση → ενεργοποιεί/απενεργοποιεί τον εργαζόμενο.']},
    {'v': '12.65', 'b': '345', 'date': '15/06/2026', 'time': '01:00', 'title': 'Mini Market (ΥΠΟΚ 0003) + κέντρα κόστους',
     'items': ['Νέο υποκατάστημα «Mini Market» (ΥΠΟΚ 0003, Γιαννουλάκης) — μόνο μισθοδοσία/νομικά, εκτός λειτουργικού.',
               'Κέντρα κόστους: τα 5 ξενοδοχεία + Headquarter. Το «Κεντρικό» (Γιαννουλάκης) πάει στο Headquarter.']},
    {'v': '12.64', 'b': '344', 'date': '15/06/2026', 'time': '00:20', 'title': 'Διόρθωση backup (μεγάλα αρχεία) + ώρα Ελλάδας',
     'items': ['Διορθώθηκε το σφάλμα στα backup: μεγάλα αρχεία ανεβαίνουν πλέον με upload session (>4MB), ανθεκτικό dump ανά πίνακα, και τα στατιστικά δεν χάνονται σε αποτυχία.',
               'Οι ώρες στο ιστορικό backup δείχνουν πλέον ώρα Ελλάδας.',
               'Roadmap: αυτόματο backup πριν από καθαρισμό + αρχειοθέτηση obsolete στοιχείων.']},
    {'v': '12.63', 'b': '343', 'date': '14/06/2026', 'time': '23:55', 'title': 'Master: απλό import (χωρίς SharePoint sync)',
     'items': ['Η σελίδα έγινε σκέτη «Εισαγωγή Master»: ανεβάζεις το Excel και μπαίνει όλο στη βάση. Αφαιρέθηκε το κομμάτι του αυτόματου SharePoint sync.']},
    {'v': '12.62', 'b': '342', 'date': '14/06/2026', 'time': '23:40', 'title': 'Όνομα/επώνυμο χωριστά · ενοποιημένα Epsilon · πλήρης καθαρισμός',
     'items': ['Όνομα και Επώνυμο αποθηκεύονται χωριστά.',
               'Εισαγωγή Epsilon: αναγνώριση εταιρείας από το περιεχόμενο (ενοποιημένα αρχεία ανά εταιρεία).',
               'Νέο «Πλήρης καθαρισμός προσωπικού» (masteradmin) — σβήνει ΟΛΟ το προσωπικό, και τα κλειδωμένα.']},
    {'v': '12.61', 'b': '341', 'date': '14/06/2026', 'time': '23:00', 'title': 'Μητρώο: δείκτης «Χωρίς κλειδί»',
     'items': ['Νέο πλακίδιο + κόκκινη ειδοποίηση «Χωρίς κλειδί»: εμφανίζει όσους λείπει ΑΦΜ ή Κωδ. Εργαζομένου, για άμεση διόρθωση.',
               'Φίλτρο «χωρίς κλειδί» + σήμανση στη στήλη PII (⚠ ΑΦΜ / ⚠ Κωδ.).']},
    {'v': '12.60', 'b': '340', 'date': '14/06/2026', 'time': '22:30', 'title': 'Συγχρονισμός Master File (Excel → Βάση)',
     'items': ['Νέα σελίδα «Συγχρονισμός Master»: ανεβάζεις το master Excel και ενημερώνει τη βάση (Μητρώο + Συμφωνίες, κλειδί ΑΦΜ).',
               'On-demand κουμπί + νυχτερινός αυτόματος συγχρονισμός από SharePoint (όταν ρυθμιστεί). Idempotent, χωρίς διπλά.',
               'Νέα στήλη «Κωδ. Εργαζομένου» (σταθερό εσωτερικό κλειδί) στα ευαίσθητα στοιχεία.']},
    {'v': '12.59', 'b': '339', 'date': '14/06/2026', 'time': '21:45', 'title': 'Μισθοδοσία: managerial μονάδα / κέντρο κόστους (CND)',
     'items': ['Κάθε εργαζόμενος έχει τώρα ΚΑΙ «Μονάδα / Κέντρο Κόστους» (managerial: AST/CNT/IRO/SRG/PSV/CND) — μπορεί να διαφέρει από το νομικό ξενοδοχείο ασφάλισης.',
               'Π.χ. ασφαλισμένος στο Iro αλλά ανήκει στα Κεντρικά Γραφεία (CND) — όταν φιλτράρεις CND εμφανίζεται.',
               'Νέα φίλτρα & στήλη «Μονάδα» σε Μητρώο και Έλεγχο & Έγκριση.']},
    {'v': '12.58', 'b': '338', 'date': '14/06/2026', 'time': '21:15', 'title': 'Αυθεντικός χάρτης εταιρειών/ΥΠΟΚ · κατάργηση Palm',
     'items': ['Καταχωρήθηκαν οι 3 νομικές οντότητες με ΑΦΜ (Γιαννουλάκης 094082480, Σεργίου 094084694, Πισκοπιανό 094121123) και ο Κωδικός Υποκαταστήματος ανά ξενοδοχείο.',
               'Το ξενοδοχείο βγαίνει ΝΤΕΤΕΡΜΙΝΙΣΤΙΚΑ από τον κωδικό ΥΠΟΚ (Γιαννουλάκης: 0002=Asterias, 0001=Central, 0000=Iro).',
               'Καταργήθηκε το Palm Island Suites (PLM/ΦΥΤΩΡΙΑ) από τις ρυθμίσεις.']},
    {'v': '12.57', 'b': '337', 'date': '14/06/2026', 'time': '20:45', 'title': 'Διορθώσεις Epsilon: σωστό ξενοδοχείο + πολλαπλά αρχεία',
     'items': ['Διόρθωση: το «Κεντρικό» (έδρα/λογιστικό υποκατάστημα) ΔΕΝ μπερδεύεται πια με το Central Hersonissos — το ξενοδοχείο βγαίνει από την εταιρεία.',
               'Η Εισαγωγή Epsilon δέχεται πλέον ΠΟΛΛΑ αρχεία μαζί (drag & drop) και δείχνει αποτέλεσμα για το καθένα.']},
    {'v': '12.56', 'b': '336', 'date': '14/06/2026', 'time': '20:15', 'title': 'Μισθοδοσία: το Epsilon γίνεται master μητρώου (κλείδωμα)',
     'items': ['Η Εισαγωγή Epsilon δημιουργεί πλέον ΚΑΤΕΥΘΕΙΑΝ τους εργαζόμενους (χρήστες) με τα επίσημα στοιχεία (ΑΦΜ/ΑΜΚΑ/ειδικότητα/τράπεζα/ξενοδοχείο) και τους κλειδώνει.',
               'Τα κλειδωμένα μητρώα δεν τα γράφουν ούτε τα σβήνουν άλλες πηγές (πρόγραμμα/μισθοδοσία).',
               'Ο καθαρισμός εισαγμένου προσωπικού δεν αγγίζει πλέον τα κλειδωμένα.']},
    {'v': '12.55', 'b': '335', 'date': '14/06/2026', 'time': '19:40', 'title': 'Πρόγραμμα: προβολή μήνα · Μητρώο: φίλτρα',
     'items': ['Πρόγραμμα Εργασίας: επιλογή μήνα (δείχνει όλον τον μήνα) + βελάκια προηγ./επόμενου μήνα.',
               'Μητρώο μισθοδοσίας: φίλτρα εταιρεία/ξενοδοχείο/κατάσταση + τα πλακίδια (KPI) λειτουργούν ως φίλτρα.',
               'Υπενθύμιση: το βελάκι ⇄ σε μια βάρδια = ο εργαζόμενος δούλεψε σε άλλο ξενοδοχείο εκείνη τη μέρα.']},
    {'v': '12.54', 'b': '334', 'date': '14/06/2026', 'time': '19:10', 'title': 'UX: μενού Διαχείρισης σε κατηγορίες · ώρα Ελλάδας · drag & drop',
     'items': ['Το μενού «Διαχείριση» οργανώθηκε σε αναπτυσσόμενες υποκατηγορίες (Χρήστες & Πρόσβαση, Ξενοδοχεία & Τομείς, Συντήρηση, Προσωπικό & Πρόγραμμα, Μισθοδοσία, Δεδομένα & Σύστημα).',
               'Το ρολόι δείχνει πλέον ώρα Ελλάδας (Europe/Athens) ανεξάρτητα από τον server.',
               'Στις εισαγωγές αρχείων: drag & drop — σύρε τα αρχεία πάνω στη σελίδα αντί να τα επιλέγεις.']},
    {'v': '12.53', 'b': '333', 'date': '14/06/2026', 'time': '18:40', 'title': 'Έγκριση μισθοδοσίας — ειδοποίηση μόνο σε masteradmin',
     'items': ['Η ειδοποίηση/email έγκρισης μισθοδοσίας πηγαίνει προς το παρόν μόνο στους masteradmin.']},
    {'v': '12.52', 'b': '332', 'date': '14/06/2026', 'time': '18:20', 'title': 'Διόρθωση: Palm Island Suites (PLM)',
     'items': ['Σωστή αντιστοίχιση του ξενοδοχείου PLM = Palm Island Suites (πρόγραμμα & μισθοδοσία → εταιρεία ΦΥΤΩΡΙΑ ΚΡΗΤΗΣ).']},
    {'v': '12.51', 'b': '331', 'date': '14/06/2026', 'time': '18:05', 'title': 'Μισθοδοσία — Έλεγχος & Έγκριση (πίνακας Γεν. Διευθυντή)',
     'items': ['Νέα σελίδα «Έλεγχος & Έγκριση»: όλοι οι εργαζόμενοι/μήνα με εργάσιμες, ρεπό, έξτρα ώρες, Management (στο χέρι), Λογιστήριο (καθαρά), διαφορά, εκτός λογιστηρίου, πληρωτέο, υπόλοιπο + σημαίες κανόνων.',
               'Φίλτρα εταιρεία/ξενοδοχείο/τμήμα + σύνολα.',
               'Έγκριση μισθοδοσίας ανά εταιρεία: κλείδωμα + ίχνος (ποιος/πότε) + ειδοποίηση λογιστηρίου (πλατφόρμα + email) + εξαγωγή πίνακα ανά ξενοδοχείο (Excel).']},
    {'v': '12.50', 'b': '330', 'date': '14/06/2026', 'time': '17:35', 'title': 'Μισθοδοσία — Ανθεκτική εισαγωγή Epsilon',
     'items': ['Διορθώθηκε άνοιγμα αρχείων Epsilon που είχαν ασυμβατότητα μορφοποίησης (δεν φόρτωναν).',
               'Σαφές μήνυμα για παλιά αρχεία .xls (να αποθηκευτούν ως .xlsx).']},
    {'v': '12.49', 'b': '329', 'date': '14/06/2026', 'time': '17:10', 'title': 'Μισθοδοσία — Δώρα & άδεια στις δύο όψεις (Φ2.1)',
     'items': ['Η εισαγωγή Epsilon πιάνει πλέον και τα Δώρα Πάσχα/Χριστουγέννων + επίδομα/αποζημίωση αδείας (ξεχωριστά ανά εργαζόμενο).',
               'Η όψη εκτέλεσης δείχνει «Δώρα/άδεια» σπαστά και τα αθροίζει σε σύνολο (στο χέρι & κόστος εργοδότη) — σε Management και Λογιστήριο.',
               'Νέα σύνολα: Στο χέρι (σύνολο) και Κόστος εργοδότη (σύνολο).']},
    {'v': '12.48', 'b': '328', 'date': '14/06/2026', 'time': '16:30', 'title': 'Μισθοδοσία — Μηχανή & δύο όψεις (Φάση 2)',
     'items': ['Νέα «Εκτελέσεις»: υπολογισμός μισθοδοσίας ανά εταιρεία/μήνα από το Πρόγραμμα Εργασίας.',
               'Δύο όψεις: «Management» (τι θα πληρωθεί ο εργαζόμενος: μικτά συμφωνίας + έξτρα) και «Λογιστήριο» (νόμιμα καθαρά/εισφορές/κόστος).',
               'Εισαγωγή Epsilon (.xlsx): φορτώνει τα πραγματικά νόμιμα καθαρά και συμπληρώνει αυτόματα ΑΦΜ/ΑΜΚΑ/ειδικότητα/τράπεζα.',
               'Μενού «Οικονομικά» → «Μισθοδοσία» (Μητρώο/Εκτελέσεις).']},
    {'v': '12.47', 'b': '327', 'date': '14/06/2026', 'time': '15:45', 'title': 'Μισθοδοσία — Θεμέλιο (Φάση 1)',
     'items': ['Νέα ομάδα μενού «Οικονομικά» με σελίδα «Μισθοδοσία» (μόνο διαχειριστές).',
               'Μητρώο εργαζομένων ανά εταιρεία + καρτέλα με ευαίσθητα στοιχεία (ΑΦΜ/ΑΜΚΑ/ΙΒΑΝ) και συμφωνία.',
               'Νομικές οντότητες (εργοδότες) με αυτόματη αντιστοίχιση ξενοδοχείου→εταιρεία + συντελεστές 2026 (επεξεργάσιμοι).',
               'Η μηχανή υπολογισμού, τα εκκαθαριστικά και η πρόβλεψη έρχονται στη Φάση 2.']},
    {'v': '12.46', 'b': '326', 'date': '14/06/2026', 'time': '14:45', 'title': 'Βοήθεια: FAQ & Επίλυση προβλημάτων',
     'items': ['Νέα σελίδα «Βοήθεια (FAQ)» στο μενού Ενημέρωση — συχνές ερωτήσεις + επίλυση προβλημάτων με αναπτυσσόμενες απαντήσεις.',
               'Οι οδηγίες διαχείρισης εμφανίζονται μόνο σε διαχειριστές.']},
    {'v': '12.45', 'b': '325', 'date': '14/06/2026', 'time': '14:25', 'title': 'Roadmap · Multi-file importer ιστορικών',
     'items': ['Νέα σελίδα «Roadmap»: όλη η πορεία της Εστίας ανά τομέα (ολοκληρωμένα/σε εξέλιξη/σχεδιασμένα).',
               'Εισαγωγή πολλών αρχείων μαζί, με αυτόματη αναγνώριση: πρόγραμμα εργασίας ή μητρώο μισθοδοσίας.',
               'Νέα πηγή «Μητρώο Εργαζομένων» (καθαρή): τμήμα/εταιρεία/πρόσληψη/αποχώρηση + προφίλ· υποστήριξη παλιών ετών.']},
    {'v': '12.44', 'b': '324', 'date': '14/06/2026', 'time': '13:40', 'title': 'Τι νέο με ώρα · καθαρή διαχείριση εισαγμένου προσωπικού',
     'items': ['Το «Τι νέο» δείχνει πλέον ώρα μαζί με την ημερομηνία.',
               'Το εισαγμένο προσωπικό (χωρίς σύνδεση) δεν εμφανίζεται στη σελίδα «Χρήστες» — μένει καθαρή για τους πραγματικούς λογαριασμούς.',
               'Νέο κουμπί «Καθαρισμός εισαγμένου προσωπικού» για ασφαλή επανεκκαθάριση/επανεισαγωγή.']},
    {'v': '12.43', 'b': '323', 'date': '14/06/2026', 'time': '13:23', 'title': 'Πρόσβαση ανά ρόλο · Feedback · δυναμικά φίλτρα τμημάτων',
     'items': ['Νέο «Μενού ανά ρόλο» (Admin): ρυθμίζεις τι βλέπει Manager/Staff στο μενού.',
               'Οι managers (υποδοχή) βλέπουν πλέον Πρόγραμμα Εργασίας/Βλάβες/Records/Τι νέο.',
               'Νέο «Στείλε feedback» για όλους + σελίδα Feedback χρηστών (Admin).',
               'Πρόγραμμα Εργασίας: επιλογή πολλών τμημάτων με chips, δυναμικά χωρίς refresh (+ στήλη Εργάσιμες).',
               'Το «Το προφίλ μου» αφαιρέθηκε από το πλαϊνό μενού (υπάρχει στο avatar πάνω δεξιά).']},
    {'v': '12.42', 'b': '322', 'date': '14/06/2026', 'time': '12:55', 'title': 'Πρόγραμμα Εργασίας — εκκαθάριση διπλών + πρόσβαση ρόλων',
     'items': ['Εργαλείο εντοπισμού & συγχώνευσης διπλοεγγραφών προσωπικού (π.χ. ίδιο άτομο σε 2 ξενοδοχεία).',
               'Οι «Υποβολές (Λογιστήριο)» κρύβονται από μη-διαχειριστές.',
               'Οι managers βλέπουν Πρόγραμμα/Βλάβες/Logbook χωρίς πρόσβαση σε admin/εισαγωγές.']},
    {'v': '12.41', 'b': '321', 'date': '14/06/2026', 'time': '12:24', 'title': 'Πρόγραμμα Εργασίας — multi-week, εργάσιμες, διαχείριση προσωπικού',
     'items': ['Προβολή πολλών εβδομάδων μαζί (επιλογή 1–12 εβδ. μπροστά).',
               'Νέα στήλη «Εργάσιμες» (πλήθος ημερών εργασίας) ανά εργαζόμενο/εβδομάδα.',
               'Νέα σελίδα «Πρόγραμμα · Προσωπικό»: προσθήκη/επεξεργασία εργαζομένων (τμήμα/ξενοδοχείο/εταιρεία/σύνδεση).',
               'Ο κωδικός PLM δεν αντιστοιχίζεται πλέον σε ξενοδοχείο.']},
    {'v': '12.40', 'b': '320', 'date': '14/06/2026', 'time': '12:08', 'title': 'Module Πρόγραμμα Εργασίας (Βάρδιες) — Φάση 1',
     'items': ['Νέα ομάδα μενού «Προσωπικό»: Πρόγραμμα Εργασίας + Υποβολές (Λογιστήριο).',
               'Κάθε εργαζόμενος = χρήστης σε τμήμα (οργανόγραμμα)· import workbooks (χρήστες/τμήματα/βάρδιες).',
               'Φόρμα εβδομάδας Δ–Κ με κωδικούς βαρδιών, αυτόματες ώρες/ρεπό.',
               'Κανόνες (≥1 ρεπό/εβδ.), κλειδώματα προθεσμίας (Πέμπτη), ενοποιημένη αποστολή στο λογιστήριο.',
               'Τροποποίηση με νέα έκδοση + σήμανση αλλαγών· export ΠΡΟΣ ΛΟΓΙΣΤΗΡΙΟ.']},
    {'v': '12.39', 'b': '319', 'date': '14/06/2026', 'title': 'Dashboard: πλαϊνό κουμπί widgets + διόρθωση drag & drop',
     'items': ['Η μπάρα widgets ανοίγει από κομψό πλαϊνό κουμπί στη δεξιά άκρη της οθόνης.',
               'Διορθώθηκε το drag & drop — σύρε ένα widget από τη μπάρα στο πλέγμα.',
               'Εναλλακτικά, κάνε απλό κλικ σε ένα widget για να προστεθεί.']},
    {'v': '12.38', 'b': '318', 'date': '14/06/2026', 'title': 'Dashboard: πλαϊνή μπάρα widgets με drag & drop',
     'items': ['Η προσθήκη widget γίνεται από κομψή πλαϊνή μπάρα δεξιά — σύρε & άφησε στο πλέγμα.',
               'Σύρε ένα widget στη ζώνη απόρριψης για να το αφαιρέσεις.',
               'Τα προκαθορισμένα widgets μένουν πάντα διαθέσιμα στη μπάρα.',
               'Θεμέλιο για «Pin to dashboard»: ενότητα «Καρφιτσωμένα» (κάρφιτσωμα reports έρχεται σύντομα).',
               'Διόρθωση: το κλικ δεν επιλέγει πια κείμενο στα tiles.',
               'Άνοιξε το Staff HUB (προς το παρόν δείχνει τις Καταγραφές).']},
    {'v': '12.37', 'b': '317', 'date': '14/06/2026', 'title': 'Νέο Dashboard: ελεύθερο πλέγμα με resizable widgets',
     'items': ['Η «Σύνοψη» μετονομάστηκε σε Dashboard.',
               'Τα widgets αλλάζουν μέγεθος & θέση με το ποντίκι (με ελάχιστα όρια ώστε να μένουν ευανάγνωστα).',
               'Κουμπί «+ Προσθήκη widget» με μπάρα κατηγοριών — διάλεξε & πρόσθεσε εύκολα.',
               'Διακριτικό glow περίγραμμα στα tiles (έμπνευση από την οθόνη σύνδεσης).',
               'Πλήρης υποστήριξη σκούρου θέματος σε όλα τα tiles.',
               'Διόρθωση: το μενού προφίλ εμφανίζει σωστά όλες τις επιλογές.']},
    {'v': '12.36', 'b': '316', 'date': '13/06/2026', 'title': 'Ειδοποιήσεις popup · «Τι νέο» · γυάλισμα Σύνοψης & παλετών',
     'items': ['Το καμπανάκι ανοίγει popup με λίστα ειδοποιήσεων — διαβάζονται & φεύγουν επιτόπου.',
               'Νέα επιλογή «Ειδοποιήσεις» στο μενού προφίλ.',
               'Αυτή η σελίδα «Τι νέο»: ιστορικό εκδόσεων με τι άλλαξε.',
               'Σύνοψη: ξαναδουλεμένη premium εμφάνιση (καθαρές κάρτες, accent, εικονίδια) — ίδια λειτουργία tiles.',
               '4 παλέτες: το accent οδηγεί πλέον κουμπιά/links/καταστάσεις, βελτιωμένη αντίθεση.']},
    {'v': '12.34', 'b': '315', 'date': '13/06/2026', 'title': 'Σύνοψη = προσωπικό dashboard με tiles',
     'items': ['Η Σύνοψη έγινε προσωπικός πίνακας ανά χρήστη: διάλεξε & τακτοποίησε KPI/widget tiles (drag&drop).',
               'Νέα tiles: Δικές μου βλάβες, Μετρήσεις σήμερα, Απαντήσεις ερωτηματολογίων, Σημεία ελέγχου, Πρόσφατη δραστηριότητα.',
               'Σταθεροποίηση migration βάσης (race-safe).']},
    {'v': '12.33', 'b': '313', 'date': '13/06/2026', 'title': 'Αντίγραφα ασφαλείας: ρυθμίσεις & χρονοδιάγραμμα',
     'items': ['Ρυθμίσεις αυτόματου backup από το UI (ώρες/διατήρηση) χωρίς redeploy.',
               'Ημερήσιο backup 2 φορές/μέρα προς SharePoint, με όνομα ανά έκδοση.']},
    {'v': '12.31', 'b': '311', 'date': '13/06/2026', 'title': 'Module Αντιγράφων Ασφαλείας → SharePoint',
     'items': ['Πλήρη αντίγραφα δεδομένων (μετρήσεις/βλάβες/ερωτηματολόγια + εικόνες) στο εταιρικό SharePoint.',
               'Χειροκίνητο «Backup τώρα», τοπικό κατέβασμα, επαναφορά.']},
    {'v': '12.29', 'b': '309', 'date': '13/06/2026', 'title': 'Κέντρο Εισαγωγής Δεδομένων',
     'items': ['Ανέβασμα Excel/CSV με αυτόματη αντιστοίχιση στηλών & προεπισκόπηση.',
               'Εισαγωγή ιστορικών βλαβών & ερωτηματολογίων.']},
    {'v': '12.23', 'b': '—', 'date': '13/06/2026', 'title': 'Ερωτηματολόγια (Υποδοχή)',
     'items': ['Builder ερωτηματολογίων, δημόσιος σύνδεσμος για πελάτες (QR-ready), αποτελέσματα/NPS.']},
    {'v': '12.14', 'b': '—', 'date': '13/06/2026', 'title': 'Module Βλαβοληψία',
     'items': ['Δήλωση βλάβης με κατηγορίες/φωτογραφίες, Πίνακας Βλαβών (KPIs/Kanban), αυτόματη ανάθεση, SLA.']},
]

@app.context_processor
def inject_version():
    return {'app_version': APP_VERSION, 'app_build': APP_BUILD}

@app.context_processor
def inject_org_conflicts():
    """v12.170 — μετρητής διαφωνιών οργανογράμματος↔Epsilon (banner, admin-only)."""
    try:
        if not is_admin():
            return {'org_conflicts': 0}
        n = User.query.filter(User.import_hotel_id != None,
                              User.import_hotel_id != User.home_hotel_id).count()
        return {'org_conflicts': n}
    except Exception:
        return {'org_conflicts': 0}

@app.route('/version')
def app_version_json():
    """Ελαφρύ endpoint για auto-refresh όταν αλλάζει η έκδοση (deploy)."""
    return {'version': APP_VERSION, 'build': APP_BUILD}

# v12.12 — Dark mode παντού: αυτόματη ένεση του theme snippet σε ΚΑΘΕ HTML σελίδα
# (εξαιρούνται login/register που έχουν δικό τους σκούρο design, και όσες το έχουν ήδη).
_DARK_SNIPPET = ('<script>(function(){try{var c=document.cookie;'
                 'var m=(c.match(/(?:^|; )estia_theme=([^;]+)/)||[])[1]||"system";'
                 'var d=m==="dark"||(m==="system"&&window.matchMedia&&matchMedia("(prefers-color-scheme:dark)").matches);'
                 'if(d)document.documentElement.setAttribute("data-theme","dark");'
                 'var p=(c.match(/(?:^|; )estia_palette=([^;]+)/)||[])[1]||"aurora";'
                 'document.documentElement.setAttribute("data-palette",p);}catch(e){}})();</script>'
                 '<link rel="stylesheet" href="/static/estia-theme.css">'
                 '<link rel="stylesheet" href="/static/estia-palette.css">')

@app.after_request
def _inject_dark(resp):
    try:
        if (resp.content_type or '').startswith('text/html'):
            body = resp.get_data(as_text=True)
            if '</head>' in body and '/static/estia-theme.css' not in body and '/static/auth.css' not in body:
                resp.set_data(body.replace('</head>', _DARK_SNIPPET + '</head>', 1))
    except Exception:
        pass
    return resp

def has_rank(min_rank):
    u = current_user()
    return u is not None and role_rank(u.role) >= min_rank

def is_admin():
    return has_rank(ROLE_RANK['admin'])      # admin ή masteradmin

def can_log():
    return has_rank(ROLE_RANK['staff'])      # ό,τι πάνω από viewer

def allowed_hotels(u):
    if u is None:
        return []
    if role_rank(u.role) >= ROLE_RANK['admin']:
        return Hotel.query.filter_by(is_active=True).order_by(Hotel.name).all()
    assigned = [h for h in u.hotels if h.is_active]
    if assigned:
        return sorted(assigned, key=lambda h: h.name)
    return Hotel.query.filter_by(is_active=True).order_by(Hotel.name).all()   # χωρίς ανάθεση = όλα

# ── v12.19 — Global εναλλάκτης ξενοδοχείου (session scope) ────────────────────
def active_hotel_id():
    """Το ενεργό ξενοδοχείο από τον global εναλλάκτη (None = όλα)."""
    v = session.get('active_hotel_id')
    try:
        return int(v) if v not in (None, '', 'all') else None
    except Exception:
        return None

def scoped_hotels(u):
    """allowed_hotels περιορισμένα στο ενεργό ξενοδοχείο (αν έχει επιλεγεί & επιτρέπεται)."""
    hs = allowed_hotels(u)
    aid = active_hotel_id()
    if aid:
        sub = [h for h in hs if h.id == aid]
        if sub:
            return sub
    return hs

def scoped_hotel_ids(u):
    return {h.id for h in scoped_hotels(u)}

@app.route('/set-hotel/<hid>')
def set_hotel(hid):
    if 'user_id' not in session:
        return ('', 401)
    session['active_hotel_id'] = '' if hid in ('all', '0', '') else hid
    return ('', 204)

@app.context_processor
def inject_hotelscope():
    u = current_user()
    return {'nav_hotels': (allowed_hotels(u) if u else []), 'active_hid': active_hotel_id()}

# ── v12.28 — Global φίλτρο περιόδου (session scope) ───────────────────────────
PERIOD_LABELS = [
    ('all', 'Όλα'), ('today', 'Σήμερα'), ('yesterday', 'Χθες'),
    ('l7', 'Τελευταίες 7 ημέρες'), ('week', 'Τρέχουσα εβδομάδα'),
    ('month', 'Τρέχων μήνας'), ('l30', 'Τελευταίες 30 ημέρες'),
    ('ytd', 'Από αρχή έτους (YTD)'), ('year', 'Τρέχον έτος'),
    ('ly', 'Πέρσι (LY)'), ('custom', 'Προσαρμογή…'),
]

def _period_bounds(key, today=None):
    t = today or datetime.now().date()
    if key == 'today': return t, t
    if key == 'yesterday': d = t - timedelta(days=1); return d, d
    if key == 'l7': return t - timedelta(days=6), t
    if key == 'week': sft = t - timedelta(days=t.weekday()); return sft, sft + timedelta(days=6)
    if key == 'month':
        sft = t.replace(day=1)
        nm = (sft.replace(day=28) + timedelta(days=4)).replace(day=1)
        return sft, nm - timedelta(days=1)
    if key == 'l30': return t - timedelta(days=29), t
    if key == 'ytd': return t.replace(month=1, day=1), t
    if key == 'year': return t.replace(month=1, day=1), t.replace(month=12, day=31)
    if key == 'ly': return date(t.year - 1, 1, 1), date(t.year - 1, 12, 31)
    return None, None

def active_period():
    key = session.get('active_period', 'all')
    label = dict(PERIOD_LABELS).get(key, 'Όλα')
    start = end = None
    if key == 'custom':
        try: start = datetime.strptime(session.get('period_from', ''), '%Y-%m-%d').date()
        except Exception: start = None
        try: end = datetime.strptime(session.get('period_to', ''), '%Y-%m-%d').date()
        except Exception: end = None
        label = ('%s → %s' % (start.strftime('%d/%m/%Y'), end.strftime('%d/%m/%Y'))) if (start and end) else 'Προσαρμογή'
    elif key != 'all':
        start, end = _period_bounds(key)
    return {'key': key, 'label': label, 'start': start, 'end': end,
            'from': session.get('period_from', '') if key == 'custom' else '',
            'to':   session.get('period_to', '')   if key == 'custom' else ''}

def apply_period(q, col, dt=False):
    """Φιλτράρει query με την ενεργή περίοδο. col=date column (ή datetime αν dt=True). 'Όλα' = no-op."""
    p = active_period()
    sft, e = p['start'], p['end']
    if sft is None and e is None:
        return q
    if dt:
        if sft: q = q.filter(col >= datetime(sft.year, sft.month, sft.day))
        if e:   q = q.filter(col < datetime(e.year, e.month, e.day) + timedelta(days=1))
    else:
        if sft: q = q.filter(col >= sft)
        if e:   q = q.filter(col <= e)
    return q

@app.route('/set-period/<key>')
def set_period(key):
    if 'user_id' not in session:
        return ('', 401)
    keys = [k for k, _ in PERIOD_LABELS]
    session['active_period'] = key if key in keys else 'all'
    if key == 'custom':
        session['period_from'] = (request.args.get('from') or '').strip()
        session['period_to'] = (request.args.get('to') or '').strip()
    return ('', 204)

@app.context_processor
def inject_period():
    return {'active_period': active_period(), 'PERIOD_LABELS': PERIOD_LABELS}

def can_access_pool(u, pool):
    if u is None or pool is None:
        return False
    if role_rank(u.role) >= ROLE_RANK['admin']:
        return True
    assigned = {h.id for h in u.hotels if h.is_active}
    if not assigned:
        return True   # χωρίς ανάθεση = όλα
    return pool.hotel_id in assigned

def can_access_water_system(u, ws):
    """v12.3 — scoping δικτύου νερού (mirror can_access_pool)."""
    if u is None or ws is None:
        return False
    if role_rank(u.role) >= ROLE_RANK['admin']:
        return True
    assigned = {h.id for h in u.hotels if h.is_active}
    if not assigned:
        return True   # χωρίς ανάθεση = όλα
    return ws.hotel_id in assigned

def log_activity(action, detail='', hotel_id=None):
    try:
        if hotel_id is None:
            try: hotel_id = active_hotel_id()
            except Exception: hotel_id = None
        db.session.add(ActivityLog(user_id=session.get('user_id'), hotel_id=hotel_id,
                                   action=(action or '')[:60], detail=(detail or '')[:300]))
        db.session.commit()
    except Exception:
        db.session.rollback()

# ──────────────────────────────────────────────────────────────────────────
#  MODELS
# ──────────────────────────────────────────────────────────────────────────
user_hotels = db.Table('user_hotels',
    db.Column('user_id', db.Integer, db.ForeignKey('user.id'), primary_key=True),
    db.Column('hotel_id', db.Integer, db.ForeignKey('hotel.id'), primary_key=True),
)

class ActivityLog(db.Model):
    id         = db.Column(db.Integer, primary_key=True)
    user_id    = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=True)
    hotel_id   = db.Column(db.Integer, db.ForeignKey('hotel.id'), nullable=True)   # v12.22
    action     = db.Column(db.String(60))
    detail     = db.Column(db.String(300))
    created_at = db.Column(db.DateTime, default=datetime.now)
    user       = db.relationship('User')
    hotel      = db.relationship('Hotel')

class User(db.Model):
    id         = db.Column(db.Integer, primary_key=True)
    username   = db.Column(db.String(50), unique=True, nullable=False)
    password   = db.Column(db.String(200), nullable=False)
    full_name  = db.Column(db.String(100), nullable=False)
    role       = db.Column(db.String(20), default='staff')
    language   = db.Column(db.String(5), default='el')
    email      = db.Column(db.String(120))
    phone      = db.Column(db.String(40))
    approved   = db.Column(db.Boolean, default=True)
    avatar     = db.Column(db.Text)                       # data URL (base64) εικόνας προφίλ
    dashboard  = db.Column(db.Text)                       # v12.34 — προσωπική διάταξη tiles (JSON)
    # v12.40 — Module Πρόγραμμα Εργασίας (οργανόγραμμα + βάρδιες)
    department_id     = db.Column(db.Integer)
    employer          = db.Column(db.String(120))
    subunit           = db.Column(db.String(20))
    home_hotel_id     = db.Column(db.Integer)
    import_hotel_id   = db.Column(db.Integer)   # v12.170 — πρόταση Epsilon (πίνακας διαφωνιών)
    position_id       = db.Column(db.Integer)   # v12.179 soft FK -> job_position.id (org-owned θέση)
    login_enabled     = db.Column(db.Boolean)
    employment_active = db.Column(db.Boolean)
    is_active  = db.Column(db.Boolean, default=True)
    created_at = db.Column(db.DateTime, default=datetime.now)
    hotels     = db.relationship('Hotel', secondary=user_hotels, backref='members')

class WaterRecord(db.Model):
    id          = db.Column(db.Integer, primary_key=True)
    user_id     = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    record_date = db.Column(db.Date, default=date.today, nullable=False)
    period      = db.Column(db.String(10), nullable=False)
    recorded_at = db.Column(db.DateTime, default=datetime.now)
    updated_at  = db.Column(db.DateTime, nullable=True)
    updated_by  = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=True)
    clo2_tank        = db.Column(db.Float)
    clo2_kitchen     = db.Column(db.Float)
    clo2_remote      = db.Column(db.Float)
    clo2_dhw_out     = db.Column(db.Float)
    clo2_dhw_return  = db.Column(db.Float)
    clo2_ro          = db.Column(db.Float)
    location_kitchen = db.Column(db.String(100))
    location_remote  = db.Column(db.String(100))
    temp_tank         = db.Column(db.Float)
    temp_dhw_out      = db.Column(db.Float)
    temp_dhw_return   = db.Column(db.Float)
    temp_ro           = db.Column(db.Float)
    temp_kitchen_cold = db.Column(db.Float)
    temp_kitchen_hot  = db.Column(db.Float)
    temp_remote_cold  = db.Column(db.Float)
    temp_remote_hot   = db.Column(db.Float)
    ph_tank = db.Column(db.Float)
    notes   = db.Column(db.Text)
    water_system_id = db.Column(db.Integer, db.ForeignKey('water_system.id'), nullable=True)  # v12.3
    user         = db.relationship('User', foreign_keys=[user_id], backref='water_records')
    updated_user = db.relationship('User', foreign_keys=[updated_by])
    water_system = db.relationship('WaterSystem')

class Hotel(db.Model):
    id         = db.Column(db.Integer, primary_key=True)
    name       = db.Column(db.String(120), unique=True, nullable=False)
    is_active  = db.Column(db.Boolean, default=True)
    company_id = db.Column(db.Integer)   # v12.47 — Μισθοδοσία: νομικός εργοδότης (payroll.Company)
    ypok_code  = db.Column(db.String(8))  # v12.58 — Κωδικός Υποκαταστήματος (ΑΠΔ/Epsilon)
    created_at = db.Column(db.DateTime, default=datetime.now)
    pools = db.relationship('Pool', backref='hotel', order_by='Pool.name')

class Pool(db.Model):
    id         = db.Column(db.Integer, primary_key=True)
    hotel_id   = db.Column(db.Integer, db.ForeignKey('hotel.id'), nullable=False)
    name       = db.Column(db.String(120), nullable=False)
    location   = db.Column(db.String(120))
    pool_type  = db.Column(db.String(20), default='pool')
    volume_m3  = db.Column(db.Float)          # όγκος σε m³ (για δοσολογία)
    is_active  = db.Column(db.Boolean, default=True)
    created_at = db.Column(db.DateTime, default=datetime.now)

class PoolRecord(db.Model):
    id          = db.Column(db.Integer, primary_key=True)
    pool_id     = db.Column(db.Integer, db.ForeignKey('pool.id'), nullable=False)
    user_id     = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    record_date = db.Column(db.Date, default=date.today, nullable=False)
    period      = db.Column(db.String(10), nullable=False)
    recorded_at = db.Column(db.DateTime, default=datetime.now)
    updated_at  = db.Column(db.DateTime, nullable=True)
    updated_by  = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=True)
    free_chlorine     = db.Column(db.Float)
    combined_chlorine = db.Column(db.Float)
    ph                = db.Column(db.Float)
    temp              = db.Column(db.Float)
    turbidity         = db.Column(db.Float)
    cyanuric_acid     = db.Column(db.Float)
    total_alkalinity  = db.Column(db.Float)
    orp               = db.Column(db.Float)
    backwash_done     = db.Column(db.Boolean, default=False)
    notes             = db.Column(db.Text)
    pool         = db.relationship('Pool')
    user         = db.relationship('User', foreign_keys=[user_id])
    updated_user = db.relationship('User', foreign_keys=[updated_by])


# v12.3 — Δίκτυο Νερού ανά ξενοδοχείο (mirror Pool). Ένα ξενοδοχείο μπορεί να έχει >1.
class WaterSystem(db.Model):
    id         = db.Column(db.Integer, primary_key=True)
    hotel_id   = db.Column(db.Integer, db.ForeignKey('hotel.id'), nullable=False)
    name       = db.Column(db.String(120), nullable=False)
    location   = db.Column(db.String(120))
    is_active  = db.Column(db.Boolean, default=True)
    created_at = db.Column(db.DateTime, default=datetime.now)
    hotel      = db.relationship('Hotel')


class ReminderSent(db.Model):
    day = db.Column(db.String(10), primary_key=True)        # 'YYYY-MM-DD' lock ανά ημέρα
    sent_at = db.Column(db.DateTime, default=datetime.now)

class FaultReport(db.Model):
    id          = db.Column(db.Integer, primary_key=True)
    user_id     = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=True)
    hotel_id    = db.Column(db.Integer, db.ForeignKey('hotel.id'), nullable=True)
    pool_id     = db.Column(db.Integer, db.ForeignKey('pool.id'), nullable=True)
    area        = db.Column(db.String(20))
    message     = db.Column(db.Text)
    status      = db.Column(db.String(12), default='open')   # open / resolved
    answer      = db.Column(db.Text)
    answered_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=True)
    created_at  = db.Column(db.DateTime, default=datetime.now)
    resolved_at = db.Column(db.DateTime, nullable=True)
    user = db.relationship('User', foreign_keys=[user_id])
    pool = db.relationship('Pool')

class Notification(db.Model):
    id         = db.Column(db.Integer, primary_key=True)
    user_id    = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    text       = db.Column(db.String(300))
    link       = db.Column(db.String(120))
    is_read    = db.Column(db.Boolean, default=False)
    created_at = db.Column(db.DateTime, default=datetime.now)

# v12.38 — Καρφιτσωμένα widgets (pin to dashboard). Νέος πίνακας· create_all το δημιουργεί.
class PinnedWidget(db.Model):
    id         = db.Column(db.Integer, primary_key=True)
    user_id    = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    title      = db.Column(db.String(80), nullable=False)
    icon       = db.Column(db.String(40))
    link       = db.Column(db.String(300))
    created_at = db.Column(db.DateTime, default=datetime.now)

# v12.9 — Μηνύματα μεταξύ χρηστών (νέος πίνακας· create_all το δημιουργεί αυτόματα)
class Message(db.Model):
    id           = db.Column(db.Integer, primary_key=True)
    sender_id    = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    recipient_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    body         = db.Column(db.Text, nullable=False)
    is_read      = db.Column(db.Boolean, default=False)
    created_at   = db.Column(db.DateTime, default=datetime.now)
    sender    = db.relationship('User', foreign_keys=[sender_id])
    recipient = db.relationship('User', foreign_keys=[recipient_id])

class EmailLog(db.Model):
    id         = db.Column(db.Integer, primary_key=True)
    subject    = db.Column(db.String(200))
    recipients = db.Column(db.String(300))
    ok         = db.Column(db.Boolean, default=False)
    via        = db.Column(db.String(10))
    created_at = db.Column(db.DateTime, default=datetime.now)

class Setting(db.Model):
    key   = db.Column(db.String(40), primary_key=True)
    value = db.Column(db.Text)

# ── Generic monitoring engine (Πλατφόρμα Συντήρησης) ──
class MonitorTemplate(db.Model):
    key       = db.Column(db.String(30), primary_key=True)   # 'tank','energy',...
    name      = db.Column(db.String(80))
    icon      = db.Column(db.String(30), default='ti-checklist')
    frequency = db.Column(db.String(10), default='daily')    # twice|daily|weekly|monthly
    sort      = db.Column(db.Integer, default=0)
    is_active = db.Column(db.Boolean, default=True)
    params    = db.relationship('MonitorParam', backref='template', order_by='MonitorParam.sort',
                                cascade='all, delete-orphan')

class MonitorParam(db.Model):
    id           = db.Column(db.Integer, primary_key=True)
    template_key = db.Column(db.String(30), db.ForeignKey('monitor_template.key'))
    pkey         = db.Column(db.String(40))      # κλειδί τιμής
    label        = db.Column(db.String(80))
    unit         = db.Column(db.String(20), default='')
    min_v        = db.Column(db.Float)
    max_v        = db.Column(db.Float)
    action_low   = db.Column(db.String(200))
    action_high  = db.Column(db.String(200))
    periodic     = db.Column(db.Boolean, default=False)   # μόνο πρωί/πρώτη βάρδια
    sort         = db.Column(db.Integer, default=0)
    # v12.270 — Φ3 Βιβλιοθήκη Μετρήσεων (A1: μία κοινή): είδος εισόδου, κατηγορία, ενεργό
    kind         = db.Column(db.String(10))               # 'num' | 'bool' | 'text' (κενό = heuristic)
    category     = db.Column(db.String(40), default='')   # ομαδοποίηση παλέτας (Πισίνα/Νερό/Γενικό)
    is_active    = db.Column(db.Boolean, default=True)

class Area(db.Model):
    id           = db.Column(db.Integer, primary_key=True)
    hotel_id     = db.Column(db.Integer, db.ForeignKey('hotel.id'), nullable=False)
    template_key = db.Column(db.String(30), db.ForeignKey('monitor_template.key'), nullable=False)
    name         = db.Column(db.String(120))
    location     = db.Column(db.String(120))
    is_active    = db.Column(db.Boolean, default=True)
    created_at   = db.Column(db.DateTime, default=datetime.now)
    engine_only  = db.Column(db.Boolean, default=False)   # Φ2: σημείο ενιαίας μηχανής (κρυφό από legacy «Τομείς» μέχρι Φ3)
    legacy_kind  = db.Column(db.String(10))               # Φ2: 'pool' | 'water' (προέλευση)
    legacy_id    = db.Column(db.Integer)                  # Φ2: Pool.id / WaterSystem.id
    node_id      = db.Column(db.Integer)                  # Φ-Α: κόμβος δέντρου δικτύων (MonitorNode.id· null=σημερινή συμπεριφορά)
    hotel        = db.relationship('Hotel')
    template     = db.relationship('MonitorTemplate')

class Reading(db.Model):
    id          = db.Column(db.Integer, primary_key=True)
    area_id     = db.Column(db.Integer, db.ForeignKey('area.id'), nullable=False)
    template_key= db.Column(db.String(30))
    user_id     = db.Column(db.Integer, db.ForeignKey('user.id'))
    record_date = db.Column(db.Date, default=date.today)
    period      = db.Column(db.String(10), default='day')
    recorded_at = db.Column(db.DateTime, default=datetime.now)
    updated_at  = db.Column(db.DateTime)
    updated_by  = db.Column(db.Integer, db.ForeignKey('user.id'))
    values      = db.Column(db.Text)     # JSON {pkey: value}
    notes       = db.Column(db.Text)
    source_kind = db.Column(db.String(10))   # Φ2: 'pool' | 'water' αν είναι αντίγραφο legacy record
    source_id   = db.Column(db.Integer)      # Φ2: PoolRecord.id / WaterRecord.id (idempotency)
    area        = db.relationship('Area')
    user        = db.relationship('User', foreign_keys=[user_id])

FREQ_PERIODS = {'twice': ['morning', 'afternoon'], 'daily': ['day'], 'weekly': ['day'], 'monthly': ['day']}
FREQ_LABEL   = {'twice': '2×/ημέρα (πρωί/απόγευμα)', 'daily': 'Καθημερινά', 'weekly': 'Εβδομαδιαία', 'monthly': 'Μηνιαία'}

def periods_for(freq):
    return FREQ_PERIODS.get(freq, ['day'])

def area_actions(reading):
    """Generic έλεγχος ορίων -> ενέργειες, με βάση τις παραμέτρους του template."""
    out = []
    try:
        vals = json.loads(reading.values or '{}')
    except Exception:
        vals = {}
    tpl = MonitorTemplate.query.get(reading.template_key)
    if not tpl:
        return out
    for p in tpl.params:
        v = vals.get(p.pkey)
        if v is None:
            continue
        try:
            v = float(v)
        except (ValueError, TypeError):
            continue
        act = None
        if p.min_v is not None and v < p.min_v:
            act = p.action_low or ('Χαμηλό: ' + p.label)
        elif p.max_v is not None and v > p.max_v:
            act = p.action_high or ('Υψηλό: ' + p.label)
        if act:
            out.append({'label': p.label, 'value': v, 'unit': p.unit, 'action': act})
    return out


def flt(data, key):
    try:
        return float(data[key]) if data.get(key) not in (None, '') else None
    except (ValueError, TypeError):
        return None


# ──────────────────────────────────────────────────────────────────────────
#  EMAIL (Microsoft Graph ή SMTP) + log
# ──────────────────────────────────────────────────────────────────────────
def _graph_token():
    data = urllib.parse.urlencode({
        'client_id': GRAPH_CLIENT_ID, 'client_secret': GRAPH_CLIENT_SECRET,
        'scope': 'https://graph.microsoft.com/.default', 'grant_type': 'client_credentials',
    }).encode()
    req = urllib.request.Request('https://login.microsoftonline.com/' + GRAPH_TENANT_ID + '/oauth2/v2.0/token', data=data)
    with urllib.request.urlopen(req, timeout=30) as r:
        return json.loads(r.read().decode())['access_token']

def _send_graph(subject, html, recips, attachments=None):
    token = _graph_token()
    payload = {'message': {'subject': subject, 'body': {'contentType': 'HTML', 'content': html},
               'toRecipients': [{'emailAddress': {'address': a}} for a in recips]}, 'saveToSentItems': True}
    if attachments:
        import base64 as _b64
        payload['message']['attachments'] = [
            {'@odata.type': '#microsoft.graph.fileAttachment', 'name': fn,
             'contentType': mt, 'contentBytes': _b64.b64encode(b).decode()} for fn, b, mt in attachments]
    req = urllib.request.Request('https://graph.microsoft.com/v1.0/users/' + GRAPH_SENDER + '/sendMail',
        data=json.dumps(payload).encode(),
        headers={'Authorization': 'Bearer ' + token, 'Content-Type': 'application/json'})
    with urllib.request.urlopen(req, timeout=30) as r:
        return r.status in (200, 202)

def _send_smtp(subject, html, recips, attachments=None):
    msg = MIMEMultipart(); msg['From'] = EMAIL_FROM; msg['To'] = ', '.join(recips); msg['Subject'] = subject
    msg.attach(MIMEText(html, 'html', 'utf-8'))
    if attachments:
        from email.mime.application import MIMEApplication
        for fn, b, mt in attachments:
            part = MIMEApplication(b, _subtype=(mt.split('/')[-1] if '/' in mt else 'octet-stream'))
            part.add_header('Content-Disposition', 'attachment', filename=fn)
            msg.attach(part)
    sv = smtplib.SMTP_SSL(SMTP_SERVER, SMTP_PORT); sv.login(EMAIL_FROM, EMAIL_PASSWORD)
    sv.sendmail(EMAIL_FROM, recips, msg.as_string()); sv.quit()
    return True

def send_email(subject, html, recips=None, attachments=None):
    recips = recips or EMAIL_TO_LIST
    via = 'graph' if GRAPH_CLIENT_ID else ('smtp' if EMAIL_PASSWORD else 'none')
    ok = False
    if via != 'none':
        try:
            ok = _send_graph(subject, html, recips, attachments) if via == 'graph' else _send_smtp(subject, html, recips, attachments)
        except Exception as e:
            print('email error (' + via + '):', e); ok = False
    try:
        db.session.add(EmailLog(subject=(subject or '')[:200], recipients=', '.join(recips)[:300], ok=ok, via=via))
        db.session.commit()
    except Exception:
        try:
            db.session.rollback()
        except Exception:
            pass
    return ok


THEME_DEFAULTS = {'primary': '#193847', 'accent': '#BB9549', 'app_title': 'Εστία', 'logo': '',
                  # v12.11 — εξατομίκευση σελίδας login/register
                  'login_logo': '', 'login_logo_size': '88', 'login_font': 'Space Grotesk',
                  'login_tagline': 'we reinvent the way we work',
                  'social_facebook': '', 'social_linkedin': ''}
LOGIN_FONTS = ['Space Grotesk', 'Poppins', 'Montserrat', 'Inter', 'Sora', 'System']

def get_theme():
    t = dict(THEME_DEFAULTS)
    try:
        for row in Setting.query.filter(Setting.key.like('theme_%')).all():
            k = row.key[6:]
            if k in t and row.value is not None:
                t[k] = row.value
    except Exception:
        pass
    return t

def get_ai_config():
    cfg = {'provider': AI_PROVIDER, 'anthropic': ANTHROPIC_API_KEY, 'openai': OPENAI_API_KEY, 'model': AI_MODEL}
    try:
        for row in Setting.query.filter(Setting.key.like('ai_%')).all():
            k = row.key[3:]
            if k in cfg and row.value:
                cfg[k] = row.value
    except Exception:
        pass
    return cfg


# ──────────────────────────────────────────────────────────────────────────
#  WATER LOG
# ──────────────────────────────────────────────────────────────────────────
def apply_record(record, data, period):
    record.clo2_tank        = flt(data, 'clo2_tank')
    record.clo2_kitchen     = flt(data, 'clo2_kitchen')
    record.clo2_remote      = flt(data, 'clo2_remote')
    record.location_kitchen = data.get('location_kitchen', '')
    record.location_remote  = data.get('location_remote', '')
    record.temp_tank        = flt(data, 'temp_tank')
    record.temp_dhw_out     = flt(data, 'temp_dhw_out')
    record.temp_dhw_return  = flt(data, 'temp_dhw_return')
    record.temp_ro          = flt(data, 'temp_ro')
    record.temp_kitchen_cold = flt(data, 'temp_kitchen_cold')
    record.temp_kitchen_hot  = flt(data, 'temp_kitchen_hot')
    record.temp_remote_cold  = flt(data, 'temp_remote_cold')
    record.temp_remote_hot   = flt(data, 'temp_remote_hot')
    record.notes = data.get('notes', '')
    if period == 'morning':
        record.clo2_dhw_out    = flt(data, 'clo2_dhw_out')
        record.clo2_dhw_return = flt(data, 'clo2_dhw_return')
        record.clo2_ro         = flt(data, 'clo2_ro')
        record.ph_tank         = flt(data, 'ph_tank')

def send_report_email(record, user):
    if not EMAIL_PASSWORD and not GRAPH_CLIENT_ID:
        return False
    period_gr = 'Πρωι' if record.period == 'morning' else 'Απογευμα'
    # v12.3 — δυναμικό ξενοδοχείο + δίκτυο (αντί hardcoded «Sergios»)
    _ws = record.water_system
    place = ''
    if _ws:
        place = (_ws.hotel.name + ' · ' + _ws.name) if _ws.hotel else _ws.name
    place_sfx = f' ({place})' if place else ''
    def row(label, val, unit='', min_v=None, max_v=None):
        if val is None:
            return f'<tr><td style="padding:7px 8px;border:1px solid #eee;">{label}</td><td style="padding:7px 8px;border:1px solid #eee;">-</td><td style="padding:7px 8px;border:1px solid #eee;color:#888;">{unit}</td></tr>'
        ok = (min_v is None or val >= min_v) and (max_v is None or val <= max_v)
        color = '#16a34a' if ok else '#dc2626'
        icon  = 'OK' if ok else 'ΠΡΟΣΟΧΗ'
        limit = f'min {min_v}{unit}' if min_v else (f'max {max_v}{unit}' if max_v else '')
        return f'<tr><td style="padding:7px 8px;border:1px solid #eee;">{label}</td><td style="padding:7px 8px;border:1px solid #eee;color:{color};font-weight:500;">{icon}: {val} {unit}</td><td style="padding:7px 8px;border:1px solid #eee;color:#888;">{limit}</td></tr>'
    loc_kit = f' ({record.location_kitchen})' if record.location_kitchen else ''
    loc_rem = f' ({record.location_remote})' if record.location_remote else ''
    clo2_rows  = row('Δεξαμενη', record.clo2_tank, 'ppm', 1.0, 2.0)
    clo2_rows += row(f'Κουζινα{loc_kit}', record.clo2_kitchen, 'ppm', 1.0, 2.0)
    clo2_rows += row(f'Απομακρυσμενο{loc_rem}', record.clo2_remote, 'ppm', 1.0, 2.0)
    if record.period == 'morning':
        clo2_rows += row('Αναχωρηση ΖΝΧ', record.clo2_dhw_out, 'ppm', 1.0, 2.0)
        clo2_rows += row('Επιστροφη ΖΝΧ', record.clo2_dhw_return, 'ppm', 1.0, 2.0)
        clo2_rows += row('Αντ. Οσμωση', record.clo2_ro, 'ppm', 1.0, 2.0)
    temp_rows  = row('Δεξαμενη', record.temp_tank, 'C', None, 20.0)
    temp_rows += row('Κολεκτερ ΖΝΧ (Αναχ.)', record.temp_dhw_out, 'C', 60.0, None)
    temp_rows += row('Κολεκτερ Ανακυκλ. (Επιστρ.)', record.temp_dhw_return, 'C', 50.0, None)
    temp_rows += row('Αντ. Οσμωση', record.temp_ro, 'C')
    temp_rows += row(f'Κουζινα Κρυο{loc_kit}', record.temp_kitchen_cold, 'C')
    temp_rows += row(f'Κουζινα Ζεστο{loc_kit}', record.temp_kitchen_hot, 'C', 50.0, None)
    temp_rows += row(f'Απομακρυσμενο Κρυο{loc_rem}', record.temp_remote_cold, 'C')
    temp_rows += row(f'Απομακρυσμενο Ζεστο{loc_rem}', record.temp_remote_hot, 'C', 50.0, None)
    _wa = compute_water_actions(record)
    if _wa:
        _li = ''.join('<li style="margin-bottom:4px;' + ('color:#b91c1c;font-weight:600;' if a['urgent'] else '') + '">' + ('ΕΠΕΙΓΟΝ — ' if a['urgent'] else '') + a['label'] + ': ' + a['action'] + '</li>' for a in _wa)
        actions_html = '<h2 style="font-size:15px;color:#b45309;margin-top:18px;">Προτεινομενες ενεργειες (ΖΝΧ / νερο)</h2><ul style="background:#fffbeb;border:1px solid #fde68a;border-radius:6px;padding:12px 12px 12px 28px;color:#333;">' + _li + '</ul>'
    else:
        actions_html = ''
    html = f"""
    <div style="font-family:Arial,sans-serif;max-width:620px;margin:0 auto;">
      <div style="background:#0369a1;color:white;padding:20px;border-radius:8px 8px 0 0;">
        <h1 style="margin:0;font-size:20px;">Εστία — Νερά Χρήσης{place_sfx} {period_gr}</h1>
        <p style="margin:5px 0 0;opacity:0.8;">{record.record_date.strftime('%d/%m/%Y')} | Υπευθυνος: {user.full_name}</p>
      </div>
      <div style="background:#f9f9f9;padding:20px;border:1px solid #eee;">
        <h2 style="font-size:15px;color:#333;border-bottom:2px solid #0369a1;padding-bottom:6px;">CLO2 (ppm) - Στοχος: 1.0-2.0 ppm</h2>
        <table style="width:100%;border-collapse:collapse;">
          <tr style="background:#0369a1;color:white;"><th style="padding:8px;text-align:left;">Σημειο</th><th style="padding:8px;text-align:left;">Μετρηση</th><th style="padding:8px;text-align:left;">Ορια</th></tr>
          {clo2_rows}
        </table>
        <h2 style="font-size:15px;color:#333;border-bottom:2px solid #0369a1;padding-bottom:6px;margin-top:20px;">Θερμοκρασια (C)</h2>
        <table style="width:100%;border-collapse:collapse;">
          <tr style="background:#0369a1;color:white;"><th style="padding:8px;text-align:left;">Σημειο</th><th style="padding:8px;text-align:left;">Μετρηση</th><th style="padding:8px;text-align:left;">Ορια</th></tr>
          {temp_rows}
        </table>
        {actions_html}
        {f'<h2 style="font-size:15px;color:#333;margin-top:20px;">pH</h2><p style="background:#fff;padding:10px;border:1px solid #eee;">Δεξαμενη: {record.ph_tank}</p>' if record.period == 'morning' and record.ph_tank else ''}
        {f'<h2 style="font-size:15px;color:#333;margin-top:20px;">Παρατηρησεις</h2><p style="background:#fff;padding:10px;border:1px solid #eee;">{record.notes}</p>' if record.notes else ''}
      </div>
      <div style="background:#f0f0f0;padding:12px;text-align:center;font-size:12px;color:#888;border-radius:0 0 8px 8px;">
        Εστία — CONDIAN HOTELS{place_sfx} · {record.record_date.strftime('%d/%m/%Y')} · {period_gr}
      </div>
    </div>"""
    return send_email(f'Εστία — Νερά Χρήσης{place_sfx} {period_gr} {record.record_date.strftime("%d/%m/%Y")}', html, EMAIL_TO_LIST)


# ──────────────────────────────────────────────────────────────────────────
#  POOL LOG
# ──────────────────────────────────────────────────────────────────────────
def apply_pool_record(record, data, period):
    record.free_chlorine     = flt(data, 'free_chlorine')
    record.combined_chlorine = flt(data, 'combined_chlorine')
    record.ph                = flt(data, 'ph')
    record.temp              = flt(data, 'temp')
    record.turbidity         = flt(data, 'turbidity')
    record.backwash_done     = data.get('backwash_done') in ('1', 'on', 'true', 'True')
    record.notes             = data.get('notes', '')
    if period == 'morning':
        record.cyanuric_acid    = flt(data, 'cyanuric_acid')
        record.total_alkalinity = flt(data, 'total_alkalinity')
        record.orp              = flt(data, 'orp')

def send_pool_report_email(record, user):
    if not EMAIL_PASSWORD and not GRAPH_CLIENT_ID:
        return False
    period_gr = 'Πρωι' if record.period == 'morning' else 'Απογευμα'
    pool   = record.pool
    hotel  = pool.hotel.name if pool and pool.hotel else ''
    point  = f' — {pool.location}' if pool and pool.location else ''
    def row(label, val, unit, key):
        min_v, max_v = POOL_LIMITS.get(key, (None, None))
        if val is None:
            return f'<tr><td style="padding:7px 8px;border:1px solid #eee;">{label}</td><td style="padding:7px 8px;border:1px solid #eee;">-</td><td style="padding:7px 8px;border:1px solid #eee;color:#888;">{unit}</td></tr>'
        ok = (min_v is None or val >= min_v) and (max_v is None or val <= max_v)
        color = '#16a34a' if ok else '#dc2626'
        icon  = 'OK' if ok else 'ΠΡΟΣΟΧΗ'
        if min_v is not None and max_v is not None:
            limit = f'{min_v}-{max_v} {unit}'
        elif min_v is not None:
            limit = f'min {min_v} {unit}'
        elif max_v is not None:
            limit = f'max {max_v} {unit}'
        else:
            limit = unit
        return f'<tr><td style="padding:7px 8px;border:1px solid #eee;">{label}</td><td style="padding:7px 8px;border:1px solid #eee;color:{color};font-weight:500;">{icon}: {val} {unit}</td><td style="padding:7px 8px;border:1px solid #eee;color:#888;">{limit}</td></tr>'
    rows  = row('Ελευθερο χλωριο', record.free_chlorine, 'mg/L', 'free_chlorine')
    rows += row('Συνδεδεμενο χλωριο', record.combined_chlorine, 'mg/L', 'combined_chlorine')
    rows += row('pH', record.ph, '', 'ph')
    rows += row('Θερμοκρασια', record.temp, 'C', 'temp')
    rows += row('Θολοτητα', record.turbidity, 'NTU', 'turbidity')
    if record.period == 'morning':
        rows += row('Κυανουρικο οξυ', record.cyanuric_acid, 'mg/L', 'cyanuric_acid')
        rows += row('Ολικη αλκαλικοτητα', record.total_alkalinity, 'mg/L', 'total_alkalinity')
        rows += row('ORP (Redox)', record.orp, 'mV', 'orp')
    backwash = 'Ναι' if record.backwash_done else 'Οχι'
    _acts = compute_pool_actions(record)
    if _acts:
        _li = ''.join('<li style="margin-bottom:4px;' + ('color:#b91c1c;font-weight:600;' if a['urgent'] else '') + '">' + ('ΕΠΕΙΓΟΝ — ' if a['urgent'] else '') + a['label'] + ': ' + a['action'] + '</li>' for a in _acts)
        actions_html = '<h2 style="font-size:15px;color:#b45309;margin-top:18px;">Προτεινομενες ενεργειες</h2><ul style="background:#fffbeb;border:1px solid #fde68a;border-radius:6px;padding:12px 12px 12px 28px;color:#333;">' + _li + '</ul><p style="font-size:11px;color:#888;">' + SAFETY_NOTE + '</p>'
    else:
        actions_html = ''
    html = f"""
    <div style="font-family:Arial,sans-serif;max-width:620px;margin:0 auto;">
      <div style="background:#0e7490;color:white;padding:20px;border-radius:8px 8px 0 0;">
        <h1 style="margin:0;font-size:20px;">{hotel} - Πισινα {period_gr}</h1>
        <p style="margin:5px 0 0;opacity:0.85;">{pool.name if pool else ''}{point}</p>
        <p style="margin:5px 0 0;opacity:0.8;">{record.record_date.strftime('%d/%m/%Y')} | Υπευθυνος: {user.full_name}</p>
      </div>
      <div style="background:#f9f9f9;padding:20px;border:1px solid #eee;">
        <table style="width:100%;border-collapse:collapse;">
          <tr style="background:#0e7490;color:white;"><th style="padding:8px;text-align:left;">Παραμετρος</th><th style="padding:8px;text-align:left;">Μετρηση</th><th style="padding:8px;text-align:left;">Ορια</th></tr>
          {rows}
        </table>
        <p style="margin-top:14px;font-size:13px;color:#555;">Ανταποπλυση φιλτρου (backwash): <b>{backwash}</b></p>
        {actions_html}
        {f'<h2 style="font-size:15px;color:#333;margin-top:16px;">Παρατηρησεις</h2><p style="background:#fff;padding:10px;border:1px solid #eee;">{record.notes}</p>' if record.notes else ''}
      </div>
      <div style="background:#f0f0f0;padding:12px;text-align:center;font-size:12px;color:#888;border-radius:0 0 8px 8px;">
        {hotel} - Πισινα {pool.name if pool else ''} - {record.record_date.strftime('%d/%m/%Y')} - {period_gr}
      </div>
    </div>"""
    return send_email(f'{hotel} - Πισινα {pool.name if pool else ""} {period_gr} {record.record_date.strftime("%d/%m/%Y")}', html, EMAIL_TO_LIST)


# ──────────────────────────────────────────────────────────────────────────
#  AI ASSISTANT helpers
# ──────────────────────────────────────────────────────────────────────────
def resolve_provider():
    c = get_ai_config()
    if c['provider'] == 'anthropic' and c['anthropic']:
        return 'anthropic'
    if c['provider'] == 'openai' and c['openai']:
        return 'openai'
    if c['provider'] == 'auto':
        if c['anthropic']:
            return 'anthropic'
        if c['openai']:
            return 'openai'
    return None

def call_llm(system_prompt, messages):
    """messages: list of {'role':'user'|'assistant','content':str}. Returns (reply, error)."""
    provider = resolve_provider()
    if not provider:
        return None, 'not_configured'
    c = get_ai_config()
    try:
        if provider == 'anthropic':
            model = c['model'] or 'claude-sonnet-4-6'
            payload = {'model': model, 'max_tokens': 1024,
                       'system': system_prompt, 'messages': messages}
            req = urllib.request.Request(
                'https://api.anthropic.com/v1/messages',
                data=json.dumps(payload).encode('utf-8'),
                headers={'content-type': 'application/json',
                         'x-api-key': c['anthropic'],
                         'anthropic-version': '2023-06-01'})
            with urllib.request.urlopen(req, timeout=60) as r:
                data = json.loads(r.read().decode('utf-8'))
            return data['content'][0]['text'], None
        else:  # openai
            model = c['model'] or 'gpt-4o-mini'
            msgs = [{'role': 'system', 'content': system_prompt}] + messages
            payload = {'model': model, 'max_tokens': 1024, 'messages': msgs}
            req = urllib.request.Request(
                'https://api.openai.com/v1/chat/completions',
                data=json.dumps(payload).encode('utf-8'),
                headers={'content-type': 'application/json',
                         'authorization': 'Bearer ' + c['openai']})
            with urllib.request.urlopen(req, timeout=60) as r:
                data = json.loads(r.read().decode('utf-8'))
            return data['choices'][0]['message']['content'], None
    except urllib.error.HTTPError as e:
        body = ''
        try:
            body = e.read().decode('utf-8')[:300]
        except Exception:
            pass
        return None, f'http_{e.code}: {body}'
    except Exception as e:
        return None, str(e)

def build_pool_context(pool):
    if not pool:
        return 'Δεν έχει επιλεγεί συγκεκριμένη πισίνα — ρώτα τον χρήστη ποια πισίνα αφορά.'
    lines = [
        f'Ξενοδοχείο: {pool.hotel.name if pool.hotel else "-"}',
        f'Πισίνα: {pool.name}' + (f' ({pool.location})' if pool.location else ''),
        f'Τύπος: {pool.pool_type}',
        f'Όγκος: {(str(pool.volume_m3) + " m3") if pool.volume_m3 else "ΑΓΝΩΣΤΟΣ — ζήτησε να τον επιβεβαιώσει ο χρήστης"}',
    ]
    recs = (PoolRecord.query.filter_by(pool_id=pool.id)
            .order_by(PoolRecord.record_date.desc(), PoolRecord.recorded_at.desc())
            .limit(2).all())
    if recs:
        lines.append('Τελευταίες μετρήσεις:')
        for r in recs:
            per = 'Πρωί' if r.period == 'morning' else 'Απόγευμα'
            parts = []
            def add(lbl, val, key, unit=''):
                if val is None:
                    return
                mn, mx = POOL_LIMITS.get(key, (None, None))
                ok = (mn is None or val >= mn) and (mx is None or val <= mx)
                parts.append(f'{lbl}={val}{unit}' + ('' if ok else ' [ΕΚΤΟΣ ΟΡΙΩΝ]'))
            add('ελ.χλώριο', r.free_chlorine, 'free_chlorine', ' mg/L')
            add('συνδ.χλώριο', r.combined_chlorine, 'combined_chlorine', ' mg/L')
            add('pH', r.ph, 'ph')
            add('θερμ', r.temp, 'temp', ' C')
            add('θολότητα', r.turbidity, 'turbidity', ' NTU')
            add('κυανουρικό', r.cyanuric_acid, 'cyanuric_acid', ' mg/L')
            add('αλκαλικότητα', r.total_alkalinity, 'total_alkalinity', ' mg/L')
            add('ORP', r.orp, 'orp', ' mV')
            lines.append(f'  {r.record_date.strftime("%d/%m")} {per}: ' + (', '.join(parts) if parts else '—'))
    else:
        lines.append('Δεν υπάρχουν καταγεγραμμένες μετρήσεις για αυτή την πισίνα ακόμα.')
    lines.append('Επιθυμητά όρια: ελ.χλώριο 0.4-1.5 mg/L, συνδ.χλώριο <=0.5, pH 7.2-7.8, θερμ <=32C, θολότητα <=1 NTU, κυανουρικό <=75, αλκαλικότητα 80-120, ORP >=650 mV.')
    return '\n'.join(lines)


def build_pool_report_pdf(rep_date, records):
    """Branded PDF αναφορά πισινών για μια ημέρα (fpdf2 + DejaVuSans)."""
    from fpdf import FPDF
    NAVY=(25,56,71); GOLD=(187,149,73); GREY=(120,120,120); RED=(200,30,30); GREEN=(20,140,60)
    pdf = FPDF(orientation='P', unit='mm', format='A4')
    pdf.set_auto_page_break(True, margin=15)
    pdf.add_font('dv', '', os.path.join(BASE_DIR, 'assets', 'fonts', 'DejaVuSans.ttf'))
    pdf.add_font('dv', 'B', os.path.join(BASE_DIR, 'assets', 'fonts', 'DejaVuSans-Bold.ttf'))
    pdf.add_page()
    try:
        pdf.image(os.path.join(BASE_DIR, 'static', 'img', 'logo.png'), x=12, y=10, h=14)
    except Exception:
        pass
    pdf.set_xy(32, 11); pdf.set_font('dv', 'B', 16); pdf.set_text_color(*NAVY)
    pdf.cell(0, 8, 'Εστία — CONDIAN HOTELS · Αναφορά Πισινών', ln=1)
    pdf.set_x(32); pdf.set_font('dv', '', 11); pdf.set_text_color(*GREY)
    pdf.cell(0, 6, 'Ημερομηνία: ' + rep_date.strftime('%d/%m/%Y'), ln=1)
    pdf.ln(8)
    if not records:
        pdf.set_font('dv', '', 12); pdf.set_text_color(*GREY)
        pdf.cell(0, 8, 'Δεν υπάρχουν καταγραφές για αυτή την ημέρα.', ln=1)
    cur = None
    for r in records:
        pname = (r.pool.hotel.name + ' — ' + r.pool.name) if (r.pool and r.pool.hotel) else (r.pool.name if r.pool else '—')
        if pname != cur:
            cur = pname
            pdf.ln(2); pdf.set_font('dv', 'B', 12); pdf.set_text_color(*NAVY); pdf.set_fill_color(240,243,245)
            pdf.cell(0, 8, '  ' + pname, ln=1, fill=True)
        per = 'Πρωί' if r.period == 'morning' else 'Απόγευμα'
        pdf.set_font('dv', 'B', 10); pdf.set_text_color(*GOLD)
        pdf.cell(0, 6, per + ' — ' + (r.user.full_name if r.user else ''), ln=1)
        def line(lbl, val, key, unit=''):
            if val is None: return
            mn, mx = POOL_LIMITS.get(key, (None, None))
            ok = (mn is None or val >= mn) and (mx is None or val <= mx)
            pdf.set_font('dv', '', 10); pdf.set_text_color(40,40,40); pdf.cell(62, 5, '   ' + lbl)
            pdf.set_text_color(*(GREEN if ok else RED))
            pdf.cell(0, 5, str(val) + unit + ('' if ok else '  (εκτός ορίων)'), ln=1)
        line('Ελεύθερο χλώριο', r.free_chlorine, 'free_chlorine', ' mg/L')
        line('Συνδεδεμένο χλώριο', r.combined_chlorine, 'combined_chlorine', ' mg/L')
        line('pH', r.ph, 'ph')
        line('Θερμοκρασία', r.temp, 'temp', ' C')
        line('Θολότητα', r.turbidity, 'turbidity', ' NTU')
        line('Κυανουρικό', r.cyanuric_acid, 'cyanuric_acid', ' mg/L')
        line('Αλκαλικότητα', r.total_alkalinity, 'total_alkalinity', ' mg/L')
        line('ORP', r.orp, 'orp', ' mV')
        if r.notes:
            pdf.set_x(pdf.l_margin); pdf.set_font('dv', '', 9); pdf.set_text_color(*GREY)
            pdf.multi_cell(pdf.epw, 5, '   Σημ: ' + r.notes)
        _acts = compute_pool_actions(r)
        if _acts:
            pdf.set_x(pdf.l_margin); pdf.set_font('dv', 'B', 9); pdf.set_text_color(180, 83, 9)
            pdf.multi_cell(pdf.epw, 5, 'Προτεινόμενες ενέργειες:')
            for a in _acts:
                pdf.set_x(pdf.l_margin); pdf.set_font('dv', '', 9)
                pdf.set_text_color(185, 28, 28) if a['urgent'] else pdf.set_text_color(80, 80, 80)
                pdf.multi_cell(pdf.epw, 4.6, ('• ΕΠΕΙΓΟΝ — ' if a['urgent'] else '• ') + a['label'] + ': ' + a['action'])
    return bytes(pdf.output())


# v12.5 — Records: print-ready εξαγωγές (PDF + XLSX)
_RECORDS_HEADERS = ['Ημ/νία', 'Ώρα', 'Τύπος', 'Βάρδια', 'Ξενοδοχείο', 'Πισίνα / Δίκτυο', 'Υπεύθυνος', 'Διόρθωση']

def _records_row(it):
    return [
        it['date'].strftime('%d/%m/%Y') if it.get('date') else '',
        _gr_time(it['when'], '%H:%M') if it.get('when') else '',
        _RECORDS_KIND_LABEL.get(it['kind'], it['kind']),
        _RECORDS_PERIOD_LABEL.get(it['period'], it['period'] or ''),
        it.get('hotel') or '', it.get('place') or '', it.get('user') or '',
        'διορθώθηκε' if it.get('updated') else '',
    ]

def build_records_pdf(items, ftype='all'):
    """Branded, print-ready PDF λίστας υποβολών (Records) — fpdf2 landscape."""
    from fpdf import FPDF
    NAVY=(25,56,71); GREY=(120,120,120)
    pdf = FPDF(orientation='L', unit='mm', format='A4')
    pdf.set_auto_page_break(True, margin=12)
    pdf.add_font('dv', '', os.path.join(BASE_DIR, 'assets', 'fonts', 'DejaVuSans.ttf'))
    pdf.add_font('dv', 'B', os.path.join(BASE_DIR, 'assets', 'fonts', 'DejaVuSans-Bold.ttf'))
    pdf.add_page()
    try:
        pdf.image(os.path.join(BASE_DIR, 'static', 'img', 'logo.png'), x=12, y=9, h=13)
    except Exception:
        pass
    pdf.set_xy(30, 10); pdf.set_font('dv', 'B', 15); pdf.set_text_color(*NAVY)
    pdf.cell(0, 8, 'Εστία — CONDIAN HOTELS · Records', ln=1)
    pdf.set_x(30); pdf.set_font('dv', '', 10); pdf.set_text_color(*GREY)
    pdf.cell(0, 6, _RECORDS_TYPE_LABEL.get(ftype, 'Όλες οι υποβολές')
             + ' · Εκτύπωση: ' + date.today().strftime('%d/%m/%Y')
             + ' · Σύνολο: ' + str(len(items)), ln=1)
    pdf.ln(6)
    widths = [24, 16, 22, 24, 62, 62, 50, 26]
    pdf.set_font('dv', 'B', 9); pdf.set_text_color(255, 255, 255); pdf.set_fill_color(*NAVY)
    for h, w in zip(_RECORDS_HEADERS, widths):
        pdf.cell(w, 8, h, border=0, fill=True, align='L')
    pdf.ln(8)
    if not items:
        pdf.set_font('dv', '', 11); pdf.set_text_color(*GREY)
        pdf.cell(0, 10, 'Δεν υπάρχουν υποβολές.', ln=1)
        return bytes(pdf.output())
    fill = False
    for it in items:
        pdf.set_fill_color(243, 247, 250) if fill else pdf.set_fill_color(255, 255, 255)
        pdf.set_font('dv', '', 8.5); pdf.set_text_color(40, 40, 40)
        for val, w in zip(_records_row(it), widths):
            s = str(val)
            while s and pdf.get_string_width(s) > w - 2 and len(s) > 3:
                s = s[:-2]
            pdf.cell(w, 7, s, border=0, fill=True, align='L')
        pdf.ln(7)
        fill = not fill
    return bytes(pdf.output())

def build_records_xlsx(items, ftype='all'):
    """Print-ready Excel (.xlsx) λίστας υποβολών — openpyxl."""
    import io
    from openpyxl import Workbook
    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    from openpyxl.utils import get_column_letter
    from openpyxl.worksheet.properties import PageSetupProperties
    wb = Workbook(); ws = wb.active; ws.title = 'Records'
    ncol = len(_RECORDS_HEADERS)
    ws.merge_cells(start_row=1, start_column=1, end_row=1, end_column=ncol)
    t = ws.cell(row=1, column=1,
                value='Εστία — CONDIAN HOTELS · Records (' + _RECORDS_TYPE_LABEL.get(ftype, 'Όλες οι υποβολές') + ')')
    t.font = Font(bold=True, size=14, color='193847')
    ws.merge_cells(start_row=2, start_column=1, end_row=2, end_column=ncol)
    ws.cell(row=2, column=1,
            value='Εκτύπωση: ' + date.today().strftime('%d/%m/%Y') + ' · Σύνολο: ' + str(len(items))
            ).font = Font(size=10, color='777777')
    hdr = 4
    navy = PatternFill('solid', fgColor='193847')
    thin = Side(style='thin', color='DDDDDD')
    border = Border(left=thin, right=thin, top=thin, bottom=thin)
    for j, h in enumerate(_RECORDS_HEADERS, 1):
        c = ws.cell(row=hdr, column=j, value=h)
        c.font = Font(bold=True, color='FFFFFF'); c.fill = navy
        c.alignment = Alignment(horizontal='left', vertical='center'); c.border = border
    for i, it in enumerate(items):
        for j, v in enumerate(_records_row(it), 1):
            c = ws.cell(row=hdr + 1 + i, column=j, value=v)
            c.border = border; c.alignment = Alignment(horizontal='left', vertical='center')
    for j, w in enumerate([13, 8, 11, 12, 26, 26, 22, 13], 1):
        ws.column_dimensions[get_column_letter(j)].width = w
    ws.freeze_panes = 'A%d' % (hdr + 1)
    ws.print_title_rows = '%d:%d' % (hdr, hdr)
    ws.page_setup.orientation = 'landscape'
    ws.page_setup.fitToWidth = 1; ws.page_setup.fitToHeight = 0
    ws.sheet_properties.pageSetUpPr = PageSetupProperties(fitToPage=True)
    buf = io.BytesIO(); wb.save(buf); return buf.getvalue()


# ── Background email senders (τρέχουν σε thread, με app context) ──
def _bg_send_water(rid, uid):
    with app.app_context():
        r = WaterRecord.query.get(rid); u = User.query.get(uid)
        if r and u:
            send_report_email(r, u)

def _bg_send_pool(rid, uid):
    with app.app_context():
        r = PoolRecord.query.get(rid); u = User.query.get(uid)
        if r and u:
            send_pool_report_email(r, u)

def _bg_send_fault(fid):
    with app.app_context():
        fr = FaultReport.query.get(fid)
        if fr:
            send_fault_email(fr)


# ──────────────────────────────────────────────────────────────────────────
#  AUTH
# ──────────────────────────────────────────────────────────────────────────
def landing_for(user):
    r = role_rank(user.role)
    if r >= ROLE_RANK['admin']:
        return url_for('dashboard')
    if r >= ROLE_RANK['viewer'] and r != ROLE_RANK['staff']:
        return url_for('pools_dashboard')   # manager / viewer
    return url_for('pools_app')             # staff
@app.route('/')
def index():
    if 'user_id' in session:
        user = User.query.get(session['user_id'])
        if user:
            return render_template('shell.html', user=user, is_admin=is_admin(),
                                   is_master=(user.role == 'masteradmin'), can_log=can_log(),
                                   rank=role_rank(user.role), RANK=ROLE_RANK)
    return redirect(url_for('login'))

@app.route('/login', methods=['GET', 'POST'])
def login():
    error = None
    if request.method == 'POST':
        _k = _login_key()
        if _login_blocked(_k):
            return render_template('login.html', error='Πολλες αποτυχημενες προσπαθειες. Δοκιμασε ξανα σε λιγα λεπτα.', reg_open=REGISTRATION_OPEN)
        username = request.form.get('username', '').strip()
        password = request.form.get('password', '')
        user = User.query.filter_by(username=username, is_active=True).first()
        if user and user.approved and getattr(user, 'login_enabled', True) is not False and check_password_hash(user.password, password):
            _login_reset(_k)
            session['user_id']   = user.id
            session['user_name'] = user.full_name
            session['user_role'] = user.role
            session['language']  = user.language
            log_activity('login')
            return redirect(url_for('index'))
        _login_record_fail(_k)
        error = 'Λαθος username η password'
    return render_template('login.html', error=error, reg_open=REGISTRATION_OPEN)

@app.route('/logout')
def logout():
    log_activity('logout')
    session.clear()
    return redirect(url_for('login'))

# ── v12.9 — Μηνύματα / Ρυθμίσεις / Λογαριασμός ──
@app.route('/messages')
def messages():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    uid = session['user_id']
    box = request.args.get('box', 'in')
    if box == 'out':
        items = (Message.query.filter_by(sender_id=uid)
                 .order_by(Message.created_at.desc()).limit(100).all())
    else:
        items = (Message.query.filter_by(recipient_id=uid)
                 .order_by(Message.created_at.desc()).limit(100).all())
        for m in items:
            if not m.is_read:
                m.is_read = True
        db.session.commit()
    people = (User.query.filter(User.id != uid, User.is_active == True, User.approved == True)
              .order_by(User.full_name).all())
    return render_template('messages.html', items=items, box=box, people=people, me=current_user())

@app.route('/messages/send', methods=['POST'])
def messages_send():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    uid = session['user_id']
    rid = request.form.get('recipient_id', type=int)
    body = (request.form.get('body') or '').strip()
    if rid and body and User.query.get(rid):
        db.session.add(Message(sender_id=uid, recipient_id=rid, body=body[:4000]))
        me = current_user()
        db.session.add(Notification(user_id=rid,
            text='Νέο μήνυμα από ' + (me.full_name if me else ''), link='/messages?embed=1'))
        db.session.commit()
    return redirect('/messages?box=out&embed=1')

@app.route('/settings')
def settings_page():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    return render_template('settings.html', me=current_user())

@app.route('/account')
def account_page():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    return render_template('account.html', me=current_user())

@app.route('/register', methods=['GET', 'POST'])
def register():
    if not REGISTRATION_OPEN:
        return redirect(url_for('login'))
    if not REG_CODE:
        return render_template('register.html', enabled=False, error=None, done=False)
    error = None
    if request.method == 'POST':
        fm = request.form
        if fm.get('reg_code', '').strip() != REG_CODE:
            error = 'Λάθος κωδικός εγγραφής'
        elif not (fm.get('username') and fm.get('password') and fm.get('full_name')):
            error = 'Συμπλήρωσε ονοματεπώνυμο, username και κωδικό'
        elif User.query.filter_by(username=fm['username'].strip()).first():
            error = 'Το username υπάρχει ήδη'
        else:
            db.session.add(User(
                username=fm['username'].strip(),
                password=generate_password_hash(fm['password']),
                full_name=fm['full_name'].strip(),
                email=fm.get('email', '').strip(),
                phone=fm.get('phone', '').strip(),
                role='staff', language=fm.get('language', 'el'),
                approved=False, is_active=True))
            db.session.commit()
            return render_template('register.html', enabled=True, error=None, done=True)
    return render_template('register.html', enabled=True, error=error, done=False)

@app.route('/profile', methods=['GET', 'POST'])
def profile():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    user = User.query.get(session['user_id'])
    saved = False
    if request.method == 'POST':
        fm = request.form
        user.email = fm.get('email', '').strip()
        user.phone = fm.get('phone', '').strip()
        if fm.get('language') in ('el', 'en'):
            user.language = fm['language']; session['language'] = fm['language']
        if fm.get('password'):
            user.password = generate_password_hash(fm['password'])
        photo = request.files.get('photo')
        if photo and photo.filename and photo.mimetype and photo.mimetype.startswith('image/'):
            raw = photo.read()
            if 0 < len(raw) <= 800 * 1024:
                import base64
                user.avatar = 'data:' + photo.mimetype + ';base64,' + base64.b64encode(raw).decode()
        db.session.commit()
        log_activity('profile_update')
        saved = True
    return render_template('profile.html', user=user, saved=saved)


# ──────────────────────────────────────────────────────────────────────────
#  WATER LOG ROUTES
# ──────────────────────────────────────────────────────────────────────────
def water_systems_payload(user=None):
    """v12.3 — allowed ξενοδοχεία + ενεργά δίκτυα νερού τους (mirror hotels_payload)."""
    if user is not None and role_rank(user.role) < ROLE_RANK['admin']:
        hotels = allowed_hotels(user)
    else:
        hotels = Hotel.query.filter_by(is_active=True).order_by(Hotel.name).all()
    payload = []
    for h in hotels:
        systems = (WaterSystem.query.filter_by(hotel_id=h.id, is_active=True)
                   .order_by(WaterSystem.name).all())
        payload.append({'id': h.id, 'name': h.name,
                        'systems': [{'id': s.id, 'name': s.name, 'location': s.location or ''}
                                    for s in systems]})
    return hotels, payload

_POOL_FIELDS = ['free_chlorine','combined_chlorine','ph','temp','turbidity',
                'cyanuric_acid','total_alkalinity','orp']
_WATER_FIELDS = ['clo2_tank','clo2_kitchen','clo2_remote','clo2_dhw_out','clo2_dhw_return','clo2_ro',
                 'location_kitchen','location_remote','temp_tank','temp_dhw_out','temp_dhw_return','temp_ro',
                 'temp_kitchen_cold','temp_kitchen_hot','temp_remote_cold','temp_remote_hot','ph_tank']

def _pool_preset(pool_id, period):
    """v12.83 — preset για άνοιγμα φόρμας πισίνας από την οθόνη «Σήμερα» (με prefill αν υπάρχει εγγραφή)."""
    if not pool_id:
        return None
    try:
        pid = int(pool_id)
    except (TypeError, ValueError):
        return None
    per = period if period in ('morning', 'afternoon') else 'morning'
    rec = PoolRecord.query.filter_by(pool_id=pid, record_date=date.today(), period=per).first()
    vals = {}
    if rec:
        for f in _POOL_FIELDS:
            v = getattr(rec, f, None)
            if v is not None:
                vals[f] = v
        if rec.backwash_done:
            vals['backwash_done'] = True
        if rec.notes:
            vals['notes'] = rec.notes
    return {'kind': 'pool', 'id': pid, 'period': per, 'values': vals, 'editing': bool(rec)}

def _water_preset(sys_id, period):
    """v12.83 — preset για άνοιγμα φόρμας νερών από την οθόνη «Σήμερα» (με prefill αν υπάρχει εγγραφή)."""
    if not sys_id:
        return None
    try:
        sid = int(sys_id)
    except (TypeError, ValueError):
        return None
    per = period if period in ('morning', 'afternoon') else 'morning'
    rec = WaterRecord.query.filter_by(water_system_id=sid, record_date=date.today(), period=per).first()
    vals = {}
    if rec:
        for f in _WATER_FIELDS:
            v = getattr(rec, f, None)
            if v is not None and v != '':
                vals[f] = v
        if rec.notes:
            vals['notes'] = rec.notes
    return {'kind': 'water', 'id': sid, 'period': per, 'values': vals, 'editing': bool(rec)}


@app.route('/app')
def water_app():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    # v12.231 Φ3c-3: legacy φόρμα Νερών παγωμένη → νέα ενιαία φόρμα μετρήσεων
    return redirect(url_for('measurements_entry') + ('?embed=1' if request.args.get('embed') else ''))
    if not can_log():
        return redirect(url_for('pools_dashboard'))
    user = User.query.get(session['user_id'])
    hotels, systems_json = water_systems_payload(user)
    # σημερινές καταγραφές ανά δίκτυο: {system_id: {period: record_id}}
    done = {}
    for r in WaterRecord.query.filter_by(record_date=date.today()).all():
        if r.water_system_id:
            done.setdefault(str(r.water_system_id), {})[r.period] = r.id
    preset = _water_preset(request.args.get('sys'), request.args.get('period'))
    return render_template('app.html', user=user, hotels=hotels,
                           systems_json=systems_json, done=done,
                           preset=preset, ai_configured=(resolve_provider() is not None))

@app.route('/submit', methods=['POST'])
def submit():
    if 'user_id' not in session:
        return jsonify({'success': False, 'message': 'Μη εξουσιοδοτημενο'}), 401
    # v12.231 Φ3c-3: παγωμένη legacy υποβολή Νερών
    return jsonify({'success': False, 'message': 'Η παλιά φόρμα Νερών αποσύρθηκε. Χρησιμοποίησε «Καταγραφή μετρήσεων».'}), 410
    user   = User.query.get(session['user_id'])
    data   = request.form
    period = data.get('period', 'morning')
    ws_id  = data.get('water_system_id')
    ws = WaterSystem.query.filter_by(id=ws_id, is_active=True).first() if ws_id else None
    if not ws:
        return jsonify({'success': False, 'message': 'Επιλεξτε δικτυο νερου'}), 400
    if not can_access_water_system(user, ws):
        return jsonify({'success': False, 'message': 'Δεν εχεις προσβαση σε αυτο το δικτυο'}), 403
    # v12.3 — dedup ΑΝΑ δίκτυο (όχι πια κοινό record για όλους)
    record = WaterRecord.query.filter_by(water_system_id=ws.id,
                                         record_date=date.today(), period=period).first()
    if record:
        apply_record(record, data, period)
        record.updated_at = datetime.now()
        record.updated_by = user.id
    else:
        record = WaterRecord(user_id=user.id, water_system_id=ws.id,
                             record_date=date.today(), period=period)
        apply_record(record, data, period)
        db.session.add(record)
    db.session.commit()
    place = (ws.hotel.name + ' · ' + ws.name) if ws.hotel else ws.name
    log_activity('water_submit', f'{place} ({period})', hotel_id=(ws.hotel_id if ws else None))
    _wa = compute_water_actions(record)
    if _wa:
        notify_admins((('ΕΠΕΙΓΟΝ — ' if any(x['urgent'] for x in _wa) else '')
                       + f'Μέτρηση νερού/ΖΝΧ εκτός ορίων — {place}.'), '/dashboard')
    threading.Thread(target=_bg_send_water, args=(record.id, user.id), daemon=True).start()
    period_gr = 'Πρωι' if period == 'morning' else 'Απογευμα'
    return jsonify({'success': True, 'record_id': record.id,
                    'message': f'Καταγραφη {ws.name} ({period_gr}) αποθηκευτηκε!'})

@app.route('/edit/<int:record_id>', methods=['GET', 'POST'])
def edit_record(record_id):
    if 'user_id' not in session:
        return redirect(url_for('login'))
    user   = User.query.get(session['user_id'])
    record = WaterRecord.query.get_or_404(record_id)
    if record.water_system and not can_access_water_system(user, record.water_system):
        return redirect(url_for('water_app'))
    if not is_admin() and record.record_date != date.today():
        return redirect(url_for('water_app'))
    if request.method == 'POST':
        apply_record(record, request.form, record.period)
        record.updated_at = datetime.now()
        record.updated_by = user.id
        db.session.commit()
        if user.role == 'admin':
            return redirect(url_for('dashboard') + '?success=updated')
        return redirect(url_for('water_app'))
    return render_template('edit.html', user=user, record=record)

@app.route('/api/record/<int:record_id>')
def api_record(record_id):
    if 'user_id' not in session:
        return jsonify({}), 401
    r = WaterRecord.query.get_or_404(record_id)
    _ws = r.water_system
    return jsonify({
        'id': r.id, 'period': r.period,
        'water_system_id': r.water_system_id,
        'water_system': (_ws.name if _ws else None),
        'hotel': (_ws.hotel.name if _ws and _ws.hotel else None),
        'record_date': r.record_date.strftime('%d/%m/%Y'),
        'clo2_tank': r.clo2_tank, 'clo2_kitchen': r.clo2_kitchen,
        'clo2_remote': r.clo2_remote, 'clo2_dhw_out': r.clo2_dhw_out,
        'clo2_dhw_return': r.clo2_dhw_return, 'clo2_ro': r.clo2_ro,
        'location_kitchen': r.location_kitchen, 'location_remote': r.location_remote,
        'temp_tank': r.temp_tank, 'temp_dhw_out': r.temp_dhw_out,
        'temp_dhw_return': r.temp_dhw_return, 'temp_ro': r.temp_ro,
        'temp_kitchen_cold': r.temp_kitchen_cold, 'temp_kitchen_hot': r.temp_kitchen_hot,
        'temp_remote_cold': r.temp_remote_cold, 'temp_remote_hot': r.temp_remote_hot,
        'ph_tank': r.ph_tank, 'notes': r.notes or ''
    })

@app.route('/set-language/<lang>')
def set_language(lang):
    if lang in ['el', 'en', 'uk'] and 'user_id' in session:
        session['language'] = lang
        user = User.query.get(session['user_id'])
        if user:
            user.language = lang
            db.session.commit()
    return redirect(request.referrer or url_for('water_app'))


# ──────────────────────────────────────────────────────────────────────────
#  POOL LOG ROUTES
# ──────────────────────────────────────────────────────────────────────────
def hotels_payload(user=None):
    if user is not None and role_rank(user.role) < ROLE_RANK['admin']:
        hotels = allowed_hotels(user)
    else:
        hotels = Hotel.query.filter_by(is_active=True).order_by(Hotel.name).all()
    payload = [{
        'id': h.id, 'name': h.name,
        'pools': [{'id': p.id, 'name': p.name, 'location': p.location or '',
                   'type': p.pool_type, 'volume_m3': p.volume_m3}
                  for p in h.pools if p.is_active]
    } for h in hotels]
    return hotels, payload

@app.route('/katagrafes')
def katagrafes_today():
    """v12.83 — (αποσύρθηκε v12.237) η παλιά «Σήμερα» αντικαταστάθηκε από την engine «Σήμερα»."""
    if 'user_id' not in session:
        return redirect(url_for('login'))
    # v12.237: απόσυρση παλιάς «Σήμερα» → νέα engine «Σήμερα»
    return redirect(url_for('measurements_today') + ('?embed=1' if request.args.get('embed') else ''))
    if not can_log():
        return redirect(url_for('pools_dashboard'))
    user = current_user()
    hotels = scoped_hotels(user)
    today = date.today()
    # done maps: pool_id/system_id -> {period: record_id}
    pdone = {}
    for r in PoolRecord.query.filter_by(record_date=today).all():
        pdone.setdefault(r.pool_id, {})[r.period] = r.id
    wdone = {}
    for r in WaterRecord.query.filter_by(record_date=today).all():
        if r.water_system_id:
            wdone.setdefault(r.water_system_id, {})[r.period] = r.id
    cards = []
    for h in hotels:
        pools = [p for p in h.pools if p.is_active]
        systems = (WaterSystem.query.filter_by(hotel_id=h.id, is_active=True)
                   .order_by(WaterSystem.name).all())
        if not pools and not systems:
            continue
        cards.append({
            'hotel': h.name,
            'pools': [{'id': p.id, 'name': p.name, 'location': p.location or '',
                       'done': pdone.get(p.id, {})} for p in pools],
            'systems': [{'id': s.id, 'name': s.name, 'location': s.location or '',
                         'done': wdone.get(s.id, {})} for s in systems],
        })
    # σύνολα για κεφαλίδα προόδου
    tot = sum(len(c['pools']) + len(c['systems']) for c in cards) * 2
    dn = 0
    for c in cards:
        for p in c['pools']: dn += len(p['done'])
        for s in c['systems']: dn += len(s['done'])
    hids = {h.id for h in hotels}
    # v12.92 — Ειδοποιήσεις εκτός ορίου (σημερινές μετρήσεις σε scope)
    alerts = []
    _per_gr = {'morning': 'Πρωί', 'afternoon': 'Απόγευμα'}
    for r in PoolRecord.query.filter_by(record_date=today).all():
        if not r.pool or r.pool.hotel_id not in hids:
            continue
        acts = compute_pool_actions(r)
        if acts:
            alerts.append({'place': (r.pool.hotel.name + ' · ' + r.pool.name) if r.pool.hotel else r.pool.name,
                           'period': _per_gr.get(r.period, r.period), 'kind': 'pool',
                           'urgent': any(a['urgent'] for a in acts), 'items': acts})
    for r in WaterRecord.query.filter_by(record_date=today).all():
        ws = r.water_system
        if not ws or ws.hotel_id not in hids:
            continue
        acts = compute_water_actions(r)
        if acts:
            alerts.append({'place': (ws.hotel.name + ' · ' + ws.name) if ws.hotel else ws.name,
                           'period': _per_gr.get(r.period, r.period), 'kind': 'water',
                           'urgent': any(a['urgent'] for a in acts), 'items': acts})
    alerts.sort(key=lambda a: not a['urgent'])
    # v12.92 — Ανοιχτές βλάβες (scope)
    faults_open, faults_count = [], 0
    try:
        from faults import Fault, TERMINAL, STATUS_LABELS
        fq = Fault.query.filter(~Fault.status.in_(TERMINAL), Fault.hotel_id.in_(hids or [-1]))
        faults_count = fq.count()
        for f in fq.order_by(Fault.submitted_at.desc()).limit(6).all():
            faults_open.append({'code': f.code, 'desc': (f.description or '')[:90],
                                'priority': f.priority, 'status': STATUS_LABELS.get(f.status, f.status),
                                'hotel': f.hotel.name if f.hotel else '', 'id': f.id,
                                'high': (f.priority == 'Υψηλή')})
    except Exception:
        pass
    return render_template('katagrafes.html', user=user, cards=cards,
                           total_slots=tot, done_slots=dn,
                           alerts=alerts, faults_open=faults_open, faults_count=faults_count,
                           is_admin=is_admin())


@app.route('/pools')
def pools_app():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    # v12.231 Φ3c-3: legacy φόρμα Πισινών παγωμένη → νέα ενιαία φόρμα μετρήσεων
    return redirect(url_for('measurements_entry') + ('?embed=1' if request.args.get('embed') else ''))
    if not can_log():
        return redirect(url_for('pools_dashboard'))
    user = User.query.get(session['user_id'])
    hotels, hotels_json = hotels_payload(user)
    todays = PoolRecord.query.filter_by(record_date=date.today()).all()
    done = {}
    for r in todays:
        done.setdefault(str(r.pool_id), []).append(r.period)
    preset = _pool_preset(request.args.get('pool'), request.args.get('period'))
    return render_template('pools.html', user=user, hotels=hotels,
                           hotels_json=hotels_json, done=done, limits=POOL_LIMITS,
                           action_rules=ACTION_RULES, param_labels=PARAM_LABELS, safety_note=SAFETY_NOTE,
                           preset=preset, ai_configured=(resolve_provider() is not None))

@app.route('/submit-pool', methods=['POST'])
def submit_pool():
    if 'user_id' not in session or not can_log():
        return jsonify({'success': False, 'message': 'Μη εξουσιοδοτημενο'}), 401
    # v12.231 Φ3c-3: παγωμένη legacy υποβολή Πισινών
    return jsonify({'success': False, 'message': 'Η παλιά φόρμα Πισινών αποσύρθηκε. Χρησιμοποίησε «Καταγραφή μετρήσεων».'}), 410
    user   = User.query.get(session['user_id'])
    data   = request.form
    period = data.get('period', 'morning')
    pool_id = data.get('pool_id')
    pool = Pool.query.filter_by(id=pool_id, is_active=True).first() if pool_id else None
    if not pool:
        return jsonify({'success': False, 'message': 'Επιλεξτε πισινα'}), 400
    if not can_access_pool(user, pool):
        return jsonify({'success': False, 'message': 'Δεν εχεις προσβαση σε αυτη την πισινα'}), 403
    record = PoolRecord.query.filter_by(pool_id=pool.id, record_date=date.today(), period=period).first()
    if record:
        apply_pool_record(record, data, period)
        record.updated_at = datetime.now()
        record.updated_by = user.id
    else:
        record = PoolRecord(pool_id=pool.id, user_id=user.id, record_date=date.today(), period=period)
        apply_pool_record(record, data, period)
        db.session.add(record)
    db.session.commit()
    log_activity('pool_submit', f'{pool.name} ({period})', hotel_id=pool.hotel_id)
    _a = compute_pool_actions(record)
    if _a:
        notify_admins((('ΕΠΕΙΓΟΝ — ' if any(x['urgent'] for x in _a) else '') + 'Μέτρηση εκτός ορίων: ' + pool.name), '/pools/dashboard')
    threading.Thread(target=_bg_send_pool, args=(record.id, user.id), daemon=True).start()
    period_gr = 'Πρωι' if period == 'morning' else 'Απογευμα'
    return jsonify({'success': True, 'message': f'Καταγραφη {pool.name} ({period_gr}) αποθηκευτηκε!'})

@app.route('/pools/edit/<int:record_id>', methods=['GET', 'POST'])
def edit_pool_record(record_id):
    if 'user_id' not in session or not can_log():
        return redirect(url_for('login'))
    user   = current_user()
    record = PoolRecord.query.get_or_404(record_id)
    if not can_access_pool(user, record.pool):
        return redirect(url_for('pools_app'))
    if not is_admin() and record.record_date != date.today():
        return redirect(url_for('pools_app'))
    if request.method == 'POST':
        apply_pool_record(record, request.form, record.period)
        record.updated_at = datetime.now()
        record.updated_by = user.id
        db.session.commit()
        log_activity('pool_edit', record.pool.name if record.pool else '')
        if user.role == 'admin':
            return redirect(url_for('pools_dashboard') + '?success=updated')
        return redirect(url_for('pools_app'))
    return render_template('pool_edit.html', user=user, record=record, limits=POOL_LIMITS)

@app.route('/api/pool-record/<int:record_id>')
def api_pool_record(record_id):
    if 'user_id' not in session:
        return jsonify({}), 401
    r = PoolRecord.query.get_or_404(record_id)
    return jsonify({
        'id': r.id, 'period': r.period, 'pool_id': r.pool_id,
        'record_date': r.record_date.strftime('%d/%m/%Y'),
        'free_chlorine': r.free_chlorine, 'combined_chlorine': r.combined_chlorine,
        'ph': r.ph, 'temp': r.temp, 'turbidity': r.turbidity,
        'cyanuric_acid': r.cyanuric_acid, 'total_alkalinity': r.total_alkalinity,
        'orp': r.orp, 'backwash_done': r.backwash_done, 'notes': r.notes or ''
    })

@app.route('/pools/report.pdf')
def pool_report_pdf():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    ds = request.args.get('date')
    try:
        rep_date = datetime.strptime(ds, '%Y-%m-%d').date() if ds else date.today()
    except ValueError:
        rep_date = date.today()
    records = (PoolRecord.query.filter_by(record_date=rep_date)
               .order_by(PoolRecord.pool_id, PoolRecord.period).all())
    pdf_bytes = build_pool_report_pdf(rep_date, records)
    fname = 'pool-report-' + rep_date.strftime('%Y-%m-%d') + '.pdf'
    return Response(pdf_bytes, mimetype='application/pdf',
                    headers={'Content-Disposition': 'attachment; filename=' + fname})


# ──────────────────────────────────────────────────────────────────────────
#  AI ASSISTANT ROUTES
# ──────────────────────────────────────────────────────────────────────────
@app.route('/assistant')
def assistant_page():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    user = User.query.get(session['user_id'])
    hotels, hotels_json = hotels_payload(current_user())
    return render_template('assistant.html', user=user,
                           hotels_json=hotels_json,
                           configured=(resolve_provider() is not None))

@app.route('/api/assistant', methods=['POST'])
def api_assistant():
    if 'user_id' not in session:
        return jsonify({'reply': '', 'error': 'auth'}), 401
    data = request.get_json(force=True, silent=True) or {}
    raw  = data.get('messages', [])
    pool_id = data.get('pool_id')
    messages = []
    for m in raw[-12:]:
        role = m.get('role')
        content = (m.get('content') or '').strip()
        if role in ('user', 'assistant') and content:
            messages.append({'role': role, 'content': content[:4000]})
    if not messages:
        return jsonify({'reply': '', 'error': 'empty'}), 400
    pool = Pool.query.filter_by(id=pool_id, is_active=True).first() if pool_id else None
    context = build_pool_context(pool)
    system = POOL_ASSISTANT_PROMPT + '\n\n# ΔΕΔΟΜΕΝΑ ΕΦΑΡΜΟΓΗΣ (τρέχουσα κατάσταση)\n' + context
    reply, err = call_llm(system, messages)
    if err == 'not_configured':
        return jsonify({'reply': '', 'error': 'not_configured'})
    if err:
        return jsonify({'reply': '', 'error': err}), 502
    return jsonify({'reply': reply, 'error': None})


# ──────────────────────────────────────────────────────────────────────────
#  ADMIN DASHBOARDS
# ──────────────────────────────────────────────────────────────────────────
@app.route('/dashboard')
def dashboard():
    # v12.113 — πρόσβαση βάσει δικαιώματος μενού (water_dash), όχι μόνο admin.
    if 'user_id' not in session:
        return redirect(url_for('login'))
    # v12.234: Πίνακας Νερών αποσύρθηκε → ενιαία Στατιστικά μετρήσεων
    return redirect(url_for('measurements_stats') + ('?embed=1' if request.args.get('embed') else ''))
    import extras as _E
    me = current_user()
    if not (is_admin() or _E.menu_allows('water_dash', me)):
        return redirect(url_for('measurements_today')) if can_log() else redirect(url_for('login'))
    # scope ξενοδοχείων: admin=όλα, αλλιώς μόνο τα ανατεθειμένα (mirror Πίνακα Πισινών)
    if is_admin():
        hotels = Hotel.query.filter_by(is_active=True).order_by(Hotel.name).all()
    else:
        hotels = allowed_hotels(me)
    hids = {h.id for h in hotels}
    systems = [s for s in WaterSystem.query.filter_by(is_active=True)
               .order_by(WaterSystem.hotel_id, WaterSystem.name).all() if s.hotel_id in hids]
    sys_ids = [s.id for s in systems]
    # v12.3 — φίλτρα ανά ξενοδοχείο / δίκτυο / χρήστη
    f_hotel  = request.args.get('hotel_id', type=int) or active_hotel_id()
    f_system = request.args.get('system_id', type=int)
    f_staff  = request.args.get('staff_id', type=int)
    def _scope(q):
        if f_system and f_system in sys_ids:
            return q.filter(WaterRecord.water_system_id == f_system)
        if f_hotel:
            sids = [s.id for s in systems if s.hotel_id == f_hotel]
            return q.filter(WaterRecord.water_system_id.in_(sids or [-1]))
        return q.filter(WaterRecord.water_system_id.in_(sys_ids or [-1]))
    q = _scope(WaterRecord.query)
    if f_staff:
        q = q.filter(WaterRecord.user_id == f_staff)
    q = apply_period(q, WaterRecord.record_date)
    records = q.order_by(WaterRecord.record_date.desc(), WaterRecord.period).limit(60).all()
    users   = User.query.filter_by(is_active=True, approved=True).all()
    today_q = _scope(WaterRecord.query.filter_by(record_date=date.today()))
    today_records = today_q.order_by(WaterRecord.water_system_id).all()
    today_m = [r for r in today_records if r.period == 'morning']
    today_a = [r for r in today_records if r.period == 'afternoon']
    return render_template('dashboard.html', records=records, users=users,
                           today_morning_list=today_m, today_afternoon_list=today_a,
                           hotels=hotels, systems=systems, is_admin=is_admin(),
                           f_hotel=f_hotel, f_system=f_system, f_staff=f_staff)


# v12.4 — Χρήστες: δική τους σελίδα στη Διαχείριση (έφυγαν από τον Πίνακα Νερών)
@app.route('/dashboard/users')
def users_admin():
    if not is_admin():
        return redirect(url_for('login'))
    # v12.44 — το εισαγμένο προσωπικό (login_enabled=False) ΔΕΝ εμφανίζεται εδώ· μένει καθαρή για πραγματικούς λογαριασμούς
    users   = [u for u in User.query.filter_by(is_active=True, approved=True).all() if getattr(u, 'login_enabled', None) is not False]
    inactive = [u for u in User.query.filter_by(is_active=False).all() if getattr(u, 'login_enabled', None) is not False]
    pending = User.query.filter_by(is_active=True, approved=False).all()
    try:                                  # v12.15 — ειδικότητες (Module Βλαβοληψία)
        import faults as _flt
        fault_specs = [s.name for s in _flt.Specialty.query.filter_by(is_active=True).order_by(_flt.Specialty.sort, _flt.Specialty.name).all()]
        user_specs = {}
        for us in _flt.UserSpecialty.query.all():
            user_specs.setdefault(us.user_id, []).append(us.specialty)
    except Exception:
        fault_specs, user_specs = [], {}
    return render_template('users_admin.html', users=users + inactive, pending=pending,
                           all_hotels=Hotel.query.filter_by(is_active=True).order_by(Hotel.name).all(),
                           role_labels=ROLE_LABELS, me=current_user(),
                           fault_specs=fault_specs, user_specs=user_specs)


# v12.4 — Records: ενιαίο feed υποβολών (πισίνες + νερά χρήσης), role-scoped
_RECORDS_TYPE_LABEL = {'all': 'Όλες οι υποβολές', 'pools': 'Πισίνες', 'water': 'Νερά Χρήσης', 'area': 'Τομείς'}
_RECORDS_KIND_LABEL = {'pool': 'Πισίνα', 'water': 'Νερά', 'area': 'Τομέας'}
_RECORDS_PERIOD_LABEL = {'morning': 'Πρωί', 'afternoon': 'Απόγευμα', 'day': 'Ημέρα'}

_POOL_VAL_LABELS = [
    ('free_chlorine', 'Ελ. χλώριο', 'mg/L'), ('combined_chlorine', 'Δεσμ. χλώριο', 'mg/L'),
    ('ph', 'pH', ''), ('temp', 'Θερμοκρασία', '°C'), ('turbidity', 'Θολότητα', 'NTU'),
    ('cyanuric_acid', 'Κυανουρικό', 'mg/L'), ('total_alkalinity', 'Αλκαλικότητα', 'mg/L'), ('orp', 'ORP', 'mV'),
]
_WATER_VAL_LABELS = [
    ('clo2_tank', 'ClO₂ δεξαμενή', 'ppm'), ('clo2_kitchen', 'ClO₂ κουζίνα', 'ppm'), ('clo2_remote', 'ClO₂ απομακρ.', 'ppm'),
    ('clo2_dhw_out', 'ClO₂ ΖΝΧ προσαγ.', 'ppm'), ('clo2_dhw_return', 'ClO₂ ΖΝΧ επιστρ.', 'ppm'), ('clo2_ro', 'ClO₂ αντ.όσμωση', 'ppm'),
    ('ph_tank', 'pH δεξαμενή', ''),
    ('temp_tank', 'Θ δεξαμενή', '°C'), ('temp_dhw_out', 'Θ ΖΝΧ προσαγ.', '°C'), ('temp_dhw_return', 'Θ ΖΝΧ επιστρ.', '°C'),
    ('temp_ro', 'Θ RO', '°C'), ('temp_kitchen_cold', 'Θ κουζίνα κρύο', '°C'), ('temp_kitchen_hot', 'Θ κουζίνα ζεστό', '°C'),
    ('temp_remote_cold', 'Θ απομ. κρύο', '°C'), ('temp_remote_hot', 'Θ απομ. ζεστό', '°C'),
]

def _vals_from_cols(r, label_map):
    out = []
    for attr, lbl, unit in label_map:
        v = getattr(r, attr, None)
        if v is None:
            continue
        out.append({'label': lbl, 'value': v, 'unit': unit})
    return out

def _reading_values(r):
    try:
        vals = json.loads(r.values or '{}')
    except Exception:
        vals = {}
    out = []
    tpl = MonitorTemplate.query.get(r.template_key) if r.template_key else None
    if tpl:
        for p in tpl.params:
            v = vals.get(p.pkey)
            if v in (None, ''):
                continue
            out.append({'label': p.label, 'value': v, 'unit': p.unit or ''})
    else:
        for k, v in vals.items():
            if v in (None, ''):
                continue
            out.append({'label': k, 'value': v, 'unit': ''})
    return out


def _records_items(user, ftype='all'):
    """Κοινός builder της λίστας Records (page + εξαγωγές PDF/XLSX)."""
    hids = scoped_hotel_ids(user)
    items = []
    if ftype in ('all', 'pools'):
        pids = [p.id for p in Pool.query.all() if p.hotel_id in hids]
        _pq = apply_period(PoolRecord.query.filter(PoolRecord.pool_id.in_(pids or [-1])), PoolRecord.record_date)
        for r in (_pq.order_by(PoolRecord.recorded_at.desc()).limit(2000).all()):
            items.append({
                'kind': 'pool', 'src': 'legacy', 'id': r.id, 'when': r.recorded_at, 'date': r.record_date,
                'period': r.period,
                'hotel': r.pool.hotel.name if r.pool and r.pool.hotel else '—',
                'place': r.pool.name if r.pool else '—',
                'user': r.user.full_name if r.user else '—',
                'updated': bool(r.updated_at),
                'edit_url': '/pools/edit/%d' % r.id,
                'mvals': _vals_from_cols(r, _POOL_VAL_LABELS),
            })
    if ftype in ('all', 'water'):
        sids = [s.id for s in WaterSystem.query.all() if s.hotel_id in hids]
        _wq = apply_period(WaterRecord.query.filter(WaterRecord.water_system_id.in_(sids or [-1])), WaterRecord.record_date)
        for r in (_wq.order_by(WaterRecord.recorded_at.desc()).limit(2000).all()):
            ws = r.water_system
            items.append({
                'kind': 'water', 'src': 'legacy', 'id': r.id, 'when': r.recorded_at, 'date': r.record_date,
                'period': r.period,
                'hotel': ws.hotel.name if ws and ws.hotel else '—',
                'place': ws.name if ws else '—',
                'user': r.user.full_name if r.user else '—',
                'updated': bool(r.updated_at),
                'edit_url': '/edit/%d' % r.id,
                'mvals': _vals_from_cols(r, _WATER_VAL_LABELS),
            })
    # v12.225 (Φ3c-1) — γνήσιες καταχωρήσεις ΜΗΧΑΝΗΣ (Reading, ΟΧΙ migrated αντίγραφα):
    #   εμφανίζονται μαζί με τα legacy, ΧΩΡΙΣ διπλά (source_kind IS NULL). Κατηγοριοποίηση ανά template.
    _ZNX_TPL = ('znx', 'znx_tank', 'znx_kitchen', 'znx_remote', 'znx_dhw', 'znx_ro')
    _ft_of = {'pool': 'pools', 'water': 'water', 'area': 'area'}
    _rq = apply_period(Reading.query.filter(Reading.source_kind.is_(None)), Reading.record_date)
    for r in (_rq.order_by(Reading.recorded_at.desc()).limit(2000).all()):
        a = r.area
        if not a or a.hotel_id not in hids:
            continue
        tk = r.template_key or (a.template_key if a else '') or ''
        kind = 'pool' if tk == 'pool' else ('water' if tk in _ZNX_TPL else 'area')
        if ftype not in ('all', _ft_of[kind]):
            continue
        items.append({
            'kind': kind, 'src': 'reading', 'id': r.id, 'when': r.recorded_at, 'date': r.record_date,
            'period': r.period,
            'hotel': a.hotel.name if a and a.hotel else '—',
            'place': (a.name or '—'),
            'user': r.user.full_name if r.user else '—',
            'updated': bool(r.updated_at),
            'edit_url': None,
            'mvals': _reading_values(r),
        })
    items.sort(key=lambda x: x['when'] or datetime.min, reverse=True)
    return items

@app.route('/records')
def records_feed():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    if can_log() and role_rank(current_user().role) < ROLE_RANK['manager']:
        return redirect(url_for('measurements_today'))  # v12.83 — staff = μόνο καταγραφή
    user = current_user()
    ftype = request.args.get('type', 'all')          # all | pools | water
    items = _records_items(user, ftype)
    return render_template('records.html', items=items, ftype=ftype, user=user, is_admin=is_admin())

@app.route('/pools/record/<int:record_id>/delete', methods=['POST'])
def delete_pool_record(record_id):
    if not is_admin():
        return redirect(url_for('login'))
    r = PoolRecord.query.get(record_id)
    if r:
        nm = r.pool.name if r.pool else ''
        db.session.delete(r); db.session.commit()
        log_activity('pool_record_delete', f'{nm} #{record_id}')
    return redirect(request.referrer or (url_for('records_feed') + '?embed=1'))

@app.route('/water/record/<int:record_id>/delete', methods=['POST'])
def delete_water_record(record_id):
    if not is_admin():
        return redirect(url_for('login'))
    r = WaterRecord.query.get(record_id)
    if r:
        nm = r.water_system.name if r.water_system else ''
        db.session.delete(r); db.session.commit()
        log_activity('water_record_delete', f'{nm} #{record_id}')
    return redirect(request.referrer or (url_for('records_feed') + '?embed=1'))

@app.route('/areas/reading/<int:record_id>/delete', methods=['POST'])
def delete_area_reading(record_id):
    if not is_admin():
        return redirect(url_for('login'))
    r = Reading.query.get(record_id)
    if r:
        nm = r.area.name if r.area else ''
        db.session.delete(r); db.session.commit()
        log_activity('area_reading_delete', f'{nm} #{record_id}')
    return redirect(request.referrer or (url_for('records_feed') + '?embed=1'))

# v12.5 — Εξαγωγές Records (έτοιμες προς εκτύπωση)
@app.route('/records/export.pdf')
def records_export_pdf():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    user = current_user()
    ftype = request.args.get('type', 'all')
    items = _records_items(user, ftype)
    pdf_bytes = build_records_pdf(items, ftype)
    fname = 'estia-records-%s-%s.pdf' % (ftype, date.today().strftime('%Y-%m-%d'))
    return Response(pdf_bytes, mimetype='application/pdf',
                    headers={'Content-Disposition': 'attachment; filename=' + fname})

@app.route('/records/export.xlsx')
def records_export_xlsx():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    user = current_user()
    ftype = request.args.get('type', 'all')
    items = _records_items(user, ftype)
    xlsx_bytes = build_records_xlsx(items, ftype)
    fname = 'estia-records-%s-%s.xlsx' % (ftype, date.today().strftime('%Y-%m-%d'))
    return Response(xlsx_bytes,
                    mimetype='application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
                    headers={'Content-Disposition': 'attachment; filename=' + fname})

@app.route('/pools/dashboard')
def pools_dashboard():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    # v12.234: Πίνακας Πισινών αποσύρθηκε → ενιαία Στατιστικά μετρήσεων
    return redirect(url_for('measurements_stats') + ('?embed=1' if request.args.get('embed') else ''))
    if can_log() and role_rank(current_user().role) < ROLE_RANK['manager']:
        return redirect(url_for('measurements_today'))  # v12.83 — staff = μόνο καταγραφή
    user = current_user()
    hotels = allowed_hotels(user)
    hids = {h.id for h in hotels}
    pools = [p for p in Pool.query.filter_by(is_active=True).all() if p.hotel_id in hids]
    pids = {p.id for p in pools}
    # v12.5 — φίλτρα ανά ξενοδοχείο / πισίνα / χρήστη (mirror Πίνακα Νερών)
    f_hotel = request.args.get('hotel_id', type=int) or active_hotel_id()
    f_pool  = request.args.get('pool_id', type=int)
    f_staff = request.args.get('staff_id', type=int)
    def _scope(q):
        if f_pool:
            return q.filter(PoolRecord.pool_id == f_pool)
        if f_hotel:
            hpids = [p.id for p in pools if p.hotel_id == f_hotel]
            return q.filter(PoolRecord.pool_id.in_(hpids or [-1]))
        return q
    base = PoolRecord.query
    if role_rank(user.role) < ROLE_RANK['admin']:
        base = base.filter(PoolRecord.pool_id.in_(pids if pids else [-1]))
    q = _scope(base)
    if f_staff:
        q = q.filter(PoolRecord.user_id == f_staff)
    q = apply_period(q, PoolRecord.record_date)
    records = q.order_by(PoolRecord.record_date.desc(), PoolRecord.recorded_at.desc()).limit(80).all()
    users = User.query.filter_by(is_active=True, approved=True).all()
    today_records = (_scope(base.filter(PoolRecord.record_date == date.today()))
                     .order_by(PoolRecord.pool_id).all())
    today_m = [r for r in today_records if r.period == 'morning']
    today_a = [r for r in today_records if r.period == 'afternoon']
    return render_template('pools_dashboard.html', hotels=hotels, pools=pools,
                           records=records, limits=POOL_LIMITS, users=users,
                           today_morning_list=today_m, today_afternoon_list=today_a,
                           f_hotel=f_hotel, f_pool=f_pool, f_staff=f_staff,
                           is_admin=is_admin(), user=user)

@app.route('/dashboard/add-user', methods=['POST'])
def add_user():
    if not is_admin():
        return redirect(url_for('login'))
    data = request.form
    uname = (data.get('username') or '').strip()
    if not uname or not (data.get('password') or ''):
        return redirect(url_for('users_admin') + '?error=user_missing')
    if User.query.filter_by(username=uname).first():
        return redirect(url_for('users_admin') + '?error=user_exists')
    db.session.add(User(
        username=uname,
        password=generate_password_hash(data['password']),
        full_name=(data.get('full_name') or '').strip(),
        email=data.get('email', '').strip(),
        phone=data.get('phone', '').strip(),
        role=data.get('role', 'staff'),
        language=data.get('language', 'el'),
        approved=True,         # v12.123: λογαριασμός από admin = εγκεκριμένος
        login_enabled=True     # v12.123: πραγματικός λογαριασμός (εμφανίζεται & κάνει login)
    ))
    db.session.commit()
    log_activity('user_add', uname)
    return redirect(url_for('users_admin') + '?success=user_added')

@app.route('/dashboard/delete-user/<int:user_id>', methods=['POST'])
def delete_user(user_id):
    if not is_admin():
        return redirect(url_for('login'))
    user = User.query.get(user_id)
    if user and role_rank(user.role) < ROLE_RANK['admin']:
        uname = user.username
        ok = False
        try:                       # v12.124: ΟΡΙΣΤΙΚΗ διαγραφή (full cascade, προστασία κλειδωμένων)
            from payroll import _hard_delete_user as _hd
            ok, _r = _hd(user_id)
        except Exception:
            db.session.rollback(); ok = False
        if not ok:                 # fallback: τουλάχιστον απενεργοποίηση
            u2 = User.query.get(user_id)
            if u2:
                u2.is_active = False; db.session.commit()
        log_activity('user_delete', uname + (' [οριστική]' if ok else ' [απενεργ.]'))
    return redirect(url_for('users_admin'))

@app.route('/dashboard/approve-user/<int:user_id>', methods=['POST'])
def approve_user(user_id):
    if not is_admin():
        return redirect(url_for('login'))
    u = User.query.get(user_id)
    if u:
        u.approved = True; u.is_active = True
        db.session.commit()
        log_activity('user_approve', u.username)
        notify(u.id, 'Ο λογαριασμός σου εγκρίθηκε. Καλώς ήρθες!', '/pools')
    return redirect(url_for('users_admin') + '?success=user_approved')

def _protected(actor, target):
    # δεν επιτρέπεται σε χαμηλότερο ρόλο να πειράξει masteradmin
    return role_rank(target.role) >= ROLE_RANK['masteradmin'] and role_rank(actor.role) < ROLE_RANK['masteradmin']

@app.route('/dashboard/edit-user/<int:user_id>', methods=['POST'])
def edit_user(user_id):
    if not is_admin():
        return redirect(url_for('login'))
    actor = current_user(); u = User.query.get(user_id)
    if not u or _protected(actor, u):
        return redirect(url_for('users_admin'))
    fm = request.form
    u.full_name = (fm.get('full_name') or u.full_name).strip() or u.full_name
    u.email = fm.get('email', '').strip()
    u.phone = fm.get('phone', '').strip()
    # v12.12 — πλήρες edit: username (με έλεγχο μοναδικότητας) + γλώσσα
    new_un = (fm.get('username') or '').strip()
    if new_un and new_un != u.username and not User.query.filter_by(username=new_un).first():
        u.username = new_un
    if fm.get('language') in ('el', 'en', 'uk'):
        u.language = fm['language']
    new_role = fm.get('role', u.role)
    if new_role in ROLE_RANK and role_rank(new_role) <= role_rank(actor.role):
        u.role = new_role
    hids = [int(x) for x in fm.getlist('hotel_ids') if x.isdigit()]
    u.hotels = Hotel.query.filter(Hotel.id.in_(hids)).all() if hids else []
    db.session.commit()
    log_activity('user_edit', u.username)
    return redirect(url_for('users_admin') + '?success=user_edited')

@app.route('/dashboard/reset-password/<int:user_id>', methods=['POST'])
def reset_password(user_id):
    if not is_admin():
        return redirect(url_for('login'))
    actor = current_user(); u = User.query.get(user_id)
    if u and not _protected(actor, u):
        newpw = request.form.get('password', '').strip()
        if newpw:
            u.password = generate_password_hash(newpw); db.session.commit()
            log_activity('user_reset_password', u.username)
    return redirect(url_for('users_admin') + '?success=password_reset')

@app.route('/dashboard/toggle-user/<int:user_id>', methods=['POST'])
def toggle_user(user_id):
    if not is_admin():
        return redirect(url_for('login'))
    actor = current_user(); u = User.query.get(user_id)
    if u and u.id != actor.id and not _protected(actor, u):
        u.is_active = not u.is_active; db.session.commit()
        log_activity('user_toggle', u.username + (' -> active' if u.is_active else ' -> inactive'))
    return redirect(url_for('users_admin'))

# ── Εβδομαδιαία κάλυψη ──
@app.route('/pools/coverage')
def pools_coverage():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    # v12.237: «Εβδομαδιαία κάλυψη» αποσύρθηκε → ενότητα «Κάλυψη» στα Στατιστικά
    return redirect(url_for('measurements_stats') + ('?embed=1' if request.args.get('embed') else ''))
    if can_log() and role_rank(current_user().role) < ROLE_RANK['manager']:
        return redirect(url_for('measurements_today'))  # v12.83 — staff = μόνο καταγραφή
    user = current_user()
    hids = {h.id for h in allowed_hotels(user)}
    pools = [p for p in Pool.query.filter_by(is_active=True).order_by(Pool.hotel_id, Pool.name).all() if p.hotel_id in hids]
    days = [date.today() - timedelta(days=i) for i in range(6, -1, -1)]
    recs = PoolRecord.query.filter(
        PoolRecord.pool_id.in_([p.id for p in pools] or [-1]),
        PoolRecord.record_date.in_(days)).all()
    cov = {}
    for r in recs:
        cov.setdefault((r.pool_id, r.record_date), set()).add(r.period)
    grid = []
    for p in pools:
        cells = []
        for d in days:
            sset = cov.get((p.id, d), set())
            cells.append({'m': 'morning' in sset, 'a': 'afternoon' in sset})
        grid.append({'pool': p, 'cells': cells})
    return render_template('coverage.html', grid=grid, days=days)

# ── Αναφορά βλάβης: μεταφέρθηκε στο faults.py (Module Βλαβοληψία v12.14) ──

# ── In-app ειδοποιήσεις ──
@app.route('/notifications')
def notifications():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    items = Notification.query.filter_by(user_id=session['user_id']).order_by(Notification.id.desc()).limit(100).all()
    return render_template('notifications.html', items=items)

def _wants_json():
    return (request.is_json or request.args.get('ajax')
            or request.headers.get('X-Requested-With') == 'XMLHttpRequest'
            or 'application/json' in (request.headers.get('Accept') or ''))

@app.route('/notifications/read', methods=['POST'])
def notifications_read():
    if 'user_id' not in session:
        return ('', 401) if _wants_json() else redirect(url_for('login'))
    Notification.query.filter_by(user_id=session['user_id'], is_read=False).update({'is_read': True})
    db.session.commit()
    return ('', 204) if _wants_json() else redirect(url_for('notifications'))

@app.route('/notifications/read/<int:nid>', methods=['POST'])
def notifications_read_one(nid):
    if 'user_id' not in session:
        return ('', 401)
    n = Notification.query.filter_by(id=nid, user_id=session['user_id']).first()
    if n and not n.is_read:
        n.is_read = True
        db.session.commit()
    return ('', 204)

@app.route('/notifications/list')
def notifications_list():
    if 'user_id' not in session:
        return jsonify({'items': [], 'unread': 0}), 401
    items = Notification.query.filter_by(user_id=session['user_id']).order_by(Notification.id.desc()).limit(20).all()
    unread = Notification.query.filter_by(user_id=session['user_id'], is_read=False).count()
    def _ago(dt):
        if not dt: return ''
        sec = (datetime.now() - dt).total_seconds()
        if sec < 60: return 'μόλις τώρα'
        if sec < 3600: return f'{int(sec//60)}\' πριν'
        if sec < 86400: return f'{int(sec//3600)}ω πριν'
        if sec < 604800: return f'{int(sec//86400)}η πριν'
        return dt.strftime('%d/%m %H:%M')
    return jsonify({'unread': unread, 'items': [
        {'id': n.id, 'text': n.text or '', 'link': (n.link or ''),
         'is_read': bool(n.is_read), 'ago': _ago(n.created_at)} for n in items]})

# ── v12.36 — «Τι νέο» (ιστορικό εκδόσεων, admin) ─────────────────────────────
@app.route('/dashboard/whatsnew')
def whatsnew():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    return render_template('whatsnew.html', changelog=CHANGELOG,
                           cur_version=APP_VERSION, cur_build=APP_BUILD)

# ── v12.45 — ROADMAP ολόκληρης της Εστίας (status: done/progress/planned/idea) ──
ROADMAP = [
    {'area': 'Συντήρηση', 'items': [
        {'t': 'Module Βλάβες (workflow, SLA, αυτόματη ανάθεση, ιστορικό)', 's': 'done'},
        {'t': 'Πισίνες & Νερά Χρήσης — μετρήσεις, όρια, ενέργειες', 's': 'done'},
        {'t': 'Μητρώο Εξοπλισμού (assets, κόστος, QR) — δένει με Βλάβες', 's': 'planned'},
        {'t': 'Προληπτική συντήρηση / προγραμματισμένες εργασίες', 's': 'idea'},
    ]},
    {'area': 'Προσωπικό / HR', 'items': [
        {'t': 'Πρόγραμμα Εργασίας — βάρδιες, κανόνες, υποβολή λογιστηρίου', 's': 'done'},
        {'t': 'Οργανόγραμμα (εργαζόμενοι σε τμήματα) + εκκαθάριση διπλών', 's': 'done'},
        {'t': 'Multi-file importer ιστορικών (παλιά έτη/προσωπικό)', 's': 'progress'},
        {'t': 'Μισθοδοσία — θεμέλιο: μητρώο/εταιρείες/PII/συντελεστές (Φ1)', 's': 'progress'},
        {'t': 'Μισθοδοσία — μηχανή Management+Λογιστήριο + import Epsilon (Φ2)', 's': 'progress'},
        {'t': 'Μισθοδοσία — εκκαθαριστικά PDF, reporting, πρόβλεψη (Φ3+)', 's': 'planned'},
        {'t': 'Αξιολόγηση προσωπικού (πάνω στα ερωτηματολόγια)', 's': 'planned'},
        {'t': 'Αξιολόγηση: διπλή αξιολόγηση για εργαζόμενο σε >1 τμήμα ανά περίοδο (π.χ. Φεβ-Μαρ Συντήρηση, Απρ-Νοε Κουζίνα)', 's': 'planned'},
        {'t': 'Αξιολόγηση: τράπεζα ερωτήσεων + σύνθεση ερωτηματολογίου με drag&drop (βάρος ανά φύλλο)', 's': 'idea'},
        {'t': 'Οργανόγραμμα — κονσόλα HR: …, διαχείριση τμημάτων (✓), επιλεγμένα=μοναδικό φίλτρο (✓), νέα διάταξη όλα-σε-πλέγμα #5 (✓)· επόμενα: ΣΥΖΗΤΗΣΗ #3 τυπικό οργανόγραμμα/#6 παρελθόν/#8 HQ, δικαιώματα υπευθύνου, διπλό ιστορικό, cross-hotel κλείδωμα (E3)', 's': 'progress'},
    ]},
    {'area': 'Guest experience / Υποδοχή', 'items': [
        {'t': 'Ερωτηματολόγια (builder, δημόσιος σύνδεσμος, NPS)', 's': 'done'},
        {'t': 'Logbook (Παράπονα/Συμβάντα) — 1.762 ιστορικά', 's': 'progress'},
        {'t': 'Guest App (αιτήματα δωματίου)', 's': 'idea'},
    ]},
    {'area': 'Πλατφόρμα', 'items': [
        {'t': 'Ρόλοι/δικαιώματα, μενού ανά ρόλο, multi-hotel scope', 's': 'done'},
        {'t': 'Dashboard με widgets, dark mode, παλέτες', 's': 'done'},
        {'t': 'Backups → SharePoint, Κέντρο Εισαγωγής, Feedback χρηστών', 's': 'done'},
        {'t': 'Αυτόματο backup πριν από κάθε καθαρισμό/μαζική διαγραφή', 's': 'planned'},
        {'t': 'Αρχειοθέτηση/απενεργοποίηση όποιου στοιχείου δεν χρησιμοποιείται', 's': 'planned'},
        {'t': 'Συμμάζεμα οθονών προφίλ → 2 οθόνες (Επίσημο Μητρώο 🔒 + Διαχείριση Προσωπικού)', 's': 'planned'},
        {'t': 'In-app Documentation (/dashboard/docs) — όλες οι λειτουργίες, auto-updated ανά version', 's': 'planned'},
        {'t': 'Ξεκαθάρισμα «προσωπικό vs χρήστες login» (αν το προσωπικό πρέπει να είναι users ή merge με master)', 's': 'idea'},
        {'t': 'Μεταφράσεις EN/UK', 's': 'planned'},
        {'t': 'Σύνδεση μισθοδοσίας με ΕΡΓΑΝΗ/Epsilon (μέσω προγράμματος)', 's': 'idea'},
    ]},
    {'area': 'Διασφαλίσεις & Διακυβέρνηση (καθώς μεγαλώνει)', 'items': [
        {'t': 'Διαδικασία: Impact-note + εντοπισμός conflicts ΠΡΙΝ από κάθε δομική/κοινή αλλαγή — προχωράμε μόνο με ρητή έγκριση', 's': 'progress'},
        {'t': 'ΜΙΑ πηγή αλήθειας: People=User (μητρώο), τα πάντα linkάρουν με user_id· καμία παράλληλη βάση· Χάρτης Κυριότητας Δεδομένων', 's': 'progress'},
        {'t': 'ΙΣΧΥΕΙ ΣΕ ΟΛΑ ΤΑ MODULES (Βλάβες/Πελάτες/κ.λπ.): owner-screen ανά οντότητα (edit από ΕΝΑ σημείο, αλλού read/βοηθός με ΙΔΙΟ endpoint)· διπλή επιβεβαίωση για δομικές αλλαγές', 's': 'progress'},
        {'t': 'Συνέπεια: ένα state-machine ανά record, κανόνες διαγραφής/εξάρτησης, ένας generator κωδικών/domain, permissions matrix σε ένα σημείο', 's': 'planned'},
        {'t': 'Backups: τεστ ΠΡΑΓΜΑΤΙΚΗΣ επαναφοράς + backup πριν από migrations/μαζικές διαγραφές', 's': 'planned'},
        {'t': 'Migrations: τεστ σε αντίγραφο προϋπάρχουσας βάσης πριν deploy (αργότερα: Alembic)', 's': 'planned'},
        {'t': 'Απόδοση: indexes + pagination + eager loading στις βαριές λίστες', 's': 'planned'},
        {'t': 'Ασφάλεια: CSRF tokens + συνεπείς έλεγχοι δικαιωμάτων σε κάθε route', 's': 'planned'},
        {'t': 'Αυτόματα smoke-tests (fresh + προϋπάρχουσα βάση + κύρια routes) πριν από κάθε έκδοση', 's': 'planned'},
        {'t': 'Εικόνες εκτός DB (object storage) όταν μεγαλώσει ο όγκος — η DB να μη φουσκώνει', 's': 'idea'},
    ]},
]
ROADMAP_STATUS = {
    'done':     ('✓ Ολοκληρώθηκε', '#dcfce7', '#166534'),
    'progress': ('● Σε εξέλιξη',    '#dbeafe', '#185FA5'),
    'planned':  ('◷ Σχεδιασμένο',   '#fef3c7', '#92400e'),
    'idea':     ('💡 Ιδέα',         '#f1f5f9', '#64748b'),
}

@app.route('/dashboard/roadmap')
def roadmap_page():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    return render_template('roadmap.html', roadmap=ROADMAP, status=ROADMAP_STATUS,
                           recent=CHANGELOG[:12], cur_version=APP_VERSION, cur_build=APP_BUILD)

# ── v12.46 — Βοήθεια: FAQ + Επίλυση προβλημάτων (admin items κρυφά από non-admin) ──
FAQ_ITEMS = [
    {'c': 'Γενικά', 'q': 'Πώς αλλάζω σε σκούρο θέμα ή χρώματα;',
     'a': 'Πάνω δεξιά στο εικονίδιο προφίλ → «Θέμα» (Φωτεινό/Σκούρο/Σύστημα) και «Παλέτα» για χρώματα.'},
    {'c': 'Γενικά', 'q': 'Βλέπω μόνο το ξενοδοχείο μου — γιατί;',
     'a': 'Ο λογαριασμός σου είναι συνδεδεμένος με συγκεκριμένο ξενοδοχείο. Αν έχεις πρόσβαση σε πολλά, χρησιμοποίησε τον εναλλάκτη ξενοδοχείου πάνω-πάνω.'},
    {'c': 'Γενικά', 'q': 'Πού βλέπω τι άλλαξε ή τι έρχεται;',
     'a': 'Ενημέρωση → «Τι νέο» (αλλαγές ανά έκδοση) και «Roadmap» (η πορεία της πλατφόρμας).'},
    {'c': 'Γενικά', 'q': 'Πώς στέλνω σχόλιο, ιδέα ή πρόβλημα;',
     'a': 'Ενημέρωση → «Στείλε feedback». Φτάνει αμέσως στη διοίκηση και μας βοηθάει να προτεραιοποιήσουμε.'},
    {'c': 'Πρόγραμμα Εργασίας', 'q': 'Πώς καταχωρώ βάρδια;',
     'a': 'Προσωπικό → «Πρόγραμμα Εργασίας» → διάλεξε ξενοδοχείο/εβδομάδα → κλικ σε ένα κελί → διάλεξε κωδικό. Για «ΕΡΓ» βάζεις και ώρες (π.χ. 07:00–15:30).'},
    {'c': 'Πρόγραμμα Εργασίας', 'q': 'Τι σημαίνουν οι κωδικοί;',
     'a': 'ΕΡΓ=Εργασία (με ώρες), ΑΝ=Ρεπό, ΑΔ=Κανονική άδεια, ΑΣΘ=Αναρρωτική, ΑΠ=Αδικαιολόγητη απουσία, ΑΡΓ=Αργία, ΑΝΕΥ=Άνευ αποδοχών, Ειδ.Α=Ειδική άδεια. Υπάρχει υπόμνημα στο κάτω μέρος της σελίδας.'},
    {'c': 'Πρόγραμμα Εργασίας', 'q': 'Πώς δουλεύω σε πολλές εβδομάδες ή τμήματα μαζί;',
     'a': 'Με τον επιλογέα «Εβδομάδες» βλέπεις όσες εβδομάδες θες μπροστά. Με τα chips τμημάτων (π.χ. Housekeeping, Ρεσεψιόν) δείχνεις/κρύβεις τμήματα δυναμικά.'},
    {'c': 'Πρόγραμμα Εργασίας', 'q': 'Πώς στέλνω το πρόγραμμα στο λογιστήριο;',
     'a': 'Στην εβδομάδα που θες, κουμπί «Αποστολή στο λογιστήριο». Στέλνει όλα τα τμήματα του ξενοδοχείου μαζί. Πρέπει πρώτα να περνούν οι κανόνες (π.χ. κάθε εργαζόμενος ≥1 ρεπό/εβδομάδα).'},
    {'c': 'Πρόγραμμα Εργασίας', 'q': 'Γιατί δεν με αφήνει να αλλάξω την τρέχουσα εβδομάδα;',
     'a': 'Η εβδομάδα «κλειδώνει» στην προθεσμία (προεπιλογή: Πέμπτη της προηγούμενης εβδομάδας). Δουλεύεις τις επόμενες εβδομάδες· για έκτακτη αλλαγή χρειάζεται ο admin.'},
    {'c': 'Λογαριασμοί & Πρόσβαση', 'q': 'Δεν βλέπω κάποια επιλογή στο μενού.',
     'a': 'Κάθε ρόλος βλέπει διαφορετικά πράγματα. Ζήτα από τον διαχειριστή να σου το ενεργοποιήσει (Διαχείριση → «Μενού ανά ρόλο»).', 'admin': False},
    {'c': 'Εισαγωγές (Διαχείριση)', 'q': 'Πώς βάζω μαζικά εργαζόμενους & προγράμματα;', 'admin': True,
     'a': 'Διαχείριση → «Πρόγραμμα · Εισαγωγή». Ανέβασε πολλά .xlsx μαζί — η πλατφόρμα αναγνωρίζει αυτόματα μητρώο μισθοδοσίας ή πρόγραμμα. Πρόταση: πρώτα τα μητρώα (καθαρή λίστα), μετά τα προγράμματα (βάρδιες).'},
    {'c': 'Εισαγωγές (Διαχείριση)', 'q': 'Πώς ρυθμίζω προθεσμίες/κανόνες/αργίες;', 'admin': True,
     'a': 'Διαχείριση → «Πρόγραμμα · Ρυθμίσεις»: ημέρα/ώρα προθεσμίας, ορίζοντα εβδομάδων, αργίες, κανόνες υποβολής, κωδικοί βαρδιών.'},
]
TROUBLE_ITEMS = [
    {'p': 'Δεν μπορώ να συνδεθώ.',
     's': 'Έλεγξε username/κωδικό. Αν είσαι «εισαγμένο προσωπικό» δεν έχεις δικαίωμα σύνδεσης — ζήτα από τον διαχειριστή να ενεργοποιήσει τον λογαριασμό και να σου δώσει κωδικό.'},
    {'p': 'Η βάρδια δεν αποθηκεύεται / βγάζει «Κλειδωμένη εβδομάδα».',
     's': 'Πέρασε η προθεσμία αυτής της εβδομάδας. Δούλεψε σε επόμενη εβδομάδα, ή ζήτα από τον admin να ξεκλειδώσει.'},
    {'p': 'Η «Αποστολή στο λογιστήριο» μπλοκάρεται.',
     's': 'Κάποιος κανόνας δεν περνά (π.χ. εργαζόμενος χωρίς ρεπό). Δες τη λίστα ελέγχων κάτω από το πρόγραμμα, διόρθωσε και ξαναπροσπάθησε.'},
    {'p': 'Βλέπω διπλά ονόματα μετά από εισαγωγή.', 'admin': True,
     's': 'Διαχείριση → «Πρόγραμμα · Προσωπικό» → ενότητα «Πιθανές διπλοεγγραφές»: διάλεξε ποιον κρατάς και πάτησε «Συγχώνευση».'},
    {'p': 'Εργαζόμενος σε λάθος τμήμα ή ξενοδοχείο.', 'admin': True,
     's': 'Διαχείριση → «Πρόγραμμα · Προσωπικό»: άλλαξε τμήμα/ξενοδοχείο και «Αποθήκευση».'},
    {'p': 'Ανέβασα λάθος/«βρόμικο» αρχείο.', 'admin': True,
     's': 'Διαχείριση → «Πρόγραμμα · Εισαγωγή» → «Καθαρισμός εισαγμένου προσωπικού» (σβήνει μόνο εισαγμένους, όχι πραγματικούς λογαριασμούς). Μετά ξανακάνε εισαγωγή από καθαρή πηγή.'},
    {'p': 'Το αρχείο βγήκε «Άγνωστο» στην εισαγωγή.', 'admin': True,
     's': 'Δεν είχε αναγνωρίσιμη δομή. Χρειάζεται είτε γραμμή «ΤΜΗΜΑ» με στήλες ημερομηνιών (πρόγραμμα), είτε κεφαλίδα μητρώου με «Τμήμα»+«Επώνυμο/Ονοματεπώνυμο».'},
    {'p': 'Δεν λαμβάνω ειδοποιήσεις email.', 'admin': True,
     's': 'Διαχείριση → «Email»: έλεγξε τη ρύθμιση αποστολής. Επίσης βεβαιώσου ότι ο λογαριασμός έχει σωστό email.'},
    {'p': 'Η σελίδα δείχνει παλιά δεδομένα.',
     's': 'Πάτησε ανανέωση (refresh) στον browser. Αν συνεχίζει, αποσυνδέσου και ξανασυνδέσου.'},
]

@app.route('/dashboard/help')
def help_page():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    adm = is_admin()
    faqs = [x for x in FAQ_ITEMS if adm or not x.get('admin')]
    trbl = [x for x in TROUBLE_ITEMS if adm or not x.get('admin')]
    cats = []
    for x in faqs:
        if x['c'] not in cats:
            cats.append(x['c'])
    return render_template('help.html', faqs=faqs, trbl=trbl, cats=cats, is_admin=adm)

# ── Inbox/διαχείριση βλαβών: μεταφέρθηκε στο faults.py (Module Βλαβοληψία v12.14) ──

# ── Email admin (Graph/SMTP, test, manual, log) ──
@app.route('/dashboard/email')
def email_admin():
    if not is_admin():
        return redirect(url_for('login'))
    logs = EmailLog.query.order_by(EmailLog.id.desc()).limit(50).all()
    mode = 'Microsoft Graph (365)' if GRAPH_CLIENT_ID else ('SMTP (' + SMTP_SERVER + ')' if EMAIL_PASSWORD else 'Δεν έχει ρυθμιστεί')
    return render_template('email_admin.html', logs=logs, mode=mode)

@app.route('/dashboard/email/test', methods=['POST'])
def email_test():
    if not is_admin():
        return redirect(url_for('login'))
    to = request.form.get('to', '').strip()
    recips = [to] if to else EMAIL_TO_LIST
    send_email('Εστία — Δοκιμαστικό email', '<p>Δοκιμαστικό email από την Εστία (CONDIAN HOTELS). Αν το βλέπεις, η αποστολή λειτουργεί.</p>', recips)
    log_activity('email_test', ', '.join(recips))
    return redirect(url_for('email_admin'))

@app.route('/dashboard/email/reminder', methods=['POST'])
def email_reminder_now():
    if not is_admin():
        return redirect(url_for('login'))
    miss = missing_today()
    if miss:
        send_reminder_email(miss)
    log_activity('email_reminder_manual', str(len(miss)))
    return redirect(url_for('email_admin'))

# ── Theming (admin) ──
@app.route('/dashboard/theme', methods=['GET', 'POST'])
def theme_admin():
    if not is_admin():
        return redirect(url_for('login'))
    import re as _re
    if request.method == 'POST':
        def setk(k, v):
            row = Setting.query.get('theme_' + k)
            if row:
                row.value = v
            else:
                db.session.add(Setting(key='theme_' + k, value=v))
        for k in ['primary', 'accent']:
            v = request.form.get(k, '')
            if _re.match(r'^#[0-9a-fA-F]{6}$', v):
                setk(k, v)
        setk('app_title', (request.form.get('app_title', 'Εστία') or 'Εστία')[:60])
        if request.form.get('reset_logo'):
            setk('logo', '')
        else:
            logo = request.files.get('logo')
            if logo and logo.filename and logo.mimetype and logo.mimetype.startswith('image/'):
                raw = logo.read()
                if 0 < len(raw) <= 400 * 1024:
                    import base64
                    setk('logo', 'data:' + logo.mimetype + ';base64,' + base64.b64encode(raw).decode())
        # v12.11 — εξατομίκευση login/register + social
        lsz = (request.form.get('login_logo_size', '88') or '88').strip()
        setk('login_logo_size', lsz if (lsz.isdigit() and 24 <= int(lsz) <= 200) else '88')
        lf = request.form.get('login_font', 'Space Grotesk')
        if lf in LOGIN_FONTS:
            setk('login_font', lf)
        setk('login_tagline', (request.form.get('login_tagline', '') or '')[:120])
        setk('social_facebook', (request.form.get('social_facebook', '') or '').strip()[:200])
        setk('social_linkedin', (request.form.get('social_linkedin', '') or '').strip()[:200])
        if request.form.get('reset_login_logo'):
            setk('login_logo', '')
        else:
            llogo = request.files.get('login_logo')
            if llogo and llogo.filename and llogo.mimetype and llogo.mimetype.startswith('image/'):
                lraw = llogo.read()
                if 0 < len(lraw) <= 400 * 1024:
                    import base64
                    setk('login_logo', 'data:' + llogo.mimetype + ';base64,' + base64.b64encode(lraw).decode())
        db.session.commit(); log_activity('theme_update')
        return redirect(url_for('theme_admin') + '?saved=1')
    return render_template('theme_admin.html', theme=get_theme(), login_fonts=LOGIN_FONTS)

# ── AI σύνδεση (masteradmin) ──
@app.route('/dashboard/ai', methods=['GET', 'POST'])
def ai_admin():
    u = current_user()
    if u is None or u.role != 'masteradmin':
        return redirect(url_for('login'))
    def setk(k, v):
        row = Setting.query.get('ai_' + k)
        if row:
            row.value = v
        else:
            db.session.add(Setting(key='ai_' + k, value=v))
    if request.method == 'POST':
        setk('provider', request.form.get('provider', 'auto'))
        setk('model', request.form.get('model', '').strip())
        if request.form.get('clear_keys'):
            setk('anthropic', ''); setk('openai', '')
        else:
            ak = request.form.get('anthropic', '').strip()
            if ak and not ak.startswith('*'):
                setk('anthropic', ak)
            ok = request.form.get('openai', '').strip()
            if ok and not ok.startswith('*'):
                setk('openai', ok)
        db.session.commit(); log_activity('ai_config')
        return redirect(url_for('ai_admin') + '?saved=1')
    c = get_ai_config()
    masked = {'anthropic': ('*' * 8 + c['anthropic'][-4:]) if c['anthropic'] else '',
              'openai': ('*' * 8 + c['openai'][-4:]) if c['openai'] else ''}
    return render_template('ai_admin.html', cfg=c, masked=masked, configured=(resolve_provider() is not None))

# ── GENERIC AREAS (Πλατφόρμα Συντήρησης) ──
def can_access_area(u, area):
    if u is None or area is None:
        return False
    if role_rank(u.role) >= ROLE_RANK['admin']:
        return True
    assigned = {h.id for h in u.hotels if h.is_active}
    if not assigned:
        return True
    return area.hotel_id in assigned

def _templates_json():
    out = {}
    for t in MonitorTemplate.query.filter_by(is_active=True).order_by(MonitorTemplate.sort).all():
        out[t.key] = {'name': t.name, 'icon': t.icon, 'frequency': t.frequency,
                      'params': [{'pkey': p.pkey, 'label': p.label, 'unit': p.unit,
                                  'min': p.min_v, 'max': p.max_v, 'periodic': p.periodic} for p in t.params]}
    return out

def reading_cells(r):
    try:
        vals = json.loads(r.values or '{}')
    except Exception:
        vals = {}
    tpl = MonitorTemplate.query.get(r.template_key); cells = []
    if tpl:
        for p in tpl.params:
            v = vals.get(p.pkey); ok = True
            if v is not None:
                try:
                    fv = float(v); ok = (p.min_v is None or fv >= p.min_v) and (p.max_v is None or fv <= p.max_v)
                except Exception:
                    pass
            cells.append({'label': p.label, 'value': v, 'unit': p.unit, 'ok': ok})
    return cells

@app.route('/areas')
def areas_app():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    if not can_log():
        return redirect(url_for('areas_dashboard'))
    user = current_user()
    hids = {h.id for h in allowed_hotels(user)}
    areas = [a for a in Area.query.filter_by(is_active=True).order_by(Area.template_key, Area.name).all() if a.hotel_id in hids and not a.engine_only]
    areas_json = [{'id': a.id, 'name': a.name, 'location': a.location or '',
                   'hotel': a.hotel.name if a.hotel else '', 'template': a.template_key} for a in areas]
    todays = Reading.query.filter_by(record_date=date.today()).all()
    done = {}
    for r in todays:
        done.setdefault(str(r.area_id), []).append(r.period)
    return render_template('areas.html', user=user, areas_json=areas_json,
                           templates=_templates_json(), done=done, freq_periods=FREQ_PERIODS)

@app.route('/areas/submit', methods=['POST'])
def areas_submit():
    if 'user_id' not in session or not can_log():
        return jsonify({'success': False, 'message': 'Μη εξουσιοδοτημενο'}), 401
    user = current_user(); data = request.form
    area = Area.query.filter_by(id=data.get('area_id'), is_active=True).first() if data.get('area_id') else None
    if not area:
        return jsonify({'success': False, 'message': 'Επιλεξτε τομεα'}), 400
    if not can_access_area(user, area):
        return jsonify({'success': False, 'message': 'Δεν εχεις προσβαση'}), 403
    period = data.get('period', 'day')
    tpl = MonitorTemplate.query.get(area.template_key)
    vals = {}
    for p in (tpl.params if tpl else []):
        v = data.get(p.pkey, '')
        if v not in (None, ''):
            try:
                vals[p.pkey] = float(v)
            except (ValueError, TypeError):
                pass
    rec = Reading.query.filter_by(area_id=area.id, record_date=date.today(), period=period).first()
    if rec:
        rec.values = json.dumps(vals); rec.notes = data.get('notes', '')
        rec.updated_at = datetime.now(); rec.updated_by = user.id
    else:
        rec = Reading(area_id=area.id, template_key=area.template_key, user_id=user.id,
                      record_date=date.today(), period=period, values=json.dumps(vals), notes=data.get('notes', ''))
        db.session.add(rec)
    db.session.commit()
    log_activity('area_submit', area.name, hotel_id=area.hotel_id)
    acts = area_actions(rec)
    if acts:
        notify_admins('Εκτός ορίων: ' + area.name + ' (' + str(len(acts)) + ')', '/areas/dashboard')
    return jsonify({'success': True, 'message': 'Καταγραφη ' + area.name + ' αποθηκευτηκε!'})

@app.route('/areas/dashboard')
def areas_dashboard():
    if 'user_id' not in session:
        return redirect(url_for('login'))
    user = current_user()
    hids = scoped_hotel_ids(user)
    areas = [a for a in Area.query.filter_by(is_active=True).all() if a.hotel_id in hids and not a.engine_only]
    aids = {a.id for a in areas}
    q = Reading.query
    if role_rank(user.role) < ROLE_RANK['admin']:
        q = q.filter(Reading.area_id.in_(aids if aids else [-1]))
    recs = q.order_by(Reading.record_date.desc(), Reading.recorded_at.desc()).limit(120).all()
    rows = [(r, reading_cells(r)) for r in recs]
    return render_template('areas_dashboard.html', areas=areas, rows=rows,
                           is_admin=is_admin(), templates=MonitorTemplate.query.order_by(MonitorTemplate.sort).all())

# ── v12.34 — Προσωπικό dashboard με tiles (drag&drop, ανά χρήστη) ─────────────
# Κατάλογος διαθέσιμων tiles. kind: 'kpi'|'widget'. scope: 'all'=όλοι · 'admin'=admin/master.
# v12.37 — κατηγορίες (ομάδες) + γεωμετρία πλέγματος (GridStack 12 στηλών)
CAT_LABELS = {'quality':'Ποιότητα','faults':'Βλάβες','records':'Καταγραφές','org':'Οργανισμός','people':'Χρήστες','pinned':'Καρφιτσωμένα'}
CATEGORIES = [('quality','Ποιότητα','ti-checkup-list'),('faults','Βλάβες','ti-tool'),
              ('records','Καταγραφές','ti-clipboard-check'),('org','Οργανισμός','ti-building'),
              ('people','Χρήστες','ti-users')]
TILE_CATALOG = [
    {'id':'compliance',   'title':'Συμμόρφωση σήμερα',          'icon':'ti-checkup-list',   'kind':'kpi',    'scope':'all',  'cat':'quality','w':3,'h':2,'minw':2,'minh':2},
    {'id':'alerts',       'title':'Εκτός ορίων',                'icon':'ti-alert-triangle', 'kind':'kpi',    'scope':'all',  'cat':'quality','w':3,'h':2,'minw':2,'minh':2},
    {'id':'open_faults',  'title':'Ανοιχτές βλάβες',            'icon':'ti-tool',           'kind':'kpi',    'scope':'all',  'cat':'faults', 'w':3,'h':2,'minw':2,'minh':2},
    {'id':'my_faults',    'title':'Δικές μου βλάβες',           'icon':'ti-user-cog',       'kind':'kpi',    'scope':'all',  'cat':'faults', 'w':3,'h':2,'minw':2,'minh':2},
    {'id':'pool_today',   'title':'Μετρήσεις πισινών σήμερα',   'icon':'ti-pool',           'kind':'kpi',    'scope':'all',  'cat':'records','w':3,'h':2,'minw':2,'minh':2},
    {'id':'water_today',  'title':'Μετρήσεις νερών σήμερα',     'icon':'ti-droplet',        'kind':'kpi',    'scope':'all',  'cat':'records','w':3,'h':2,'minw':2,'minh':2},
    {'id':'surveys_today','title':'Απαντήσεις ερωτημ. σήμερα',  'icon':'ti-clipboard-check','kind':'kpi',    'scope':'all',  'cat':'records','w':3,'h':2,'minw':2,'minh':2},
    {'id':'hotels',       'title':'Ξενοδοχεία',                 'icon':'ti-building',       'kind':'kpi',    'scope':'all',  'cat':'org',    'w':3,'h':2,'minw':2,'minh':2},
    {'id':'checkpoints',  'title':'Σημεία ελέγχου',             'icon':'ti-map-pin',        'kind':'kpi',    'scope':'all',  'cat':'org',    'w':3,'h':2,'minw':2,'minh':2},
    {'id':'users',        'title':'Χρήστες',                    'icon':'ti-users',          'kind':'kpi',    'scope':'admin','cat':'people', 'w':3,'h':2,'minw':2,'minh':2},
    {'id':'pending',      'title':'Εκκρεμείς εγγραφές',         'icon':'ti-user-plus',      'kind':'kpi',    'scope':'admin','cat':'people', 'w':3,'h':2,'minw':2,'minh':2},
    {'id':'coverage',     'title':'Κάλυψη ανά ξενοδοχείο',      'icon':'ti-checkup-list',   'kind':'widget', 'scope':'all',  'cat':'org',    'w':5,'h':4,'minw':3,'minh':3},
    {'id':'alerts_list',  'title':'Εκτός ορίων (πρόσφατα)',     'icon':'ti-alert-triangle', 'kind':'widget', 'scope':'all',  'cat':'quality','w':4,'h':4,'minw':3,'minh':3},
    {'id':'faults_list',  'title':'Ανοιχτές βλάβες (λίστα)',    'icon':'ti-tool',           'kind':'widget', 'scope':'all',  'cat':'faults', 'w':4,'h':4,'minw':3,'minh':3},
    {'id':'activity',     'title':'Πρόσφατη δραστηριότητα',     'icon':'ti-history',        'kind':'widget', 'scope':'all',  'cat':'people', 'w':4,'h':4,'minw':3,'minh':3},
    {'id':'pending_list', 'title':'Εκκρεμείς εγγραφές (λίστα)', 'icon':'ti-user-plus',      'kind':'widget', 'scope':'admin','cat':'people', 'w':4,'h':3,'minw':3,'minh':2},
]
DEFAULT_TILES_ADMIN = ['compliance','alerts','open_faults','pending','hotels','checkpoints',
                       'coverage','alerts_list','faults_list','pending_list']
DEFAULT_TILES_STAFF = ['my_faults','open_faults','pool_today','water_today','surveys_today',
                       'coverage','alerts_list','faults_list']

def _tile_allowed(t, admin):
    return admin or t.get('scope') != 'admin'

def _pin_dict(pw):
    return {'id': 'pin_%d' % pw.id, 'title': pw.title, 'icon': pw.icon or 'ti-pin',
            'kind': 'pinned', 'scope': 'all', 'cat': 'pinned',
            'w': 4, 'h': 3, 'minw': 3, 'minh': 2, 'link': pw.link or '', 'pid': pw.id}

def _user_pinned(user):
    try:
        return [_pin_dict(p) for p in PinnedWidget.query.filter_by(user_id=user.id).order_by(PinnedWidget.id).all()]
    except Exception:
        return []

def _allowed_catalog(user, admin):
    return [t for t in TILE_CATALOG if _tile_allowed(t, admin)] + _user_pinned(user)

def _user_layout(user, admin):
    """Λίστα {id,x,y,w,h} για τον χρήστη (saved geometry ή default auto-flow), φιλτραρισμένη."""
    valid = {t['id']: t for t in _allowed_catalog(user, admin)}
    items = None
    try:
        raw = getattr(user, 'dashboard', None)
        if raw:
            data = json.loads(raw)
            if isinstance(data, dict) and isinstance(data.get('items'), list):
                items = []
                for it in data['items']:
                    tid = it.get('id')
                    if tid in valid:
                        d = valid[tid]
                        items.append({'id': tid, 'x': it.get('x'), 'y': it.get('y'),
                                      'w': max(d['minw'], int(it.get('w') or d['w'])),
                                      'h': max(d['minh'], int(it.get('h') or d['h']))})
            elif isinstance(data, dict) and isinstance(data.get('tiles'), list):
                items = [{'id': i, 'x': None, 'y': None, 'w': valid[i]['w'], 'h': valid[i]['h']} for i in data['tiles'] if i in valid]
            elif isinstance(data, list):
                items = [{'id': i, 'x': None, 'y': None, 'w': valid[i]['w'], 'h': valid[i]['h']} for i in data if i in valid]
    except Exception:
        items = None
    if not items:
        deflist = DEFAULT_TILES_ADMIN if admin else DEFAULT_TILES_STAFF
        items = [{'id': i, 'x': None, 'y': None, 'w': valid[i]['w'], 'h': valid[i]['h']} for i in deflist if i in valid]
    seen, out = set(), []
    for it in items:
        if it['id'] not in seen:
            seen.add(it['id']); out.append(it)
    return out

@app.route('/overview')
def overview():
    user = current_user()
    if not user:
        return redirect(url_for('login'))
    admin = is_admin()
    today = date.today()
    hotels = scoped_hotels(user)
    hids = {h.id for h in hotels}
    F = __import__('faults').Fault

    # — Κάλυψη ανά ξενοδοχείο + συμμόρφωση —
    cov = []; total_exp = total_done = 0
    for h in hotels:
        exp = done = 0
        for p in [p for p in h.pools if p.is_active]:
            exp += 2
            done += min(2, PoolRecord.query.filter_by(pool_id=p.id, record_date=today).count())
        for a in Area.query.filter(Area.hotel_id == h.id, Area.is_active == True, Area.engine_only.isnot(True)).all():
            tpl = MonitorTemplate.query.get(a.template_key)
            n = len(periods_for(tpl.frequency)) if tpl else 1
            exp += n
            done += min(n, Reading.query.filter_by(area_id=a.id, record_date=today).count())
        pct = int(100 * done / exp) if exp else 100
        cov.append({'hotel': h.name, 'exp': exp, 'done': done, 'pct': pct})
        total_exp += exp; total_done += done
    compliance = int(100 * total_done / total_exp) if total_exp else 100

    # — Εκτός ορίων (alerts) —
    rec_dates = [today, today - timedelta(days=1)]
    alerts = []
    for r in PoolRecord.query.filter(PoolRecord.record_date.in_(rec_dates)).order_by(PoolRecord.recorded_at.desc()).limit(80).all():
        if r.pool and r.pool.hotel_id not in hids: continue
        for a in compute_pool_actions(r):
            alerts.append({'where': (r.pool.hotel.name + ' / ' + r.pool.name) if (r.pool and r.pool.hotel) else (r.pool.name if r.pool else '—'),
                           'label': a['label'], 'urgent': a['urgent'], 'date': r.record_date})
    for r in Reading.query.filter(Reading.record_date.in_(rec_dates), Reading.source_kind.is_(None)).order_by(Reading.recorded_at.desc()).limit(80).all():
        if r.area and r.area.hotel_id not in hids: continue
        for a in area_actions(r):
            alerts.append({'where': (r.area.hotel.name + ' / ' + r.area.name) if (r.area and r.area.hotel) else '—',
                           'label': a['label'], 'urgent': False, 'date': r.record_date})
    alerts = sorted(alerts, key=lambda x: (not x['urgent'], x['date']))[:15]

    # — Βλάβες (νέο μοντέλο Fault) —
    _open = F.query.filter(F.hotel_id.in_(hids or [-1])).filter(~F.status.in_(('done','not_done','resubmitted')))
    open_faults_n = _open.count()
    faults = _open.order_by(F.submitted_at.desc()).limit(10).all()
    my_faults_n = F.query.filter(F.hotel_id.in_(hids or [-1])).filter(
        ~F.status.in_(('done','not_done','resubmitted'))).filter(
        (F.assigned_user_id == user.id) | (F.submitted_by == user.id)).count()

    # — Μετρήσεις σήμερα (v12.237: από τη ΜΗΧΑΝΗ — Reading σε engine σημεία) —
    _ZNX_TPL = ('znx', 'znx_tank', 'znx_kitchen', 'znx_remote', 'znx_dhw', 'znx_ro')
    pool_today = (db.session.query(Reading.id).join(Area, Reading.area_id == Area.id).filter(
        Area.hotel_id.in_(hids or [-1]), Reading.record_date == today, Reading.template_key == 'pool').count())
    try:
        water_today = (db.session.query(Reading.id).join(Area, Reading.area_id == Area.id).filter(
            Area.hotel_id.in_(hids or [-1]), Reading.record_date == today, Reading.template_key.in_(_ZNX_TPL)).count())
    except Exception:
        water_today = 0
    try:
        SR = __import__('surveys').SurveyResponse
        _t0 = datetime.now().replace(hour=0, minute=0, second=0, microsecond=0)
        surveys_today = SR.query.filter(SR.submitted_at >= _t0).count()
    except Exception:
        surveys_today = 0

    pending = User.query.filter_by(is_active=True, approved=False).all()
    activity = ActivityLog.query.order_by(ActivityLog.id.desc()).limit(12).all()
    checkpoints = sum(len([p for p in h.pools if p.is_active]) for h in hotels) + \
                  Area.query.filter(Area.hotel_id.in_(hids or [-1]), Area.is_active == True, Area.engine_only.isnot(True)).count()

    tiledata = {
        'compliance': compliance, 'alerts': len(alerts), 'open_faults': open_faults_n,
        'my_faults': my_faults_n, 'pool_today': pool_today, 'water_today': water_today,
        'surveys_today': surveys_today, 'hotels': len(hotels), 'checkpoints': checkpoints,
        'users': User.query.filter_by(is_active=True, approved=True).count(), 'pending': len(pending),
        'cov': cov, 'alerts_list': alerts, 'faults_list': faults, 'pending_list': pending, 'activity': activity,
    }
    catalog = _allowed_catalog(user, admin)
    cmap = {t['id']: t for t in catalog}
    layout = _user_layout(user, admin)
    items = [{**cmap[it['id']], **it} for it in layout if it['id'] in cmap]
    predefined = [t for t in catalog if t.get('kind') != 'pinned']
    pinned = [t for t in catalog if t.get('kind') == 'pinned']
    return render_template('overview.html', tiledata=tiledata, items=items, catalog=catalog,
                           predefined=predefined, pinned=pinned, categories=CATEGORIES, cat_labels=CAT_LABELS,
                           is_admin=admin, areas_labels=AREA_LABELS, action_labels=ACTION_LABELS)

@app.route('/overview/layout', methods=['POST'])
def overview_layout():
    user = current_user()
    if not user:
        return ('', 401)
    admin = is_admin()
    valid = {t['id'] for t in TILE_CATALOG if _tile_allowed(t, admin)}
    try:
        payload = request.get_json(silent=True) or {}
        if payload.get('reset'):
            user.dashboard = None
            db.session.commit()
            return ('', 204)
        cmap = {t['id']: t for t in _allowed_catalog(user, admin)}
        out, seen = [], set()
        # νέα μορφή: items με γεωμετρία
        for it in (payload.get('items') or []):
            tid = it.get('id')
            if tid in cmap and tid not in seen:
                seen.add(tid); d = cmap[tid]
                def _ci(v, dv):
                    try: return int(v)
                    except Exception: return dv
                w = max(d['minw'], min(12, _ci(it.get('w'), d['w'])))
                h = max(d['minh'], _ci(it.get('h'), d['h']))
                rec = {'id': tid, 'w': w, 'h': h}
                if it.get('x') is not None: rec['x'] = max(0, _ci(it.get('x'), 0))
                if it.get('y') is not None: rec['y'] = max(0, _ci(it.get('y'), 0))
                out.append(rec)
        # συμβατότητα: παλιά μορφή tiles[]
        if not out and isinstance(payload.get('tiles'), list):
            for tid in payload['tiles']:
                if tid in cmap and tid not in seen:
                    seen.add(tid); d = cmap[tid]
                    out.append({'id': tid, 'w': d['w'], 'h': d['h']})
        user.dashboard = json.dumps({'items': out})
        db.session.commit()
        return ('', 204)
    except Exception as e:
        db.session.rollback()
        return (str(e), 400)

@app.route('/dashboard/pin', methods=['POST'])
def dashboard_pin():
    user = current_user()
    if not user:
        return ('', 401)
    p = request.get_json(silent=True) or {}
    title = (p.get('title') or '').strip()[:80]
    link = (p.get('link') or '').strip()[:300]
    icon = (p.get('icon') or 'ti-pin').strip()[:40]
    if not title:
        return jsonify({'error': 'title required'}), 400
    pw = PinnedWidget(user_id=user.id, title=title, link=link, icon=icon)
    db.session.add(pw); db.session.commit()
    return jsonify({'id': 'pin_%d' % pw.id, 'pid': pw.id})

@app.route('/dashboard/unpin/<int:pid>', methods=['POST'])
def dashboard_unpin(pid):
    user = current_user()
    if not user:
        return ('', 401)
    pw = PinnedWidget.query.filter_by(id=pid, user_id=user.id).first()
    if pw:
        db.session.delete(pw); db.session.commit()
    return ('', 204)

# ── v12.21 — Καταγραφή χρηστών (Activity Log) ────────────────────────────────
ACTION_LABELS = {
    'login': 'Σύνδεση', 'logout': 'Αποσύνδεση', 'profile_update': 'Ενημέρωση προφίλ',
    'water_submit': 'Καταγραφή νερών', 'pool_submit': 'Καταγραφή πισίνας', 'pool_edit': 'Διόρθωση πισίνας',
    'area_submit': 'Καταγραφή τομέα',
    'user_add': 'Νέος χρήστης', 'user_delete': 'Διαγραφή χρήστη', 'user_approve': 'Έγκριση χρήστη',
    'user_edit': 'Επεξεργασία χρήστη', 'user_reset_password': 'Επαναφορά κωδικού', 'user_toggle': 'Ενεργοπ./Απενεργ. χρήστη',
    'email_test': 'Δοκιμή email', 'email_reminder_manual': 'Χειροκίνητη υπενθύμιση',
    'theme_update': 'Αλλαγή εμφάνισης', 'ai_config': 'Ρύθμιση AI',
    'area_add': 'Νέος τομέας', 'template_new': 'Νέο template', 'template_edit': 'Επεξεργασία template',
    'seed_demo': 'Demo δεδομένα', 'water_system_add': 'Νέο δίκτυο νερού',
    'water_system_edit': 'Επεξεργασία δικτύου', 'water_system_delete': 'Διαγραφή δικτύου',
    'fault_report': 'Δήλωση βλάβης', 'faults_bulk': 'Μαζική ενέργεια βλαβών',
}
def _act_label(a):
    return ACTION_LABELS.get(a, a or '—')

ACTION_ICON = {
    'login': 'login', 'logout': 'logout', 'profile_update': 'user-edit',
    'water_submit': 'droplet', 'pool_submit': 'pool', 'pool_edit': 'edit', 'area_submit': 'checklist',
    'user_add': 'user-plus', 'user_delete': 'user-x', 'user_approve': 'user-check',
    'user_edit': 'user-edit', 'user_reset_password': 'key', 'user_toggle': 'toggle-right',
    'email_test': 'mail', 'email_reminder_manual': 'mail-forward', 'theme_update': 'palette', 'ai_config': 'plug',
    'area_add': 'map-pin-plus', 'template_new': 'template', 'template_edit': 'template', 'seed_demo': 'database',
    'water_system_add': 'plus', 'water_system_edit': 'edit', 'water_system_delete': 'trash',
    'fault_report': 'tool', 'faults_bulk': 'checks',
}
def _act_icon(a):
    return ACTION_ICON.get(a, 'point')

@app.route('/dashboard/activity')
def activity_log():
    if not is_admin():
        return redirect(url_for('login'))
    user = current_user()
    f_user   = request.args.get('user_id', type=int)
    f_action = request.args.get('action')
    search   = (request.args.get('q') or '').strip()
    chip     = request.args.get('chip')          # today | 7d | 30d | logins
    now = datetime.now()
    midnight = now.replace(hour=0, minute=0, second=0, microsecond=0)
    aid = active_hotel_id()                       # v12.22 — scope ανά ξενοδοχείο
    def hscope(query):
        return query.filter(ActivityLog.hotel_id == aid) if aid else query
    base = apply_period(hscope(ActivityLog.query), ActivityLog.created_at, dt=True)
    q = apply_period(hscope(ActivityLog.query), ActivityLog.created_at, dt=True)
    if chip == 'today':  q = q.filter(ActivityLog.created_at >= midnight)
    elif chip == '7d':   q = q.filter(ActivityLog.created_at >= now - timedelta(days=7))
    elif chip == '30d':  q = q.filter(ActivityLog.created_at >= now - timedelta(days=30))
    elif chip == 'logins': q = q.filter(ActivityLog.action == 'login')
    if f_user:   q = q.filter(ActivityLog.user_id == f_user)
    if f_action: q = q.filter(ActivityLog.action == f_action)
    if search:
        like = '%%%s%%' % search
        q = q.filter((ActivityLog.detail.ilike(like)) | (ActivityLog.action.ilike(like)))
    logs = q.order_by(ActivityLog.created_at.desc()).limit(400).all()
    users = User.query.filter_by(is_active=True).order_by(User.full_name).all()
    actions = sorted({a[0] for a in db.session.query(ActivityLog.action).distinct().all() if a[0]})
    kpi = {
        'today':        hscope(ActivityLog.query).filter(ActivityLog.created_at >= midnight).count(),
        'active_today': hscope(db.session.query(ActivityLog.user_id)).filter(ActivityLog.created_at >= midnight).distinct().count(),
        'logins_today': hscope(ActivityLog.query).filter(ActivityLog.created_at >= midnight, ActivityLog.action == 'login').count(),
        'week':         hscope(ActivityLog.query).filter(ActivityLog.created_at >= now - timedelta(days=7)).count(),
    }
    # v12.22 — 7ήμερο mini γράφημα (scoped)
    spark = []
    for i in range(6, -1, -1):
        d0 = (midnight - timedelta(days=i))
        d1 = d0 + timedelta(days=1)
        cnt = hscope(ActivityLog.query).filter(ActivityLog.created_at >= d0, ActivityLog.created_at < d1).count()
        spark.append({'label': d0.strftime('%d/%m'), 'dow': ['Δε','Τρ','Τε','Πε','Πα','Σα','Κυ'][d0.weekday()], 'count': cnt})
    spark_max = max([x['count'] for x in spark] or [1]) or 1
    week_start = now - timedelta(days=7)
    per_user = []
    for u in users:
        last = hscope(ActivityLog.query).filter(ActivityLog.user_id == u.id).order_by(ActivityLog.created_at.desc()).first()
        cw = hscope(ActivityLog.query).filter(ActivityLog.user_id == u.id, ActivityLog.created_at >= week_start).count()
        if aid and not last and cw == 0:
            continue                              # σε scope ξενοδοχείου, κρύψε άσχετους χρήστες
        per_user.append({
            'u': u,
            'today': hscope(ActivityLog.query).filter(ActivityLog.user_id == u.id, ActivityLog.created_at >= midnight).count(),
            'week':  cw,
            'last':  last,
        })
    per_user.sort(key=lambda x: (x['last'].created_at if x['last'] else datetime.min), reverse=True)
    active_hotel = Hotel.query.get(aid) if aid else None
    return render_template('activity_log.html', logs=logs, users=users, actions=actions,
                           act_label=_act_label, act_icon=_act_icon, ACTION_LABELS=ACTION_LABELS,
                           f_user=f_user, f_action=f_action, search=search, chip=chip,
                           kpi=kpi, per_user=per_user, user=user, spark=spark, spark_max=spark_max,
                           active_hotel=active_hotel,
                           qs=request.query_string.decode('utf-8'))

# admin: areas management
@app.route('/dashboard/areas', methods=['GET', 'POST'])
def areas_admin():
    if not is_admin():
        return redirect(url_for('login'))
    if request.method == 'POST':
        hid = request.form.get('hotel_id'); tk = request.form.get('template_key'); nm = request.form.get('name', '').strip()
        if hid and tk and nm:
            db.session.add(Area(hotel_id=int(hid), template_key=tk, name=nm, location=request.form.get('location', '').strip()))
            db.session.commit(); log_activity('area_add', nm)
        return redirect(url_for('areas_admin') + '?saved=1')
    return render_template('areas_admin.html',
                           hotels=Hotel.query.filter_by(is_active=True).order_by(Hotel.name).all(),
                           templates=MonitorTemplate.query.filter_by(is_active=True).order_by(MonitorTemplate.sort).all(),
                           areas=Area.query.filter(Area.is_active == True, Area.engine_only.isnot(True)).all())

@app.route('/dashboard/area/<int:area_id>/delete', methods=['POST'])
def area_delete(area_id):
    if not is_admin():
        return redirect(url_for('login'))
    a = Area.query.get(area_id)
    if a:
        a.is_active = False; db.session.commit()
    return redirect(url_for('areas_admin'))

# admin: template editor (hybrid)
@app.route('/dashboard/templates')
def templates_admin():
    # v12.254 — ενσωματώθηκε στη «Μετρήσεις · Ρυθμίσεις → Παράμετροι». Ανακατεύθυνση.
    if not is_admin():
        return redirect(url_for('login'))
    url = url_for('measurements_console') + '?tab=templates'
    if request.args.get('embed'):
        url += '&embed=1'
    return redirect(url)

@app.route('/dashboard/template/new', methods=['POST'])
def template_new():
    if not is_admin():
        return redirect(url_for('login'))
    key = (request.form.get('key', '').strip().lower() or '')
    key = ''.join(ch for ch in key if ch.isalnum() or ch == '_')[:30]
    if key and not MonitorTemplate.query.get(key):
        db.session.add(MonitorTemplate(key=key, name=request.form.get('name', key).strip(),
                       icon=request.form.get('icon', 'ti-checklist').strip() or 'ti-checklist',
                       frequency=request.form.get('frequency', 'daily'), sort=99))
        db.session.commit(); log_activity('template_new', key)
    return redirect(url_for('template_edit', key=key) if key else url_for('templates_admin'))

@app.route('/dashboard/template/<key>', methods=['GET', 'POST'])
def template_edit(key):
    if not is_admin():
        return redirect(url_for('login'))
    tpl = MonitorTemplate.query.get(key)
    if not tpl:
        return redirect(url_for('templates_admin'))
    if request.method == 'POST':
        tpl.name = request.form.get('name', tpl.name).strip()
        tpl.icon = request.form.get('icon', tpl.icon).strip() or tpl.icon
        tpl.frequency = request.form.get('frequency', tpl.frequency)
        def fnum(v):
            try:
                return float(v) if v not in (None, '') else None
            except (ValueError, TypeError):
                return None
        # update existing params
        for p in list(tpl.params):
            if request.form.get('del_%d' % p.id):
                db.session.delete(p); continue
            p.label = request.form.get('label_%d' % p.id, p.label).strip()
            p.unit = request.form.get('unit_%d' % p.id, p.unit).strip()
            p.min_v = fnum(request.form.get('min_%d' % p.id))
            p.max_v = fnum(request.form.get('max_%d' % p.id))
            p.action_low = request.form.get('alow_%d' % p.id, '').strip()
            p.action_high = request.form.get('ahigh_%d' % p.id, '').strip()
            p.periodic = bool(request.form.get('per_%d' % p.id))
        # new param
        npk = request.form.get('new_pkey', '').strip().lower()
        npk = ''.join(ch for ch in npk if ch.isalnum() or ch == '_')[:40]
        if npk and request.form.get('new_label', '').strip():
            db.session.add(MonitorParam(template_key=tpl.key, pkey=npk,
                           label=request.form.get('new_label').strip(),
                           unit=request.form.get('new_unit', '').strip(),
                           min_v=fnum(request.form.get('new_min')), max_v=fnum(request.form.get('new_max')),
                           action_low=request.form.get('new_alow', '').strip(),
                           action_high=request.form.get('new_ahigh', '').strip(),
                           periodic=bool(request.form.get('new_per')), sort=len(tpl.params) + 1))
        db.session.commit(); log_activity('template_edit', key)
        return redirect(url_for('template_edit', key=key) + '?saved=1')
    return render_template('template_edit.html', tpl=tpl, freq_label=FREQ_LABEL)

# ── Demo data seeder (admin) — τυχαίες καταγραφές 1-31/5 ──
@app.route('/admin/seed-demo')
def seed_demo():
    if not is_admin():
        return redirect(url_for('login'))
    import random
    user = current_user()
    pools = Pool.query.filter_by(is_active=True).all()
    if not pools:
        return 'Δεν υπάρχουν πισίνες. <a href="/pools/dashboard">Πίσω</a>'
    yr = int(request.args.get('year', date.today().year))
    created = 0
    for p in pools:
        d = date(yr, 5, 1)
        while d <= date(yr, 5, 31):
            for period in ('morning', 'afternoon'):
                if PoolRecord.query.filter_by(pool_id=p.id, record_date=d, period=period).first():
                    continue
                r = PoolRecord(pool_id=p.id, user_id=user.id, record_date=d, period=period)
                r.free_chlorine     = round(random.uniform(0.5, 1.3), 2) if random.random() > 0.10 else round(random.uniform(0.1, 0.39), 2)
                r.combined_chlorine = round(random.uniform(0.0, 0.4), 2) if random.random() > 0.10 else round(random.uniform(0.55, 0.9), 2)
                r.ph                = round(random.uniform(7.2, 7.8), 1) if random.random() > 0.10 else round(random.uniform(7.9, 8.3), 1)
                r.temp              = round(random.uniform(24, 30), 1)
                r.turbidity         = round(random.uniform(0.1, 0.8), 1) if random.random() > 0.10 else round(random.uniform(1.1, 1.8), 1)
                if period == 'morning':
                    r.cyanuric_acid    = round(random.uniform(20, 60))
                    r.total_alkalinity = round(random.uniform(80, 120)) if random.random() > 0.10 else round(random.uniform(60, 79))
                    r.orp              = round(random.uniform(650, 760)) if random.random() > 0.10 else round(random.uniform(580, 640))
                r.backwash_done = random.random() > 0.85
                r.recorded_at = datetime(yr, 5, d.day, 8 if period == 'morning' else 17, random.randint(0, 59))
                db.session.add(r); created += 1
            d += timedelta(days=1)
    db.session.commit()
    log_activity('seed_demo', f'{created} records {yr}-05')
    return ('<div style="font-family:Arial;padding:40px;text-align:center;">'
            '<h2 style="color:#193847;">Δημιουργήθηκαν ' + str(created) + ' demo καταγραφές (1-31/5/' + str(yr) + ')</h2>'
            '<p><a href="/pools/dashboard" style="color:#193847;">→ Pool dashboard</a> &nbsp; '
            '<a href="/pools/coverage" style="color:#193847;">→ Κάλυψη</a></p></div>')

# v12.7 — Διαχείριση Ξενοδοχείων & Πισινών (μεταφέρθηκε από τον Πίνακα Πισινών)
@app.route('/dashboard/hotels')
def hotels_admin():
    if not is_admin():
        return redirect(url_for('login'))
    hotels = Hotel.query.filter_by(is_active=True).order_by(Hotel.name).all()
    pools  = Pool.query.filter_by(is_active=True).all()
    return render_template('hotels_admin.html', hotels=hotels, pools=pools, is_admin=True)

@app.route('/dashboard/add-hotel', methods=['POST'])
def add_hotel():
    if not is_admin():
        return redirect(url_for('login'))
    name = request.form.get('name', '').strip()
    if name and not Hotel.query.filter_by(name=name).first():
        db.session.add(Hotel(name=name))
        db.session.commit()
    return redirect(url_for('hotels_admin') + '?success=hotel_added')

@app.route('/dashboard/delete-hotel/<int:hotel_id>', methods=['POST'])
def delete_hotel(hotel_id):
    if not is_admin():
        return redirect(url_for('login'))
    hotel = Hotel.query.get(hotel_id)
    if hotel:
        hotel.is_active = False
        for p in hotel.pools:
            p.is_active = False
        db.session.commit()
    return redirect(url_for('hotels_admin'))

@app.route('/dashboard/add-pool', methods=['POST'])
def add_pool():
    if not is_admin():
        return redirect(url_for('login'))
    data = request.form
    hotel_id = data.get('hotel_id')
    name = data.get('name', '').strip()
    if hotel_id and name:
        db.session.add(Pool(
            hotel_id=int(hotel_id),
            name=name,
            location=data.get('location', '').strip(),
            pool_type=data.get('pool_type', 'pool'),
            volume_m3=flt(data, 'volume_m3')
        ))
        db.session.commit()
    return redirect(url_for('hotels_admin') + '?success=pool_added')

@app.route('/dashboard/delete-pool/<int:pool_id>', methods=['POST'])
def delete_pool(pool_id):
    if not is_admin():
        return redirect(url_for('login'))
    pool = Pool.query.get(pool_id)
    if pool:
        pool.is_active = False
        db.session.commit()
    return redirect(url_for('hotels_admin'))


# ── v12.3 — Διαχείριση Δικτύων Νερού (mirror add/delete pool) ──
@app.route('/dashboard/add-water-system', methods=['POST'])
def add_water_system():
    if not is_admin():
        return redirect(url_for('login'))
    data = request.form
    hotel_id = data.get('hotel_id')
    name = (data.get('name') or '').strip()
    if hotel_id and name:
        db.session.add(WaterSystem(hotel_id=int(hotel_id), name=name,
                                   location=(data.get('location') or '').strip()))
        db.session.commit()
        log_activity('water_system_add', name)
    return redirect(url_for('dashboard') + '?success=system_added')

@app.route('/dashboard/edit-water-system/<int:system_id>', methods=['POST'])
def edit_water_system(system_id):
    if not is_admin():
        return redirect(url_for('login'))
    ws = WaterSystem.query.get(system_id)
    if ws:
        name = (request.form.get('name') or '').strip()
        if name:
            ws.name = name
        ws.location = (request.form.get('location') or '').strip()
        db.session.commit()
        log_activity('water_system_edit', ws.name)
    return redirect(url_for('dashboard') + '?success=system_edited')

@app.route('/dashboard/delete-water-system/<int:system_id>', methods=['POST'])
def delete_water_system(system_id):
    if not is_admin():
        return redirect(url_for('login'))
    ws = WaterSystem.query.get(system_id)
    if ws:
        ws.is_active = False   # soft delete — οι καταγραφές μένουν
        db.session.commit()
        log_activity('water_system_delete', ws.name)
    return redirect(url_for('dashboard'))


# ──────────────────────────────────────────────────────────────────────────
#  CHART APIs
# ──────────────────────────────────────────────────────────────────────────
@app.route('/api/history')
def api_history():
    if 'user_id' not in session:
        return jsonify([])
    q = WaterRecord.query.filter_by(period='morning')
    sid = request.args.get('system_id', type=int)   # v12.3 — προαιρετικό φίλτρο δικτύου
    if sid:
        q = q.filter_by(water_system_id=sid)
    records = q.order_by(WaterRecord.record_date.desc()).limit(14).all()
    return jsonify([{
        'date': r.record_date.strftime('%d/%m'),
        'clo2_tank': r.clo2_tank,
        'temp_dhw_out': r.temp_dhw_out,
        'temp_dhw_return': r.temp_dhw_return,
        'temp_tank': r.temp_tank,
    } for r in records])

@app.route('/api/pool-history/<int:pool_id>')
def api_pool_history(pool_id):
    if 'user_id' not in session:
        return jsonify([])
    records = (PoolRecord.query.filter_by(pool_id=pool_id, period='morning')
               .order_by(PoolRecord.record_date.desc()).limit(14).all())
    return jsonify([{
        'date': r.record_date.strftime('%d/%m'),
        'free_chlorine': r.free_chlorine,
        'ph': r.ph,
        'temp': r.temp,
    } for r in records])


# ──────────────────────────────────────────────────────────────────────────
#  INIT  (τρέχει και κάτω από gunicorn)
# ──────────────────────────────────────────────────────────────────────────
def _add_col(table, col, ddl):
    """v12.35 — Ανθεκτικό, idempotent & race-safe (gunicorn 2 workers).
    Postgres: ADD COLUMN IF NOT EXISTS. SQLite: inspect-check.
    Κάθε στήλη έχει ΔΙΚΟ της try/except — μία αποτυχία ΔΕΝ μπλοκάρει τις υπόλοιπες."""
    try:
        if db.engine.dialect.name == 'postgresql':
            db.session.execute(text(f'ALTER TABLE "{table}" ADD COLUMN IF NOT EXISTS {ddl}'))
            db.session.commit()
        else:
            insp = db.inspect(db.engine)
            if table in insp.get_table_names():
                cols = [c['name'] for c in insp.get_columns(table)]
                if col not in cols:
                    db.session.execute(text(f'ALTER TABLE "{table}" ADD COLUMN {ddl}'))
                    db.session.commit()
    except Exception as e:
        db.session.rollback()
        print(f'_add_col {table}.{col} skipped: {e}')

def ensure_columns():
    """Ελαφρύ migration: πρόσθεσε νέες στήλες σε ήδη υπάρχουσα βάση.
    v12.35: κάθε _add_col αυτόνομο (δικό του commit/rollback) → καμία στήλη δεν παρασύρει τις άλλες."""
    truth = 'true' if db.engine.dialect.name == 'postgresql' else '1'
    _add_col('pool', 'volume_m3', 'volume_m3 FLOAT')
    _add_col('user', 'email', 'email VARCHAR(120)')
    _add_col('user', 'phone', 'phone VARCHAR(40)')
    _add_col('user', 'approved', f'approved BOOLEAN DEFAULT {truth}')
    _add_col('user', 'avatar', 'avatar TEXT')
    _add_col('water_record', 'water_system_id', 'water_system_id INTEGER')  # v12.3
    _add_col('activity_log', 'hotel_id', 'hotel_id INTEGER')  # v12.22
    _add_col('survey_response', 'import_hash', 'import_hash VARCHAR(40)')  # v12.30
    _add_col('user', 'dashboard', 'dashboard TEXT')  # v12.34 — προσωπική διάταξη tiles (JSON)
    # v12.40 — Module Πρόγραμμα Εργασίας (User columns ΠΡΙΝ το init_db seed)
    _add_col('user', 'department_id', 'department_id INTEGER')
    _add_col('user', 'employer', 'employer VARCHAR(120)')
    _add_col('user', 'subunit', 'subunit VARCHAR(20)')
    _add_col('user', 'home_hotel_id', 'home_hotel_id INTEGER')
    _add_col('user', 'import_hotel_id', 'import_hotel_id INTEGER')  # v12.170
    _add_col('user', 'position_id', 'position_id INTEGER')  # v12.179
    _add_col('user', 'login_enabled', 'login_enabled BOOLEAN')
    _add_col('user', 'employment_active', 'employment_active BOOLEAN')
    # v12.218 — Φ2 ενοποίηση μετρήσεων (σημεία/αντίγραφα legacy)
    _add_col('area', 'engine_only', 'engine_only BOOLEAN')
    _add_col('area', 'legacy_kind', 'legacy_kind VARCHAR(10)')
    _add_col('area', 'legacy_id', 'legacy_id INTEGER')
    _add_col('reading', 'source_kind', 'source_kind VARCHAR(10)')
    _add_col('reading', 'source_id', 'source_id INTEGER')
    # v12.270 — Φ3 Βιβλιοθήκη Μετρήσεων (A1)
    _add_col('monitor_param', 'kind', 'kind VARCHAR(10)')
    _add_col('monitor_param', 'category', "category VARCHAR(40)")
    _add_col('monitor_param', 'is_active', f'is_active BOOLEAN DEFAULT {truth}')
    # v12.271 — Φ-Α δομή δικτύων (δέντρο)
    _add_col('area', 'node_id', 'node_id INTEGER')
    _add_col('monitor_node', 'hotels', 'hotels VARCHAR(120)')

def init_db():
    with app.app_context():
        db.create_all()
        ensure_columns()
        try:
            # v12.25 — Bootstrap πρωτου masteradmin ΜΟΝΟ σε αδεια βαση, με credentials απο env.
            # Δεν υπαρχουν πλεον demo accounts με σταθερους κωδικους μεσα στον κωδικα.
            if User.query.count() == 0:
                _admin_user = os.environ.get('BOOTSTRAP_ADMIN_USER', 'admin')
                _admin_pw   = os.environ.get('BOOTSTRAP_ADMIN_PASSWORD')
                if not _admin_pw:
                    _admin_pw = secrets.token_urlsafe(12)
                    print(f'[ESTIA] Αρχικος masteradmin "{_admin_user}" με ΤΥΧΑΙΟ κωδικο: {_admin_pw}')
                    print('[ESTIA] Αλλαξε τον κωδικο αμεσως μετα την πρωτη συνδεση.')
                db.session.add(User(username=_admin_user, password=generate_password_hash(_admin_pw),
                                    full_name='Διαχειριστης', role='masteradmin', language='el',
                                    approved=True, is_active=True))
                db.session.commit()
                print('Βαση δεδομενων και αρχικος διαχειριστης δημιουργηθηκαν')
            if not Hotel.query.first():
                sergios = Hotel(name='Sergios Hotel')
                db.session.add(sergios)
                db.session.flush()
                db.session.add(Pool(hotel_id=sergios.id, name='Κύρια Πισίνα', location='Pool bar', pool_type='pool', volume_m3=200))
                db.session.add(Pool(hotel_id=sergios.id, name='Παιδική Πισίνα', location='Pool bar', pool_type='kids', volume_m3=20))
                db.session.commit()
                print('Δημιουργηθηκε δειγμα ξενοδοχειου & πισινων')

            # v12.3 — default Δίκτυο Νερού + backfill παλιών records (idempotent, ασφαλές για υπάρχουσα βάση)
            try:
                _h0 = Hotel.query.first()
                if _h0 and not WaterSystem.query.first():
                    db.session.add(WaterSystem(hotel_id=_h0.id, name='Κεντρικό Δίκτυο', location='Μηχανοστάσιο'))
                    db.session.commit()
                _ws0 = WaterSystem.query.first()
                if _ws0:
                    WaterRecord.query.filter_by(water_system_id=None).update({'water_system_id': _ws0.id})
                    db.session.commit()
            except Exception as _e:
                db.session.rollback()
                print(f'water_system seed skipped: {_e}')

            if not MonitorTemplate.query.first():
                tank = MonitorTemplate(key='tank', name='Στάθμες & Δεξαμενές', icon='ti-stack-2', frequency='daily', sort=10)
                db.session.add(tank); db.session.flush()
                db.session.add_all([
                    MonitorParam(template_key='tank', pkey='level_pct', label='Στάθμη', unit='%', min_v=20, max_v=100, action_low='Χαμηλή στάθμη — έλεγξε πλήρωση/διαρροή.', sort=1),
                    MonitorParam(template_key='tank', pkey='pressure', label='Πίεση', unit='bar', sort=2),
                    MonitorParam(template_key='tank', pkey='temp', label='Θερμοκρασία', unit='C', sort=3),
                ])
                energy = MonitorTemplate(key='energy', name='Ενέργεια & Μετρητές', icon='ti-bolt', frequency='daily', sort=20)
                db.session.add(energy); db.session.flush()
                db.session.add_all([
                    MonitorParam(template_key='energy', pkey='electricity_kwh', label='Ρεύμα (ένδειξη)', unit='kWh', sort=1),
                    MonitorParam(template_key='energy', pkey='water_m3', label='Νερό (ένδειξη)', unit='m3', sort=2),
                    MonitorParam(template_key='energy', pkey='gas_m3', label='Φυσικό αέριο (ένδειξη)', unit='m3', sort=3),
                ])
                db.session.commit()
                print('Δημιουργηθηκαν generic templates')

            if not Area.query.first():
                _h = Hotel.query.first()
                if _h:
                    db.session.add(Area(hotel_id=_h.id, template_key='tank', name='Κεντρική Δεξαμενή', location='Μηχανοστάσιο'))
                    db.session.add(Area(hotel_id=_h.id, template_key='energy', name='Γενικός Μετρητής', location='Πίνακας ΔΕΗ'))
                    db.session.commit()
                    print('Δημιουργηθηκαν δειγματικα areas')
        except Exception as e:
            db.session.rollback()
            print(f'init_db seed skipped: {e}')


def _athens_now():
    try:
        from zoneinfo import ZoneInfo
        return datetime.now(ZoneInfo('Europe/Athens'))
    except Exception:
        return datetime.now() + timedelta(hours=3)

def missing_today():
    today = date.today()
    miss = []
    for p in Pool.query.filter_by(is_active=True).all():
        recs = {r.period for r in PoolRecord.query.filter_by(pool_id=p.id, record_date=today).all()}
        gaps = [per for per in ('morning', 'afternoon') if per not in recs]
        if gaps:
            hotel = p.hotel.name if p.hotel else ''
            miss.append(f"{hotel} — {p.name}: " + ', '.join('Πρωί' if g == 'morning' else 'Απόγευμα' for g in gaps))
    # v12.3 — έλεγχος ανά δίκτυο νερού (όχι πια ενιαίο/Sergios)
    for ws in WaterSystem.query.filter_by(is_active=True).all():
        recs = {r.period for r in WaterRecord.query.filter_by(water_system_id=ws.id, record_date=today).all()}
        gaps = [per for per in ('morning', 'afternoon') if per not in recs]
        if gaps:
            hotel = ws.hotel.name if ws.hotel else ''
            miss.append(f"Νερά Χρήσης — {hotel} · {ws.name}: " + ', '.join('Πρωί' if g == 'morning' else 'Απόγευμα' for g in gaps))
    return miss

def send_reminder_email(miss):
    if (not EMAIL_PASSWORD and not GRAPH_CLIENT_ID) or not miss:
        return False
    recips = list(EMAIL_TO_LIST)
    for u in User.query.filter_by(is_active=True, approved=True).all():
        if u.email and u.email not in recips:
            recips.append(u.email)
    items = ''.join(f'<li>{m}</li>' for m in miss)
    html = f'<div style="font-family:Arial,sans-serif"><h3 style="color:#193847">Εκκρεμείς καταγραφές σήμερα</h3><ul>{items}</ul><p style="color:#888;font-size:12px">Εστία — CONDIAN HOTELS · αυτόματη υπενθύμιση</p></div>'
    return send_email('Εστία — Υπενθύμιση καταγραφών - ' + date.today().strftime('%d/%m/%Y'), html, recips)

def reminder_tick():
    with app.app_context():
        now = _athens_now()
        if now.hour != REMINDER_HOUR:
            return
        today = now.strftime('%Y-%m-%d')
        if ReminderSent.query.get(today):
            return
        try:
            db.session.add(ReminderSent(day=today)); db.session.commit()
        except Exception:
            db.session.rollback(); return   # άλλος worker το ανέλαβε
        miss = missing_today()
        if miss:
            send_reminder_email(miss)
            print(f'[reminder] {today}: {len(miss)} εκκρεμή')

def reminder_loop():
    while True:
        try:
            reminder_tick()
        except Exception as e:
            print('[reminder] loop error:', e)
        time.sleep(1800)   # κάθε 30 λεπτά

def start_scheduler():
    if ENABLE_SCHEDULER:
        threading.Thread(target=reminder_loop, daemon=True).start()
        print('[scheduler] reminder loop started')


def seed_team():
    """Μία φορά: δημιουργεί την πραγματική ομάδα CONDIAN."""
    with app.app_context():
        try:
            if Setting.query.get('seeded_team_v1'):
                return
            team = [
                ('giakoumakis',  'Giakoumakis Giannis',    'masteradmin', 'g.giakoumakis@condianhotels.gr', '+306973728931'),
                ('giannoulakis', 'Γιαννουλάκης Δημήτρης',  'admin',       'dimitris@condianhotels.gr',      '+306936647778'),
                ('xypakis',      'Ξυπάκης Μάνος',          'manager',     'm.xypakis@condianhotels.gr',     '+306972238222'),
                ('smyrnakis',    'Σμυρνάκης Χριστόφορος',  'manager',     'c.smyrnakis@condianhotels.gr',   '+306992015939'),
                ('flouris',      'Φλουρής Στέφανος',       'manager',     's.flouris@condianhotels.gr',     '+306931549656'),
            ]
            # v12.25 — ο αρχικος κωδικος ομαδας ερχεται απο env (fallback για συμβατοτητα).
            # Συσταση: ορισε TEAM_DEFAULT_PASSWORD στο Railway και ζητα αλλαγη στην 1η συνδεση.
            _team_pw = os.environ.get('TEAM_DEFAULT_PASSWORD', 'condian2026')
            for un, fn, role, em, ph in team:
                u = User.query.filter_by(username=un).first()
                if u:
                    u.full_name = fn; u.role = role; u.email = em; u.phone = ph
                    u.approved = True; u.is_active = True
                else:
                    db.session.add(User(username=un, password=generate_password_hash(_team_pw),
                                        full_name=fn, role=role, email=em, phone=ph,
                                        approved=True, is_active=True, language='el'))
            db.session.add(Setting(key='seeded_team_v1', value='1'))
            db.session.commit()
            print('Team CONDIAN seeded')
        except Exception as e:
            db.session.rollback(); print('seed_team skipped:', e)


# v12.81 — Ανθεκτική φόρτωση plug-ins: ένα ξεχασμένο/σπασμένο module ΔΕΝ ρίχνει όλη την εφαρμογή.
def _try_import(_n):
    try:
        return __import__(_n)   # ΠΡΙΝ το create_all (μοντέλα + routes)
    except Exception as _e:
        import traceback as _tb; _tb.print_exc()
        print('[plugin] ΠΡΟΣΟΧΗ: το module "%s" δεν φορτώθηκε — συνεχίζω χωρίς αυτό: %s' % (_n, _e))
        return None
faults   = _try_import('faults')
surveys  = _try_import('surveys')
imports  = _try_import('imports')
backup   = _try_import('backup')
schedule = _try_import('schedule')
extras   = _try_import('extras')
payroll  = _try_import('payroll')
people   = _try_import('people')
diag     = _try_import('diag')
menu     = _try_import('menu')
evaluations = _try_import('evaluations')
console  = _try_import('console')
measurements = _try_import('measurements')   # Φ1 ενοποίηση μετρήσεων (ΠΡΙΝ init_db: create_all να πιάσει MonitorPeriod)
init_db()
if backup:   backup.ensure_backup_columns()   # v12.33 — auto-migration backup_log + seed ρυθμίσεων
if evaluations:
    evaluations.ensure_eval_columns()  # auto-migration ΠΡΩΤΑ: eval_template.hotel_id (πριν από οποιοδήποτε query)
    evaluations.ensure_eval_setup()   # seed ΜΟΝΟ του βασικού προτύπου CONDIAN σε ΑΔΕΙΑ βάση (one-time)
    # ΣΗΜ.: ΔΕΝ καλείται πλέον ensure_eval_dept_templates() — τα F&B templates μπήκαν μία φορά (reference)
    #       και πλέον τα διαχειρίζεται ο χρήστης. Η εφαρμογή δεν δημιουργεί/πειράζει πρότυπα αυτόματα.
    evaluations._seed_menu_evals()    # one-time: 'evals' στο menu_vis (manager on)
seed_team()
if faults:   faults.seed_faults()
if surveys:  surveys.seed_surveys()
if schedule: schedule.ensure_schedule_columns()
if schedule: schedule.seed_schedule()
if payroll:  payroll.ensure_payroll_columns()
if payroll:  payroll.seed_payroll()
if measurements: measurements.seed_measurement_engine()   # Φ1: seed templates Πισίνα/ΖΝΧ + περίοδοι (idempotent)
if measurements: measurements.seed_node_catalog()         # Φ-Α: κατάλογος-αναφορά δέντρου δικτύων (idempotent)
if menu: menu.seed_menu_meta()   # v12.239: seed workspace+ρόλοι μενού (μόνο αν λείπει)
start_scheduler()
if backup:   backup.start_backup_scheduler()
if payroll:  payroll.start_master_sync_scheduler()

if __name__ == '__main__':
    app.run(debug=False, host='0.0.0.0', port=int(os.environ.get('PORT', 5000)))
