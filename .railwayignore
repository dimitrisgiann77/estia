# ───────────────────────────────────────────────────────────
# ESTIA — .railwayignore
# Αρχεία που ΜΕΝΟΥΝ στο repo (versioned, τα διαβάζει ο Claude τοπικά)
# αλλά ΔΕΝ ανεβαίνουν στο deployed container (Railway/Nixpacks) —
# δεν χρειάζονται στο runtime.
# ───────────────────────────────────────────────────────────

# Οδηγίες Claude Code (χρησιμεύουν ΜΟΝΟ τοπικά κατά την ανάπτυξη)
CLAUDE.md
.claude/
